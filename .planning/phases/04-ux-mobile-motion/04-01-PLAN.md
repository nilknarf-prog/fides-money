---
phase: 04-ux-mobile-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-ui.jsx
  - assets/fides.css
autonomous: true
requirements: [MOTION-01]
must_haves:
  truths:
    - "Reabrir um modal durante a janela de closing cancela o setTimeout e remonta/anima entrada limpa (sem modal duplicado/estado preso)"
    - "Com prefers-reduced-motion: reduce ativo, o hook pula o delay de closing e desmonta imediatamente, e as animações de modal não animam"
  artifacts:
    - path: "assets/fides-ui.jsx"
      provides: "Hook useModalClose(open, onClose) exposto em window.FidesUI, retornando { rendered, closing, requestClose }"
      contains: "useModalClose"
    - path: "assets/fides.css"
      provides: "Keyframes de saída de modal (fade-out + slide-down) e regras .is-closing + gate prefers-reduced-motion"
      contains: "is-closing"
  key_links:
    - from: "assets/fides-ui.jsx"
      to: "assets/fides.css"
      via: "hook seta closing → modais aplicam classe is-closing que dispara os keyframes -out"
      pattern: "is-closing"
  prohibitions:
    - statement: "O JS de orquestração de fechamento (useModalClose: estado closing + setTimeout de unmount) NÃO PODE importar nem chamar mutações de store ou APIs de dados financeiros (addTransaction, pay_card_invoice, escrita de store, qualquer coisa sob api/*). Deve gerenciar APENAS o tempo de render/unmount."
      status: resolved
      verification: judgment
---

<objective>
Criar a fundação de motion de saída de modal: o hook compartilhado `useModalClose(open, onClose)` em `assets/fides-ui.jsx` (D-01) e os keyframes/regras CSS de saída em `assets/fides.css`. Nenhum modal é alterado aqui — esta plan só produz as peças que a Wave 2 consome.

Purpose: Hoje os modais usam `if (!open) return null` → unmount instantâneo → saída sem animação (corte abrupto). CSS puro não anima um nó que o React removeu do DOM. O hook adia o unmount ~180ms para a animação de saída tocar, e centraliza a lógica de timing para os 3 modais do escopo (evita triplicar). Implementa MOTION-01 (saída) e dobra MOTION-PERF (GPU hint + reduced-motion) na camada CSS.
Output: `window.FidesUI.useModalClose` + keyframes `fds-fadeOut`/`fds-slideDown` + regras `.is-closing` + gate `@media (prefers-reduced-motion: reduce)` em `fides.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-mobile-motion/04-SPEC.md
@.planning/phases/04-ux-mobile-motion/04-CONTEXT.md
@assets/fides-ui.jsx
@assets/fides.css
</context>

<artifacts_this_phase_produces>
Símbolos NOVOS criados por esta plan (a Wave 2 depende deles):
- `useModalClose(open, onClose)` — hook em `assets/fides-ui.jsx`, exposto via `window.FidesUI.useModalClose`.
- Retornos do hook: `rendered` (bool — substitui o gate `open` no `if (!open) return null`), `closing` (bool — verdadeiro durante a janela de saída ~180ms), `requestClose` (função — seta `closing`, agenda unmount via setTimeout, e é o que cada botão de fechar chama no lugar de `onClose`).
- Keyframes `fds-fadeOut` (opacity 1→0) e `fds-slideDown` (translateY(0)→translateY(12px) + opacity 1→0) em `assets/fides.css`.
- Regras `.fds-modal-backdrop.is-closing` e `.fds-modal.is-closing` que aplicam os keyframes `-out` e GPU hint.
- Bloco `@media (prefers-reduced-motion: reduce)` em `fides.css` que desliga as animações de entrada e saída do modal.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Criar o hook useModalClose em fides-ui.jsx</name>
  <files>assets/fides-ui.jsx</files>
  <read_first>
    - assets/fides-ui.jsx (LER TODO o arquivo): é um IIFE `(function(){ 'use strict'; ... })()` que usa `React.createElement` (NÃO JSX) e expõe a API pública em `window.FidesUI = { ... }` no fim (~:231-237). Os hooks existentes `useToast` (~:47) e `useConfirm` (~:186) mostram o padrão de hook neste arquivo. NÃO há imports de store/dados aqui — manter assim.
    - assets/fides-contas.jsx:147-148 (analog do gate `if (!open) return null` num modal alvo).
  </read_first>
  <action>
    Definir uma nova função `useModalClose(open, onClose)` dentro do IIFE de `assets/fides-ui.jsx` (junto aos outros hooks, antes do bloco `window.FidesUI = {...}`), implementando D-01/D-03/D-05.
    Retorno: objeto `{ rendered, closing, requestClose }`.
    Estado interno via `React.useState`: `rendered` (bool) e `closing` (bool). Guardar o id do `setTimeout` num `React.useRef` (ex.: `timerRef`).
    `requestClose`: usar `React.useCallback`. Comportamento — se reduced-motion ativo (ver abaixo) OU sem necessidade de janela, chamar `onClose()` e desmontar imediato (sem setar `closing`); caso contrário setar `closing=true`, agendar via `setTimeout` (~180ms; declarar como constante nomeada, ex.: `CLOSE_MS = 180`) um callback que seta `closing=false`, `rendered=false` e chama `onClose()`. Salvar o id retornado em `timerRef.current`.
    Detecção de reduced-motion (D-05): `window.matchMedia('(prefers-reduced-motion: reduce)').matches` — checar com guarda (`window.matchMedia &&`) para robustez.
    Sincronização com `open` via `React.useEffect([open])` (D-03 — race de re-open): quando `open` vira true → limpar `timerRef.current` (clearTimeout) se existir, setar `closing=false` e `rendered=true` (remonta/anima entrada limpa, cancelando qualquer closing em andamento). Quando `open` vira false e o componente está montado → NÃO desmontar direto; deixar o fechamento ser orquestrado por `requestClose` (a chamada de fechar dispara requestClose). Cleanup do `useEffect`: `clearTimeout(timerRef.current)` para não vazar timer no unmount real.
    PROIBIÇÃO (must-NOT, R2): o hook NÃO pode importar nem chamar qualquer mutação de store ou API financeira (`addTransaction`, `pay_card_invoice`, `useFides` write, `api/*`). Ele só lida com `open`, `onClose`, timers e os dois booleans de render. Nenhum `import`/referência a store é permitido.
    Expor o hook: adicionar `useModalClose: useModalClose,` ao objeto literal `window.FidesUI = { ... }` (~:231).
    Manter o estilo do arquivo: `var`/funções nomeadas como os hooks existentes; NÃO introduzir JSX (este arquivo não usa JSX).
  </action>
  <acceptance_criteria>
    - `grep -n "function useModalClose" assets/fides-ui.jsx` retorna 1 ocorrência.
    - `grep -n "useModalClose:" assets/fides-ui.jsx` mostra a chave adicionada dentro de `window.FidesUI`.
    - `grep -nE "rendered|closing|requestClose" assets/fides-ui.jsx` mostra os três nomes presentes no corpo do hook (retorno do objeto).
    - `grep -n "matchMedia" assets/fides-ui.jsx` mostra a checagem de `prefers-reduced-motion` dentro do hook.
    - `grep -n "setTimeout" assets/fides-ui.jsx` e `grep -n "clearTimeout" assets/fides-ui.jsx` ambos presentes (timer + cancelamento de race).
    - Revisão de código (judgment): o corpo de `useModalClose` não contém nenhuma referência a `addTransaction`, `pay_card_invoice`, `useFides`, `fidesDb`, `api/` nem escrita de store — apenas timing de render.
    - Carregar a app em iOS Safari 400×512px e abrir um console: `window.FidesUI.useModalClose` é uma função (não undefined). (Verificação observável; nenhum modal muda de comportamento ainda nesta plan.)
  </acceptance_criteria>
  <verify>
    grep -n "function useModalClose" assets/fides-ui.jsx && grep -n "useModalClose:" assets/fides-ui.jsx && grep -nE "rendered|closing|requestClose|matchMedia|setTimeout|clearTimeout" assets/fides-ui.jsx
  </verify>
  <done>
    `useModalClose(open, onClose)` existe em `fides-ui.jsx`, retorna `{ rendered, closing, requestClose }`, cancela timer no re-open, pula delay sob reduced-motion, está exposto em `window.FidesUI`, e não toca dados financeiros.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar keyframes de saída + regras .is-closing + gate reduced-motion em fides.css</name>
  <files>assets/fides.css</files>
  <read_first>
    - assets/fides.css:998-1020 (bloco MODAL): `.fds-modal-backdrop` usa `animation: fds-fadeIn 0.2s` (:1006); `@keyframes fds-fadeIn` (:1008); `.fds-modal` usa `animation: fds-slideUp 0.25s cubic-bezier(.2,.7,.2,1)` (:1018); `@keyframes fds-slideUp` (:1020 — `from { transform: translateY(12px); opacity: 0 } to { transform: translateY(0); opacity: 1 }`). As variantes de saída devem espelhar essas, na direção inversa.
  </read_first>
  <action>
    No mesmo arquivo `assets/fides.css`, logo após o par de entrada existente (após :1020), adicionar as peças de saída (Claude's Discretion confirmada: estender o par `fds-fadeIn`/`fds-slideUp` com variantes `-out` no MESMO arquivo, NÃO migrar para `fui-*`):
    - `@keyframes fds-fadeOut` — `from { opacity: 1 } to { opacity: 0 }`.
    - `@keyframes fds-slideDown` — `from { transform: translateY(0); opacity: 1 } to { transform: translateY(12px); opacity: 0 }` (espelho do `fds-slideUp`, slide para baixo).
    - Regra `.fds-modal-backdrop.is-closing` — `animation: fds-fadeOut` com duração ~0.18s (alinhada com `CLOSE_MS=180` da Task 1).
    - Regra `.fds-modal.is-closing` — `animation: fds-slideDown` ~0.18s com a mesma curva `cubic-bezier(.2,.7,.2,1)`; aplicar `pointer-events: none` (D-04 — travar clique acidental durante o fade-out); manter `animation-fill-mode: forwards` para o nó terminar invisível antes do unmount.
    - GPU hint (MOTION-PERF): em `.fds-modal` e `.fds-modal.is-closing`, garantir hint de GPU para a transição de transform/opacity — adicionar `will-change: transform, opacity` (ou `transform: translateZ(0)`) onde a animação roda. Nenhuma animação pode passar de 300ms (entrada já é 0.2/0.25s; saída ~0.18s — ok).
    - Bloco reduced-motion (D-05 / MOTION-PERF): adicionar `@media (prefers-reduced-motion: reduce) { ... }` que zera/desliga as animações de entrada e saída do modal — ex.: `.fds-modal-backdrop, .fds-modal, .fds-modal-backdrop.is-closing, .fds-modal.is-closing { animation: none !important; }`. Isso garante "abre/fecha sem movimento" mesmo com a classe `is-closing` aplicada (o hook já pula o delay no JS; o CSS reforça o no-motion).
    NÃO alterar as regras de entrada existentes (:998-1020) além de acrescentar o GPU hint; NÃO tocar em toasts/`fui-*`.
  </action>
  <acceptance_criteria>
    - `grep -n "@keyframes fds-fadeOut" assets/fides.css` e `grep -n "@keyframes fds-slideDown" assets/fides.css` ambos presentes.
    - `grep -n "is-closing" assets/fides.css` mostra as regras `.fds-modal-backdrop.is-closing` e `.fds-modal.is-closing`.
    - `grep -n "pointer-events: none" assets/fides.css` aparece dentro do bloco `.fds-modal.is-closing` (D-04).
    - `grep -nE "will-change|translateZ" assets/fides.css` mostra o GPU hint no contexto do modal (MOTION-PERF).
    - `grep -n "prefers-reduced-motion" assets/fides.css` mostra o bloco @media que desliga as animações de modal (D-05).
    - Inspeção: nenhuma das durações de animação de modal (entrada ou saída) excede 300ms.
  </acceptance_criteria>
  <verify>
    grep -nE "@keyframes fds-fadeOut|@keyframes fds-slideDown|is-closing|pointer-events: none|will-change|translateZ|prefers-reduced-motion" assets/fides.css
  </verify>
  <done>
    `fides.css` tem keyframes `fds-fadeOut`/`fds-slideDown`, regras `.is-closing` (com `pointer-events:none` e GPU hint) e um gate `@media (prefers-reduced-motion: reduce)` que desliga o motion do modal — tudo ≤300ms. Pronto para a Wave 2 plugar nos 3 modais.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Fase CSS/UI sem mudança de dados/API/auth. O único vetor relevante é de ESCOPO: garantir que o JS de motion não vire um caminho de escrita de dados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (scope leak) | `useModalClose` em `fides-ui.jsx` | mitigate | O hook só lida com `open`/`onClose`/timers/render. Verificação por code review (judgment): nenhum import/chamada de `addTransaction`, `pay_card_invoice`, escrita de store ou `api/*`. Encodado em `must_haves.prohibitions`. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Front é React Babel-standalone SEM bundler/npm; esta plan não instala nenhum pacote. Sem superfície de supply-chain. |
</threat_model>

<verification>
- `useModalClose` existe, exposto em `window.FidesUI`, retorna `{ rendered, closing, requestClose }`, cancela timer no re-open e pula delay sob reduced-motion.
- `fides.css` tem keyframes de saída, regras `.is-closing` (pointer-events + GPU hint) e gate reduced-motion; nenhuma animação >300ms.
- Code review confirma que o hook não toca dados financeiros (prohibition R2).
</verification>

<success_criteria>
- O hook e o CSS de saída existem e são consistentes (CLOSE_MS ↔ duração ~0.18s).
- Nenhum modal foi alterado nesta plan (isolamento — a Wave 2 faz o wiring).
- Reduced-motion e GPU hint cobertos (MOTION-PERF).
</success_criteria>

<output>
Create `.planning/phases/04-ux-mobile-motion/04-01-SUMMARY.md` when done
</output>
