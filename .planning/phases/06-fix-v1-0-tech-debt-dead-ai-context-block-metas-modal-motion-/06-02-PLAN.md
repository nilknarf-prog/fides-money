---
phase: 06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-metas.jsx
autonomous: true
requirements: [DEBT-02]
must_haves:
  truths:
    - "Os 8 modais fds-modal-backdrop de fides-metas.jsx fecham com a mesma saída animada (fade-out + slide-down) dos 3 modais da fase 04 — via useModalClose + classe is-closing — sem corte abrupto (DEBT-02 / D-04)"
    - "Os 4 painéis realmente montados na tela (SimularPanel, RevisarPanel, AplicarPanel, overlay Em breve) permanecem sempre montados enquanto animam a saída, e só desmontam após a animação — abrir → fechar mostra transição visível"
    - "Com prefers-reduced-motion: reduce, os modais de metas fecham sem movimento e sem delay (unmount imediato via requestClose)"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "8 modais fds-modal-backdrop consumindo window.FidesUI.useModalClose (rendered/closing/requestClose + classe is-closing), com o padrão de mount ajustado para painéis sempre montados via prop open"
      contains: "useModalClose"
  key_links:
    - from: "assets/fides-metas.jsx"
      to: "assets/fides-ui.jsx"
      via: "window.FidesUI.useModalClose(open, onClose) controla render e saída animada dos modais de metas"
      pattern: "FidesUI.useModalClose"
    - from: "assets/fides-metas.jsx"
      to: "assets/fides.css"
      via: "classe is-closing no .fds-modal-backdrop/.fds-modal dispara os keyframes fds-fadeOut/fds-slideDown (já existentes da fase 04)"
      pattern: "is-closing"
  prohibitions:
    - statement: "NÃO adicionar CSS de animação novo — os keyframes de saída (fds-fadeOut/fds-slideDown), as regras .is-closing e o gate prefers-reduced-motion já existem em fides.css (fides.css:1022-1044) e miram fds-modal-backdrop/fds-modal, exatamente as classes destes modais. Só aplicar a classe via JS."
      status: resolved
      verification: judgment
    - statement: "O wiring de saída NÃO PODE alterar nem introduzir chamadas de mutação de store/API (goals, useFides write, api/*). requestClose substitui APENAS a orquestração de unmount; onConfirm/onClose de dados/estado permanecem chamando suas funções — só passam a fechar via requestClose para a saída animar."
      status: resolved
      verification: judgment
    - statement: "NÃO transformar os 4 componentes hoje definidos-mas-nunca-montados (AportarModal, AjustarPlanoModal, MetConfirmDeleteModal, ConfigurarModal) em modais montados/funcionais — Metas continua read-only (botões apontam para o overlay 'Em breve'). Eles recebem o mesmo wiring de useModalClose por consistência (D-04 = 'ligar TODOS os 8'), mas seguem não-montados; NÃO criar mount sites novos para eles."
      status: resolved
      verification: judgment
---

<objective>
Fechar a warning DEBT-02 (WARN-3) do milestone v1.0: ligar TODOS os 8 modais `fds-modal-backdrop` de `assets/fides-metas.jsx` ao hook `window.FidesUI.useModalClose`, replicando o padrão EXATO da fase 04 (classe `is-closing` + gate reduced-motion). Hoje esses modais fazem `if (!cond) return null` → unmount instantâneo → saída sem animação (corte abrupto), enquanto os 3 modais da fase 04 fazem fade+slide na saída.

Purpose: consistência de motion user-visible. O hook `useModalClose` e todo o CSS de saída já existem da fase 04 e miram exatamente `fds-modal-backdrop`/`fds-modal` — só falta plugar. É wiring de UI, sem novo comportamento (D-04).
Output: os 8 modais de metas com entrada+saída animadas, sem corte abrupto, com re-open limpo e gate reduced-motion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion-/06-CONTEXT.md
@.planning/phases/04-ux-mobile-motion/04-01-PLAN.md
@.planning/phases/04-ux-mobile-motion/04-02-PLAN.md
@assets/fides-metas.jsx
@assets/fides-ui.jsx
</context>

<artifacts_this_phase_produces>
Nenhum símbolo público novo — consome `window.FidesUI.useModalClose` (fase 04). Mudanças por modal, replicando o padrão de 04-02:
- Chamada `const { rendered, closing, requestClose } = window.FidesUI.useModalClose(open, <onClose|onCancel|setEmBreve(false)>)` no topo de cada componente de modal.
- Gate de render trocado para `if (!rendered) return null` (nos que hoje têm `if (!open)`/`if (!meta)`/`if (!dívida)` etc.).
- Classe `is-closing` aplicada condicionalmente (`+ (closing ? " is-closing" : "")`) no `.fds-modal-backdrop` E no `.fds-modal` de cada modal.
- Todos os handlers de fechar (clique no backdrop, botão X, Cancelar, "Entendi"/"Fechar", "Registrei") passam a chamar `requestClose` no lugar da chamada de fechar direta.
- **Ajuste de mount (crítico)** para os 4 painéis hoje montados via `{cond && <Panel/>}`: passar a montá-los sempre e controlar por prop `open`, para que a saída anime antes do unmount (padrão idêntico ao dos 3 modais da fase 04). Ver Task 1/Task 2 para o detalhe por painel.

**Contexto essencial descoberto na análise (LER antes de executar):** os 8 `fds-modal-backdrop` dividem-se em 2 grupos de mount:
1. **Montados de fato na tela** (precisam do ajuste de mount para animar a saída): `SimularPanel` (:317), `RevisarPanel` (:409), `AplicarPanel` (:476) — montados via `{simularDivida && ...}`/`{revisarOpen && ...}`/`{aplicarOpen && ...}` (:679-681); e o overlay inline **Em breve** (:653-676) via `{emBreve && ...}`.
2. **Definidos mas NUNCA montados** (componentes placeholder; recebem o mesmo wiring por D-04, mas seguem sem mount site): `AportarModal` (:99), `AjustarPlanoModal` (:187), `MetConfirmDeleteModal` (:268, já tem prop `open`), `ConfigurarModal` (:527). Os botões "Aportar"/"Ajustar plano" e o `MetDotsMenu` apontam todos para `setEmBreve(true)` (:778-780, :834, :840) — nenhum monta esses 4.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire os 4 painéis MONTADOS de metas (SimularPanel, RevisarPanel, AplicarPanel, overlay Em breve) ao useModalClose com mount por prop open</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-ui.jsx:236-284 — contrato do hook `useModalClose(open, onClose)` → `{ rendered, closing, requestClose }`. `requestClose` roda a saída (~180ms `CLOSE_MS`) e SÓ ENTÃO chama `onClose`; sob reduced-motion chama `onClose` imediato. O `useEffect([open])` cancela o closing e remonta limpo quando `open` volta a true.
    - .planning/phases/04-ux-mobile-motion/04-02-PLAN.md — padrão EXATO a replicar (troca `if (!open) return null` → `if (!rendered) return null`; classe `is-closing` no backdrop e no modal; handlers de fechar → `requestClose`).
    - assets/fides-metas.jsx:294-395 — `SimularPanel({ dívida, onClose })`; gate `if (!dívida) return null` (:295); backdrop `onClick={onClose}` (:317); botão X `onClick={onClose}` (:324); botão "Entendi" `onClick={onClose}` (:390).
    - assets/fides-metas.jsx:398-469 — `RevisarPanel({ assinaturas, onClose })`; gate `if (!assinaturas || assinaturas.length === 0) return null` (:400); backdrop `onClick={onClose}` (:409); X `onClick={onClose}` (:416); "Fechar" `onClick={onClose}` (:459); "Registrei, obrigado" `onClick={onClose}` (:461).
    - assets/fides-metas.jsx:472-516 — `AplicarPanel({ totalContas, onClose })`; sem early-return null; backdrop `onClick={onClose}` (:476); X `onClick={onClose}` (:483); "Entendi" `onClick={onClose}` (:511).
    - assets/fides-metas.jsx:652-676 — overlay inline **Em breve** dentro de `MetasStudio`: `{emBreve && (<div className="fds-modal-backdrop" onClick={() => setEmBreve(false)}> ... )}`; X e botão "Entendi" também chamam `setEmBreve(false)` (:658, :668).
    - assets/fides-metas.jsx:612-615 — estado no `MetasStudio`: `emBreve`, `simularDivida`, `revisarOpen`, `aplicarOpen`.
    - assets/fides-metas.jsx:679-681 — os mount sites atuais dos 3 painéis: `{simularDivida && <SimularPanel .../>}`, `{revisarOpen && <RevisarPanel .../>}`, `{aplicarOpen && <AplicarPanel .../>}`.
  </read_first>
  <action>
    Objetivo: os 4 painéis MONTADOS animam a saída. Como o CSS puro não anima um nó que o React já removeu, cada painel precisa permanecer montado durante a janela de saída — logo o gate de existência migra do pai (`{cond && ...}`) para dentro do componente via `useModalClose(open, onClose)` + `if (!rendered) return null`.

    Para SimularPanel, RevisarPanel e AplicarPanel (aplicar o padrão de 04-02 em cada um):
    1. Adicionar uma prop `open` na assinatura do componente (ex.: `function SimularPanel({ open, dívida, onClose })`).
    2. No topo do corpo, ANTES de qualquer early-return de dados: `const { rendered, closing, requestClose } = window.FidesUI.useModalClose(open, onClose);`.
    3. Manter os early-returns de dados (ex.: `if (!dívida) return null`, `if (!assinaturas...) return null`) — mas ADICIONAR o gate de render do hook: trocar a lógica para retornar null quando `!rendered` (o painel some após a animação). Ordenar: primeiro `if (!rendered) return null;`, depois os guards de dados existentes (que continuam válidos quando há dados).
    4. Raiz do return: `className="fds-modal-backdrop"` → `className={"fds-modal-backdrop" + (closing ? " is-closing" : "")}` e `onClick={onClose}` → `onClick={requestClose}`.
    5. `.fds-modal`: `className="fds-modal"` → `className={"fds-modal" + (closing ? " is-closing" : "")}` (manter `style` e o `onClick={e => e.stopPropagation()}`).
    6. Todos os botões de fechar (X, "Entendi", "Fechar", "Registrei, obrigado") → `onClick={requestClose}` no lugar de `onClick={onClose}`.

    No `MetasStudio` (mount sites :679-681), trocar o padrão `{cond && <Panel/>}` por montagem SEMPRE com prop `open`:
    - `<SimularPanel open={!!simularDivida} dívida={simularDivida} onClose={() => setSimularDivida(null)}/>`
    - `<RevisarPanel open={revisarOpen} assinaturas={assinaturas} onClose={() => setRevisarOpen(false)}/>`
    - `<AplicarPanel open={aplicarOpen} totalContas={totalContas} onClose={() => setAplicarOpen(false)}/>`
    (Remover o `simularDivida &&` / `revisarOpen &&` / `aplicarOpen &&` da frente — o gate agora é interno via `rendered`.)

    Para o overlay inline **Em breve** (:652-676): este NÃO é um componente separado (é JSX inline gated por `{emBreve && ...}`). Extrair para um pequeno componente ou envolver com o hook não é trivial inline. Escolha de implementação (Claude's Discretion): extrair o overlay para um componente `EmBreveModal({ open, onClose })` no topo do arquivo (junto aos outros modais), aplicando o mesmo padrão (useModalClose + rendered + is-closing + requestClose nos 3 pontos de fechar), e montá-lo em `MetasStudio` sempre, como `<EmBreveModal open={emBreve} onClose={() => setEmBreve(false)}/>` (substituindo o bloco `{emBreve && (...)}`). Os call sites que hoje fazem `setEmBreve(true)` para ABRIR permanecem inalterados (abrir continua sendo `setEmBreve(true)`; o fechar interno vira `requestClose`).

    PROIBIÇÃO: NÃO adicionar CSS novo (keyframes/regras já existem em fides.css:1022-1044). NÃO introduzir chamada de store/API (`useFides` write, `goals`, `api/*`). A única mudança é orquestração de render/unmount + classe. `setSimularDivida`/`setRevisarOpen`/`setAplicarOpen`/`setEmBreve` continuam sendo o `onClose` (estado do pai) — só passam a ser chamados via `requestClose` (que os invoca ao fim da animação).
  </action>
  <acceptance_criteria>
    - `grep -c "useModalClose" assets/fides-metas.jsx` retorna ≥ 4 (SimularPanel, RevisarPanel, AplicarPanel, EmBreveModal — mais os da Task 2).
    - `grep -c "is-closing" assets/fides-metas.jsx` mostra a classe condicional aplicada (backdrop + modal) nesses painéis.
    - `grep -c "requestClose" assets/fides-metas.jsx` presente nos handlers de fechar dos 4 painéis montados.
    - `grep -n "simularDivida &&" assets/fides-metas.jsx` e `grep -n "revisarOpen &&" assets/fides-metas.jsx` e `grep -n "aplicarOpen &&" assets/fides-metas.jsx` NÃO retornam mais os mount sites gated (viraram `open={...}`); os componentes agora montam sempre.
    - `grep -n "emBreve &&" assets/fides-metas.jsx` não retorna mais o bloco de overlay inline (virou `<EmBreveModal open={emBreve} .../>`).
    - `grep -c "@keyframes" assets/fides-metas.jsx` retorna 0 (nenhum CSS de animação foi adicionado no JSX; regras vivem em fides.css).
    - Revisão (judgment): nenhuma chamada nova a `useFides` write / `goals` mutação / `api/*` foi introduzida nos painéis.
    - Observável (400×512 iOS Safari): abrir "Simular"/"Revisar assinaturas"/"Aplicar"/"Em breve" → fechar (X, backdrop, Entendi/Fechar) mostra fade-out + slide-down visível antes de sumir; reabrir logo em seguida monta limpo; com reduced-motion fecha instantâneo sem movimento.
  </acceptance_criteria>
  <verify>
    grep -nE "useModalClose|if \(!rendered\) return null|is-closing|requestClose|open=\{" assets/fides-metas.jsx | head -50
  </verify>
  <done>
    SimularPanel, RevisarPanel, AplicarPanel e o overlay "Em breve" (agora EmBreveModal) consomem `useModalClose`, montam sempre via prop `open`, aplicam `is-closing` e fecham via `requestClose` — saída animada visível, re-open limpo, reduced-motion sem movimento; nenhum CSS novo e nenhuma escrita de dados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire os 4 modais DEFINIDOS-MAS-NÃO-MONTADOS de metas (AportarModal, AjustarPlanoModal, MetConfirmDeleteModal, ConfigurarModal) ao useModalClose por consistência (D-04 = todos os 8)</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx:89-179 — `AportarModal({ meta, onConfirm, onClose })`; gate `if (!meta) return null` (:91); backdrop `onClick={onClose}` (:99); X `onClick={onClose}` (:108); "Cancelar" `onClick={onClose}` (:166); "Confirmar aporte" `onClick={() => v > 0 && onConfirm(v)}` (:170 — ação de dados, NÃO é fechar).
    - assets/fides-metas.jsx:182-262 — `AjustarPlanoModal({ meta, onConfirm, onClose })`; gate `if (!meta) return null` (:183); backdrop `onClick={onClose}` (:187); X `onClick={onClose}` (:194); form `onSubmit` chama `onConfirm(...)` (:201 — dados); "Cancelar" `onClick={onClose}` (:255); submit "Salvar alterações" (:256).
    - assets/fides-metas.jsx:265-291 — `MetConfirmDeleteModal({ open, nome, onConfirm, onCancel })` — JÁ tem prop `open` e `if (!open) return null` (:266); backdrop `onClick={onCancel}` (:268); X `onClick={onCancel}` (:275); "Cancelar" `onClick={onCancel}` (:283); "Excluir" `onClick={onConfirm}` (:284 — dados). AQUI o callback de fechar é `onCancel`.
    - assets/fides-metas.jsx:519-587 — `ConfigurarModal({ sobra, metas, onConfirm, onClose })`; sem prop `open` nem early-return null; backdrop `onClick={onClose}` (:527); X `onClick={onClose}` (:534); "Cancelar" `onClick={onClose}` (:574); "Aplicar distribuição" `onClick={() => ... onConfirm(dist)}` (:578 — dados).
    - assets/fides-metas.jsx:777-845 — CONFIRMAÇÃO de que estes 4 componentes NÃO são montados: `MetDotsMenu` (:777-781) e os botões "Aportar"/"Ajustar plano" (:834, :840) chamam `setEmBreve(true)`; não há `<AportarModal/>`/`<AjustarPlanoModal/>`/`<MetConfirmDeleteModal/>`/`<ConfigurarModal/>` em lugar nenhum do arquivo (grep confirma: só as 4 definições `function ...`).
    - .planning/phases/04-ux-mobile-motion/04-02-PLAN.md Task 3 — padrão para modal com callback `onCancel` (como MetConfirmDeleteModal): passar `onCancel` como o `onClose` do hook.
  </read_first>
  <action>
    D-04 pede ligar TODOS os 8 modais `fds-modal-backdrop` ao `useModalClose`. Estes 4 são placeholders (definidos, nunca montados) — recebem o MESMO wiring por consistência e para ficarem prontos caso venham a ser montados no futuro, MAS permanecem sem mount site (Metas segue read-only via overlay "Em breve"). Aplicar o padrão de 04-02 em cada um:

    Para `AportarModal`, `AjustarPlanoModal`, `ConfigurarModal` (callback de fechar = `onClose`):
    1. Adicionar prop `open` na assinatura (ex.: `function AportarModal({ open, meta, onConfirm, onClose })`).
    2. Topo do corpo: `const { rendered, closing, requestClose } = window.FidesUI.useModalClose(open, onClose);`.
    3. Gate: adicionar `if (!rendered) return null;` no topo; manter os guards de dados existentes (`if (!meta) return null`) logo depois.
    4. Backdrop: `className={"fds-modal-backdrop" + (closing ? " is-closing" : "")}`, `onClick={requestClose}`.
    5. `.fds-modal`: `className={"fds-modal" + (closing ? " is-closing" : "")}` (manter `style` e `stopPropagation`).
    6. Botões de FECHAR (X, "Cancelar", "Fechar") → `onClick={requestClose}`.
    7. Botões de AÇÃO de dados (`onConfirm(v)`, `onConfirm(dist)`, submit do form que chama `onConfirm(...)`) — manter a chamada de dados; para a saída animar no confirmar, envolver num handler que chama `onConfirm(...)` e em seguida `requestClose()` (a gravação permanece via `onConfirm`; `requestClose` só orquestra o fechar animado). NÃO alterar o payload nem a lógica de `onConfirm`.

    Para `MetConfirmDeleteModal` (JÁ tem prop `open`; callback de fechar = `onCancel`):
    1. NÃO adicionar prop `open` (já existe). Adicionar: `const { rendered, closing, requestClose } = window.FidesUI.useModalClose(open, onCancel);` (passar `onCancel` como o onClose do hook).
    2. Trocar `if (!open) return null` (:266) por `if (!rendered) return null`.
    3. Backdrop/`.fds-modal`: aplicar `is-closing` como acima.
    4. X (:275) e "Cancelar" (:283): `onClick={requestClose}`.
    5. "Excluir" (:284): manter `onConfirm` como ação; envolver em handler `() => { onConfirm(); requestClose(); }` para animar a saída ao excluir.

    PROIBIÇÃO: NÃO criar mount sites novos para estes 4 (não montá-los em MetasStudio) — eles seguem definidos-mas-não-montados; Metas permanece read-only. NÃO adicionar CSS. NÃO alterar a lógica/payload de `onConfirm`/`onSubmit` (dados intactos). NÃO introduzir escrita de store/API.
  </action>
  <acceptance_criteria>
    - `grep -c "useModalClose" assets/fides-metas.jsx` agora conta os 4 desta task somados aos 4 da Task 1 → total ≥ 8 (um por modal `fds-modal-backdrop`).
    - `grep -c "function AportarModal\|function AjustarPlanoModal\|function MetConfirmDeleteModal\|function ConfigurarModal" assets/fides-metas.jsx` continua 4 (componentes preservados, não removidos).
    - `grep -c "<AportarModal\|<AjustarPlanoModal\|<MetConfirmDeleteModal\|<ConfigurarModal" assets/fides-metas.jsx` retorna 0 (nenhum mount site novo criado — boundary read-only respeitado).
    - `grep -n "if (!open) return null" assets/fides-metas.jsx` NÃO retorna mais o site do MetConfirmDeleteModal (:266 virou `rendered`).
    - `grep -c "is-closing" assets/fides-metas.jsx` e `grep -c "requestClose" assets/fides-metas.jsx` cobrem também estes 4 modais (backdrop + modal + handlers de fechar).
    - Revisão (judgment): `onConfirm`/`onSubmit` mantêm payload e lógica idênticos; o único acréscimo é `requestClose()` após a chamada de dados; nenhuma escrita de store/API nova.
  </acceptance_criteria>
  <verify>
    grep -nE "function (AportarModal|AjustarPlanoModal|MetConfirmDeleteModal|ConfigurarModal)|useModalClose|if \(!rendered\) return null|is-closing|requestClose" assets/fides-metas.jsx | head -60
  </verify>
  <done>
    Os 4 modais placeholder (AportarModal, AjustarPlanoModal, MetConfirmDeleteModal, ConfigurarModal) consomem `useModalClose`, aplicam `is-closing` e fecham via `requestClose` (MetConfirmDeleteModal via `onCancel`); confirmações de dados animam a saída via `onConfirm()`+`requestClose()`; nenhum foi montado (Metas segue read-only); nenhum CSS novo, dados intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Fase de wiring CSS/UI. Vetor relevante: o wiring de fechamento NÃO pode virar caminho de escrita de dados nem montar funcionalidade de escrita nova (Metas é read-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering (scope leak) | wiring de fechamento dos 8 modais de metas | mitigate | A mudança é só orquestração de unmount (open/cond → rendered, onClose/onCancel → requestClose, classe is-closing) e o ajuste de mount dos 4 painéis. `onConfirm`/`onSubmit`/estado do pai ficam inalterados; nenhuma escrita de store/API adicionada. Verificação: code review (judgment) + grep (nenhum mount site novo dos 4 placeholders; nenhum @keyframes no JSX). Encodado em `must_haves.prohibitions`. |
| T-06-02b | Elevation (scope creep) | 4 componentes placeholder | mitigate | Metas permanece read-only: os 4 componentes definidos-mas-não-montados NÃO ganham mount site; recebem só o wiring do hook por consistência (D-04). Verificação: grep `<AportarModal`/`<AjustarPlanoModal`/`<MetConfirmDeleteModal`/`<ConfigurarModal` = 0. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Front é React Babel-standalone SEM bundler/npm (PROJECT.md §3). Nenhum pacote instalado; sem superfície de supply-chain. |
</threat_model>

<verification>
- Os 8 modais `fds-modal-backdrop` de fides-metas.jsx consomem `useModalClose`, aplicam `is-closing` e fecham via `requestClose`.
- Os 4 painéis montados (SimularPanel, RevisarPanel, AplicarPanel, EmBreve) montam sempre via prop `open` e animam a saída antes do unmount; re-open limpo; reduced-motion sem movimento.
- Os 4 placeholders recebem o wiring mas seguem sem mount site (Metas read-only).
- Nenhum CSS de animação novo (regras vivem em fides.css:1022-1044); nenhuma escrita de store/API introduzida (code review).
</verification>

<success_criteria>
- AC do ROADMAP Phase 06 #2: os 8 modais `fds-modal-backdrop` de fides-metas.jsx abrem/fecham com a mesma animação de saída dos demais, sem corte abrupto.
- Boundary respeitado: sem novo comportamento de escrita; sem CSS novo; Metas permanece read-only.
</success_criteria>

<output>
Create `.planning/phases/06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion-/06-02-SUMMARY.md` when done
</output>
