---
phase: 06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-orcamento.jsx
  - assets/fides.css
  - assets/fides-transacoes.jsx
  - assets/fides-studio.jsx
autonomous: true
requirements: [DEBT-01, DEBT-03, DEBT-04]
must_haves:
  truths:
    - "buildAiContext() não contém mais nenhuma leitura de totals.receitas/totals.despesas — a função ainda produz a linha 'Status do Planejamento (50·30·20)' e a linha de tendência do mês anterior (DEBT-01)"
    - "Toda definição de --warn-soft no repositório resolve para #FEF0D6 — não sobra nenhum #FEF3C7 em nenhum arquivo CSS (DEBT-03)"
    - "Salvar uma nova transação e fechar o modal dispara setModalOpen(false) uma única vez, pela via animada requestClose→onClose — o modal fecha com a saída animada e reabre limpo (DEBT-04)"
  artifacts:
    - path: "assets/fides-orcamento.jsx"
      provides: "buildAiContext() sem o bloco morto receitas/despesas"
      contains: "Status do Planejamento"
    - path: "assets/fides.css"
      provides: "--warn-soft alinhado a #FEF0D6 nas 2 definições"
      contains: "--warn-soft"
    - path: "assets/fides-studio.jsx"
      provides: "onSave do NovaTransacaoModal sem o setModalOpen(false) redundante"
      contains: "NovaTransacaoModal"
  key_links:
    - from: "assets/fides-transacoes.jsx"
      to: "assets/fides-studio.jsx"
      via: "handleSave chama onSave (grava dados) e requestClose (fecha animado); o fechar acontece só via requestClose→onClose"
      pattern: "requestClose"
    - from: "assets/fides.css"
      to: "assets/tokens.css"
      via: "--warn-soft de fides.css converge para o valor canônico de tokens.css (#FEF0D6)"
      pattern: "--warn-soft"
  prohibitions:
    - statement: "DEBT-01: NÃO repurposar o bloco removido com planned/realized (duplicaria a linha de planejamento) nem buscar receita/despesa reais (fora de escopo). Apenas REMOVER. As linhas de groups e prevTxs permanecem inalteradas."
      status: resolved
      verification: judgment
    - statement: "DEBT-03: NÃO tocar tokens.css nem fides-studio.css (ambos já #FEF0D6, canônicos). Alterar SOMENTE as 2 definições de --warn-soft em fides.css."
      status: resolved
      verification: judgment
    - statement: "DEBT-04: A correção é SOMENTE remoção do fechar redundante. NÃO alterar addTransaction/addTransactions (gravação de dados permanece no onSave). NÃO tocar no CategoriaModal nem em outros modais."
      status: resolved
      verification: judgment
---

<objective>
Fechar 3 das 4 warnings de dívida técnica do milestone v1.0 (`v1.0-MILESTONE-AUDIT.md`) — todas mecânicas e de arquivos independentes:
- **DEBT-01 (WARN-1):** remover o bloco morto em `buildAiContext()` que lê `totals.receitas/totals.despesas` (campos que nunca existem).
- **DEBT-03 (WARN-4):** alinhar o token `--warn-soft` em `fides.css` (2 definições) de `#FEF3C7` para o canônico `#FEF0D6`.
- **DEBT-04 (WARN-2):** eliminar o `setModalOpen(false)` duplicado no caminho salvar-e-fechar (roteando o fechar por um único caminho: o `requestClose` animado).

Purpose: Zerar dívida técnica antes do ship v1.0 sem re-arquitetar — correções exatamente como diagnosticadas na auditoria (D-01, D-02, D-03 do CONTEXT).
Output: `buildAiContext()` enxuto, `--warn-soft` consistente em todo o CSS, e fechamento de modal por via única.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion-/06-CONTEXT.md
@assets/fides-orcamento.jsx
@assets/fides.css
@assets/fides-transacoes.jsx
@assets/fides-studio.jsx
</context>

<artifacts_this_phase_produces>
Nenhum símbolo público novo — fase de cleanup. Mudanças:
- `assets/fides-orcamento.jsx`: `buildAiContext()` perde o bloco `if (totals && (totals.receitas != null || totals.despesas != null)) {...}` (~11 linhas). O helper `fmtVal` permanece usado em dezenas de outros pontos — nenhum órfão.
- `assets/fides.css`: as 2 ocorrências de `--warn-soft: #FEF3C7;` viram `--warn-soft: #FEF0D6;`.
- `assets/fides-studio.jsx`: o handler `onSave` do `NovaTransacaoModal` deixa de chamar `setModalOpen(false)` (a gravação `addTransaction/addTransactions` permanece); o fechar passa a acontecer só via `requestClose` do próprio modal (que chama `onClose` → `setModalOpen(false)` uma vez).
- `assets/fides-transacoes.jsx`: nenhuma mudança estrutural obrigatória — `handleSave` já chama `onSave?.()` seguido de `requestClose()`; confirmar que o único `setModalOpen` da cadeia agora vem de `requestClose→onClose`. (Ler para confirmar; só editar se a análise indicar um segundo caminho de fechar.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-01 — remover o bloco morto receitas/despesas de buildAiContext()</name>
  <files>assets/fides-orcamento.jsx</files>
  <read_first>
    - assets/fides-orcamento.jsx:1017-1045 — a função `buildAiContext()` inteira. O bloco a REMOVER é :1019-1029: `if (totals && (totals.receitas != null || totals.despesas != null)) { ... }`, incluindo as duas `var receitas`/`var despesas` (:1020-1021) e todas as ramificações `parts.push('Receitas do mês...' / 'Despesas do mês...')` internas.
    - As linhas que DEVEM permanecer intactas logo abaixo: :1030-1040 (bloco `if (groups && groups.length > 0)` que produz `parts.push('Status do Planejamento (50·30·20): ...')`) e :1041-1043 (bloco `if (prevTxs && prevTxs.length > 0)` que produz a linha de tendência do mês anterior). O `return parts.join(' ')` (:1044) permanece.
    - `.planning/v1.0-MILESTONE-AUDIT.md` frontmatter WARNING-1: confirma `totals` só carrega `planned/realized/projection/catsWithLimit` — nunca `receitas/despesas` — então o bloco é permanentemente morto.
    - Confirmação de que `fmtVal` não fica órfão: `grep -c "fmtVal(" assets/fides-orcamento.jsx` deve continuar >1 após a remoção (helper usado em muitos outros pontos — :125, :129, :135, :182, etc.).
  </read_first>
  <action>
    Em `assets/fides-orcamento.jsx`, dentro de `buildAiContext()`, remover integralmente o bloco condicional que começa em `if (totals && (totals.receitas != null || totals.despesas != null)) {` (:1019) e termina no `}` de fechamento desse `if` (:1029). Isso inclui as declarações `var receitas`/`var despesas` e todas as ramificações `parts.push(...)` de receitas/despesas dentro dele.
    Manter `var parts = [];` no topo (:1018) e todo o restante da função inalterado: o bloco `groups` (Status do Planejamento 50·30·20), o bloco `prevTxs` (tendência do mês anterior) e o `return parts.join(' ')`.
    NÃO repurposar o bloco com `totals.planned`/`totals.realized` (duplicaria a linha de planejamento) e NÃO adicionar busca de receita/despesa reais (fora de escopo — D-01). A única ação é DELEÇÃO.
    NÃO remover nem alterar o helper `fmtVal` (:34) — permanece usado em dezenas de pontos.
  </action>
  <acceptance_criteria>
    - `grep -c "totals.receitas" assets/fides-orcamento.jsx` retorna 0 (o campo morto sumiu do arquivo).
    - `grep -c "totals.despesas" assets/fides-orcamento.jsx` retorna 0.
    - `grep -c "Receitas do mês" assets/fides-orcamento.jsx` retorna 0 e `grep -c "Despesas do mês" assets/fides-orcamento.jsx` retorna 0 (as strings do bloco morto sumiram).
    - `grep -c "Status do Planejamento" assets/fides-orcamento.jsx` retorna 1 (a linha de planejamento PERMANECE — não foi removida por engano).
    - `grep -c "histórico do mês anterior" assets/fides-orcamento.jsx` retorna 1 (a linha de tendência prevTxs PERMANECE).
    - `grep -c "fmtVal(" assets/fides-orcamento.jsx` retorna > 1 (helper não ficou órfão).
    - Revisão (judgment): `buildAiContext()` ainda retorna `parts.join(' ')` com pelo menos as linhas de planejamento + tendência quando há dados; nenhuma referência a receita/despesa reais foi introduzida.
  </acceptance_criteria>
  <verify>
    grep -nE "totals\.(receitas|despesas)|Receitas do mês|Despesas do mês|Status do Planejamento|histórico do mês anterior" assets/fides-orcamento.jsx
  </verify>
  <done>
    O bloco morto `receitas/despesas` não existe mais em `buildAiContext()`; a função continua emitindo o Status do Planejamento (50·30·20) e a linha de tendência do mês anterior; `fmtVal` segue em uso.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEBT-03 — alinhar --warn-soft para #FEF0D6 nas 2 definições de fides.css</name>
  <files>assets/fides.css</files>
  <read_first>
    - assets/fides.css:40 — `--warn-soft:  #FEF3C7;` (1ª definição, no bloco de tokens raiz).
    - assets/fides.css:102 — `--warn-soft:  #FEF3C7;` (2ª definição, provável override de tema/variante).
    - `.planning/v1.0-MILESTONE-AUDIT.md` frontmatter WARNING-4: valor canônico = `#FEF0D6` (o de `tokens.css:43` e `fides-studio.css:38`). Convergir PARA tokens.css, não a partir dele.
    - Consumidores de `--warn-soft` que NÃO devem mudar (só herdam o novo valor): :548, :678, :696, :774, :952 (`.fds-tag.warn`, `.fds-mini-tag.warn`, `.fds-budget-pct.near`, background genérico, `.fds-status.pendente`).
  </read_first>
  <action>
    Em `assets/fides.css`, trocar o valor das DUAS definições de `--warn-soft` de `#FEF3C7` para `#FEF0D6`:
    - :40 `--warn-soft:  #FEF3C7;` → `--warn-soft:  #FEF0D6;`
    - :102 `--warn-soft:  #FEF3C7;` → `--warn-soft:  #FEF0D6;`
    Preservar espaçamento/indentação exatos das linhas (só o hex muda). NÃO tocar em nenhuma linha que apenas CONSOME `var(--warn-soft)` (elas passam a herdar o novo valor automaticamente).
    NÃO editar `tokens.css` nem `fides-studio.css` (já são `#FEF0D6`, canônicos — D-02).
  </action>
  <acceptance_criteria>
    - `grep -c "#FEF3C7" assets/fides.css` retorna 0 (nenhum hex antigo sobrou no arquivo).
    - `grep -c "--warn-soft:.*#FEF0D6" assets/fides.css` retorna 2 (as duas definições agora canônicas).
    - `grep -rn "#FEF3C7" assets/*.css` (todos os CSS de assets) não retorna nenhuma linha — o valor divergente sumiu do repositório.
    - `grep -rn "\-\-warn-soft:" assets/tokens.css assets/fides-studio.css` continua mostrando `#FEF0D6` (arquivos canônicos intocados).
  </acceptance_criteria>
  <verify>
    grep -n "warn-soft" assets/fides.css && echo "--- resto do repo deve estar em FEF0D6 ---" && grep -rn "#FEF3C7" assets/ || echo "OK: nenhum FEF3C7 restante"
  </verify>
  <done>
    Ambas as definições de `--warn-soft` em `fides.css` valem `#FEF0D6`; nenhum `#FEF3C7` resta em `assets/*.css`; `tokens.css`/`fides-studio.css` inalterados.
  </done>
</task>

<task type="auto">
  <name>Task 3: DEBT-04 — eliminar o setModalOpen(false) duplicado no caminho salvar-e-fechar</name>
  <files>assets/fides-studio.jsx, assets/fides-transacoes.jsx</files>
  <read_first>
    - assets/fides-studio.jsx:72-79 — montagem do `NovaTransacaoModal`: `open={modalOpen}`, `onClose={() => setModalOpen(false)}` (:73), e o `onSave={(tx) => { ...addTransactions/addTransaction...; setModalOpen(false); }}` (:74-78). O `setModalOpen(false)` em :77 é o fechamento REDUNDANTE a remover.
    - assets/fides-transacoes.jsx:1240-1273 — `handleSave`: no ramo salvar-e-fechar (:1269-1272) chama `onSave?.(...)` (:1270) e em seguida `requestClose()` (:1271). `requestClose` (via `window.FidesUI.useModalClose`, fides-ui.jsx:247-267) roda a animação de saída e SÓ ENTÃO chama `onClose` → `setModalOpen(false)`. Logo, hoje `setModalOpen(false)` dispara 2x: uma via `onSave` (:77 do studio) e uma via `requestClose→onClose` (:73 do studio).
    - assets/fides-ui.jsx:247-267 — contrato de `requestClose`: seta `closing`, aguarda `CLOSE_MS` (~180ms) e chama `onClose()`; sob reduced-motion chama `onClose()` imediato. Ou seja, `onClose` é sempre invocado exatamente uma vez por `requestClose`.
    - `.planning/phases/04-ux-mobile-motion/04-02-PLAN.md` (padrão + armadilha child→parent re-open): a saída anima porque o fechar passa por `requestClose`; se o pai fechar por conta própria (setModalOpen(false) direto no onSave) o unmount corta a animação. Manter o fechar SÓ via requestClose preserva a saída animada e o re-open limpo.
  </read_first>
  <action>
    Corrigir o double-write roteando o fechar por um único caminho — o `requestClose` animado do próprio modal (D-03).
    Em `assets/fides-studio.jsx`, no handler `onSave` do `NovaTransacaoModal` (:74-78): REMOVER a linha `setModalOpen(false);` (:77). Manter a gravação de dados (`if (Array.isArray(tx)) addTransactions(tx); else addTransaction(tx);`). Assim o `onSave` passa a ser SÓ gravação; o fechar vem exclusivamente de `requestClose → onClose` (:73), disparando `setModalOpen(false)` uma única vez, após a animação de saída.
    Em `assets/fides-transacoes.jsx`, `handleSave` (:1269-1272): confirmar por leitura que o ramo salvar-e-fechar já chama `onSave?.(...)` + `requestClose()` — NÃO adicionar nem duplicar `setModalOpen`/close aqui. Só editar este arquivo se a leitura revelar um segundo caminho de fechar (ex.: um `onClose()` direto além do `requestClose`); nesse caso, rotear esse caminho também por `requestClose`. Caso contrário, este arquivo não muda.
    PROIBIÇÃO: NÃO alterar `addTransaction`/`addTransactions`/`transferFunds` nem a lógica de `buildTxs`/`canSave` (gravação intacta). NÃO tocar no `CategoriaModal` (:80) nem em outros modais.
  </action>
  <acceptance_criteria>
    - `grep -c "setModalOpen(false)" assets/fides-studio.jsx` retorna 1 (sobra apenas o `onClose={() => setModalOpen(false)}` em :73; o do onSave foi removido).
    - `grep -n "setModalOpen(false)" assets/fides-studio.jsx` mostra a única ocorrência dentro do `onClose` do `NovaTransacaoModal` — NÃO dentro do corpo do `onSave`.
    - `grep -n "onSave={" assets/fides-studio.jsx` seguido de inspeção (judgment): o corpo do `onSave` chama `addTransactions`/`addTransaction` mas NÃO chama `setModalOpen`.
    - `grep -c "requestClose" assets/fides-transacoes.jsx` continua ≥ 1 (o fechar animado do NovaTransacaoModal permanece intacto).
    - Revisão (judgment): nenhuma chamada de gravação de dados (`addTransaction`, `addTransactions`, `transferFunds`) foi adicionada, removida ou alterada.
    - Observável (400×512 iOS Safari): abrir "Lançar", preencher, Salvar → o modal fecha COM a animação de saída (fade-out + slide-down) e reabrir "Lançar" logo em seguida mostra o modal limpo (sem estado preso, sem flicker de duplo-fechar).
  </acceptance_criteria>
  <verify>
    grep -n "setModalOpen(false)" assets/fides-studio.jsx && grep -n "requestClose" assets/fides-transacoes.jsx
  </verify>
  <done>
    O caminho salvar-e-fechar dispara `setModalOpen(false)` uma única vez (via `requestClose→onClose`); o `onSave` só grava dados; a saída animada e o re-open limpo permanecem.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Fase de cleanup (JS morto + token CSS + roteamento de fechar). Sem mudança de dados/API/auth. Único vetor relevante: a correção de fechar NÃO pode virar um caminho de escrita de dados nem alterar a gravação existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (scope leak) | remoção do bloco em buildAiContext() | mitigate | Ação é só deleção do bloco morto; o prompt da IA continua com planejamento + tendência. Verificação por grep (Status do Planejamento = 1) + code review (judgment): nenhuma busca de receita/despesa reais introduzida. |
| T-06-04 | Tampering (scope leak) | remoção do setModalOpen redundante | mitigate | Mudança é só roteamento de fechar (remoção do close redundante do onSave). `addTransaction`/`addTransactions` inalterados. Verificação: grep (setModalOpen=1 no studio) + code review confirma gravação intacta. Encodado em `must_haves.prohibitions`. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Front é React Babel-standalone SEM bundler/npm (PROJECT.md §3 — stack imutável). Nenhum pacote instalado; sem superfície de supply-chain. |
</threat_model>

<verification>
- DEBT-01: `buildAiContext()` sem bloco receitas/despesas; grep confirma 0 ocorrências de `totals.receitas`/`totals.despesas` e 1 de `Status do Planejamento`.
- DEBT-03: `--warn-soft` = `#FEF0D6` nas 2 definições de fides.css; nenhum `#FEF3C7` em `assets/*.css`; tokens.css/fides-studio.css intocados.
- DEBT-04: `setModalOpen(false)` uma única vez no fluxo salvar-e-fechar (via requestClose→onClose); onSave só grava; saída animada e re-open limpo preservados.
- Code review confirma que nenhuma gravação de dados foi adicionada/alterada nas 3 correções.
</verification>

<success_criteria>
- 3 warnings da auditoria fechadas exatamente como diagnosticado (D-01, D-02, D-03).
- Todos os greps de acceptance passam; nenhum efeito colateral em consumidores de `--warn-soft`, no prompt da IA (planejamento + tendência mantidos) ou na gravação de transações.
</success_criteria>

<output>
Create `.planning/phases/06-fix-v1-0-tech-debt-dead-ai-context-block-metas-modal-motion-/06-01-SUMMARY.md` when done
</output>
