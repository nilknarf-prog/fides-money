---
phase: 09-transacoes-power-tools-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-studio.jsx
  - assets/fides-studio.css
autonomous: true
requirements: [TX-07]
must_haves:
  truths:
    - "Cmd+K (Mac) / Ctrl+K (Windows) abre o command palette de qualquer página do studio"
    - "Digitar um termo filtra transações/contas/categorias em memória e mostra resultados"
    - "Escolher um resultado navega para a página correspondente via setActive e fecha o palette"
    - "O input readOnly morto do masthead deixa de ser decorativo e passa a abrir o palette ao clicar"
  artifacts:
    - path: "assets/fides-studio.jsx"
      provides: "Componente CommandPalette + estado paletteOpen em FidesStudioShell + listener global keydown + prop onOpenSearch no StudioMasthead"
      contains: "CommandPalette"
    - path: "assets/fides-studio.css"
      provides: "Estilo do overlay do command palette"
  key_links:
    - from: "assets/fides-studio.jsx (FidesStudioShell)"
      to: "assets/fides-studio.jsx (setActive)"
      via: "CommandPalette recebe onNav={setActive} e navega ao escolher resultado"
      pattern: "onNav"
---

<objective>
Dar vida ao afordance ⌘K que já existe e está morto no masthead (input `readOnly` com `<kbd>⌘K</kbd>`, fides-studio.jsx:481-485). Montar um componente `CommandPalette` novo em `FidesStudioShell`, com atalho global de teclado (Cmd/Ctrl+K) e busca em memória sobre `transactions`/`accounts`/`cards`/`categories` (dados já no contexto, zero query nova), navegando entre as 6 páginas via `setActive`.

Purpose: TX-07 absorve UX-01/B7. O afordance visual foi desenhado antes desta fase — só falta o comportamento. Client-only, sem dependência externa (componente próprio ~100 linhas, não `cmdk`).
Output: `CommandPalette` funcional montado como irmão de `NovaTransacaoModal`/`CategoriaModal`, aberto por Cmd/Ctrl+K e por clique no input do masthead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transacoes-power-tools-analytics/09-RESEARCH.md
@.planning/phases/09-transacoes-power-tools-analytics/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: componente CommandPalette (listener global + busca em memória + navegação)</name>
  <files>assets/fides-studio.jsx, assets/fides-studio.css</files>
  <read_first>
    - assets/fides-studio.jsx linhas 40-85 (`FidesStudioShell`: `active`/`setActive` linha 41, `modalOpen` 43, montagem de `NovaTransacaoModal`/`CategoriaModal` 72-79, lista das 6 páginas válidas linha 69)
    - assets/fides-studio.jsx linhas 312-313 e 480-485 (`StudioMasthead` props atuais e o afordance de busca morto)
    - assets/fides-ui.jsx linhas 120-128 (padrão de `document.addEventListener('keydown', ...)` com cleanup no `useEffect`)
    - assets/fides-transacoes.jsx linhas 213-222 (idioma de busca em memória a replicar: filtrar por desc/val/categoria com `.toLowerCase().indexOf(q) >= 0`)
    - assets/fides-store.jsx linhas 1342-1379 (o que `useFides()` expõe: `transactions`, `accounts`, `cards`, `categories`, `goals`)
    - 09-PATTERNS.md secção "TX-07"
  </read_first>
  <action>
    Criar um componente novo `CommandPalette({ open, onClose, onNav })` em assets/fides-studio.jsx (function de módulo, mesmo escopo de `StudioMasthead`). Ele:

    1. Retorna `null` quando `!open` (gate simples de render; declarar quaisquer hooks ANTES desse return para respeitar Rules of Hooks — lembrete do bug da Phase 07 em STATE.md).
    2. Consome `useFides()` para `transactions`, `accounts`, `cards`, `categories` — sem query nova.
    3. Mantém um estado local `query` (`React.useState('')`) com `<input>` controlado (`value`/`onChange`), autofocado ao abrir. Renderizar o texto dos resultados como `{children}` JSX puro — NUNCA `dangerouslySetInnerHTML` (React auto-escapa; ver threat_model).
    4. Deriva resultados via `React.useMemo` sobre `query`: buscar transações pelo idioma de fides-transacoes.jsx:213-222 (desc/val/categoria), e itens de navegação para contas/cartões/categorias cujo nome/label casa com a query. Cada resultado carrega a página-alvo (`dashboard|transacoes|orcamento|contas|metas|perfil`).
    5. Ao escolher um resultado (clique ou Enter no item ativo), chamar `onNav(page)` e depois `onClose()`.
    6. Fechar com Escape e com clique no backdrop (mesmo padrão dos modais existentes).

    Reusar o shell visual dos modais existentes (`fds-modal-backdrop`/`fds-modal`, já estilizados em fides.css) para o overlay, adicionando em assets/fides-studio.css apenas as classes específicas do palette (ex.: `.stu-cmdk`, `.stu-cmdk-input`, `.stu-cmdk-item`, `.stu-cmdk-item.on`) — lista de resultados, item ativo destacado, empty state "Nenhum resultado". Não introduzir animação acima de 300ms sem GPU hint (convenção de perf da Phase 03).
  </action>
  <acceptance_criteria>
    - assets/fides-studio.jsx contém `function CommandPalette(` recebendo `{ open, onClose, onNav }`
    - O componente NÃO usa `dangerouslySetInnerHTML` em nenhum ponto (input controlado + JSX escapado)
    - Todos os `React.useState`/`React.useMemo`/`React.useEffect` do componente são declarados antes de qualquer `return` condicional
    - A busca filtra `transactions` pelo mesmo predicado de fides-transacoes.jsx:213-222 (desc/val/categoria) e inclui contas/cartões/categorias como itens navegáveis
    - assets/fides-studio.css contém as classes do palette (ex.: `.stu-cmdk`) e um empty state visível quando não há resultado
  </acceptance_criteria>
  <verify>
    Recarregar o app. Abrir manualmente o palette (via Task 2). Digitar o nome de uma transação existente → o resultado aparece. Digitar o nome de uma categoria/conta → aparece como item navegável. Escolher um resultado de navegação → a página troca. Verificar no DevTools que o texto do resultado é renderizado escapado (colar uma descrição com `<b>` numa transação de teste e confirmar que aparece literal, não como HTML).
  </verify>
  <done>
    `CommandPalette` renderiza resultados de busca em memória sem query nova, navega via `onNav`, e é seguro contra XSS (React auto-escape, sem innerHTML).
  </done>
</task>

<task type="auto">
  <name>Task 2: montar palette em FidesStudioShell + atalho Cmd/Ctrl+K + ligar input do masthead</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx linhas 40-85 (`FidesStudioShell` — onde adicionar `paletteOpen`, o `useEffect` do keydown, e montar `<CommandPalette/>` junto aos outros modais 72-79)
    - assets/fides-studio.jsx linhas 312-313 (assinatura `StudioMasthead({ onAdd, onGear, active })` — adicionar prop `onOpenSearch`)
    - assets/fides-studio.jsx linhas 480-485 (o input `readOnly` a trocar por `onClick={onOpenSearch}`)
    - assets/fides-ui.jsx linhas 120-128 (cleanup do listener)
  </read_first>
  <action>
    Em `FidesStudioShell` (fides-studio.jsx:40-85):
    1. Adicionar estado `const [paletteOpen, setPaletteOpen] = React.useState(false);` junto aos outros useState (linhas 41-43).
    2. Adicionar um `React.useEffect(() => {...}, [])` com listener global em `document` para `keydown`: quando `(e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k'`, chamar `e.preventDefault()` e `setPaletteOpen(true)`. Retornar cleanup `removeEventListener` (padrão fides-ui.jsx:120-128). Deps `[]`.
    3. Passar `onOpenSearch={() => setPaletteOpen(true)}` ao `<StudioMasthead .../>` (linha 62).
    4. Montar `<CommandPalette open={paletteOpen} onClose={() => setPaletteOpen(false)} onNav={setActive}/>` como irmão de `NovaTransacaoModal`/`CategoriaModal` (dentro do return de `FidesStudioShell`, junto às linhas 72-79).

    Em `StudioMasthead` (fides-studio.jsx:312): adicionar `onOpenSearch` à desestruturação de props. Na linha 481-485, trocar o `<input ... readOnly/>` para disparar o palette: `onClick={onOpenSearch}` no wrapper `.stu-mast-search` (ou no input, mantendo `readOnly` para não abrir teclado nativo — o clique abre o palette). Manter o `<kbd>⌘K</kbd>` visual.
  </action>
  <acceptance_criteria>
    - `FidesStudioShell` declara `paletteOpen`/`setPaletteOpen` e um `useEffect` que registra e remove (cleanup) um listener `keydown` em `document`
    - O predicado do atalho é `(e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k'` com `e.preventDefault()`
    - `<CommandPalette open={paletteOpen} onClose={...} onNav={setActive}/>` está montado dentro de `FidesStudioShell`
    - `StudioMasthead` recebe e usa `onOpenSearch`; o input `.stu-mast-search` abre o palette ao clicar (não fica mais inerte)
    - O listener é removido no cleanup (sem leak entre remounts)
  </acceptance_criteria>
  <verify>
    Recarregar o app em qualquer página. Pressionar Cmd+K (Mac) ou Ctrl+K (Windows) → o palette abre. Pressionar Escape → fecha. Clicar no campo "Buscar transações, contas, categorias…" do topo → o palette abre. Confirmar que o atalho funciona a partir de Dashboard, Transações e Metas (listener global). Console sem erro após navegar entre páginas várias vezes (sem leak de listener).
  </verify>
  <done>
    Cmd/Ctrl+K abre o palette de qualquer página, o input do masthead deixa de ser decorativo, e o listener é limpo corretamente no unmount.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input do usuário (query de busca) → DOM | Texto digitado e texto de transações/categorias são renderizados na lista de resultados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-XSS | Tampering / Information Disclosure | `CommandPalette` (render de resultados) | mitigate | React auto-escapa `{t.desc}`/labels em JSX; PROIBIDO `dangerouslySetInnerHTML` no palette; input controlado (`value`/`onChange`), mesmo padrão do campo de busca de fides-transacoes.jsx:645-650 |
| T-09-02 | Information Disclosure | busca em memória | accept | Palette só lê `transactions`/`accounts`/`cards` do próprio usuário (já sob RLS via contexto); nenhuma query nova |
| T-09-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo — componente próprio, sem `cmdk` nem CDN novo (09-RESEARCH "Alternatives Considered") |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 09-02)

Símbolos NOVOS criados por este plano:
- `CommandPalette({ open, onClose, onNav })` — componente novo em `assets/fides-studio.jsx`
- `paletteOpen`/`setPaletteOpen` — estado em `FidesStudioShell`
- `onOpenSearch` — nova prop de `StudioMasthead`
- `useEffect` de listener global `keydown` (Cmd/Ctrl+K) em `FidesStudioShell`
- Classes CSS novas em `assets/fides-studio.css` (ex.: `.stu-cmdk`, `.stu-cmdk-input`, `.stu-cmdk-item`)
</artifacts_produced>

<verification>
- Cmd/Ctrl+K abre o palette de qualquer página; Escape/backdrop fecham.
- Busca filtra em memória (sem query nova); navegação via `setActive` funciona.
- Sem `dangerouslySetInnerHTML`; texto de resultados escapado por React.
- Listener global removido no cleanup (sem leak).
</verification>

<success_criteria>
O afordance ⌘K deixa de ser morto: atalho global + clique no input abrem um palette funcional que busca dados já em memória e navega entre páginas, sem dependência nova e sem superfície de XSS.
</success_criteria>

<output>
Create `.planning/phases/09-transacoes-power-tools-analytics/09-02-SUMMARY.md` when done
</output>
