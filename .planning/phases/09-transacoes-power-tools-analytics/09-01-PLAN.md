---
phase: 09-transacoes-power-tools-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-store.jsx
autonomous: true
requirements: [TX-03, TX-04]
must_haves:
  truths:
    - "spendByCategoryRange(fromYM, toYM) retorna gasto por categoria agregado no range, excluindo is_transfer"
    - "rangeTransactions(fromYM, toYM) retorna transações de múltiplos meses (superset de monthTransactions), sem filtrar is_transfer"
    - "spendByCategory e monthTransactions permanecem inalterados — Dashboard e fides-claude.jsx continuam funcionando"
  artifacts:
    - path: "assets/fides-store.jsx"
      provides: "monthsInRange, spendByCategoryRange, rangeTransactions + exposição no provider e no fallback (TX-03/TX-04 foundation)"
      contains: "spendByCategoryRange"
  key_links:
    - from: "assets/fides-store.jsx (value do provider)"
      to: "assets/fides-transacoes.jsx (Transacoes via useFides)"
      via: "spendByCategoryRange e rangeTransactions expostos no context value"
      pattern: "spendByCategoryRange"
---

<objective>
Criar a fundação de analytics cross-month no store: as derivações `spendByCategoryRange` e `rangeTransactions` que reprocessam o array `transactions` (já 100% em memória) sobre um range de meses, sem novas queries Supabase. Esta é a base que a Wave 2 (Plan 09-04) consome para desbloquear a lista em modo range (TX-04) e o widget de gasto por categoria cross-month (TX-03).

Purpose: hoje `spendByCategory` trava em `monthTransactions` (mês único) e é consumido por `DashboardStudio` e `fides-claude.jsx` — não pode mudar de assinatura. A solução é adicionar derivações paralelas, nunca editar as existentes.
Output: `assets/fides-store.jsx` com `monthsInRange`, `spendByCategoryRange`, `rangeTransactions` expostos no context value e no fallback de `useFides()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transacoes-power-tools-analytics/09-RESEARCH.md
@.planning/phases/09-transacoes-power-tools-analytics/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: helper monthsInRange + derivações spendByCategoryRange e rangeTransactions</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx linhas 1047-1200 (bloco "Month helpers" com `prevMonth`/`monthLabel`; `monthTransactions` em 1063-1065; `spendByCategory` em 1106-1115 — molde a NÃO alterar; `categoryUsage` 1120-1153; `budgetGroups` 1155-1182)
    - assets/fides-data.jsx linha 47 (`txMonth` global = `t.mes || derivado de t.d` — usar para membership de mês, consistente com `monthTransactions`)
    - assets/fides-store.jsx linha 1109 (idioma de exclusão `t.val < 0 && !t.isTransfer` a replicar exatamente)
    - 09-PATTERNS.md secção "TX-03/TX-04" e 09-RESEARCH.md "Pattern 3"
  </read_first>
  <action>
    Adicionar, no bloco "Month helpers" de assets/fides-store.jsx (junto a `prevMonth`/`monthLabel`, ~linha 1047-1059), uma função pura de módulo `monthsInRange(fromYM, toYM)` que retorna um array de strings `YYYY-MM` inclusivo de `fromYM` até `toYM` (iterar incrementando mês, virar ano em m>12), seguindo o exemplo de 09-RESEARCH.md "Code Examples". Não usar libs de data — só split/parse manual como `prevMonth` já faz.

    Adicionar, imediatamente ABAIXO de `spendByCategory` (após a linha 1115, sem tocar em `spendByCategory`), a derivação `spendByCategoryRange` como `React.useCallback((fromYM, toYM) => {...}, [transactions, categories])`: construir `const months = new Set(monthsInRange(fromYM, toYM))`, iterar `transactions` (o array COMPLETO, não `monthTransactions`), acumular por categoria apenas quando `t.val < 0 && !t.isTransfer && months.has(txMonth(t))`, e retornar o MESMO shape de `spendByCategory` — array de `{ key, val, label, tint, emoji }` ordenado por `val` desc, com fallback `categories[key] || { label: key, tint: '#888', emoji: '🏷️' }`.

    Adicionar, junto a `monthTransactions` (~linha 1063), a derivação `rangeTransactions` como `React.useCallback((fromYM, toYM) => {...}, [transactions])`: `const months = new Set(monthsInRange(fromYM, toYM)); return transactions.filter(t => months.has(txMonth(t)));` — SEM filtrar `is_transfer` nem `val` (a lista mostra tudo, incluindo movimentações com badge, igual a `monthTransactions`). Usar `txMonth(t)` (não `t.mes` cru) para membership, garantindo que compras de cartão caiam no mês de fatura correto (mesma semântica de `monthTransactions`).
  </action>
  <acceptance_criteria>
    - assets/fides-store.jsx contém `function monthsInRange(` (ou `const monthsInRange =`) no bloco Month helpers, retornando array de `YYYY-MM`
    - assets/fides-store.jsx contém `const spendByCategoryRange = React.useCallback(` com deps `[transactions, categories]`, iterando `transactions` (não `monthTransactions`) e com o predicado `t.val < 0 && !t.isTransfer && months.has(txMonth(t))`
    - assets/fides-store.jsx contém `const rangeTransactions = React.useCallback(` com deps `[transactions]`, filtrando por `months.has(txMonth(t))` e SEM excluir `is_transfer`
    - `spendByCategory` (1106-1115) e `monthTransactions` (1063-1065) permanecem byte-idênticos (não editados)
    - `spendByCategoryRange` retorna o mesmo shape `{ key, val, label, tint, emoji }` de `spendByCategory` (reuso direto no `Donut`/`CategoryChart`)
  </acceptance_criteria>
  <verify>
    Recarregar o app no browser (client-only, sem deploy). Console sem erros de parse/Babel. No console do DevTools, com o provider montado, confirmar que as novas derivações existem e agregam corretamente: escolher um range de 2-3 meses conhecidos e conferir que a soma de `spendByCategoryRange(from, to)` para uma categoria bate com a soma manual das despesas daquela categoria nesses meses, e que pagamentos de fatura (is_transfer) NÃO entram na soma. Confirmar que o Dashboard (donut, top5) continua idêntico ao anterior (spendByCategory intacto).
  </verify>
  <done>
    `monthsInRange`, `spendByCategoryRange` e `rangeTransactions` existem em fides-store.jsx, agregam sobre `transactions` completo com semântica correta de `is_transfer` e `txMonth`, e as derivações antigas ficam intactas.
  </done>
</task>

<task type="auto">
  <name>Task 2: expor spendByCategoryRange e rangeTransactions no provider e no fallback</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx linhas 1340-1425 (objeto `value` do provider — `spendByCategory` exposto ~linha 1371, `monthTransactions` ~linha 1370; fallback de `useFides()` 1392-1424 com `spendByCategory: SPEND_BY_CATEGORY` ~linha 1414 e `monthTransactions: TRANSACTIONS` ~linha 1399)
  </read_first>
  <action>
    No objeto `value` do provider (~linha 1369-1371, secção "Derived"), adicionar `spendByCategoryRange` e `rangeTransactions` à lista exposta, ao lado de `spendByCategory`/`monthTransactions` — sem remover nenhum campo existente.

    No fallback de `useFides()` (o objeto retornado quando não há provider, ~linhas 1398-1420), adicionar entradas seguras no-op com o mesmo shape de retorno: `spendByCategoryRange: () => [],` e `rangeTransactions: () => [],`. Isso mantém os dashboards de referência V3 sem provider funcionando (mesmo motivo dos outros no-ops do fallback).
  </action>
  <acceptance_criteria>
    - O objeto `value` do provider inclui `spendByCategoryRange` e `rangeTransactions` (verificável por leitura da secção "Derived" ~1369-1371)
    - O fallback de `useFides()` inclui `spendByCategoryRange: () => []` e `rangeTransactions: () => []`
    - `useFides().spendByCategoryRange` e `useFides().rangeTransactions` são funções chamáveis tanto com provider (dados reais) quanto sem (retorno vazio)
  </acceptance_criteria>
  <verify>
    Recarregar o app. No DevTools, com o provider ativo, confirmar que `useFides().spendByCategoryRange` e `useFides().rangeTransactions` são funções e retornam, respectivamente, array de `{key,val,...}` e array de transações ao receber `(fromYM, toYM)`. App carrega sem erro no console.
  </verify>
  <done>
    Ambas as derivações são consumíveis via `useFides()` por qualquer componente (pronto para o Plan 09-04), com fallback no-op seguro.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → cliente (já existente) | `transactions` já vem filtrado por `user_id` sob RLS em `refreshData`; esta fase só reprocessa em memória o que já foi autorizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `spendByCategoryRange`/`rangeTransactions` | accept | Agregam apenas `transactions` do próprio usuário (já sob RLS existente); nenhuma nova query, nenhum dado de outro usuário acessível. V4 não aplicável (sem novo endpoint) |
| T-09-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo nesta fase (client-only, zero deps — 09-RESEARCH "Package Legitimacy Audit: N/A") |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 09-01)

Símbolos NOVOS criados por este plano (excluir da verificação de drift — são criações, não referências pré-existentes):
- `monthsInRange(fromYM, toYM)` — função pura de módulo em `assets/fides-store.jsx`
- `spendByCategoryRange` — `React.useCallback((fromYM, toYM) => [...])` em `assets/fides-store.jsx`
- `rangeTransactions` — `React.useCallback((fromYM, toYM) => [...])` em `assets/fides-store.jsx`
- Novas chaves no context `value` e no fallback de `useFides()`: `spendByCategoryRange`, `rangeTransactions`
</artifacts_produced>

<verification>
- App carrega sem erro de console após reload (Babel-standalone in-browser).
- `spendByCategory` e `monthTransactions` inalterados (Dashboard e IA idênticos ao pré-fase).
- `spendByCategoryRange(from,to)` exclui `is_transfer`; `rangeTransactions(from,to)` inclui movimentações.
</verification>

<success_criteria>
As três derivações existem, seguem os padrões locais (useCallback com deps explícitas, exclusão de is_transfer só nas somas), e ficam expostas via `useFides()` sem quebrar consumidores existentes.
</success_criteria>

<output>
Create `.planning/phases/09-transacoes-power-tools-analytics/09-01-SUMMARY.md` when done
</output>
