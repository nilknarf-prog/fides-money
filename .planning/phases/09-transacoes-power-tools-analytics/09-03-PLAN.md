---
phase: 09-transacoes-power-tools-analytics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-transacoes.jsx
  - assets/fides-transacoes.css
autonomous: true
requirements: [TX-01, TX-02]
must_haves:
  truths:
    - "No filtro avançado, Contas e Cartões aparecem em subseções separadas e rotuladas"
    - "Marcar só cartões faz a lista mostrar apenas transações cujo acctInfo.kind === 'cartao'"
    - "A lista pagina em blocos de 20/50/100 conforme o seletor, com navegação de página"
    - "Trocar filtro/sort/mês/tamanho de página reseta para a primeira página"
    - "Os totais de grupo (ordenar por categoria/conta) refletem só a página atual — sinalizado na UI"
  artifacts:
    - path: "assets/fides-transacoes.jsx"
      provides: "Subseções Contas/Cartões em TxAdvFiltersModal (TX-01) + pagedSorted/pageSize/page + seletor 20/50/100 e navegação (TX-02)"
      contains: "pagedSorted"
    - path: "assets/fides-transacoes.css"
      provides: "Estilo do seletor de quantidade e da navegação de página"
  key_links:
    - from: "assets/fides-transacoes.jsx (grouped)"
      to: "assets/fides-transacoes.jsx (pagedSorted)"
      via: "grouped passa a agrupar pagedSorted em vez de sorted"
      pattern: "pagedSorted"
---

<objective>
Duas power-tools na tela de Transações, ambas em `fides-transacoes.jsx`: (TX-01) separar visualmente Contas e Cartões no `TxAdvFiltersModal` para isolar transações de cartão de crédito; (TX-02) paginação client-side com seletor de 20/50/100 e navegação de página, já que hoje `grouped` renderiza tudo. Ambas puramente em memória — os dados já estão 100% carregados.

Purpose: perf (não renderizar centenas de linhas de uma vez) + paridade com o PlannerFin + poder isolar gastos de cartão. Nenhuma mudança de modelo de dados: os IDs de conta e cartão já não colidem e `acctNameOf()` já distingue `kind`.
Output: `fides-transacoes.jsx` com subseções Contas/Cartões no modal de filtros e uma cadeia `sorted → pagedSorted → grouped` com controles de página.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-transacoes-power-tools-analytics/09-RESEARCH.md
@.planning/phases/09-transacoes-power-tools-analytics/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TX-01 — subseções Contas e Cartões no TxAdvFiltersModal</name>
  <files>assets/fides-transacoes.jsx, assets/fides-transacoes.css</files>
  <read_first>
    - assets/fides-transacoes.jsx linhas 37-43 (`acctNameOf` — distingue `kind: 'conta'` vs `'cartao'`, reusar sem alterar)
    - assets/fides-transacoes.jsx linhas 46-158 (`TxAdvFiltersModal` inteiro: props linha 46, `toggleAcct` 62-67, `allAccts` linha 75, seção "Contas e cartões" 106-121)
    - assets/fides-transacoes.jsx linhas 229-233 (aplicação do filtro `advFilters.contasSelected` sobre `t.acct` — o array já é compartilhado; NÃO precisa mudar essa lógica)
    - 09-PATTERNS.md secção "TX-01"
  </read_first>
  <action>
    Em `TxAdvFiltersModal`, dividir a seção única "Contas e cartões" (linhas 106-121) em DUAS subseções rotuladas — "Contas" e "Cartões" — mantendo o MESMO array `draft.contasSelected` e a MESMA função `toggleAcct` (os IDs não colidem entre `accounts` e `cards`, então a lógica de dados não muda). Em vez de `allAccts = accounts.concat(cards)`, iterar `accounts` na subseção "Contas" e `cards` na subseção "Cartões". Cada chip usa `a.color` para o dot (cartões caem em `var(--ink-3)` quando sem cor, como `acctNameOf` já faz).

    Adicionar, no cabeçalho da subseção "Cartões", um atalho "Selecionar todos os cartões" que faz `setDraft(Object.assign({}, draft, { contasSelected: <união de contasSelected atual com cards.map(c => c.id)> }))` — reusando o idioma de `toggleAcct`, sem duplicar IDs. Se todos os cartões já estiverem selecionados, o atalho os desmarca (toggle de grupo).

    Adicionar em assets/fides-transacoes.css o estilo mínimo do rótulo/atalho da subseção Cartões (pode reusar `.fds-tx-adv-label`/`.fds-tx-adv-chips` existentes; só o botão de atalho precisa de classe nova, ex.: `.fds-tx-adv-selall`).
  </action>
  <acceptance_criteria>
    - `TxAdvFiltersModal` renderiza duas subseções `<section className="fds-tx-adv-section">` distintas com `<h3>` "Contas" e "Cartões"
    - A subseção "Contas" itera `accounts`; a subseção "Cartões" itera `cards`
    - Ambas escrevem/leem o mesmo `draft.contasSelected` via `toggleAcct` (nenhum novo array de estado no modal)
    - Existe um atalho "Selecionar todos os cartões" que popula `contasSelected` com `cards.map(c => c.id)` (união, sem duplicar) e desmarca quando todos já estão marcados
    - Aplicar o filtro com só cartões marcados faz a lista mostrar apenas transações cujo `acctNameOf(t.acct,...).kind === 'cartao'`
  </acceptance_criteria>
  <verify>
    Recarregar o app → Transações → abrir "Filtros avançados". Confirmar duas subseções separadas: "Contas" e "Cartões". Marcar só um ou dois cartões → Aplicar → conferir que a lista mostra apenas transações desses cartões (nenhuma transação de conta corrente). Usar "Selecionar todos os cartões" → confirma que marca todos os cartões de uma vez; clicar de novo → desmarca. Marcar uma conta e um cartão juntos → ambos filtram como esperado.
  </verify>
  <done>
    O filtro avançado separa Contas de Cartões visualmente, com atalho de grupo, sem mudar o modelo de dados (mesmo `contasSelected`), e isola transações de cartão corretamente.
  </done>
</task>

<task type="auto">
  <name>Task 2: TX-02 — paginação client-side (pagedSorted) sobre sorted, antes de grouped</name>
  <files>assets/fides-transacoes.jsx</files>
  <read_first>
    - assets/fides-transacoes.jsx linhas 163-186 (bloco de `useState` de `Transacoes` — onde adicionar `pageSize`/`page`, no MESMO nível, antes do primeiro `return` condicional na linha 662)
    - assets/fides-transacoes.jsx linhas 244-279 (`sorted`) e 282-311 (`grouped` — passa a agrupar `pagedSorted`)
    - assets/fides-transacoes.jsx linhas 474-492 (`toggleSelectAll`/`selTxs` operam sobre `sorted` inteiro — decisão de escopo de seleção)
    - 09-PATTERNS.md secção "TX-02"; 09-RESEARCH.md Pitfall 1 e Pitfall 2
  </read_first>
  <action>
    No bloco de `useState` de `Transacoes` (linhas 163-186, antes de qualquer `return`), adicionar `const [pageSize, setPageSize] = React.useState(20);` (valores permitidos 20|50|100) e `const [page, setPage] = React.useState(0);`.

    Adicionar `React.useEffect(function() { setPage(0); }, [filtered, sortBy, sortOrder, pageSize]);` para resetar a página ao mudar filtro/sort/tamanho (Open Question 2 do research: resetar em mudança de `filtered`).

    Inserir, ENTRE `sorted` (linha 244) e `grouped` (linha 282), `const pagedSorted = React.useMemo(function() { return sorted.slice(page * pageSize, (page + 1) * pageSize); }, [sorted, page, pageSize]);`. Trocar a entrada de `grouped` de `sorted` para `pagedSorted` (agrupar a fatia paginada — NUNCA paginar depois de agrupar, senão os totais de grupo quebram: Pitfall 1). Manter as deps de `grouped` coerentes (`pagedSorted` no lugar de `sorted`).

    NÃO alterar `toggleSelectAll`/`selTxs` (linhas 474-492): "selecionar todas" segue operando sobre `sorted` inteiro (bulk-action mais útil) — mas isso será sinalizado na UI na Task 3 (Pitfall 2). Usar o estilo funcional ES5 (`function() {...}`) do arquivo, não arrow terse, para consistência local.
  </action>
  <acceptance_criteria>
    - `Transacoes` declara `pageSize` (default 20) e `page` (default 0) no mesmo bloco de useState, antes de qualquer `return` condicional (Rules of Hooks)
    - Existe `const pagedSorted = React.useMemo(` fatiando `sorted.slice(page * pageSize, (page + 1) * pageSize)` com deps `[sorted, page, pageSize]`
    - `grouped` passa a receber/iterar `pagedSorted` (não `sorted`)
    - Um `useEffect` reseta `page` para 0 quando `filtered`/`sortBy`/`sortOrder`/`pageSize` mudam
    - `toggleSelectAll`/`selTxs` permanecem sobre `sorted` (não regridem)
  </acceptance_criteria>
  <verify>
    Recarregar → Transações num mês com mais de 20 lançamentos. Confirmar que só ~20 linhas renderizam por vez. Trocar sort para "Categoria" e conferir que os cabeçalhos de grupo somam apenas os itens da página atual (comportamento esperado, será rotulado na Task 3). Mudar filtro/sort → a lista volta para a primeira página.
  </verify>
  <done>
    A lista pagina sobre `sorted` (antes do agrupamento), reseta corretamente, e não quebra a semântica de grupo (agrupamento roda só na fatia).
  </done>
</task>

<task type="auto">
  <name>Task 3: TX-02 — seletor 20/50/100, navegação de página e sinalização de escopo</name>
  <files>assets/fides-transacoes.jsx, assets/fides-transacoes.css</files>
  <read_first>
    - assets/fides-transacoes.jsx linhas 595-615 (idioma de pills `fds-tx-v2-sort-pill` a copiar para o seletor 20/50/100)
    - assets/fides-transacoes.jsx linhas 684-726 (cabeçalho da lista: contador `fds-tx-v2-list-count` linhas 715-717 — reusar para "X de Y" e escopo de página)
    - assets/fides-transacoes.jsx linhas 662-682 (empty state — não regredir)
    - assets/fides-transacoes.css (classes `fds-tx-v2-sort*` como referência de estilo)
  </read_first>
  <action>
    Adicionar um seletor de quantidade por página (20 | 50 | 100) reusando o idioma de pills de `fds-tx-v2-sort` (linhas 595-615) — ex.: uma linha "Itens por página" com três pills que chamam `setPageSize(n)`; o pill ativo reflete `pageSize`.

    Adicionar navegação de página no rodapé/cabeçalho da lista: botões Anterior/Próxima que fazem `setPage(p => p-1)` / `setPage(p => p+1)`, desabilitados nos limites, e um indicador "Página N de M" onde `M = Math.max(1, Math.ceil(sorted.length / pageSize))`. Basear o total em `sorted.length` (todos os itens do filtro), não em `pagedSorted`.

    Atualizar o contador `fds-tx-v2-list-count` (linhas 715-717) para deixar explícito o escopo de exibição: mostrar quantas estão visíveis nesta página vs. total do filtro (ex.: "mostrando 20 de 340"). Quando o sort for por categoria/conta E houver mais de uma página, exibir uma nota curta de que os totais de grupo refletem só a página atual (Pitfall 1). Quando houver seleção ativa com paginação, o contador de seleção deve deixar claro que "selecionar todas" cobre o filtro inteiro, não só a página (Pitfall 2).

    Adicionar em assets/fides-transacoes.css as classes novas do seletor de quantidade e dos botões de navegação (ex.: `.fds-tx-v2-pagesize`, `.fds-tx-v2-pagenav`), reusando tokens de cor/spacing existentes.
  </action>
  <acceptance_criteria>
    - Existe um seletor com três opções (20/50/100) que chama `setPageSize` e destaca o valor ativo
    - Existem controles Anterior/Próxima que mudam `page`, desabilitados no início/fim, com indicador "Página N de M" derivado de `sorted.length` e `pageSize`
    - O contador de lista mostra "visíveis nesta página" vs. total do filtro
    - Com sort por categoria/conta e múltiplas páginas, há nota visível de que os totais de grupo são da página atual
    - assets/fides-transacoes.css contém as classes novas (ex.: `.fds-tx-v2-pagesize`, `.fds-tx-v2-pagenav`); o empty state anterior continua funcionando quando `sorted.length === 0`
  </acceptance_criteria>
  <verify>
    Recarregar → Transações num mês com >100 lançamentos. Trocar o seletor entre 20/50/100 → a contagem de linhas renderizadas muda de acordo. Usar Anterior/Próxima → a página avança e volta; "Página N de M" atualiza; botões desabilitam nos limites. Ordenar por Categoria com 2+ páginas → confirmar a nota de escopo de grupo. Selecionar "todas" com paginação ativa → o texto deixa claro que cobre o filtro inteiro.
  </verify>
  <done>
    Usuário controla o tamanho da página e navega entre páginas, com contadores e avisos de escopo corretos, sem regressão do empty state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filtro/seleção do usuário → render da lista | Interações puramente client-side sobre `transactions` já carregado sob RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering (integridade de UI) | paginação vs. seleção em massa | mitigate | "Selecionar todas" cobre `sorted` inteiro (não só a página) — sinalizado explicitamente na UI para evitar exclusão acidental além do visível (Pitfall 2) |
| T-09-04 | Information Disclosure | filtro Cartões | accept | Só reordena/filtra dados do próprio usuário (RLS existente); nenhum dado novo exposto |
| T-09-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo — `Array.slice` nativo (09-RESEARCH "Standard Stack") |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 09-03)

Símbolos NOVOS criados por este plano:
- Subseções "Contas"/"Cartões" e atalho "Selecionar todos os cartões" em `TxAdvFiltersModal`
- `pageSize`/`setPageSize`, `page`/`setPage` — estado em `Transacoes`
- `pagedSorted` — `React.useMemo` entre `sorted` e `grouped`
- `useEffect` de reset de página em mudança de filtro/sort/pageSize
- Classes CSS novas em `assets/fides-transacoes.css` (ex.: `.fds-tx-adv-selall`, `.fds-tx-v2-pagesize`, `.fds-tx-v2-pagenav`)
</artifacts_produced>

<verification>
- Filtro Cartões isola transações `kind === 'cartao'`; atalho de grupo funciona.
- Lista pagina 20/50/100; navegação e contadores corretos; reset ao mudar filtro/sort.
- Totais de grupo (categoria/conta) rotulados como "página atual"; seleção-em-massa sinalizada.
- Nenhum hook após `return` condicional (Rules of Hooks).
</verification>

<success_criteria>
Transações ganha isolamento de cartões no filtro avançado e paginação real com seletor de quantidade, sem mudar o modelo de dados e sem quebrar seleção em massa nem totais de grupo.
</success_criteria>

<output>
Create `.planning/phases/09-transacoes-power-tools-analytics/09-03-SUMMARY.md` when done
</output>
