---
phase: 07-crud-metas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
autonomous: false
requirements: [META-01, META-02, META-03, META-04]
tags: [supabase, migration, schema, goals]
user_setup: []

must_haves:
  truths:
    - "A tabela live public.goals possui a coluna target_date DATE nullable"
    - "A tabela live public.goals possui a coluna description text nullable"
    - "O espelho supabase/schema.sql reflete as duas colunas novas"
    - "Metas existentes continuam legíveis (colunas nullable, sem backfill obrigatório)"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Espelho da tabela goals com target_date + description"
      contains: "target_date"
  key_links:
    - from: "MCP apply_migration"
      to: "public.goals (live)"
      via: "ALTER TABLE ... ADD COLUMN target_date DATE / description text"
      pattern: "add column target_date"
    - from: "supabase/schema.sql"
      to: "public.goals (live)"
      via: "espelho manual das colunas novas (regra PROJECT.md §4)"
      pattern: "target_date"
---

<objective>
[BLOCKING] Adicionar duas colunas nullable à tabela `public.goals` — `target_date DATE` (prazo da meta, D-01/D-02/D-03) e `description text` (descrição, D-10) — via Supabase MCP `apply_migration` (que executa o DDL direto no banco live), depois espelhar em `supabase/schema.sql`.

Purpose: Sem essas colunas, a camada de escrita (Plan 02) e os formulários (Plan 03) não têm onde persistir prazo e descrição. Esta é a fundação de todo o phase — todas as escritas e o `normalizeGoal` dependem das colunas existirem no banco live. Type/build checks são irrelevantes aqui (front sem bundler, sem tipos gerados); a prova real é as colunas existirem na tabela live e round-tripping funcionar.
Output: Colunas `target_date` + `description` na tabela live `goals` + espelho atualizado em `supabase/schema.sql`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crud-metas/07-CONTEXT.md
@supabase/schema.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verificar schema live de goals via MCP antes de migrar</name>
  <files>public.goals (banco live, via Supabase MCP — leitura only)</files>
  <read_first>
    - supabase/schema.sql (espelho local §"TABELA: goals", ~L72-87) — estado esperado ANTES da migration: colunas id, user_id, name, emoji, target, current, monthly_contrib, tint, completed, completed_at, created_at. SEM target_date, SEM description.
    - .planning/PROJECT.md §4 — regra inviolável: "Mudanças de schema: MCP apply_migration + espelho. Nunca assumir schema — verificar via MCP."
  </read_first>
  <action>
    Inspecionar o schema atual de `public.goals` no banco live (project_id `nhwarucfecoqcahcosga`) usando o Supabase MCP (ex.: ferramenta de listagem de tabelas ou consulta a `information_schema.columns`). Capturar a lista de colunas existentes por leitura direta do banco (NÃO inferir do espelho `supabase/schema.sql`). Confirmar que `target_date` e `description` ainda não existem, e reportar as colunas observadas para aprovação humana antes de qualquer DDL. Se `target_date`/`description` já existirem, PARAR e reportar (migration seria desnecessária e o espelho pode estar desatualizado).
  </action>
  <what-built>
    O executor deve usar o Supabase MCP (ferramenta de listagem/inspeção de schema, ex.: `list_tables` ou consulta a `information_schema.columns` para `public.goals`) para capturar as colunas ATUAIS de `public.goals` no banco live (project_id `nhwarucfecoqcahcosga`). Isto NÃO é uma suposição a partir do espelho — é uma leitura direta do banco, conforme regra PROJECT.md §4.
  </what-built>
  <how-to-verify>
    1. Rodar a inspeção de schema via MCP e confirmar que `public.goals` HOJE tem as colunas do espelho (name, emoji, target, current, monthly_contrib, tint, completed, completed_at, created_at, id, user_id).
    2. Confirmar que `target_date` e `description` NÃO existem ainda (se já existirem, PARAR e reportar — a migration seria idempotente/desnecessária e o espelho pode estar desatualizado).
    3. Reportar a lista de colunas observada para aprovação humana antes de aplicar qualquer DDL.
  </how-to-verify>
  <acceptance_criteria>
    - A lista de colunas atuais de `public.goals` foi obtida via MCP (leitura direta do banco live), não inferida do espelho.
    - Está confirmado que `target_date` e `description` ainda NÃO existem na tabela live.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação é via MCP schema inspection + confirmação humana; sem bundler/tipos gerados, não há teste automatizado. Prova = colunas observadas no banco live.</automated>
    <human-check>Colunas atuais de goals confirmadas via MCP; target_date/description ausentes.</human-check>
  </verify>
  <resume-signal>Digite "aprovado" após confirmar as colunas atuais via MCP, ou reporte divergência.</resume-signal>
  <done>Schema live de goals verificado via MCP; ausência de target_date/description confirmada.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar migration via MCP apply_migration (target_date DATE + description text, ambos nullable)</name>
  <files>public.goals (banco live, via Supabase MCP apply_migration)</files>
  <read_first>
    - supabase/schema.sql (§"TABELA: goals", ~L72-87) — colunas existentes e convenção de defaults (ex.: `emoji text not null default '🎯'`, `tint text not null default '#00C37B'`).
    - .planning/phases/07-crud-metas/07-CONTEXT.md D-01/D-02/D-03 (target_date DATE nullable) e D-10 (description text nullable, mesma migration).
  </read_first>
  <action>
    Aplicar UMA migration via Supabase MCP `apply_migration` (nome sugerido: `add_target_date_and_description_to_goals`) que executa contra a tabela `public.goals`: adicionar coluna `target_date` do tipo `DATE` NULLABLE (sem default, sem NOT NULL — meta sem prazo é permitida, D-03), e adicionar coluna `description` do tipo `text` NULLABLE (sem default, sem NOT NULL, D-10). Usar `add column if not exists` para idempotência. NÃO alterar, remover ou tocar em nenhuma outra coluna de `goals` (current/monthly_contrib/completed permanecem intactas — D-07/D-08 afetam só o form, não o schema). NÃO adicionar backfill, trigger, index ou constraint de data — a validação `target_date >= hoje` é client-side via input `min` (D-04); o banco/RLS não a força (resíduo aceito em ASVS L1, ver threat model). `apply_migration` É o push neste projeto — não existe `supabase db push` nem arquivo `supabase/migrations/*.sql`.
  </action>
  <acceptance_criteria>
    - A tabela live `public.goals` passa a ter a coluna `target_date` do tipo `date`, nullable (`is_nullable = YES`), verificável via MCP schema inspection após a migration.
    - A tabela live `public.goals` passa a ter a coluna `description` do tipo `text`, nullable (`is_nullable = YES`), verificável via MCP.
    - Nenhuma outra coluna de `goals` foi alterada (contagem e tipos das colunas pré-existentes idênticos ao estado da Task 1).
    - Inserir uma linha de teste em `goals` com `target_date` e `description` preenchidos e com ambos nulos funciona (round-trip); linha de teste removida ao fim.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — sem bundler/tipos. Prova = MCP schema inspection pós-migration mostra target_date (date, nullable) e description (text, nullable) em public.goals + insert de teste round-trip.</automated>
  </verify>
  <done>Colunas target_date (date, nullable) e description (text, nullable) existem na tabela live public.goals; demais colunas intactas; round-trip de insert/select confirmado.</done>
</task>

<task type="auto">
  <name>Task 3: Espelhar as colunas novas em supabase/schema.sql</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - supabase/schema.sql (§"TABELA: goals", ~L72-87) — bloco `create table if not exists public.goals (...)` a espelhar.
    - .planning/PROJECT.md §4 — regra: espelho em `supabase/*.sql` após apply_migration.
  </read_first>
  <action>
    Editar `supabase/schema.sql`, no bloco `create table if not exists public.goals (...)`, adicionando duas linhas de coluna que reflitam exatamente o que foi aplicado no banco live: `target_date date` (sem `not null`, sem default) e `description text` (sem `not null`, sem default). Posicionar de forma legível junto às colunas de conteúdo da meta (ex.: `target_date` logo após `tint` ou junto de `target`; `description` junto de `name`/`emoji`). Este arquivo é apenas espelho/documentação — não é executado no deploy; a única fonte de verdade do schema é o banco live (via MCP). Não tocar em outras tabelas do arquivo.
  </action>
  <acceptance_criteria>
    - `grep -n "target_date" supabase/schema.sql` retorna a linha dentro do bloco `goals`, como `date` sem `not null`.
    - `grep -n "description text" supabase/schema.sql` retorna a linha dentro do bloco `goals`, sem `not null`.
    - O bloco `create table if not exists public.goals` continua sintaticamente válido (parênteses/vírgulas coerentes).
    - Nenhuma outra tabela em `supabase/schema.sql` foi modificada (git diff restrito ao bloco goals).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "target_date" supabase/schema.sql && grep -n "description text" supabase/schema.sql</automated>
  </verify>
  <done>supabase/schema.sql contém target_date date + description text (nullable) no bloco goals; diff restrito a esse bloco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/MCP → banco live | DDL aplicado direto no Postgres de produção via MCP apply_migration; sem staging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | apply_migration DDL em tabela de produção | mitigate | Task 1 [BLOCKING] verifica schema atual via MCP antes de migrar (nunca assumir); `add column if not exists` idempotente; colunas nullable sem default → zero risco de corromper linhas existentes; sem backfill destrutivo |
| T-07-02 | Denial of Service | ALTER TABLE lock em goals | accept | `ADD COLUMN` nullable sem default é operação de metadata em Postgres (não reescreve a tabela); tabela pequena (metas do usuário); janela desprezível |
| T-07-03 | Information Disclosure | colunas novas expostas sem RLS | accept | RLS de `goals` é `for all using (auth.uid() = user_id)` (schema.sql L142) — cobre automaticamente colunas novas; não há política por-coluna a criar |
</threat_model>

<verification>
- MCP schema inspection de `public.goals` mostra `target_date` (date, nullable) e `description` (text, nullable).
- Insert de teste com/sem esses campos faz round-trip (select devolve os valores; null aceito).
- `supabase/schema.sql` espelha as duas colunas no bloco goals.
- RLS de goals (`for all using (auth.uid() = user_id)`) inalterado e continua cobrindo as colunas novas.
</verification>

<success_criteria>
- Colunas `target_date DATE` nullable e `description text` nullable existem na tabela live `public.goals` (prova via MCP, não via espelho).
- `supabase/schema.sql` atualizado como espelho.
- Nenhuma coluna pré-existente de goals alterada; metas existentes intactas.
</success_criteria>

<output>
Create `.planning/phases/07-crud-metas/07-01-SUMMARY.md` when done — incluir a lista de colunas de goals observada via MCP ANTES e DEPOIS da migration como prova.
</output>
