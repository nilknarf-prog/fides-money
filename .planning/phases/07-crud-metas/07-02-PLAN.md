---
phase: 07-crud-metas
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - assets/fides-store.jsx
autonomous: true
requirements: [META-01, META-02, META-03, META-04]
tags: [store, crud, goals, react-context]
user_setup: []

must_haves:
  truths:
    - "normalizeGoal mapeia target_date do banco para o campo prazo da UI"
    - "normalizeGoal mapeia description do banco para descricao (não mais forçado a '')"
    - "addGoal insere uma linha em goals (live) e refaz o fetch, ou muta otimista (mock)"
    - "updateGoal atualiza a linha por id (live) e refaz o fetch, ou muta otimista (mock)"
    - "deleteGoal remove a linha por id (live) e refaz o fetch, ou muta otimista (mock)"
    - "addGoal/updateGoal/deleteGoal estão expostos no context value e no fallback do useFides"
  artifacts:
    - path: "assets/fides-store.jsx"
      provides: "Trio de mutations de metas + normalizeGoal estendido"
      contains: "addGoal"
  key_links:
    - from: "addGoal (fides-store.jsx)"
      to: "window.fidesDb.from('goals').insert(...)"
      via: "insert live + await refreshData(userId); mock = setGoals otimista"
      pattern: "from\\('goals'\\)\\.insert"
    - from: "normalizeGoal (fides-store.jsx)"
      to: "row.target_date / row.description"
      via: "prazo: row.target_date || null; descricao: row.description || ''"
      pattern: "row\\.target_date"
    - from: "context value + useFides fallback"
      to: "MetasStudio (Plan 03)"
      via: "addGoal, updateGoal, deleteGoal expostos via useFides()"
      pattern: "addGoal, updateGoal, deleteGoal"
---

<objective>
Adicionar ao `FidesProvider` a camada de escrita de metas — `addGoal` / `updateGoal` / `deleteGoal` — espelhando exatamente o trio `addAccount`/`updateAccount`/`deleteAccount` (D-11), e estender `normalizeGoal` para ler as colunas novas `target_date` e `description` (D-05/D-10/D-12). Expor as três mutations no context value e no fallback do `useFides`.

Purpose: Hoje `goals` é read-only — a leitura live já existe (`refreshData`→`normalizeGoal`→`useFides().goals`), mas não há caminho de escrita. Sem esse trio, os modais do Plan 03 não têm como persistir. `normalizeGoal` hoje força `descricao: ''` e não conhece prazo — precisa mapear as colunas do Plan 01.
Output: `addGoal`/`updateGoal`/`deleteGoal` funcionais (live insert/update/delete + refetch; mock otimista) + `normalizeGoal` mapeando prazo/descrição + exposição no context/fallback.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crud-metas/07-CONTEXT.md
@.planning/phases/07-crud-metas/07-PATTERNS.md
@assets/fides-store.jsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender normalizeGoal para mapear target_date e description (D-05/D-10)</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx L90-103 (`normalizeGoal` atual — hoje força `descricao: ''` e não tem `prazo`).
    - assets/fides-store.jsx L62-72 (`normalizeAccount`) — idioma `row.field || default` a replicar.
    - .planning/phases/07-crud-metas/07-PATTERNS.md §"normalizeGoal (modify)" — mudanças exatas.
    - .planning/phases/07-crud-metas/07-CONTEXT.md D-05 (prazo ← target_date), D-10 (descricao ← description), D-02 (DATE string YYYY-MM-DD passa direto, sem parse).
  </read_first>
  <action>
    Em `normalizeGoal` (assets/fides-store.jsx L90-103): trocar `descricao: ''` por `descricao: row.description || ''` (D-10). Adicionar a chave `prazo: row.target_date || null` (D-05) — `target_date` é string DATE `YYYY-MM-DD` e passa como está, SEM `new Date(...)` nem parsing (D-02: o `<input type="date">` consome/produz `YYYY-MM-DD` diretamente). Manter `atual: Number(row.current)` e `contribuicao: Number(row.monthly_contrib)` inalterados — as colunas continuam no schema e no read (D-07/D-08 só afetam o form/write, não o normalize). Manter `criadaEm` derivado de `created_at` como está. Não alterar os outros normalizers.
  </action>
  <acceptance_criteria>
    - `normalizeGoal` retorna objeto com chave `prazo` igual a `row.target_date` quando presente, e `null` quando `row.target_date` é null/ausente.
    - `normalizeGoal` retorna `descricao` igual a `row.description` quando presente, e `''` quando null/ausente.
    - `atual` e `contribuicao` continuam presentes e derivados de `row.current`/`row.monthly_contrib` (grep confirma que as linhas não foram removidas).
    - Nenhuma chamada `new Date` foi adicionada ao mapear `prazo` (grep na função `normalizeGoal` mostra `prazo: row.target_date` sem `new Date` na mesma expressão).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "prazo: row.target_date" assets/fides-store.jsx && grep -n "descricao: row.description" assets/fides-store.jsx</automated>
  </verify>
  <done>normalizeGoal mapeia prazo←target_date (null-safe, sem parse) e descricao←description; atual/contribuicao intactos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar addGoal/updateGoal/deleteGoal espelhando o trio de accounts (D-11/D-12)</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx L506-566 (bloco `// ─── Accounts ───` com addAccount/updateAccount/deleteAccount) — molde estrutural exato.
    - assets/fides-store.jsx L190-207 (`refreshData` + goals query L200) — o refetch chamado após cada mutação live.
    - .planning/phases/07-crud-metas/07-PATTERNS.md §"addGoal / updateGoal / deleteGoal (new)" — notas de replicação (payload, tags de erro, sem RPC de balance).
    - .planning/phases/07-crud-metas/07-CONTEXT.md D-11 (padrão live insert/update/delete + refreshData; mock otimista) e D-12 (mapeamento UI→DB).
  </read_first>
  <action>
    Adicionar um bloco `// ─── Goals ───` em assets/fides-store.jsx (próximo ao bloco de Accounts, junto do `normalizeGoal`/goals query para legibilidade) com três `React.useCallback` deps `[mode, userId, refreshData]`, espelhando a estrutura do trio de accounts:

    `addGoal(g)` — ramo `mode === 'live' && userId`: `await window.fidesDb.from('goals').insert({ ... })` com payload por D-12: `user_id: userId`, `name: g.nome || ''`, `target: Number(g.alvo) || 0`, `target_date: g.prazo || null`, `description: g.descricao || ''`, `emoji: g.emoji || '🎯'`, `tint: g.tint || '#00C37B'`. NÃO setar `current` nem `monthly_contrib` (defaults 0 do schema, D-07/D-08). `if (error) throw error; await refreshData(userId);` dentro de try/catch com `console.error('[Fides] addGoal:', err.message)`. Ramo mock: `setGoals(prev => [{ ...g, id: g.id ?? ('goal_' + Date.now()), _new: true }, ...prev])`.

    `updateGoal(id, patch)` — ramo live: `await window.fidesDb.from('goals').update(patch).eq('id', id)` (patch já vem DB-shaped do modal — ver Plan 03; NÃO reproduzir o ramo especial `__targetBal`/`set_account_balance` do updateAccount, pois goals não tem RPC de saldo derivado). `if (error) throw error; await refreshData(userId);` try/catch tag `'[Fides] updateGoal:'`. Ramo mock: `setGoals(prev => prev.map(x => x.id === id ? { ...x, ...patch } : x))`.

    `deleteGoal(id)` — ramo live: `await window.fidesDb.from('goals').delete().eq('id', id)`. `if (error) throw error; await refreshData(userId);` try/catch tag `'[Fides] deleteGoal:'`. Ramo mock: `setGoals(prev => prev.filter(x => x.id !== id))`.

    Usar o setter de estado de metas já existente (o mesmo `setGoals` usado em `refreshData` L207) — confirmar o nome exato do setter ao ler o arquivo. As escritas live rodam sob a RLS de goals (`auth.uid() = user_id`) — o `insert` deve incluir `user_id: userId` (o filtro `.eq('id', id)` em update/delete é escopado pela RLS ao dono; sem IDOR).
  </action>
  <acceptance_criteria>
    - `grep -c "from('goals').insert" assets/fides-store.jsx` ≥ 1 e o payload NÃO contém as chaves `current` nem `monthly_contrib` (grep confirma ausência dessas chaves no objeto do insert de goals).
    - `updateGoal` usa `from('goals').update(patch).eq('id', id)` e NÃO contém `set_account_balance` nem desestruturação `__targetBal` (grep na função updateGoal).
    - `deleteGoal` usa `from('goals').delete().eq('id', id)`.
    - Cada uma das três funções tem ramo `mode === 'live' && userId` com `await refreshData(userId)` e ramo mock otimista com o setter de goals.
    - As três funções têm `console.error` com tags `[Fides] addGoal:` / `[Fides] updateGoal:` / `[Fides] deleteGoal:` respectivamente.
    - O insert de addGoal inclui `user_id: userId` (RLS compliance).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "from('goals').insert" assets/fides-store.jsx && grep -n "from('goals').update" assets/fides-store.jsx && grep -n "from('goals').delete" assets/fides-store.jsx</automated>
  </verify>
  <done>addGoal/updateGoal/deleteGoal existem, espelham o trio de accounts (live insert/update/delete + refreshData; mock otimista), sem RPC de balance, com payload D-12 e tags de erro corretas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Expor addGoal/updateGoal/deleteGoal no context value e no fallback do useFides (D-11)</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx L1246-1284 (objeto `value` do FidesProvider) — linha `goals,` (L1253) e linha `accounts, addAccount, updateAccount, deleteAccount,` (L1258).
    - assets/fides-store.jsx L1294-1314 (fallback do `useFides` sem provider) — `goals: [],` (L1301) e `accounts: ACCOUNTS, addAccount: () => {}, ...` (L1307).
    - .planning/phases/07-crud-metas/07-PATTERNS.md §"Context value & fallback exposure".
  </read_first>
  <action>
    No objeto `value` do FidesProvider (assets/fides-store.jsx ~L1248-1284): adicionar `addGoal, updateGoal, deleteGoal` junto da linha `goals,` (espelhando como `addAccount, updateAccount, deleteAccount` acompanham `accounts,`). No objeto de fallback do `useFides` sem provider (~L1297-1314): adicionar `addGoal: () => {}, updateGoal: () => {}, deleteGoal: () => {},` junto de `goals: [],`, no mesmo formato das linhas de accounts do fallback. Não remover nem reordenar chaves existentes.
  </action>
  <acceptance_criteria>
    - O objeto `value` do provider inclui `addGoal`, `updateGoal`, `deleteGoal` (grep confirma as três identificadas no bloco do context value).
    - O fallback do `useFides` inclui `addGoal: () => {}`, `updateGoal: () => {}`, `deleteGoal: () => {}` (no-op), para que componentes fora do provider não quebrem.
    - `goals,` no context value e `goals: []` no fallback permanecem presentes (não regride a leitura existente).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "addGoal, updateGoal, deleteGoal" assets/fides-store.jsx && grep -n "addGoal: () => {}" assets/fides-store.jsx</automated>
  </verify>
  <done>As três mutations estão no context value e no fallback do useFides; consumíveis via useFides() por MetasStudio no Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (goals) | insert/update/delete de metas do próprio usuário cruzam para o Postgres; input do form é untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation of Privilege / IDOR | updateGoal/deleteGoal por `id` | mitigate | RLS de goals `for all using (auth.uid() = user_id)` (schema.sql L142) impede agir sobre meta de outro usuário — `.eq('id', id)` sem match de dono é bloqueado pela política; addGoal força `user_id: userId` no insert |
| T-07-05 | Tampering | payload de insert (valores numéricos) | mitigate | `target: Number(g.alvo) || 0` coage número; campos ausentes caem em defaults do schema; `current`/`monthly_contrib` nunca vêm do cliente nesta fase |
| T-07-06 | Repudiation | escrita sem rastro | accept | Fora de escopo L1; created_at registra criação; sem requisito de audit log para metas pessoais |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — nenhum pacote instalado neste plano (front sem bundler/npm; só edição de JSX in-file); sem gate de legitimidade a acionar |
</threat_model>

<verification>
- `normalizeGoal` devolve `prazo` (←target_date, null-safe) e `descricao` (←description); `atual`/`contribuicao` intactos.
- `addGoal` insere com payload D-12 (sem current/monthly_contrib) e refaz fetch em live; otimista em mock.
- `updateGoal`/`deleteGoal` operam por `id` (RLS escopa ao dono) + refetch; otimista em mock.
- Trio exposto em context value e fallback — `useFides().addGoal/updateGoal/deleteGoal` disponíveis.
- Tags de erro `[Fides] addGoal/updateGoal/deleteGoal:` presentes.
</verification>

<success_criteria>
- Camada de escrita de metas completa e espelhando o trio de accounts, sem RPC de balance.
- normalizeGoal lê as colunas do Plan 01 (prazo/descrição).
- Mutations disponíveis via useFides() para o Plan 03 consumir.
</success_criteria>

<output>
Create `.planning/phases/07-crud-metas/07-02-SUMMARY.md` when done.
</output>
