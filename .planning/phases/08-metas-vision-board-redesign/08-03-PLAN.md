---
phase: 08-metas-vision-board-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-store.jsx
autonomous: true
requirements: [UAT-1, UAT-2, UAT-3, UAT-4, UAT-5]
tags: [store, write-layer, storage, upload, normalizeGoal, goals]
user_setup: []

must_haves:
  truths:
    - "normalizeGoal mapeia cover (image_url), completed e completedAt do row — fecha o GAP Phase-07 #1 (completed hoje não é lido, D5 seria no-op) (UAT-3)"
    - "addGoal aceita cover/image_url e current inicial no payload de insert (UAT-1/UAT-2)"
    - "updateGoal continua aceitando patch arbitrário — callers passam { image_url }, { current } ou { completed, completed_at } sem mudar assinatura (UAT-4/UAT-5)"
    - "uploadGoalCover(userId, file) valida MIME/size/ext no client, sobe para goal-covers em path <user_id>/<uuid>.<ext> e retorna { url, path } (UAT-2)"
    - "deleteGoalCover(path) remove o objeto do Storage; aceita path direto (UAT-5)"
    - "uploadGoalCover/deleteGoalCover são expostos no value do provider e no fallback do useFides()"
    - "Todas as escritas seguem o padrão mode==='live' try/catch → window.fidesDb → if(error) throw → refreshData(userId), com tag console.error('[Fides] <fn>:', ...)"
  artifacts:
    - path: "assets/fides-store.jsx"
      provides: "normalizeGoal estendido + addGoal/updateGoal com cover/current + uploadGoalCover/deleteGoalCover expostos"
      contains: "uploadGoalCover"
  key_links:
    - from: "normalizeGoal (fides-store.jsx)"
      to: "vcard render + status filter (fides-metas.jsx)"
      via: "cover/completed/completedAt no shape UI da meta"
      pattern: "completed"
    - from: "uploadGoalCover (fides-store.jsx)"
      to: "bucket goal-covers (Supabase Storage)"
      via: "window.fidesDb.storage.from('goal-covers').upload + getPublicUrl"
      pattern: "storage.from\\('goal-covers'\\)"
  prohibitions:
    - "NÃO usar service_role — usar o window.fidesDb já autenticado (anon key + sessão)"
    - "NÃO montar o path a partir do nome do arquivo do usuário — path é <user_id>/<crypto.randomUUID()>.<ext> (anti-traversal/colisão)"
    - "NÃO confiar só na validação client — ela é UX; a policy/bucket é a fronteira de segurança real (Plan 01)"
    - "NÃO usar confirm()/alert() em nenhum fluxo (CLAUDE.md)"
    - "NÃO adicionar hardcode de chaves/URL do Supabase — usar getPublicUrl() do SDK"
---

<!-- planner-discipline-allow: service_role -->

<objective>
Plumbing da camada de escrita para o vision-board, em `assets/fides-store.jsx`: (1) `normalizeGoal` passa a mapear `cover` (de `image_url`), `completed` e `completedAt` — fechando o GAP Phase-07 #1 (hoje `completed` não é lido em lugar nenhum, tornando o filtro de status e "Já atingidas" no-ops); (2) `addGoal` aceita `cover`/`image_url` e `current` inicial; (3) novos helpers `uploadGoalCover`/`deleteGoalCover` (primeiro uso de Supabase Storage no projeto), expostos pelo provider.

Purpose: Sem o mapeamento de `completed` o D5 (filtro Ativas/Concluídas + Capítulo III) não funciona; sem `cover` o vcard não sabe qual capa renderizar; sem os helpers de Storage não há upload/limpeza de capa própria. Este plano é a espinha de dados de UAT-1..5.
Output: `fides-store.jsx` com normalizeGoal estendido, addGoal/updateGoal cientes de cover/current, e uploadGoalCover/deleteGoalCover no context.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-store.jsx
@CLAUDE.md
</context>

## Artifacts this phase produces (contribuição deste plano)

- `normalizeGoal`: novos campos `cover: row.image_url || null`, `completed: !!row.completed`, `completedAt: row.completed_at || null`.
- `addGoal`: payload de insert ganha `image_url: g.cover || null` e `current: Number(g.atual) || 0`.
- Novos helpers `uploadGoalCover(userId, file)` e `deleteGoalCover(path)` + constantes `ALLOWED_MIME`, `MAX_BYTES` e helper `extFromMime(mime)`.
- Exposição de `uploadGoalCover`/`deleteGoalCover` no `value` do provider (junto de goals/addGoal/...) e no fallback do `useFides()`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender normalizeGoal (cover/completed/completedAt) e o payload de addGoal (image_url/current)</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx (normalizeGoal :90-104; addGoal/updateGoal/deleteGoal :571-622; value do provider :1304-1340; fallback useFides :1350-1369)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (seção fides-store.jsx — additions exatas do normalizeGoal e do payload de addGoal)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pattern 3 — normalizeGoal; Pitfall 2 — por que completed precisa ser mapeado)
  </read_first>
  <action>
    Em `normalizeGoal`, acrescentar ao objeto retornado três campos, mantendo o idioma `row.field || default` já usado: `cover` derivado de `row.image_url` (null se ausente), `completed` como boolean coerção de `row.completed`, e `completedAt` de `row.completed_at` (null se ausente). Não remover nenhum campo existente (id/nome/descricao/prazo/emoji/tint/alvo/atual/contribuicao/criadaEm permanecem). Em `addGoal`, estender o objeto passado a `window.fidesDb.from('goals').insert({...})` com `image_url` (de `g.cover`, null se ausente) e `current` (Number de `g.atual`, 0 se ausente) — mantendo user_id/name/target/target_date/description/emoji/tint como estão. `updateGoal` NÃO muda de assinatura (já aceita patch arbitrário); nenhuma edição necessária nele além de confirmar que passa o patch cru. Preservar o branch mock-mode (setGoals) em ambos.
  </action>
  <acceptance_criteria>
    - `grep -c "cover: row.image_url" assets/fides-store.jsx` >= 1.
    - `grep -c "completed: !!row.completed" assets/fides-store.jsx` >= 1 e `grep -c "completedAt: row.completed_at" assets/fides-store.jsx` >= 1.
    - `grep -c "image_url:" assets/fides-store.jsx` >= 1 (no payload de addGoal) e o insert de addGoal inclui `current:`.
    - No app live: criar uma meta com valor atual > 0 persiste `current` (barra do card reflete) — verificação de comportamento após Plan 06 aplicar o campo no form; aqui basta que o insert aceite a chave sem erro no console.
    - DevTools console sem novos erros ao carregar Metas (normalizeGoal roda sobre rows reais).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "cover: row.image_url" assets/fides-store.jsx &amp;&amp; grep -c "completed: !!row.completed" assets/fides-store.jsx</automated>
  </verify>
  <done>normalizeGoal expõe cover/completed/completedAt e addGoal persiste image_url/current; mock-mode preservado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar uploadGoalCover/deleteGoalCover e expô-los no provider</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx (padrão try/catch mode==='live' de addGoal/updateGoal :571-622; value do provider :1304-1340; fallback :1350-1369)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pattern 2 — uploadGoalCover/deleteGoalCover verbatim; Don't Hand-Roll — getPublicUrl/crypto.randomUUID; Security Domain V5/V8)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (seção fides-store.jsx — helpers de Storage + convenção de tag console.error)
  </read_first>
  <action>
    Adicionar, próximo às demais funções de escrita de goals, as constantes `ALLOWED_MIME = ['image/jpeg','image/png','image/webp']`, `MAX_BYTES = 5 * 1024 * 1024`, o helper `extFromMime(mime)` (mapa mime→ext, null se desconhecido), e as duas funções async seguindo Pattern 2 da RESEARCH.md: `uploadGoalCover(userId, file)` — valida `file.type` contra ALLOWED_MIME e `file.size` contra MAX_BYTES (throw com mensagem PT-BR amigável se falhar), deriva ext do MIME validado, monta `path = userId + '/' + crypto.randomUUID() + '.' + ext` (jamais o nome do arquivo do usuário), chama `window.fidesDb.storage.from('goal-covers').upload(path, file, { contentType: file.type, upsert: false })`, `if (error) throw error`, e retorna `{ url: getPublicUrl(path).data.publicUrl, path }`; `deleteGoalCover(path)` — no-op se path vazio, chama `.storage.from('goal-covers').remove([path])`, e loga `console.error('[Fides] deleteGoalCover:', err.message)` em erro (não throw). Expor `uploadGoalCover` e `deleteGoalCover` no objeto `value` do provider (linha do `goals, addGoal, updateGoal, deleteGoal`) e adicionar stubs no objeto de fallback do `useFides()` (`uploadGoalCover: async () => {}, deleteGoalCover: async () => {}`). Usar EXCLUSIVAMENTE o `window.fidesDb` já autenticado — nunca service_role, nunca URL hardcoded.
  </action>
  <acceptance_criteria>
    - `grep -c "storage.from('goal-covers')" assets/fides-store.jsx` >= 2 (upload + remove).
    - `grep -c "crypto.randomUUID()" assets/fides-store.jsx` >= 1 e o path NÃO usa `file.name`.
    - `grep -c "uploadGoalCover" assets/fides-store.jsx` >= 3 (definição + export no value + stub no fallback) e idem para deleteGoalCover.
    - `grep -c "service_role" assets/fides-store.jsx` == 0.
    - Smoke live: em Metas, chamar `useFides().uploadGoalCover(userId, <File jpg pequeno>)` (via UI do Plan 06) retorna { url, path } e o objeto aparece em Storage → goal-covers/<user_id>/; um upload > 5MB ou de tipo não-imagem é rejeitado com throw amigável (validação client) — e mesmo forçando via devtools, a policy do bucket rejeita (Plan 01).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "storage.from('goal-covers')" assets/fides-store.jsx &amp;&amp; grep -c "crypto.randomUUID()" assets/fides-store.jsx</automated>
  </verify>
  <done>Helpers de Storage com validação client + path UUID owner-scoped, expostos no provider e fallback; sem service_role/hardcode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Supabase Storage | File do usuário atravessa; validação client é UX, policy é a fronteira |
| client → Postgres (goals.image_url) | Escrita da referência de capa; RLS de goals guarda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering/V8 | uploadGoalCover path | mitigate | Path = `<user_id>/<crypto.randomUUID()>.<ext>`; nome do arquivo do usuário nunca entra no path (anti-traversal/colisão) |
| T-08-09 | V5 Input Validation | uploadGoalCover pre-check | mitigate | Checa MIME (ALLOWED_MIME) e size (MAX_BYTES) no client como UX; backstop real é bucket allowed_mime_types/file_size_limit + RLS (Plan 01) |
| T-08-10 | Spoofing/Info Disclosure | fidesDb client | mitigate | Usa o client autenticado existente (anon key + sessão); nunca service_role; URL via getPublicUrl() (sem hardcode) |
| T-08-04 | Tampering/XSS | render da capa | accept | Capas só consumidas como background-image/img src pelos planos de UI (nunca dangerouslySetInnerHTML) |
| T-08-SC | Tampering | dependências | accept | Nenhum `npm install` — helpers usam APIs nativas (crypto.randomUUID) + SDK já carregado |
</threat_model>

<verification>
- normalizeGoal expõe cover/completed/completedAt (grep + carregamento sem erro).
- uploadGoalCover/deleteGoalCover presentes, escopados por owner, expostos no provider.
- `security-reviewer` deve rodar sobre o fluxo de upload antes do commit (CLAUDE.md — fluxo toca Storage).
</verification>

<success_criteria>
Camada de dados pronta: status (completed) legível, capa (cover) mapeada, upload/delete de capa disponíveis e seguros — desbloqueando controls (Plan 04), vcard (Plan 05) e seletor de capa (Plan 06).
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-03-SUMMARY.md` when done
</output>
