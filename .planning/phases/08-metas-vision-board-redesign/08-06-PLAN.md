---
phase: 08-metas-vision-board-redesign
plan: 06
type: execute
wave: 4
depends_on: [08-05, 08-03, 08-01, 08-02]
files_modified:
  - assets/fides-metas.jsx
  - assets/fides-metas.css
autonomous: true
requirements: [UAT-1, UAT-2, UAT-5]
tags: [frontend, cover-picker, upload, storage, modals, security, rules-of-hooks]
user_setup: []

must_haves:
  truths:
    - "CriarMetaModal e AjustarPlanoModal ganham seletor de capa com abas Galeria (grid dos presets) e Enviar foto (upload) (D1, UAT-1/UAT-2)"
    - "Escolher um preset seta cover='preset:<id>'; enviar foto chama uploadGoalCover(userId, file) e seta cover=URL pública retornada (UAT-1/UAT-2)"
    - "CriarMetaModal ganha campo Valor atual (mapeia current na criação); AjustarPlanoModal ganha campo Status (Ativa/Concluída) (D5/D6)"
    - "onConfirm dos dois modais passa cover (e current no criar, completed no editar) além dos campos existentes (emoji/nome/descrição/alvo/prazo/tint)"
    - "Ao editar trocando uma capa de upload anterior, a capa antiga é removida do Storage (deleteGoalCover); ao excluir uma meta com capa de upload, a capa é apagada do Storage (UAT-5)"
    - "Validação client de MIME/tamanho dá feedback amigável (sem confirm()/alert()); estado de upload-in-progress evita duplo envio"
    - "Todo useState do seletor de capa (aba ativa, coverValue, uploading) é declarado antes do early return do modal — sem warning de hooks (UAT-7)"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "Seletor de capa (Galeria/Enviar foto) + campos Valor atual/Status nos modais + limpeza de Storage no editar/excluir"
      contains: "uploadGoalCover"
  key_links:
    - from: "aba Enviar foto (CriarMetaModal/AjustarPlanoModal)"
      to: "uploadGoalCover (fides-store.jsx) → bucket goal-covers"
      via: "input file → uploadGoalCover(userId, file) → cover=URL"
      pattern: "uploadGoalCover"
    - from: "excluir/editar meta com capa de upload"
      to: "deleteGoalCover (fides-store.jsx)"
      via: "derivar path da URL pública (após /goal-covers/) e remover"
      pattern: "deleteGoalCover"
  prohibitions:
    - "NÃO usar confirm()/alert() para trocar/remover capa — feedback via UI/Toast (CLAUDE.md)"
    - "NÃO usar service_role nem URL hardcoded — upload via helper do Plan 03 (client autenticado)"
    - "NÃO implementar crop/edição de imagem — só upload + fit cover (scope_fence)"
    - "NÃO declarar hooks após o early return do modal (Rules of Hooks)"
    - "NÃO adicionar coluna/campo de status enum — Status usa completed boolean (scope_fence)"
---

<!-- planner-discipline-allow: confirm( -->
<!-- planner-discipline-allow: alert( -->

<objective>
Fechar o ciclo de capas em `assets/fides-metas.jsx`: dar a `CriarMetaModal` e `AjustarPlanoModal` um seletor de capa com abas Galeria (grid dos ~16 presets do Plan 02) e Enviar foto (upload via `uploadGoalCover` do Plan 03), além dos campos novos Valor atual (criar → `current`) e Status (editar → `completed`); e garantir a limpeza de Storage (remover a capa de upload ao trocar/excluir). Persiste em `goals.image_url` (coluna do Plan 01).

Purpose: É o que permite o usuário ESCOLHER/enviar a capa — UAT-1 (galeria) e UAT-2 (upload próprio) só passam com este seletor; UAT-5 exige que editar troque a capa e excluir apague o objeto do Storage. É também a superfície de upload — segurança revisada (client valida, RLS do bucket é a fronteira).
Output: seletor de capa nos dois modais, campos Valor atual/Status, e limpeza de Storage no editar/excluir.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-metas.jsx
@assets/fides-metas.css
@CLAUDE.md
</context>

## Artifacts this phase produces (contribuição deste plano)

- Seletor de capa (abas Galeria/Enviar foto) em CriarMetaModal e AjustarPlanoModal + estado local (aba ativa, coverValue, uploading) declarado antes do early return.
- Campo "Valor atual" no CriarMetaModal (→ `current`/`atual` no payload) e campo "Status" (Ativa/Concluída) no AjustarPlanoModal (→ `completed`).
- Extensão dos `onConfirm` dos dois modais com `cover` (+ `atual` no criar, `completed` no editar).
- Helper `coverStoragePath(url)` (deriva o path após `/goal-covers/`) + wiring de `deleteGoalCover` no editar (troca de capa de upload) e no excluir meta.
- Blocos CSS do seletor de capa (`.met-cover-picker*`) em fides-metas.css.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Seletor de capa (Galeria/Enviar foto) + Valor atual no CriarMetaModal</name>
  <files>assets/fides-metas.jsx, assets/fides-metas.css</files>
  <read_first>
    - assets/fides-metas.jsx (CriarMetaModal :184-261 — form/onConfirm/TINTS; COVER_PRESETS/resolveCoverUrl adicionados no Plan 05)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (Form-payload pattern — estender payload com cover/atual; hooks-before-early-return)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pattern 2 — uploadGoalCover; Security V5/V8; anti-pattern confirm/alert)
  </read_first>
  <action>
    Em CriarMetaModal, puxar `userId` e `uploadGoalCover` de `useFides()` e declarar, ANTES do early return do modal, os useState do seletor: aba ativa ('galeria'|'foto'), `coverValue` (string cover ou null) e `uploading` (bool). Renderizar o seletor de capa acima/junto dos campos: aba Galeria = grid clicável dos presets (iterar COVER_PRESETS; cada opção mostra a SVG via resolveCoverUrl e, ao clicar, seta coverValue='preset:<id>' e marca selecionado); aba Enviar foto = `<input type="file" accept="image/jpeg,image/png,image/webp">` que, no change, seta uploading=true, chama `uploadGoalCover(userId, file)`, em sucesso seta coverValue=url e mostra preview, em erro exibe a mensagem amigável do throw (Toast/inline, nunca alert()), e sempre zera uploading. Adicionar o campo "Valor atual (R$)" (input number, name="atual", default 0). Estender o objeto do `onConfirm` do submit com `cover: coverValue` e `atual: parseFloat(fd.get('atual')) || 0`, mantendo nome/descricao/emoji/alvo/prazo/tint. Desabilitar o submit enquanto `uploading`. Em fides-metas.css adicionar os blocos `.met-cover-picker*` (abas, grid de presets, preview) — sem redefinir chrome global.
  </action>
  <acceptance_criteria>
    - `grep -c "uploadGoalCover" assets/fides-metas.jsx` >= 1 e `grep -c "coverValue" assets/fides-metas.jsx` >= 2.
    - `grep -c 'name="atual"' assets/fides-metas.jsx` >= 1 (campo Valor atual no criar).
    - `grep -c "cover:" assets/fides-metas.jsx` >= 1 (payload estendido) e o onConfirm do criar inclui `atual`.
    - `grep -c "met-cover-picker" assets/fides-metas.css` >= 1.
    - Os useState do seletor estão antes do `if (!rendered) return null`; console sem warning de hooks.
    - Browser: criar meta escolhendo um preset → card renderiza a capa e persiste após F5 (UAT-1); criar meta enviando um JPG pequeno → objeto vai para goal-covers/<user_id>/, card mostra a foto, persiste (UAT-2); arquivo > 5MB/tipo inválido é rejeitado com mensagem amigável (sem alert()).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "coverValue" assets/fides-metas.jsx &amp;&amp; grep -c "met-cover-picker" assets/fides-metas.css</automated>
  </verify>
  <done>CriarMetaModal com seletor Galeria/Enviar foto + Valor atual; payload leva cover/atual; upload validado e sem alert().</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seletor de capa + Status no AjustarPlanoModal + limpeza de Storage (editar/excluir)</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (AjustarPlanoModal :264-342 — form/onConfirm; mount editTarget/deleteTarget e onConfirm de edição :781-799; MetConfirmDeleteModal :344-373)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (deleteGoalCover; owner-only delete; anti-pattern confirm/alert)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (No confirm/alert; Form-payload pattern)
  </read_first>
  <action>
    Em AjustarPlanoModal, puxar `userId`, `uploadGoalCover`, `deleteGoalCover` de useFides() e declarar (antes do early return) os useState do seletor de capa (aba, coverValue inicializado de `meta.cover`, uploading), reaproveitando o MESMO seletor Galeria/Enviar foto da Task 1 (extrair para um sub-componente compartilhado CoverPicker se conveniente — hooks-safe). Adicionar o campo Status como toggle/segmented Ativa/Concluída ligado a `meta.completed`. Estender o onConfirm com `cover: coverValue` e `completed` (boolean do toggle). No handler de edição do MetasStudio (onConfirm do AjustarPlanoModal), traduzir para o patch de updateGoal incluindo `image_url: patch.cover ?? null` e `completed: patch.completed` (+ completed_at coerente: setar ISO quando passa a concluída, null quando volta a ativa); e SE a capa anterior (`editTarget.cover`) era uma URL de Storage (não `preset:`) e mudou, chamar `deleteGoalCover(coverStoragePath(editTarget.cover))`. Adicionar o helper `coverStoragePath(url)` (retorna a parte após `/goal-covers/` quando url é de Storage; null para preset:/null). No fluxo de exclusão (onConfirm do MetConfirmDeleteModal / deleteTarget), antes/depois de deleteGoal chamar `deleteGoalCover(coverStoragePath(deleteTarget.cover))` quando a capa for de upload — sem confirm()/alert() extra (o MetConfirmDeleteModal já confirma).
  </action>
  <acceptance_criteria>
    - `grep -c "deleteGoalCover" assets/fides-metas.jsx` >= 2 (troca de capa no editar + exclusão da meta).
    - `grep -c "coverStoragePath" assets/fides-metas.jsx` >= 2 (definição + usos) e deriva o path após `/goal-covers/`.
    - AjustarPlanoModal: `grep -c "coverValue" assets/fides-metas.jsx` reflete uso também na edição; o onConfirm de edição inclui `image_url` e `completed` no patch.
    - `grep -c "confirm(" assets/fides-metas.jsx` == 0 e `grep -c "alert(" assets/fides-metas.jsx` == 0 (sem nativos).
    - Browser (UAT-5): editar uma meta troca capa/tint/valores e persiste; ao trocar uma capa de upload por outra, a antiga some do Storage; excluir uma meta com capa de upload remove a meta E apaga o objeto em goal-covers/<user_id>/ (verificar no Dashboard → Storage). Presets não disparam delete de Storage.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "deleteGoalCover" assets/fides-metas.jsx &amp;&amp; grep -c "coverStoragePath" assets/fides-metas.jsx</automated>
  </verify>
  <done>AjustarPlanoModal com seletor de capa + Status; editar/excluir limpam capas de upload do Storage; sem confirm()/alert() nativos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input file (browser) → uploadGoalCover → Storage | Arquivo do usuário atravessa; client valida (UX), RLS/bucket é a fronteira |
| delete de meta/capa → Storage remove | Só o dono remove sua pasta (RLS do Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | V5 Input Validation | aba Enviar foto | mitigate | Pré-check de MIME/size no client (feedback amigável); backstop real = allowed_mime_types/file_size_limit + RLS do bucket (Plan 01) |
| T-08-08 | Tampering/V8 | uploadGoalCover path | mitigate | Path owner-scoped com UUID vem do helper do Plan 03 — nome do arquivo do usuário nunca entra no path |
| T-08-02 | Tampering/DoS | deleteGoalCover (editar/excluir) | mitigate | RLS de delete owner-only (Plan 01) garante que só o dono apaga sua capa; coverStoragePath deriva só a pasta do próprio user |
| T-08-12 | Info Disclosure | mensagens de erro | mitigate | Erros de upload exibidos como texto amigável PT-BR (sem stack/segredo); sem confirm()/alert() nativos |
| T-08-SC | Tampering | dependências | accept | Nenhum `npm install` — usa helpers do Plan 03 + SDK já carregado |
</threat_model>

<verification>
- Galeria e upload setam cover; persistência em image_url após reload (UAT-1/UAT-2).
- Editar troca capa/valores/status e persiste; excluir/trocar limpam capa de upload do Storage (UAT-5).
- Two-user RLS smoke test (no /gsd-verify-work): user B não lê/sobrescreve/apaga objeto de user A.
- `security-reviewer` sobre o fluxo de upload antes do commit (CLAUDE.md).
- Console sem warning de hooks; sem confirm()/alert() nativos.
</verification>

<success_criteria>
Ciclo de capas completo: escolher preset, enviar foto própria, trocar e excluir com limpeza de Storage — persistido e isolado por usuário. Fecha UAT-1/UAT-2/UAT-5.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-06-SUMMARY.md` when done
</output>
