---
phase: 08-metas-vision-board-redesign
plan: 04
type: execute
wave: 2
depends_on: [08-03]
files_modified:
  - assets/fides-metas.jsx
  - assets/fides-metas.css
autonomous: true
requirements: [UAT-3, UAT-7]
tags: [frontend, controls, search, filter, status, chapter-iii, rules-of-hooks]
user_setup: []

must_haves:
  truths:
    - "Barra de controles nova: busca por nome/descrição (filtro client-side) + filtro segmentado Todas/Ativas/Concluídas + botão Nova meta (UAT-3)"
    - "A busca filtra os cards por nome ou descrição em tempo real; limpar a busca restaura a lista"
    - "O filtro segmentado segmenta por completed: Ativas = !completed, Concluídas = completed, Todas = tudo (D5, UAT-3)"
    - "Capítulo III 'Já atingidas' lista as metas concluídas reais (goals com completed) em vez do texto vazio hardcoded (D5)"
    - "Todo novo useState (search, statusFilter) é declarado no bloco de hooks no TOPO de MetasStudio, antes do early return isEmpty — sem warning 'Rendered more hooks…' (UAT-7)"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "Barra de controles + filtro client-side + Capítulo III populado por completed"
      contains: "statusFilter"
    - path: "assets/fides-metas.css"
      provides: "Layout/espaçamento da barra de controles de Metas"
      contains: "met-controls"
  key_links:
    - from: "statusFilter / search (MetasStudio state)"
      to: "grid de metas (Capítulo I) + Capítulo III"
      via: "filtro client-side sobre computed usando m.completed e m.nome/m.descricao"
      pattern: "completed"
    - from: "reuso .fds-seg / .fds-tx-v2-search"
      to: "barra de controles"
      via: "classes já estilizadas de fides.css/fides-transacoes.css"
      pattern: "fds-seg"
  prohibitions:
    - "NÃO adicionar coluna/enum de status (Pausada/Arquivada) — status deriva de completed (scope_fence)"
    - "NÃO declarar hooks depois de qualquer early return (Rules of Hooks — bug da Phase 07)"
    - "NÃO reimplementar search/segmented do zero — reutilizar .fds-tx-v2-search e .fds-seg/.fds-seg-3"
    - "NÃO tocar no vcard/hero ainda (Plan 05) nem no seletor de capa (Plan 06)"
---

<objective>
Adicionar a barra de controles do vision-board em `assets/fides-metas.jsx`: busca client-side (nome/descrição) + filtro segmentado de status (Todas/Ativas/Concluídas, derivado de `completed`) + botão "Nova meta"; e popular o Capítulo III "Já atingidas" com as metas concluídas reais (hoje é um texto vazio hardcoded). Reutiliza os padrões de chrome já existentes (`.fds-tx-v2-search`, `.fds-seg`/`.fds-seg-3`).

Purpose: UAT-3 exige busca + segmentação por status funcionando; isso só é possível agora que o Plan 03 mapeia `completed` no `normalizeGoal`. Mantém identidade Fides reusando componentes já estilizados. Este é o plano de "dados/estado" da UI — sem redesign visual dos cards (Plan 05).
Output: MetasStudio com estado local search/statusFilter, barra de controles renderizada, grid do Capítulo I filtrado e Capítulo III populado por completed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-metas.jsx
@assets/fides-metas.css
@assets/fides-transacoes.jsx
</context>

## Artifacts this phase produces (contribuição deste plano)

- Novo estado local em MetasStudio: `search`, `statusFilter` (declarados no bloco de hooks do topo, ~:714-721).
- Barra de controles `.met-controls` (busca reusando `.fds-tx-v2-search` + filtro `.fds-seg fds-seg-3` + botão Nova meta).
- Lógica de filtro client-side sobre `computed` (por nome/descrição e por `completed`).
- Capítulo III renderiza lista de metas concluídas (substitui `.met-done-empty` hardcoded).
- Bloco CSS `.met-controls*` (layout/espaçamento) em fides-metas.css.

<tasks>

<task type="auto">
  <name>Task 1: Estado local + barra de controles (busca + filtro segmentado + Nova meta)</name>
  <files>assets/fides-metas.jsx, assets/fides-metas.css</files>
  <read_first>
    - assets/fides-metas.jsx (bloco de hooks de MetasStudio :714-721; early return isEmpty :723; hero/Capítulo I :822-972)
    - assets/fides-transacoes.jsx (search :645-658 — .fds-tx-v2-search/Icon.Search/clear; segmented :1286-1297 — .fds-seg/.fds-seg-3/on)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (Segmented status filter + Search input — copiar verbatim; Rules of Hooks)
  </read_first>
  <action>
    Declarar `const [search, setSearch] = React.useState('')` e `const [statusFilter, setStatusFilter] = React.useState('todas')` DENTRO do bloco de useState no topo de MetasStudio (junto de emBreve/criarOpen/editTarget/deleteTarget), ANTES do early return isEmpty — nunca depois. Renderizar, logo acima do Capítulo I (ou entre o hero e o ChapterMark I), uma barra `.met-controls` contendo: (a) input de busca reusando a marcação `.fds-tx-v2-search` (Icon.Search, input controlado por search/setSearch, botão clear `.fds-tx-v2-search-clear` que zera search) com placeholder "Buscar por nome ou descrição…"; (b) o filtro segmentado `.fds-seg fds-seg-3` com as três opções [['todas','Todas'],['ativas','Ativas'],['concluidas','Concluídas']] aplicando classe `on` ao selecionado; (c) o botão "Nova meta" (reaproveitar o handler `setCriarOpen(true)`). Em fides-metas.css adicionar apenas o bloco `.met-controls` (flex/gap/wrap responsivo, alinhamento) — NÃO redefinir `.fds-seg`/`.fds-tx-v2-search` (já existem globalmente). Não alterar hero nem cards nesta task.
  </action>
  <acceptance_criteria>
    - `grep -c "statusFilter" assets/fides-metas.jsx` >= 3 (state + set + render).
    - `grep -c "fds-seg-3" assets/fides-metas.jsx` >= 1 e `grep -c "fds-tx-v2-search" assets/fides-metas.jsx` >= 1 (reuso, não reinvenção).
    - `grep -c "met-controls" assets/fides-metas.css` >= 1.
    - Os dois novos useState aparecem ANTES da linha do `if (isEmpty) return` (checar ordem no arquivo; console do browser sem warning "Rendered more hooks…").
    - Browser: digitar no campo filtra visualmente; clicar Ativas/Concluídas/Todas alterna a classe `on`; botão Nova meta abre CriarMetaModal.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "statusFilter" assets/fides-metas.jsx &amp;&amp; grep -c "met-controls" assets/fides-metas.css</automated>
  </verify>
  <done>Barra de controles renderiza com busca+segmentado+Nova meta reusando chrome existente; hooks no topo; sem warning.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar filtro client-side ao grid + popular Capítulo III por completed</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (computed :742-752; grid Capítulo I :886-972; Capítulo III :1064-1068 — .met-done/.met-done-empty hardcoded)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pitfall 2 — completed alimenta filtro e "Já atingidas")
  </read_first>
  <action>
    Derivar do `computed` uma lista visível para o Capítulo I aplicando: (1) filtro de status — `todas` = todas; `ativas` = itens com `!m.completed`; `concluidas` = itens com `m.completed`; (2) filtro de busca — quando `search` não vazio, manter itens cujo `nome` ou `descricao` (case-insensitive) contenham o termo. Renderizar o grid do Capítulo I a partir dessa lista filtrada (em vez de `computed` direto). Tratar o estado vazio de resultado de busca com uma mensagem curta (ex.: "Nenhuma meta encontrada para essa busca.") sem quebrar layout. No Capítulo III, substituir o `.met-done-empty` fixo: se houver metas com `completed`, renderizar uma lista compacta delas (emoji + nome + valor guardado + "Concluída em" quando `completedAt` existir); manter a mensagem vazia apenas quando não houver nenhuma concluída. Não introduzir novos hooks aqui (usar os estados já declarados na Task 1). Preservar Capítulos I/II e todos os painéis existentes.
  </action>
  <acceptance_criteria>
    - `grep -c "\.completed" assets/fides-metas.jsx` >= 2 (uso em filtro + Capítulo III).
    - O grid do Capítulo I mapeia a lista FILTRADA (não `computed` cru) — inspecionar o JSX.
    - Browser: com uma meta marcada concluída (via Plan 05 "Marcar como concluída" ou seed), o filtro Concluídas mostra só ela e o Capítulo III a lista; Ativas a esconde; Todas mostra ambas.
    - Busca por um trecho do nome reduz o grid; limpar restaura; busca sem match mostra a mensagem de vazio sem erro no console.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\.completed" assets/fides-metas.jsx</automated>
  </verify>
  <done>Grid do Capítulo I respeita busca+status; Capítulo III lista metas concluídas reais; sem novos hooks fora do topo.</done>
</task>

</tasks>

<verification>
- Busca e filtro Ativas/Concluídas/Todas funcionam sobre dados reais (completed do Plan 03).
- Capítulo III popula com metas concluídas; vazio só quando não há nenhuma.
- DevTools console sem warning de Rules of Hooks (UAT-7); sem scroll horizontal introduzido pela barra em 400×512.
</verification>

<success_criteria>
UAT-3 pronto (busca + segmentação por status) e Capítulo III vivo, com hooks íntegros — sobre a base de dados do Plan 03.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-04-SUMMARY.md` when done
</output>
