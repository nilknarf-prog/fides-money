---
phase: 08-metas-vision-board-redesign
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-metas.jsx
  - assets/fides-metas.css
autonomous: true
gap_closure: true
requirements: [GAP-INFINITY, GAP-DUP-NOVA, GAP-EMOJI]
tags: [frontend, metas, ui-polish, emoji-picker, rules-of-hooks, gap-closure]
user_setup: []

# NOTA (fora de escopo desta passada): dois gaps do 08-UAT ficam DEFERIDOS p/ passada com research-gate —
# "Capas fotos reais CC0" (needs_research) e "metas-exemplo pré-criadas" (needs_research). Nenhuma task aqui.

must_haves:
  truths:
    - "Nenhuma exibição derivada de mesesAteFim renderiza a string 'Infinity' — metas sem prazo/aporte mostram 'sem prazo' (ou vazio natural) em TODOS os pontos: lede do hero, strip 'Maior meta', e a stat 'Chega em' do card"
    - "No estado com metas, apenas UM CTA 'Nova meta' é visível — o primário escuro .met-controls-nova; o botão vazado (stu-link) que coexistia foi removido"
    - "Emoji é escolhido por um seletor visual (grid curado hand-rolled, sem npm — restrição Babel-standalone) nos modais Criar meta E Ajustar plano; no desktop, clicar abre o grid e selecionar preenche o emoji da meta"
    - "As Rules of Hooks permanecem íntegras — todo novo useState fica antes de qualquer early return; sem warning 'Rendered more hooks…' no console (bug recorrente da Phase 07)"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "helper mesesLabel(mesesAteFim) + EmojiPicker (grid curado) + remoção do CTA duplicado"
      contains: "mesesLabel"
    - path: "assets/fides-metas.css"
      provides: "Blocos CSS do EmojiPicker (grid/opções/estado selecionado)"
      contains: "emoji"
  key_links:
    - from: "EmojiPicker (value/onChange)"
      to: "onConfirm dos modais Criar/Ajustar (campo emoji)"
      via: "estado emojiValue no modal → <input type=hidden name=emoji> lido pelo FormData existente"
      pattern: "emojiValue"
    - from: "mesesLabel(mesesAteFim)"
      to: "todas as exibições de meses (hero lede, strip Maior meta, stat Chega em)"
      via: "helper único que retorna 'sem prazo' quando Infinity, senão N mês/meses"
      pattern: "mesesLabel"
  prohibitions:
    - "NÃO usar pacote npm / <script> de emoji-picker — o app roda via Babel-standalone no browser SEM build step; o picker é um grid curado escrito à mão"
    - "NÃO declarar hooks após early return (Rules of Hooks — bug da Phase 07): emojiValue e estado de abertura do picker vão no bloco de hooks do topo, antes de if(!rendered)return null"
    - "NÃO usar confirm()/alert() nativos em nenhum fluxo novo (CLAUDE.md — usar fides-ui)"
    - "NÃO remover o botão primário .met-controls-nova — remover SOMENTE o CTA vazado duplicado"
    - "NÃO alterar a lógica de filtro/status nem os cálculos de computed (pct/faltam/mesesAteFim) — só a APRESENTAÇÃO dos meses"
    - "Presentation-only: nenhuma mudança na camada de dados (fides-store.jsx) nem no schema — sem nova fronteira de confiança"
---

<!-- planner-discipline-allow: maxLength={2} -->

<objective>
Fechar os 3 gaps de UI/polish do 08-UAT (todos presentation-only, independentes do blocker de conclusão): (1) guardar `Infinity` em TODAS as exibições de meses → "sem prazo"; (2) remover o botão "Nova meta" duplicado (vazado), mantendo só o primário escuro; (3) trocar o `<input maxLength=2>` de emoji por um seletor visual (grid curado hand-rolled, sem npm) nos dois modais, porque no desktop não há teclado de emoji nativo.

Purpose: São os arranhões visíveis que o usuário reportou testando a tela; são baratos, isolados e não dependem do diagnóstico do blocker — por isso rodam PRIMEIRO (wave 1), garantindo que a incerteza diagnose-first do blocker (08-08) não segure estas correções triviais.
Output: `mesesLabel` aplicado em todos os pontos; um único CTA "Nova meta"; `EmojiPicker` funcional nos modais Criar/Ajustar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-metas-vision-board-redesign/08-UAT.md
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-metas.jsx
@assets/fides-metas.css
</context>

## Artifacts this phase produces (contribuição deste plano)

- `mesesLabel(mesesAteFim)` — helper de módulo em `fides-metas.jsx` que retorna `'sem prazo'` quando `mesesAteFim === Infinity`, senão `${n} ${n === 1 ? 'mês' : 'meses'}`.
- `EmojiPicker` — sub-componente React (grid curado de emojis de metas, hand-rolled, controlado por value/onChange) em `fides-metas.jsx`; usado por `CriarMetaModal` e `AjustarPlanoModal`.
- Estado `emojiValue` (+ estado de abertura do grid) no bloco de hooks de cada modal; `<input type="hidden" name="emoji">` para o FormData existente continuar lendo o emoji.
- Blocos CSS do EmojiPicker em `fides-metas.css` (`.met-emoji-picker*` ou equivalente).
- Remoção do `action` (CTA vazado "Nova meta") do `ChapterMark` do Capítulo I.

<tasks>

<task type="auto">
  <name>Task 1: Guardar Infinity em TODAS as exibições de meses (mesesLabel)</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (computed/mesesAteFim :997-1007; lede do hero com meses :1137-1138; strip "Maior meta" :1157-1162; stat "Chega em" do card :1223-1226; fimLabel :1002)
    - .planning/phases/08-metas-vision-board-redesign/08-UAT.md (gap "Meta sem prazo/aporte não deve exibir 'Infinity meses'" — missing: guardar em TODAS as exibições)
  </read_first>
  <action>
    Adicionar, em escopo de módulo no topo de `fides-metas.jsx` (perto de outros helpers de formatação), o helper `mesesLabel(mesesAteFim)` que retorna `'sem prazo'` quando o valor é `Infinity` (ou não-finito), e senão retorna `${n} ${n === 1 ? 'mês' : 'meses'}`. Depois, substituir por `mesesLabel(...)` TODAS as exibições textuais derivadas de `mesesAteFim` que hoje concatenam a palavra meses sem guarda:
    (a) na lede do hero (~:1138), a frase "A próxima a chegar é X, em {proxima?.mesesAteFim} {…mês/meses}" deve passar a usar `mesesLabel(proxima?.mesesAteFim)`; garanta que a frase permaneça natural quando não há prazo (ex.: renderizar "sem previsão" no lugar de "em Infinity meses" — se preferir, ajuste a preposição para não ficar "em sem prazo");
    (b) na strip "Maior meta" (~:1161), trocar `{fmtBRL(maior?.alvo)} · {maior?.mesesAteFim} meses` por `{fmtBRL(maior?.alvo)} · {mesesLabel(maior?.mesesAteFim)}`;
    (c) na stat "Chega em" do card (~:1225), que hoje já guarda com '—', trocar para `mesesLabel(m.mesesAteFim)` de modo que o caso sem prazo mostre "sem prazo" (padronizando com o resto). NÃO alterar o cálculo de `mesesAteFim`, `fim`, `fimLabel`, `pct` ou `faltam` — só a camada de apresentação. Deixar os usos de `meses` NÃO derivados de `mesesAteFim` (ex.: painéis de simulação com `r.meses`/loop de anos, e o array `meses` de nomes de mês) intactos — são sempre finitos e fora do gap.
  </action>
  <acceptance_criteria>
    - `grep -c "function mesesLabel" assets/fides-metas.jsx` == 1 (helper definido uma vez).
    - `grep -c "mesesLabel(" assets/fides-metas.jsx` >= 4 (definição/uso + 3 pontos de exibição: hero lede, Maior meta, Chega em).
    - Nenhuma exibição de `mesesAteFim` concatena a palavra meses sem passar por `mesesLabel` — confirmar por leitura das linhas ~1138/~1161/~1225 (as três agora usam `mesesLabel`).
    - Browser: criar/ter uma meta com `contribuicao == 0` (sem aporte mensal) → em nenhum lugar aparece "Infinity meses"; mostra "sem prazo" (ou vazio natural) no hero, na strip "Maior meta" e na stat "Chega em".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function mesesLabel" assets/fides-metas.jsx &amp;&amp; grep -c "mesesLabel(" assets/fides-metas.jsx</automated>
  </verify>
  <done>Um único helper mesesLabel cobre todas as exibições de meses; metas sem prazo mostram "sem prazo", nunca "Infinity".</done>
</task>

<task type="auto">
  <name>Task 2: Remover o botão "Nova meta" duplicado (vazado), manter só o primário</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (barra de controles com o primário .met-controls-nova :1197-1203; ChapterMark do Capítulo I com o CTA vazado stu-link "Nova meta" no prop action :1207-1209)
    - .planning/phases/08-metas-vision-board-redesign/08-UAT.md (gap "Deve haver apenas um botão 'Nova meta' visível (o primário escuro)")
  </read_first>
  <action>
    No estado com metas, existem hoje DOIS CTAs "Nova meta" empilhados: o primário escuro `.met-controls-nova` (~:1197-1203, na barra de controles) e um segundo vazado/link (`className="stu-link"`) passado como `action` do `ChapterMark` do Capítulo I "Em curso" (~:1207-1209). Ambos chamam `setCriarOpen(true)`. Remover o CTA vazado: apagar o prop `action={...}` do `ChapterMark roman="I"` (deixando o ChapterMark do Capítulo I sem action), mantendo INTACTO o botão primário `.met-controls-nova`. Antes de editar, confirmar por leitura que o botão a remover é o vazado (link/ghost) e que o mantido é o primário escuro. Não tocar no botão do estado vazio (`goals.length === 0`, ~:1107-1113) nem no eyebrow "Nova meta" do modal — são outras superfícies/estados.
  </action>
  <acceptance_criteria>
    - `grep -c "met-controls-nova" assets/fides-metas.jsx` >= 1 (primário mantido).
    - O `ChapterMark roman="I"` (Capítulo I "Em curso") não passa mais um `action` com "Nova meta" — confirmar por leitura de ~:1207.
    - `grep -c "Nova meta" assets/fides-metas.jsx` == 4 (era 5: caem para 4 após remover o CTA do action do Capítulo I; permanecem o eyebrow do modal :342, o botão do estado vazio :1112, o comentário :1175 e o primário :1202).
    - Browser (com metas): apenas UM botão "Nova meta" visível (o primário escuro); o vazado sumiu.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "met-controls-nova" assets/fides-metas.jsx &amp;&amp; grep -c "Nova meta" assets/fides-metas.jsx</automated>
  </verify>
  <done>Só o CTA primário "Nova meta" permanece no estado com metas; o duplicado vazado foi removido.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: EmojiPicker (grid curado hand-rolled) nos modais Criar/Ajustar</name>
  <files>assets/fides-metas.jsx, assets/fides-metas.css</files>
  <read_first>
    - assets/fides-metas.jsx (CriarMetaModal: bloco de hooks :320-324, onSubmit/onConfirm lê fd.get('emoji') :352-364, input de emoji atual :368-371; AjustarPlanoModal: bloco de hooks :434-448, onConfirm :469-480, input de emoji atual :485-488; CoverPicker como modelo de sub-componente controlado :382-388/:499-505)
    - assets/fides-metas.css (estilos de campo .fds-field/.fds-input e do CoverPicker como base de layout do grid)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (Rules of Hooks: hooks antes de early return; Form-payload pattern dos modais)
    - CLAUDE.md (React via Babel-standalone, SEM build/npm — picker é hand-rolled; Rules of Hooks causaram bug na Phase 07)
  </read_first>
  <action>
    Criar um sub-componente `EmojiPicker` (top-level em `fides-metas.jsx`, ao lado de `CoverPicker`) controlado por `value` + `onChange`: exibe um botão-gatilho mostrando o emoji atual e, ao clicar, revela um grid curado de emojis de metas escrito à mão (array literal no código — SEM npm/script externo, restrição Babel-standalone). O grid deve cobrir temas comuns de meta/finanças (>= 24 emojis; ex.: 🎯 ✈️ 🏠 🚗 💰 🎓 💍 👶 🏥 🏖️ 🌴 💻 🎁 🎉 📈 🚨 🛟 🏦 💳 🐷 ⛰️ 🏝️ 📱 🎮 📷 🚲 ⛵ 🛋️ 🐶 ⚽ 🎸 📚 🧳 🌍 ❤️ ⭐ — lista final é discricionária). Clicar num emoji chama `onChange(emoji)` e fecha o grid; o item selecionado recebe estado visual "on". As Rules of Hooks: o EmojiPicker é um componente isolado, então seus hooks (estado de aberto/fechado) ficam no seu próprio topo, antes de qualquer retorno.
    Nos DOIS modais, substituir o `<input className="fds-input" name="emoji" maxLength={2} …>` pelo `<EmojiPicker value={emojiValue} onChange={setEmojiValue}/>` MAIS um `<input type="hidden" name="emoji" value={emojiValue}/>` (para o `FormData`/`fd.get('emoji')` existente continuar funcionando sem mexer no onConfirm). Adicionar `const [emojiValue, setEmojiValue] = React.useState('🎯')` ao bloco de hooks de `CriarMetaModal` (antes do `if (!rendered) return null`) e, no `useEffect` de reset (open), setar `setEmojiValue('🎯')`. Em `AjustarPlanoModal`, adicionar `const [emojiValue, setEmojiValue] = React.useState('🎯')` ao bloco de hooks (antes dos early returns) e, no `useEffect` de init (open && meta), setar `setEmojiValue(meta.emoji || '🎯')`. Em `fides-metas.css`, adicionar os blocos do grid (botão-gatilho, grade de opções, célula selecionada) reaproveitando tokens/paleta existentes; nada de scroll horizontal em 400px.
  </action>
  <acceptance_criteria>
    - `grep -c "function EmojiPicker" assets/fides-metas.jsx` == 1 (componente definido).
    - `grep -c "EmojiPicker" assets/fides-metas.jsx` >= 3 (definição + uso nos 2 modais).
    - `grep -c "emojiValue" assets/fides-metas.jsx` >= 6 (2 useState + 2 hidden input + 2 inits no useEffect, no mínimo).
    - `grep -c 'name="emoji"' assets/fides-metas.jsx` >= 2 (hidden inputs preservam o contrato do FormData nos 2 modais).
    - `grep -c 'maxLength={2}' assets/fides-metas.jsx` == 0 (os inputs de texto de emoji foram substituídos).
    - Nenhum `import`/`<script>` de pacote de emoji foi adicionado — `grep -c "emoji-mart\|emoji-picker\|require(" assets/fides-metas.jsx` == 0.
    - Browser desktop: em Criar meta e em Ajustar plano, clicar no seletor abre o grid; escolher um emoji atualiza o chip; salvar persiste o emoji escolhido. Console sem warning "Rendered more hooks…".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function EmojiPicker" assets/fides-metas.jsx &amp;&amp; grep -c "emojiValue" assets/fides-metas.jsx &amp;&amp; grep -c 'maxLength={2}' assets/fides-metas.jsx</automated>
  </verify>
  <done>Seletor de emoji em grid curado (sem npm) funcional nos dois modais; emoji flui ao onConfirm via input hidden; hooks íntegros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Presentation-only: este plano NÃO introduz nova fronteira de confiança nem toca a camada de dados (`fides-store.jsx`), o schema ou o Storage. O emoji escolhido é uma string curta de um grid fechado (não entrada livre), renderizada como texto — sem novo vetor de injeção. Sem `npm install` (só APIs nativas/JSX), então sem superfície de supply-chain nova.

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Correções de UI sobre dados já carregados; sem novo cruzamento cliente→dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-SC | Tampering | dependências | accept | Nenhum `npm install`/`<script>` de terceiro adicionado — EmojiPicker é hand-rolled; sem novo risco de supply-chain |
| T-08-07-UI | Tampering/XSS | render do emoji | accept | Emoji vem de um grid curado fechado e é renderizado como texto (não HTML cru); sem dangerouslySetInnerHTML |
</threat_model>

<verification>
- Wave/merge: rodar os 3 greps de verify; abrir a tela de Metas em 400×512 e confirmar (a) nenhum "Infinity meses" em nenhum ponto, (b) um único CTA "Nova meta", (c) picker de emoji abrindo e persistindo nos dois modais.
- Console DevTools sem warning de Rules of Hooks ao abrir/fechar os modais.
- `git status --porcelain assets/fides-store.jsx` vazio (presentation-only — camada de dados intocada).
</verification>

<success_criteria>
Os 3 gaps de UI fechados: mesesLabel cobre todas as exibições de meses; só o CTA primário "Nova meta" permanece; EmojiPicker curado funcional nos modais Criar/Ajustar; hooks íntegros; nenhuma mudança em fides-store.jsx/schema.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-07-SUMMARY.md` when done
</output>
