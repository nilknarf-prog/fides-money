---
phase: 08-metas-vision-board-redesign
plan: 05
type: execute
wave: 3
depends_on: [08-04, 08-02]
files_modified:
  - assets/fides-metas.jsx
  - assets/fides-metas.css
autonomous: true
requirements: [UAT-4, UAT-6, UAT-7]
tags: [frontend, hero, vcard, cover, aportar, inline-update, rules-of-hooks, design]
user_setup: []

must_haves:
  truths:
    - "O wrapper do hero de Metas usa a classe própria met-hero (não a classe compartilhada do hero da home) e tem um novo bloco .met-hero em fides-metas.css com backdrop de colagem distinto (D3, UAT-6)"
    - "fides-studio.css NÃO é editado — o hero da home (Dashboard/Contas) permanece intacto (D3, scope_fence)"
    - "Os cards viram vcard: capa 186px (preset OU upload via resolveCoverUrl + scrim; fallback = gradiente tint quando sem capa), overlay (pill status derivado de completed, chip emoji, nome, descrição), corpo (valor atual grande de R$alvo, barra no tint + glow, % + faltam, 2 stats)"
    - "Existe COVER_PRESETS (16 ids → assets/covers/<id>.svg) + resolveCoverUrl(cover) que resolve preset:<id> vs URL de Storage vs null (D4)"
    - "AportarModal (hoje dead code, :89-181) é montado e ligado a updateGoal({ current: atual + valor }) — fecha o GAP Phase-07 #2 (D6, UAT-4)"
    - "Quick 'Atualizar saldo' inline no card seta current direto via updateGoal({ current }) sem reload (D6, UAT-4)"
    - "Dots-menu 'Marcar como concluída' liga a updateGoal({ completed: true, completed_at: <ISO> }) (D5)"
    - "Todo novo useState (aportarTarget etc.) fica no bloco de hooks do topo de MetasStudio, antes do early return; sub-componentes declaram hooks antes de seus early returns — sem warning de hooks (UAT-7)"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "met-hero wrapper + vcard render + resolveCoverUrl + AportarModal montado + inline atualizar saldo"
      contains: "resolveCoverUrl"
    - path: "assets/fides-metas.css"
      provides: "Bloco .met-hero (backdrop colagem) + .vcard* (capa/scrim/overlay/corpo/ações hover)"
      contains: "met-hero"
  key_links:
    - from: "resolveCoverUrl(m.cover)"
      to: "background-image da capa do vcard"
      via: "preset:<id> → COVER_PRESETS; URL de Storage → direto; null → fallback gradiente tint"
      pattern: "resolveCoverUrl"
    - from: "AportarModal (montado) / inline Atualizar saldo"
      to: "updateGoal (fides-store.jsx)"
      via: "onConfirm → updateGoal(id, { current })"
      pattern: "updateGoal"
  prohibitions:
    - "NÃO editar fides-studio.css nem a classe compartilhada do hero da home (D3, Pitfall 1)"
    - "NÃO renomear as sub-classes internas do hero (prefixo stu-hero-) — só o wrapper externo vira met-hero"
    - "NÃO renderizar capa via dangerouslySetInnerHTML — só background-image/img src"
    - "NÃO usar confirm()/alert() no fluxo de atualizar saldo — usar controle inline/modal (CLAUDE.md)"
    - "NÃO declarar hooks após early return (Rules of Hooks — bug da Phase 07)"
    - "NÃO implementar fluxo completo de troféu/conquista ao concluir (segue deferido/EmBreve — scope_fence); crop/drag-reorder fora de escopo"
---

<!-- planner-discipline-allow: className="stu-hero" -->
<!-- planner-discipline-allow: dangerouslySetInnerHTML -->

<objective>
O redesign visual do vision-board em `assets/fides-metas.jsx` + `.css`: (1) dar ao hero de Metas identidade própria `met-hero` (D3) sem tocar no hero compartilhado da home; (2) transformar `met-card` em `vcard` (capa com scrim, overlay, corpo, barra no tint, ações no hover) usando `resolveCoverUrl` + `COVER_PRESETS`; (3) fechar o GAP Phase-07 #2 montando o `AportarModal` (dead code hoje) e ligando as ações inline de saldo (D6) e "Marcar como concluída" (D5).

Purpose: É o coração do "vision board" — a tela ganha a linguagem visual do mockup aprovado mantendo a identidade editorial Fides, e as duas ações de update inline (Aportar com projeção + Atualizar saldo direto) passam a funcionar de verdade (hoje abrem "Em breve"). Cobre UAT-4/UAT-6 e reforça UAT-7.
Output: met-hero distinto, grid de vcards com capa/fallback, AportarModal vivo, atualizar-saldo inline e conclusão de meta funcionais.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-metas.jsx
@assets/fides-metas.css
@assets/fides-studio.css
</context>

## Artifacts this phase produces (contribuição deste plano)

- Classe/bloco `.met-hero` + `.met-hero::before` (backdrop colagem) em fides-metas.css; wrapper do hero em jsx passa a `met-hero`.
- `COVER_PRESETS` (16 ids → `assets/covers/<id>.svg`) + helper `resolveCoverUrl(cover)` em fides-metas.jsx.
- Render `.vcard` (capa/scrim/overlay pill+chip+nome+descrição/corpo valor+barra+stats/ações hover) + blocos CSS `.vcard*`.
- Estado `aportarTarget` (+ estado do editor inline de saldo) no topo de MetasStudio; `<AportarModal>` montado.
- Sub-componente do editor inline "Atualizar saldo" (hooks-safe).

<tasks>

<task type="auto">
  <name>Task 1: Renomear o wrapper do hero para met-hero + novo bloco CSS distinto (D3)</name>
  <files>assets/fides-metas.jsx, assets/fides-metas.css</files>
  <read_first>
    - assets/fides-metas.jsx (hero :824-880 — wrapper externo e sub-elementos)
    - assets/fides-studio.css (bloco base do hero compartilhado :306-322 — copiar o box model como ponto de partida; NÃO editar este arquivo)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (Hero rename D3 — só o wrapper externo; manter sub-classes prefixadas)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pitfall 1 — colisão de classe compartilhada)
  </read_first>
  <action>
    No jsx, alterar APENAS a className do `<section>` externo do hero (atualmente a classe compartilhada do hero da home, com `data-od-id="met-hero"`) para `met-hero`. Manter TODAS as sub-classes internas com o prefixo `stu-hero-` (eyebrow/headline/amt/lede/strip) e `stu-metric*` exatamente como estão — são o vocabulário tipográfico editorial compartilhado, não o alvo do D3. Em fides-metas.css, criar um bloco novo `.met-hero` copiando o box model base do hero compartilhado (background/border/radius/padding/flex/position/overflow/box-shadow) e substituir o `::before` por um backdrop de "colagem sutil (mask) das capas" (ex.: mask-image/gradiente evocando algumas capas) para diferenciar visualmente do hero da home. NÃO editar fides-studio.css. NÃO alterar o conteúdo textual do hero.
  </action>
  <acceptance_criteria>
    - `grep -c 'className="met-hero"' assets/fides-metas.jsx` >= 1 (wrapper renomeado).
    - `grep -c 'className="stu-hero"' assets/fides-metas.jsx` == 0 (a classe compartilhada não é mais o wrapper externo; as internas `stu-hero-*` carregam sufixo e não casam este literal).
    - `grep -c "\.met-hero" assets/fides-metas.css` >= 1 (novo bloco existe).
    - `git status --porcelain assets/fides-studio.css` vazio (fides-studio.css NÃO foi tocado).
    - Browser: hero de Metas visualmente distinto do hero da home (Dashboard/Contas inalterados); sem regressão de layout em 400×512 iOS Safari.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'className="met-hero"' assets/fides-metas.jsx &amp;&amp; grep -c "\.met-hero" assets/fides-metas.css</automated>
  </verify>
  <done>Hero de Metas com classe/estilo próprios e backdrop distinto; hero da home intacto (fides-studio.css não editado).</done>
</task>

<task type="auto">
  <name>Task 2: Render vcard (capa/scrim/overlay/corpo) + COVER_PRESETS/resolveCoverUrl</name>
  <files>assets/fides-metas.jsx, assets/fides-metas.css</files>
  <read_first>
    - assets/fides-metas.jsx (render do met-card atual :886-971 — predecessor direto a substituir; computed :742-752)
    - assets/fides-metas.css (bloco .met-card atual — base para o .vcard)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (assets/covers/<id>.svg — contrato COVER_PRESETS/resolveCoverUrl; .vcard base)
    - .planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md (anatomia do vcard — capa 186px, overlay, corpo, hover)
  </read_first>
  <action>
    Adicionar, em escopo de módulo no topo de fides-metas.jsx, o objeto `COVER_PRESETS` mapeando os 16 ids do Plan 02 (viagem/casa/carro/reserva/educacao/casamento/bebe/saude/aposentadoria/negocio/tecnologia/presente/natureza/festa/investimento/emergencia) para `'assets/covers/<id>.svg'`, e o helper `resolveCoverUrl(cover)`: null→null; começa com `preset:`→ COVER_PRESETS[id] || null; senão retorna a própria string (URL de Storage). Substituir a marcação do card no map do Capítulo I (a lista JÁ filtrada do Plan 04 — preservar o filtro) por um `.vcard`: (a) área de capa 186px cujo `background-image` vem de `resolveCoverUrl(m.cover)` (via style inline) COM um scrim gradiente por cima para legibilidade; quando `resolveCoverUrl` retorna null, usar o fallback de gradiente no `m.tint` (graceful); (b) overlay sobre a capa com pill de status (Ativa quando `!m.completed`, Concluída quando `m.completed`), chip do emoji, nome (display) e descrição; (c) corpo com valor atual grande + "de R$alvo", barra de progresso no tint com glow (reaproveitar o cálculo pct/faltam do computed), `%` + "faltam R$X", e 2 stats (Aporte mensal / Chega em; ou Guardado / Concluída em quando `m.completed`). Renderizar a capa SEMPRE como background-image/img src — nunca dangerouslySetInnerHTML. Em fides-metas.css, adicionar os blocos `.vcard*` (capa, scrim, overlay, corpo, barra) partindo do box model do `.met-card`. Preservar o `MetDotsMenu` e a estrutura de ações (serão religadas na Task 3).
  </action>
  <acceptance_criteria>
    - `grep -c "resolveCoverUrl" assets/fides-metas.jsx` >= 2 (definição + uso no render).
    - `grep -c "COVER_PRESETS" assets/fides-metas.jsx` >= 2 e o mapa lista os 16 ids do Plan 02.
    - `grep -c "vcard" assets/fides-metas.jsx` >= 1 e `grep -c "\.vcard" assets/fides-metas.css` >= 1.
    - `grep -c "dangerouslySetInnerHTML" assets/fides-metas.jsx` == 0 (capa nunca via HTML cru).
    - Browser: meta sem capa (image_url null) renderiza fallback gradiente no tint; meta com `preset:<id>` mostra a SVG; pill mostra Ativa/Concluída conforme completed; barra/%/faltam corretos; sem scroll horizontal em 400×512.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "resolveCoverUrl" assets/fides-metas.jsx &amp;&amp; grep -c "\.vcard" assets/fides-metas.css</automated>
  </verify>
  <done>Grid de vcards com capa/scrim/overlay/corpo, fallback tint gracioso e pill de status; resolveCoverUrl/COVER_PRESETS no lugar.</done>
</task>

<task type="auto">
  <name>Task 3: Montar AportarModal + inline Atualizar saldo + Marcar como concluída (D6/D5)</name>
  <files>assets/fides-metas.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (AportarModal def :89-181 — dead code a montar; bloco de hooks :714-721; padrão de mount editTarget/deleteTarget :781-799; card "Aportar" onClick :957; dots "Marcar como concluída" :902)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (Modal-mount pattern p/ AportarModal; rewire de concluída; hooks-before-early-return)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pitfall 3 — AportarModal dead code; Pitfall 4 — Rules of Hooks; anti-pattern updateGoal({current}) direto sem RPC)
  </read_first>
  <action>
    Declarar `const [aportarTarget, setAportarTarget] = React.useState(null)` no bloco de hooks do topo de MetasStudio (antes do early return). Montar `<AportarModal open={!!aportarTarget} meta={aportarTarget} onConfirm={(valor) => updateGoal(aportarTarget.id, { current: aportarTarget.atual + valor })} onClose={() => setAportarTarget(null)} />` junto dos outros modais montados. Repointar o onClick do botão "Aportar" do vcard (hoje `setEmBreve(true)`) para `setAportarTarget(m)`. Rewire o item de dots-menu "Marcar como concluída" (hoje `setEmBreve(true)`) para `updateGoal(m.id, { completed: true, completed_at: new Date().toISOString() })`. Adicionar o quick "Atualizar saldo" inline revelado no hover/tap do vcard (D6, parity PlannerFin): criar um pequeno sub-componente (ex.: SaldoInlineEditor) que declara seu próprio useState (valor) ANTES de qualquer early return, recebe a meta e um onConfirm, e chama `updateGoal(m.id, { current: <novoValor> })` ao confirmar — sem reload, sem confirm()/alert(). Manter o botão "Ajustar plano" abrindo o AjustarPlanoModal via setEditTarget(m) (não mais EmBreve). Não alterar a lógica de filtro/Capítulos do Plan 04.
  </action>
  <acceptance_criteria>
    - `grep -c "setAportarTarget" assets/fides-metas.jsx` >= 2 (state setter usado no mount + no botão).
    - `grep -c "aportarTarget" assets/fides-metas.jsx` >= 3 (state + open + meta + onConfirm).
    - `grep -c "completed: true" assets/fides-metas.jsx` >= 1 (rewire de concluída) e `grep -c "completed_at" assets/fides-metas.jsx` >= 1.
    - `grep -c "current:" assets/fides-metas.jsx` >= 2 (Aportar soma + Atualizar saldo direto).
    - O botão "Aportar" e "Marcar como concluída" NÃO chamam mais setEmBreve — inspecionar; console sem warning "Rendered more hooks…".
    - Browser: Aportar abre o AportarModal (com preview/projeção) e ao confirmar a barra reflete sem reload; "Atualizar saldo" inline muda o current sem reload; "Marcar como concluída" move a meta para Concluída/Capítulo III.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "aportarTarget" assets/fides-metas.jsx &amp;&amp; grep -c "completed: true" assets/fides-metas.jsx</automated>
  </verify>
  <done>AportarModal vivo + inline atualizar saldo + concluir meta funcionando via updateGoal, sem EmBreve nesses caminhos, hooks íntegros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage URL/preset → render da capa | String de image_url (preset ou URL pública) vira background-image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering/XSS | render do vcard | mitigate | Capa sempre via `background-image`/`img src`; nunca dangerouslySetInnerHTML — conteúdo de Storage não é interpretado como HTML |
| T-08-11 | Info Disclosure | resolveCoverUrl | accept | Preset resolve para asset same-origin; URL de Storage é pública por design (Plan 01, Open Question 2) — sem dado sensível exposto |
| T-08-SC | Tampering | dependências | accept | Nenhum `npm install` — só APIs nativas/SDK já carregado |
</threat_model>

<verification>
- Hero de Metas distinto (met-hero) sem tocar fides-studio.css (UAT-6).
- vcard com capa/fallback/overlay/corpo; sem scroll-x em 400×512 (UAT-7).
- Aportar + Atualizar saldo inline refletem sem reload; concluir move para Capítulo III (UAT-4).
- Console sem warning de Rules of Hooks (UAT-7).
</verification>

<success_criteria>
Vision-board visual entregue (hero próprio + vcards) e as duas ações de update inline (D6) + conclusão (D5) funcionais sobre a base de dados/controls dos planos anteriores.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-05-SUMMARY.md` when done
</output>
