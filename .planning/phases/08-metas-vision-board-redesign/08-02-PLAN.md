---
phase: 08-metas-vision-board-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/covers/viagem.svg
  - assets/covers/casa.svg
  - assets/covers/carro.svg
  - assets/covers/reserva.svg
  - assets/covers/educacao.svg
  - assets/covers/casamento.svg
  - assets/covers/bebe.svg
  - assets/covers/saude.svg
  - assets/covers/aposentadoria.svg
  - assets/covers/negocio.svg
  - assets/covers/tecnologia.svg
  - assets/covers/presente.svg
  - assets/covers/natureza.svg
  - assets/covers/festa.svg
  - assets/covers/investimento.svg
  - assets/covers/emergencia.svg
autonomous: true
requirements: [UAT-1]
tags: [assets, presets, svg, covers, design]
user_setup: []

must_haves:
  truths:
    - "Existem ~16 capas bespoke em assets/covers/<id>.svg, gradiente/textura na paleta tint Fides (UAT-1)"
    - "Cada arquivo é um SVG válido, auto-contido (sem <script>, sem referência de rede externa), servido estático same-origin"
    - "Os ids dos arquivos batem exatamente com o contrato de key preset:<id> que o COVER_PRESETS (Plan 05) vai mapear"
    - "As capas são on-brand (Manrope não se aplica a imagem; usar verde floresta #2D5A3D + sage-paper #F4F5F1 + acentos tint) e legíveis sob o scrim do vcard"
  artifacts:
    - path: "assets/covers/viagem.svg"
      provides: "Capa preset temática (exemplo do conjunto de ~16)"
      contains: "<svg"
  key_links:
    - from: "assets/covers/<id>.svg"
      to: "COVER_PRESETS (fides-metas.jsx, Plan 05)"
      via: "contrato de key preset:<id> → 'assets/covers/<id>.svg'"
      pattern: "assets/covers/"
  prohibitions:
    - "NÃO usar encoder .webp (não existe cwebp/imagemagick/sharp neste ambiente) — formato é .svg por resolução do D4 (RESEARCH Pitfall 5)"
    - "NÃO referenciar rede externa/CSP (fontes, imagens remotas) dentro do SVG — capas são estáticas e self-contained"
    - "NÃO incluir <script> ou event handlers no SVG (higiene: capas são renderizadas como imagem/background)"
    - "NÃO rodar npm install — presets são hand-authored, zero-tooling"
---

<objective>
Criar ~16 capas bespoke em `assets/covers/<id>.svg`: gradientes/texturas na paleta tint da marca, bundladas e servidas estáticas pela Vercel (sem rede externa/CSP). Cada capa é referenciada por key `preset:<id>` (D4 — resolução pós-pesquisa: `.svg`, não `.webp`, por ausência de encoder no ambiente — RESEARCH Pitfall 5).

Purpose: A galeria de capas (aba "Galeria" do seletor, Plan 06) e o render de capa do vcard (Plan 05) precisam de um conjunto curado de imagens on-brand. SVG gradiente é zero-tooling, nítido em qualquer DPI, minúsculo e editável — honra o D4 sem depender de tooling ausente.
Output: 16 arquivos SVG em `assets/covers/` com ids estáveis que o COVER_PRESETS (Plan 05) mapeará.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@tokens.css
</context>

## Artifacts this phase produces (contribuição deste plano)

- 16 capas SVG bespoke em `assets/covers/`: `viagem, casa, carro, reserva, educacao, casamento, bebe, saude, aposentadoria, negocio, tecnologia, presente, natureza, festa, investimento, emergencia`.
- Nova pasta de assets `assets/covers/` (primeiro diretório de imagem bundlada do projeto).

<tasks>

<task type="auto">
  <name>Task 1: Autorar ~16 capas SVG gradiente/textura na paleta tint</name>
  <files>assets/covers/viagem.svg, assets/covers/casa.svg, assets/covers/carro.svg, assets/covers/reserva.svg, assets/covers/educacao.svg, assets/covers/casamento.svg, assets/covers/bebe.svg, assets/covers/saude.svg, assets/covers/aposentadoria.svg, assets/covers/negocio.svg, assets/covers/tecnologia.svg, assets/covers/presente.svg, assets/covers/natureza.svg, assets/covers/festa.svg, assets/covers/investimento.svg, assets/covers/emergencia.svg</files>
  <read_first>
    - tokens.css (paleta de marca: verde floresta #2D5A3D, sage-paper #F4F5F1, acentos; usar como base cromática)
    - .planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md (Pitfall 5 — por que SVG e não .webp; Code Examples — contrato COVER_PRESETS/resolveCoverUrl)
    - .planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md (seção assets/covers/<id>.svg — contrato preset:<id>)
  </read_first>
  <action>
    Criar 16 arquivos SVG, um por id da lista de `files`. Cada SVG: viewBox landscape adequado a uma capa (ex.: 0 0 400 186, proporção da área de capa do vcard), `preserveAspectRatio="xMidYMid slice"` para funcionar como cover. Conteúdo = `linearGradient`/`radialGradient` + formas geométricas sutis (círculos, ondas, faixas) evocando o tema do id, dentro da paleta tint/marca (variar o hue por tema, mantendo saturação/luminosidade coerentes com verde floresta + sage-paper + os tints existentes do TINTS array em fides-metas.jsx: #2D5A3D,#2C5282,#B45309,#7C3AED,#0F766E,#9B2C2C,#0891B2,#BE185D). Cada arquivo deve ser SVG puro e auto-contido: só `<svg>` + defs/gradients/shapes; SEM `<script>`, SEM `on*` handlers, SEM `<image href="http...">` ou `@import`/fontes de rede. Manter cada arquivo pequeno (alvo &lt; 4KB). Garantir contraste suficiente para que texto branco sobre o scrim gradiente do vcard permaneça legível (tons médios-escuros predominando na metade inferior, onde o overlay do card cai). Os nomes de arquivo (sem extensão) SÃO os ids do contrato `preset:<id>` — não renomear depois sem atualizar o Plan 05.
  </action>
  <acceptance_criteria>
    - `ls assets/covers/*.svg | wc -l` == 16.
    - Para cada arquivo: `grep -c "<svg" assets/covers/<id>.svg` >= 1 (SVG válido de topo).
    - `grep -rlc "<script" assets/covers/` retorna vazio (nenhum SVG contém script) — higiene de segurança.
    - `grep -rIl "http://\|https://\|@import" assets/covers/` retorna vazio (nenhuma referência de rede externa).
    - Cada arquivo usa ao menos um `linearGradient` ou `radialGradient` (`grep -lc "Gradient" assets/covers/<id>.svg` >= 1).
    - Abrir 3-4 SVGs no browser (drag-and-drop) confirma render nítido, on-brand, sem erro de parse no console.
  </acceptance_criteria>
  <verify>
    <automated>ls assets/covers/*.svg | wc -l</automated>
  </verify>
  <done>16 capas SVG on-brand, self-contained, sem script/rede externa, com ids que casam o contrato preset:<id>.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → browser render | SVGs servidos same-origin e renderizados como imagem/background |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering/XSS | assets/covers/*.svg | mitigate | SVGs hand-authored sem `<script>`/`on*`/refs de rede; renderizados só como background-image/img src (não inline unsanitized) |
| T-08-SC | Tampering | dependências | accept | Nenhum `npm install` — presets zero-tooling (D4) |
</threat_model>

<verification>
- 16 SVGs válidos, self-contained e on-brand em assets/covers/.
- Ids casam o contrato preset:<id> consumido pelo Plan 05.
</verification>

<success_criteria>
16 capas preset SVG existem, higiênicas (sem script/rede), on-brand, prontas para o COVER_PRESETS do Plan 05 e a galeria do Plan 06.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-02-SUMMARY.md` when done
</output>
