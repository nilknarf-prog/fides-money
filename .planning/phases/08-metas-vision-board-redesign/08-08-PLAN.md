---
phase: 08-metas-vision-board-redesign
plan: 08
type: execute
wave: 2
depends_on: [08-07]
files_modified:
  - assets/fides-metas.jsx
  - assets/fides-store.jsx
  - supabase/schema.sql
  - supabase/goals-completed-fix.sql
autonomous: false
gap_closure: true
requirements: [GAP-CONCLUSAO]
tags: [blocker, metas, conclusao, diagnose-first, supabase, rls, security, gap-closure]
user_setup: []

# depends_on [08-07] é por SOBREPOSIÇÃO DE ARQUIVO (ambos editam assets/fides-metas.jsx), não semântica.
# 08-07 (UI polish) roda em wave 1 PRIMEIRO para que a incerteza diagnose-first deste blocker não segure as correções triviais.
# autonomous:false porque a Task 2 (migração/policy LIVE condicional) é checkpoint blocking-human (CLAUDE.md: security review p/ supabase/).

must_haves:
  truths:
    - "Concluir uma meta pelo menu 'Marcar como concluída' reflete END-TO-END: a pill vira Concluída, a meta SAI de Ativas, ENTRA no filtro Concluídas E aparece no Capítulo III 'Já atingidas' (com 'Concluída em <data>'), persistindo após refresh"
    - "Aportar (ou Atualizar saldo) até o saldo guardado atingir/ultrapassar o alvo AUTO-conclui a meta (completed=true + completed_at) com o mesmo reflexo end-to-end (pill/filtro/Cap III)"
    - "Se o saldo cair abaixo do alvo DEPOIS de concluída, a meta permanece Concluída — sem auto-reabertura (conclusão é marco/evento, não estado puramente derivado)"
    - "A política RLS de UPDATE em public.goals permanece owner-scoped (auth.uid() = user_id): um usuário não consegue marcar/alterar a conclusão da meta de outro usuário; completed_at não é forjável para outra conta"
  artifacts:
    - path: "assets/fides-metas.jsx"
      provides: "auto-conclusão nos onConfirm de Aportar/Atualizar saldo (helper de patch) + fix da conclusão manual conforme diagnóstico"
      contains: "completed_at"
    - path: "supabase/goals-completed-fix.sql"
      provides: "(CONDICIONAL) migração/policy espelhada SE o diagnóstico revelar coluna faltante/tipo errado/policy UPDATE ausente"
      contains: "goals"
  key_links:
    - from: "menu 'Marcar como concluída' / auto-conclusão"
      to: "updateGoal(id, { completed, completed_at }) → PATCH /goals (Supabase)"
      via: "write persistido + refreshData select('*') + normalizeGoal(completed/completedAt) → derivação pill/filtro/Cap III"
      pattern: "completed_at"
    - from: "derivação de status (pill/filtro/Cap III)"
      to: "m.completed (normalizeGoal)"
      via: "!!m.completed no filtro :1013-1020 e metasConcluidas :1020; pill :1238"
      pattern: "m.completed"
  prohibitions:
    - "NÃO codificar o fix antes do diagnóstico — a Task 1 (chrome-devtools MCP: console + network do PATCH /goals) determina a hipótese (a: write não persiste — schema/RLS/patch; b: persiste mas derivação não lê); a Task 3 RAMIFICA no resultado"
    - "NÃO adicionar migração de schema por padrão — completed/completed_at supostamente já existem live; SÓ aplicar migração/policy (Task 2) SE o diagnóstico provar coluna faltante/tipo errado/policy UPDATE ausente/bloqueante"
    - "NÃO implementar auto-reabertura (completed→ativa quando saldo cai): decisão travada desta passada — conclusão NÃO reabre por queda de saldo"
    - "NÃO enfraquecer o escopo owner (auth.uid() = user_id) de nenhuma policy de goals ao tocá-la (Broken Access Control)"
    - "NÃO usar confirm()/alert() nativos (CLAUDE.md — fides-ui)"
    - "NÃO mexer na reativação manual via toggle Status do AjustarPlanoModal (:519-522) — permanece como está"
---

<objective>
Corrigir o BLOCKER de conclusão de meta: hoje, marcar concluída (manual pelo menu OU por aporte a 100%) NÃO reflete — a pill fica Ativa, a meta não entra no filtro Concluídas nem no Capítulo III "Já atingidas". O código cliente (normalizeGoal mapeando completed/completedAt, updateGoal genérico, derivação !!m.completed no filtro/Cap III, menu chamando updateGoal({completed:true,completed_at})) JÁ está correto em `fides-metas.jsx`/`fides-store.jsx` — o que aponta fortemente para um defeito na camada de dados LIVE (coluna faltante/tipo/RLS). Por isso o fix é DIAGNOSE-FIRST: instrumentar o runtime antes de escrever qualquer correção. Além disso, não existe HOJE nenhuma auto-conclusão quando o saldo atinge o alvo — isso é adicionado independentemente do diagnóstico.

Purpose: Destrava o Teste 7 (conclusão) e o Teste 8 (Capítulo III, hoje `blocked`) do 08-UAT; entrega o modelo de status D5 (Ativa/Concluída via `completed`) de ponta a ponta.
Output: diagnóstico documentado; (condicional) migração/policy LIVE aplicada; conclusão manual e auto-conclusão funcionando end-to-end, sem auto-reabertura.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-metas-vision-board-redesign/08-UAT.md
@.planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md
@.planning/phases/08-metas-vision-board-redesign/08-RESEARCH.md
@.planning/phases/08-metas-vision-board-redesign/08-PATTERNS.md
@assets/fides-metas.jsx
@assets/fides-store.jsx
@supabase/schema.sql
@supabase/goal-covers-storage.sql
</context>

## Artifacts this phase produces (contribuição deste plano)

- Diagnóstico runtime documentado no SUMMARY: status HTTP do PATCH `/goals` ao concluir, corpo/erro, colunas reais de `public.goals` (completed/completed_at + tipo) e policies RLS de UPDATE — concluindo hipótese (a) ou (b).
- `patchComAutoConclusao(meta, novoAtual)` (ou helper equivalente) em `fides-metas.jsx`: monta o patch de `updateGoal`, adicionando `{ completed:true, completed_at: <ISO> }` quando `novoAtual >= meta.alvo && !meta.completed`; NUNCA seta `completed:false`.
- (CONDICIONAL) `supabase/goals-completed-fix.sql` + espelho em `supabase/schema.sql`: só se o diagnóstico exigir coluna/tipo/policy.
- Correção específica da conclusão manual conforme a hipótese diagnosticada (cliente e/ou dados).

<tasks>

<task type="auto">
  <name>Task 1: DIAGNOSTICAR o defeito de conclusão em runtime (chrome-devtools + Supabase MCP) — ANTES de qualquer fix</name>
  <files>(investigação — nenhum arquivo de produção editado nesta task)</files>
  <read_first>
    - assets/fides-metas.jsx (menu "Marcar como concluída" :1241-1245; derivação de filtro :1009-1020; metasConcluidas :1020; pill :1238-1240; Capítulo III :1414-1434; onConfirm Aportar :1087-1092; SaldoInlineEditor :915-922 e mount :1296-1299)
    - assets/fides-store.jsx (normalizeGoal completed/completedAt :90-106; refreshData select('*') em goals :233-244; updateGoal .update(patch).eq('id') :634-646)
    - supabase/schema.sql (tabela goals + RLS "goals: próprio usuário" auth.uid() = user_id)
    - .planning/phases/08-metas-vision-board-redesign/08-UAT.md (gap blocker: root_cause INDETERMINADO; hipóteses a/b)
  </read_first>
  <action>
    Diagnosticar em runtime ANTES de escrever qualquer correção (decisão travada desta passada). Passos:
    (1) Checagem determinística via Supabase MCP (não precisa de sessão de browser): listar as colunas reais de `public.goals` e confirmar se `completed` (boolean) e `completed_at` (timestamptz) EXISTEM e com o tipo esperado; listar as policies de `public.goals` e confirmar que há cobertura de UPDATE owner-scoped (a policy `goals: próprio usuário` é `for all using (auth.uid() = user_id)` — confirmar que existe e não foi alterada). Registrar o resultado.
    (2) Reprodução instrumentada via chrome-devtools MCP no app deployado (Vercel): abrir a tela de Metas com uma sessão logada que tenha ao menos uma meta ativa; abrir o painel Network + Console; executar "Marcar como concluída" numa meta e capturar (i) a requisição PATCH para `/rest/v1/goals` (método, corpo enviado com `completed`/`completed_at`, e o STATUS da resposta + corpo/erro), (ii) qualquer `console.error('[Fides] updateGoal: …')`. Repetir para o caminho de aporte levando `atual >= alvo`. Se não houver sessão logada/meta disponível para o chrome-devtools, coordenar com o humano para prover uma (isto pode pausar a task).
    (3) Concluir a hipótese e DOCUMENTAR no SUMMARY:
      - Hipótese (a) — o write NÃO persiste: PATCH retorna 4xx (ex.: "column completed does not exist" → coluna faltante; ou 401/403/RLS) OU o `updateGoal` cai no catch e `refreshData` nem roda. Sub-classificar: (a-schema) coluna faltante/tipo errado, (a-rls) policy UPDATE ausente/bloqueante, (a-patch) shape do patch rejeitado.
      - Hipótese (b) — o write persiste (PATCH 200/204 e um GET subsequente traz `completed:true`) mas a derivação pill/filtro/Cap III não reflete: investigar staleness de estado cliente, normalizeGoal, ou select.
    A saída desta task é o diagnóstico escrito (hipótese + evidência: status HTTP, corpo, colunas, policies) que as Tasks 2 e 3 consomem. NÃO editar código de produção aqui.
  </action>
  <acceptance_criteria>
    - O SUMMARY registra: (i) lista de colunas de `public.goals` com presença/tipo de `completed`/`completed_at`; (ii) presença/definição da policy de UPDATE owner-scoped; (iii) método + status HTTP + corpo/erro do PATCH `/goals` ao concluir; (iv) presença/ausência de `console.error('[Fides] updateGoal:')`.
    - O diagnóstico conclui EXPLICITAMENTE uma hipótese: (a-schema) | (a-rls) | (a-patch) | (b), com a evidência que a sustenta.
    - Nenhum arquivo de produção foi modificado nesta task (`git status --porcelain assets/ supabase/` vazio ao fim da Task 1).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — task diagnóstica: a prova é o registro no SUMMARY (colunas de goals, policies, status do PATCH /goals, presença de console.error) e a hipótese escolhida (a-schema/a-rls/a-patch/b). Sem git diff em assets/ ou supabase/ ao fim.</automated>
  </verify>
  <done>Hipótese do defeito de conclusão determinada com evidência runtime; Tasks 2/3 têm base para ramificar sem chutar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING · CONDICIONAL] Aplicar migração/policy LIVE em public.goals — SÓ se o diagnóstico exigir</name>
  <files>supabase/goals-completed-fix.sql (novo, condicional), supabase/schema.sql (espelho), public.goals (banco live via Supabase MCP apply_migration ou Dashboard SQL Editor)</files>
  <action>
    CONDICIONAL à hipótese da Task 1. Se (a-schema): preparar e aplicar `alter table public.goals add column if not exists completed boolean not null default false;` e/ou `add column if not exists completed_at timestamptz;`. Se (a-rls): garantir a policy de UPDATE owner-scoped (`using (auth.uid() = user_id) with check (auth.uid() = user_id)`) SEM enfraquecer o escopo por dono. Aplicar ao banco LIVE via Supabase MCP `apply_migration` (precedente 08-01; NÃO há Supabase CLI — não tente `supabase db push`), espelhar em `supabase/schema.sql` e num novo `supabase/goals-completed-fix.sql`. Se hipótese (a-patch)/(b): NO-OP — registrar no SUMMARY "sem mudança de schema/policy (hipótese X)" e seguir. Como CLAUDE.md exige security review para mudanças em `supabase/`, o humano confirma o SQL e o apply antes de tocar o LIVE.
  </action>
  <what-built>
    CONDICIONAL à Task 1. Executar SOMENTE se o diagnóstico foi (a-schema) coluna `completed`/`completed_at` faltante ou de tipo errado, OU (a-rls) policy de UPDATE ausente/bloqueante em `public.goals`. Nesse caso, preparar a correção SQL mínima e aplicá-la ao banco LIVE via Supabase MCP `apply_migration` (precedente da Phase 08-01), espelhando em `supabase/schema.sql` e num novo `supabase/goals-completed-fix.sql`:
    - (a-schema) `alter table public.goals add column if not exists completed boolean not null default false;` e/ou `alter table public.goals add column if not exists completed_at timestamptz;` (tipos alinhados a normalizeGoal: boolean + timestamp ISO).
    - (a-rls) garantir cobertura de UPDATE owner-scoped SEM enfraquecer o escopo: manter/gerar a policy no idioma do projeto (`"goals: …" … using (auth.uid() = user_id) with check (auth.uid() = user_id)`); NUNCA remover o filtro por dono. Como CLAUDE.md exige security review para mudanças em `supabase/`, o humano confirma o SQL e o apply LIVE.
    Se o diagnóstico foi (a-patch) ou (b) — ou seja, colunas existem e RLS permite o write — esta task é um NO-OP: registrar no SUMMARY "sem mudança de schema/policy necessária (hipótese X)" e seguir para a Task 3.
  </what-built>
  <how-to-verify>
    Se aplicado: confirmar no Supabase (Dashboard → Table Editor / SQL Editor ou MCP) que `public.goals` tem `completed` (boolean) e `completed_at` (timestamptz) e que a policy de UPDATE é owner-scoped (`auth.uid() = user_id`). Refazer o "Marcar como concluída" e confirmar que o PATCH `/goals` agora retorna 200/204 e um GET traz `completed:true`. Confirmar que RLS continua rejeitando update de meta de OUTRO usuário (owner-scoping preservado). Se NO-OP: confirmar que nada foi aplicado e o SUMMARY registra o motivo.
  </how-to-verify>
  <verify>
    <automated>MISSING — checkpoint blocking-human: verificação via Supabase MCP/Dashboard (colunas completed/completed_at + policy UPDATE owner-scoped) OU registro de NO-OP no SUMMARY. Retomada humana pelo resume-signal.</automated>
  </verify>
  <done>Se exigido pelo diagnóstico: coluna/policy aplicada LIVE com owner-scoping preservado e espelhada em schema.sql; senão NO-OP documentado. Task 3 pode prosseguir.</done>
  <resume-signal>Digite "aplicado" (com o resumo do que foi aplicado) ou "no-op" (diagnóstico não exigiu schema/policy) para retomar na Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Conclusão manual end-to-end (conforme diagnóstico) + auto-conclusão no aporte/atualizar saldo (sem auto-reabertura)</name>
  <files>assets/fides-metas.jsx, assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-metas.jsx (mount AportarModal :1087-1092; mount SaldoInlineEditor :1296-1299; SaldoInlineEditor def :915-922; menu concluir :1241-1245; onConfirm do AjustarPlanoModal com wasCompleted/isCompleted :1052-1074; derivação filtro/Cap III :1009-1020)
    - assets/fides-store.jsx (updateGoal :634-646; normalizeGoal :90-106; refreshData/select :229-244)
    - O DIAGNÓSTICO da Task 1 (SUMMARY) — esta task RAMIFICA nele
    - .planning/phases/08-metas-vision-board-redesign/08-CONTEXT.md (D5 modelo de status) + a decisão travada desta passada (auto-conclui ao atingir alvo; NÃO reabre por queda de saldo)
  </read_first>
  <action>
    RAMIFICAR conforme o diagnóstico da Task 1:
    - Se hipótese (a-schema)/(a-rls): a correção real foi a Task 2 (LIVE). Aqui, APENAS confirmar que o código cliente da conclusão manual (menu :1243 e derivações :1013-1020/:1238/:1417-1420) já reflete corretamente após o fix de dados — sem mudança de código se o cliente já estava correto. Documentar no SUMMARY que o cliente não precisou de alteração para o caminho manual.
    - Se hipótese (a-patch): ajustar o shape do patch no ponto que o diagnóstico apontou (ex.: chave/tipo enviado a `updateGoal`) para o formato que o PATCH aceita.
    - Se hipótese (b): corrigir o defeito de derivação/staleness identificado (ex.: normalizeGoal, select de colunas, ou refresh de estado) para que pill/filtro/Cap III leiam `completed` após o refresh.
    ADICIONAR (independente do diagnóstico) a AUTO-CONCLUSÃO: criar um helper `patchComAutoConclusao(meta, novoAtual)` que retorna `{ current: novoAtual }` e, quando `novoAtual >= meta.alvo && !meta.completed`, também `{ completed: true, completed_at: new Date().toISOString() }`. Aplicá-lo nos DOIS caminhos de saldo:
      (i) mount do `AportarModal` (~:1090): `onConfirm={(valor) => updateGoal(aportarTarget.id, patchComAutoConclusao(aportarTarget, aportarTarget.atual + valor))}`;
      (ii) mount do `SaldoInlineEditor` (~:1298): `onConfirm={(novoValor) => updateGoal(m.id, patchComAutoConclusao(m, novoValor))}`.
    REGRA TRAVADA (sem auto-reabertura): o helper NUNCA seta `completed:false` — se o saldo cair abaixo do alvo depois de concluída, a meta continua Concluída. A reativação manual segue exclusivamente pelo toggle Status do AjustarPlanoModal (:519-522) — não alterar. Não usar confirm()/alert().
  </action>
  <acceptance_criteria>
    - `grep -c "patchComAutoConclusao" assets/fides-metas.jsx` >= 3 (definição + uso no Aportar + uso no SaldoInline).
    - `grep -c "completed_at" assets/fides-metas.jsx` >= 2 (menu manual + auto-conclusão).
    - O helper NUNCA gera `completed: false` — confirmar por leitura que só há atribuição `completed: true` no caminho de auto-conclusão (sem auto-reabertura).
    - Browser (com o fix de dados da Task 2, se houve): conclusão MANUAL pelo menu → pill vira Concluída, meta sai de Ativas, entra no filtro Concluídas E no Capítulo III "Já atingidas"; persiste após refresh.
    - Browser: aportar/atualizar saldo até `atual >= alvo` → a meta auto-conclui com o MESMO reflexo end-to-end (pill/filtro/Cap III).
    - Browser: baixar o saldo de uma meta concluída para abaixo do alvo → permanece Concluída (sem auto-reabertura).
    - `git status --porcelain` mostra apenas os arquivos previstos; console sem warning de Rules of Hooks.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "patchComAutoConclusao" assets/fides-metas.jsx &amp;&amp; grep -c "completed_at" assets/fides-metas.jsx</automated>
  </verify>
  <done>Conclusão manual reflete end-to-end (pill/filtro/Cap III) e aporte/atualizar-saldo a >=alvo auto-conclui; sem auto-reabertura; owner-scoping RLS preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (browser autenticado) → PATCH /rest/v1/goals (Supabase) | O write de conclusão (`completed`/`completed_at`) cruza aqui; entrada do cliente é não-confiável e a autorização é imposta por RLS no Postgres |
| diagnóstico/apply de policy → storage de política RLS de goals | A Task 1/2 inspeciona e (condicionalmente) toca a policy de UPDATE de goals — qualquer alteração deve preservar o escopo por dono |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-BAC | Elevation of Privilege / Tampering | policy RLS UPDATE de public.goals | mitigate | Manter `using (auth.uid() = user_id) with check (auth.uid() = user_id)` em qualquer policy tocada na Task 2 — um usuário não pode marcar/alterar a conclusão da meta de outro; a Task 2 é checkpoint blocking-human com verificação explícita de owner-scoping |
| T-08-08-INT | Tampering / Repudiation | write de completed_at (auto-conclusão) | mitigate | `completed_at` é gravado pelo cliente do dono via updateGoal sob a MESMA RLS owner-scoped; não é forjável para outra conta (RLS rejeita `.eq('id')` de meta de terceiro); nenhum service_role/bypass é usado |
| T-08-08-SCHEMA | Denial of Service (dados) | migração condicional de goals | mitigate | Migração só com `if not exists` e defaults seguros (`completed default false`); aplicada via apply_migration sob review humano; espelhada em schema.sql (evita drift — ROADMAP B10) |
| T-08-08-SC | Tampering | dependências | accept | Nenhum `npm install` — só SDK Supabase já carregado + APIs nativas |

Block severity threshold: `high`. Qualquer achado da Task 1 que mostre a policy de UPDATE de goals NÃO owner-scoped é `high` → tratar na Task 2 (blocking-human) antes de prosseguir.
</threat_model>

<verification>
- Gate de fase (pós-execução): re-rodar os Testes 7 (conclusão manual) e 8 (Capítulo III, hoje `blocked`) do 08-UAT, mais o caminho de auto-conclusão por aporte a >=alvo, e o caso de queda-de-saldo-sem-reabrir.
- Segurança: confirmar (MCP/Dashboard) que a policy de UPDATE de `goals` permanece owner-scoped após qualquer toque da Task 2; tentativa cross-user de update deve ser rejeitada por RLS.
- Diagnóstico registrado no SUMMARY (hipótese + evidência) antes de qualquer edição de produção.
</verification>

<success_criteria>
Blocker fechado: marcar concluída (manual E por aporte a 100%) reflete pill Concluída + sai de Ativas + entra em Concluídas + Capítulo III, persistindo após refresh; sem auto-reabertura por queda de saldo; RLS de goals segue owner-scoped; migração/policy aplicada SOMENTE se o diagnóstico exigiu.
</success_criteria>

<output>
Create `.planning/phases/08-metas-vision-board-redesign/08-08-SUMMARY.md` when done
</output>
