---
phase: 13-ia-3-gating-premium-in-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-store.jsx
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "O store seleciona `plan` de `profiles` nos 2 caminhos de carga (login inicial e SIGNED_IN) e expõe `userPlan`/`isPremium` via useFides()"
    - "isPremium usa allow-list (`plan === 'pro' || plan === 'family'`), nunca `!== 'free'` — valor null/desconhecido cai em free (D-02, fail-closed)"
    - "A UI passa a refletir o tier real do banco, não mais o mock `USER.plan:'Pro'` de fides-data.jsx"
  artifacts:
    - "assets/fides-store.jsx: estado `userPlan` (default 'free') + derivado `isPremium` no context `value` e no fallback do useFides()"
  key_links:
    - "useFides().isPremium consumido pelo paywall (Plan 13-04) — PerfilView badge, gate do botão Análise da IA, e mapa de erro do chat"
---

<objective>
GATE-01: o store live passa a ler a coluna real `profiles.plan` (hoje só lê `name, group_targets`) e a expor `userPlan` + `isPremium` no contexto, tornando `profiles.plan` a fonte da verdade do tier — substituindo o mock morto `USER.plan:'Pro'` (fides-data.jsx:31, nunca lido por componente vivo).

Purpose: sem este read, o front não tem como esconder/desabilitar capacidades premium — a camada de UI do gate defense-in-depth (D-01) depende de `isPremium` estar disponível via useFides().
Output: `userPlan`/`isPremium` disponíveis em todo consumidor de useFides(), com allow-list fail-closed idêntica ao backend (D-02/D-4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-CONTEXT.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-RESEARCH.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md
@assets/fides-store.jsx
</context>

## Artifacts This Phase Produces

> Full phase symbol set (this plan produces the `assets/fides-store.jsx` items).

- `api/assistant.js`: `READ_FUNCTIONS`, `WRITE_FUNCTIONS` (split de `TOOLS_DECLARATION`), `buildToolsForPlan(isPremium)`, `FREE_TIER_ADDENDUM`, `FREE_TIER_MONTHLY_LIMIT` (=10), error codes `PREMIUM_REQUIRED` (403) + `FREE_MONTHLY_LIMIT` (429), vars `plan`/`isPremium` (Plan 13-03)
- **`assets/fides-store.jsx`: estado `userPlan` + derivado `isPremium`, expostos no context `value` e no fallback do `useFides()` (ESTE PLAN)**
- `assets/fides-claude.jsx`: entradas `friendlyError` `FREE_MONTHLY_LIMIT`/`PREMIUM_REQUIRED` + consumo de `isPremium` (Plan 13-04)
- `assets/fides-orcamento.jsx`: entradas `friendlyAiError` `FREE_MONTHLY_LIMIT`/`PREMIUM_REQUIRED` + gate do botão "Análise da IA" (Plan 13-04)
- `assets/fides-studio.jsx`: `PerfilView` badge de tier + CTA de upgrade + novo `UpgradeModal` (React.createElement) (Plan 13-04)
- `supabase/profiles-plan-privileges.sql`: arquivo novo — REVOKE/GRANT column-level em `profiles` (Plan 13-02)

<tasks>

<task type="auto">
  <name>Task 1: Ler `plan` nos 2 selects de profiles + estado userPlan fail-closed</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx:158 — declaração `const [userName, setUserName] = React.useState('')` (modelo do novo estado)
    - assets/fides-store.jsx:330 — select do carregamento inicial (`getAuthUser().then`), hoje `.select('name, group_targets')`
    - assets/fides-store.jsx:364 — select do re-fetch em SIGNED_IN (`onAuthStateChange`), idêntico
    - .planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md §"assets/fides-store.jsx" (padrão de select + state)
  </read_first>
  <action>
    Declarar, ao lado de `userName` (~linha 158): `const [userPlan, setUserPlan] = React.useState('free')` — default 'free' é o fail-closed do cliente (D-02). Nos DOIS blocos de select de `profiles` (linhas ~330 e ~364), acrescentar `, plan` à lista de colunas de forma idêntica, ficando `.select('name, group_targets, plan')`, e — dentro do mesmo `if (mounted)` onde já há `setUserName(profile?.name || '')` — adicionar `setUserPlan(profile?.plan || 'free')`. Não alterar o tratamento existente de `group_targets`. Implementa GATE-01 (leitura real do tier, D-4).
  </action>
  <acceptance_criteria>
    - `grep -c "select('name, group_targets, plan')" assets/fides-store.jsx` retorna 2 (os 2 caminhos de carga atualizados de forma idêntica)
    - `grep -c "setUserPlan(profile?.plan || 'free')" assets/fides-store.jsx` retorna 2
    - `grep -n "React.useState('free')" assets/fides-store.jsx` acerta a declaração de `userPlan`
    - Nenhuma ocorrência remanescente de `.select('name, group_targets')` sem `, plan` — `grep -c "select('name, group_targets')" assets/fides-store.jsx` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "select('name, group_targets, plan')" assets/fides-store.jsx; grep -c "setUserPlan(profile?.plan || 'free')" assets/fides-store.jsx; grep -c "select('name, group_targets')" assets/fides-store.jsx</automated>
  </verify>
  <done>Ambos os selects trazem `plan`; `userPlan` guarda o valor com default 'free' nos dois caminhos de carga.</done>
</task>

<task type="auto">
  <name>Task 2: Expor userPlan/isPremium (allow-list) no context value e no fallback</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx:1370-1409 — objeto `value` retornado pelo Provider (linha 1373 tem `userName, firstName, userEmail,`)
    - assets/fides-store.jsx:1422-1439 — objeto de fallback do `useFides()` sem provider (linha 1424 tem `userName: '', firstName: '', userEmail: '',`)
    - .planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md §Shared Patterns "Fail-closed allow-list tier check"
  </read_first>
  <action>
    No objeto `value` do Provider, junto de `userName, firstName, userEmail,` (~linha 1373), adicionar `userPlan, isPremium: userPlan === 'pro' || userPlan === 'family',` — allow-list idêntica ao backend; é proibido reduzir isso a uma negação direta contra o valor 'free' (D-02: valor desconhecido/null tem de cair em free). No objeto de fallback do `useFides()` (~linha 1424), junto de `userName: '', firstName: '', userEmail: '',`, adicionar `userPlan: 'free', isPremium: false,` (fail-closed). Implementa GATE-01 / D-02.
  </action>
  <acceptance_criteria>
    - `grep -n "isPremium: userPlan === 'pro' || userPlan === 'family'" assets/fides-store.jsx` acerta o objeto `value`
    - `grep -n "userPlan: 'free', isPremium: false" assets/fides-store.jsx` acerta o objeto de fallback
    - `grep -c "userPlan !== 'free'" assets/fides-store.jsx` retorna 0 (proibido — viola D-02)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "isPremium: userPlan === 'pro' || userPlan === 'family'" assets/fides-store.jsx; grep -c "userPlan: 'free', isPremium: false" assets/fides-store.jsx; grep -c "userPlan !== 'free'" assets/fides-store.jsx</automated>
    <human-check>Em `/gsd-verify-work`: `UPDATE profiles SET plan='free'` na conta de dev (D-04) → recarregar app → useFides().isPremium é false; `UPDATE ... plan='pro'` → isPremium true.</human-check>
  </verify>
  <done>useFides() expõe `userPlan` + `isPremium` (allow-list pro|family) tanto no Provider quanto no fallback; ambos fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → DB (Supabase SDK, RLS) | O store faz `SELECT plan` sob RLS row-level; a leitura é confiável (só a própria linha), mas o valor resultante vive no cliente |
| client state → UI | `isPremium` derivado no cliente controla apenas VISIBILIDADE (esconder/mostrar), não CAPACIDADE |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02c | Tampering/Spoofing | `isPremium` no client (fides-store.jsx) | low | accept | O flag de tier no cliente é conveniência de UI (D-01, camada 2). Adulterar `isPremium` via DevTools só revela/oculta botões — a capacidade real (WRITE tools, Análise) é re-decidida server-side em `api/assistant.js` (Plan 13-03), que relê `profiles.plan` a cada request. Risco residual aceito por design. |
| T-13-01 | Elevation of Privilege | `profiles.plan` (fonte lida aqui) | high | mitigate (Plan 13-02) | Este plan LÊ `plan`, não o protege. A trava contra auto-escrita de `plan` via client SDK é o REVOKE/GRANT column-level do Plan 13-02 ([BLOCKING]). Referenciado aqui para rastreabilidade — sem o Plan 13-02, o valor lido é auto-outorgável. |
</threat_model>

<verification>
- `grep -c "select('name, group_targets, plan')" assets/fides-store.jsx` == 2
- `grep` positivo de `isPremium: userPlan === 'pro' || userPlan === 'family'` e `userPlan: 'free', isPremium: false`
- `grep -c "userPlan !== 'free'" assets/fides-store.jsx` == 0 (allow-list obrigatória, D-02)
- Caminho `assets/` puro (sem `api/`/`supabase/`) — não requer security/database-reviewer nesta plan.
- Human-verify (batch em `/gsd-verify-work`): alternar `profiles.plan` via SQL Editor (D-04) reflete no `isPremium` da UI.
</verification>

<success_criteria>
- Os 2 selects de `profiles` trazem `plan`; `userPlan` default 'free' nos dois caminhos.
- `useFides()` expõe `userPlan` + `isPremium` (allow-list) no Provider e no fallback.
- Nenhuma negação direta `!== 'free'` introduzida (fail-closed preservado).
</success_criteria>

<output>
Create `.planning/phases/13-ia-3-gating-premium-in-app/13-01-SUMMARY.md` when done
</output>
