---
phase: 13-ia-3-gating-premium-in-app
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-03"]
files_modified:
  - assets/fides-claude.jsx
  - assets/fides-orcamento.jsx
  - assets/fides-studio.jsx
autonomous: true
requirements: [GATE-03, PAYWALL-01]
must_haves:
  truths:
    - "O chat (fides-claude.jsx) mapeia FREE_MONTHLY_LIMIT e PREMIUM_REQUIRED para copy amigável de paywall (aponta upgrade), não fallback genérico"
    - "A Análise da IA (fides-orcamento.jsx) mapeia os mesmos 2 códigos E esconde/desabilita o botão com CTA de upgrade quando !isPremium (camada 2 do gate, D-01)"
    - "Os DOIS mapas de erro (friendlyError + friendlyAiError) recebem os novos códigos juntos (Pitfall 4 — cópias independentes)"
    - "PerfilView (fides-studio.jsx) exibe badge de tier (Premium/Free) e, quando free, CTA de upgrade que abre um UpgradeModal (placeholder 'checkout em breve', M6)"
    - "Nenhum diálogo nativo de confirmação/alerta do browser é introduzido — paywall usa window.FidesUI (ConfirmDialog/useToast/useModalClose)"
  artifacts:
    - "assets/fides-claude.jsx: entradas FREE_MONTHLY_LIMIT/PREMIUM_REQUIRED em friendlyError + branch de dispatch"
    - "assets/fides-orcamento.jsx: entradas FREE_MONTHLY_LIMIT/PREMIUM_REQUIRED em friendlyAiError + gate do botão Análise da IA por isPremium"
    - "assets/fides-studio.jsx: PerfilView badge de tier + CTA de upgrade + novo UpgradeModal (React.createElement)"
  key_links:
    - "Consome useFides().isPremium (Plan 13-01) e os error codes emitidos pelo servidor (Plan 13-03)"
---

<objective>
PAYWALL-01 + GATE-03 (camada de visibilidade no front, D-01 — segunda camada, nunca única): as 3 superfícies de IA/perfil passam a reagir ao tier. Chat e Análise da IA ganham copy amigável de paywall para os novos códigos (FREE_MONTHLY_LIMIT / PREMIUM_REQUIRED) nos SEUS DOIS mapas independentes de erro (Pitfall 4); a Análise da IA esconde/desabilita o botão quando free (com CTA de upgrade); o PerfilView ganha badge de tier e CTA que abre um UpgradeModal placeholder apontando para o checkout do M6 (que ainda não existe — CONTEXT diz explicitamente para não travar a fase nessa dependência).

Purpose: fechar o loop de conversão — o usuário free entende que bateu o limite / que o recurso é premium, e vê para onde ir. Reuso total de fides-ui (ConfirmDialog/useToast/useModalClose) e do padrão EmBreveModal; nenhum diálogo nativo do browser (ROADMAP B9).
Output: paywall suave nas 3 superfícies + tela de upgrade acessível pelo Perfil.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-CONTEXT.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-RESEARCH.md
@.planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md
@assets/fides-claude.jsx
@assets/fides-orcamento.jsx
@assets/fides-studio.jsx
</context>

## Artifacts This Phase Produces

> Full phase symbol set (this plan produces the 3 UI-surface items).

- `api/assistant.js`: READ_FUNCTIONS, WRITE_FUNCTIONS, buildToolsForPlan, FREE_TIER_ADDENDUM, FREE_TIER_MONTHLY_LIMIT, PREMIUM_REQUIRED/FREE_MONTHLY_LIMIT (Plan 13-03)
- `assets/fides-store.jsx`: userPlan + isPremium (Plan 13-01)
- **`assets/fides-claude.jsx`: entradas friendlyError FREE_MONTHLY_LIMIT/PREMIUM_REQUIRED + dispatch (ESTE PLAN)**
- **`assets/fides-orcamento.jsx`: entradas friendlyAiError FREE_MONTHLY_LIMIT/PREMIUM_REQUIRED + gate do botão Análise da IA (ESTE PLAN)**
- **`assets/fides-studio.jsx`: PerfilView badge de tier + CTA de upgrade + UpgradeModal (React.createElement) (ESTE PLAN)**
- `supabase/profiles-plan-privileges.sql`: REVOKE/GRANT (Plan 13-02)

<tasks>

<task type="auto">
  <name>Task 1: Chat — copy de paywall para FREE_MONTHLY_LIMIT/PREMIUM_REQUIRED</name>
  <files>assets/fides-claude.jsx</files>
  <read_first>
    - assets/fides-claude.jsx:547-565 — mapa `friendlyError` (entradas por errCode, ex. USER_DAILY_LIMIT)
    - assets/fides-claude.jsx:701-718 — ladder `if (status === 429) { ... }` no caller (onde USER_DAILY_LIMIT já tem tratamento distinto)
    - assets/fides-claude.jsx:36-43 — `const fs = useFides()` + destructuring (para expor isPremium se necessário à UI)
    - .planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md §"assets/fides-claude.jsx"
  </read_first>
  <action>
    No mapa `friendlyError` (linhas 547-565), adicionar duas entradas cuja copy aponta para upgrade (diferente da copy genérica do limite diário): `FREE_MONTHLY_LIMIT` (ex.: mensagem de que a degustação gratuita de IA do mês acabou e que o premium libera IA ilimitada — sugerir ver planos no Perfil) e `PREMIUM_REQUIRED` (ex.: recurso do plano premium). No ladder do caller (linhas 701-718), tratar `errCode === 'FREE_MONTHLY_LIMIT'` no branch de `status === 429` (paralelo ao USER_DAILY_LIMIT já existente) exibindo `friendlyError('FREE_MONTHLY_LIMIT')` com cooldown apropriado; `PREMIUM_REQUIRED` (403) cai no `else` que já chama `friendlyError(errCode)` — garantir só que o mapa tem a entrada. Manter o mecanismo de dispatch existente (if/else if), não inventar novo. Se um nudge visual (toast) for desejado ao bater o cap, usar `window.FidesUI.useToast` — nunca diálogos nativos do browser (ROADMAP B9).
  </action>
  <acceptance_criteria>
    - `grep -n "FREE_MONTHLY_LIMIT" assets/fides-claude.jsx` acerta (entrada no mapa + tratamento no caller)
    - `grep -n "PREMIUM_REQUIRED" assets/fides-claude.jsx` acerta (entrada no mapa)
    - `grep -c "window.confirm(" assets/fides-claude.jsx` retorna 0 e `grep -c "alert(" assets/fides-claude.jsx` retorna 0 (sem diálogo nativo novo)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FREE_MONTHLY_LIMIT" assets/fides-claude.jsx; grep -c "PREMIUM_REQUIRED" assets/fides-claude.jsx; grep -c "window.confirm(" assets/fides-claude.jsx; grep -c "alert(" assets/fides-claude.jsx</automated>
    <human-check>Como free (após 10 msg): a 11ª no chat mostra a copy de paywall (ver planos), não o erro genérico de limite diário.</human-check>
  </verify>
  <done>friendlyError tem FREE_MONTHLY_LIMIT + PREMIUM_REQUIRED com copy de upgrade; caller trata o 429 do cap mensal; sem diálogo nativo novo.</done>
</task>

<task type="auto">
  <name>Task 2: Análise da IA — copy de paywall + gate do botão por isPremium</name>
  <files>assets/fides-orcamento.jsx</files>
  <read_first>
    - assets/fides-orcamento.jsx:1017-1031 — mapa `friendlyAiError` (estilo `var`/ES5, cópia SEPARADA de fides-claude.jsx — Pitfall 4)
    - assets/fides-orcamento.jsx:985-988 — assinatura `function PlnMesInsights(props)` (recebe groups/totals/prevTxs; NÃO lê o store hoje)
    - assets/fides-orcamento.jsx:1143-1172 — render do rodapé com o botão `pln-mi-ai-btn` (className, disabled, onClick handleAiClick) + blocos de resultado/erro
    - assets/fides-orcamento.jsx:1088-1098 — handling de `!res.ok` (onde o 403/429 do servidor chegam)
    - .planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md §"assets/fides-orcamento.jsx"
  </read_first>
  <action>
    No mapa `friendlyAiError` (linhas 1017-1031, manter o estilo `var` local do arquivo), adicionar `FREE_MONTHLY_LIMIT` e `PREMIUM_REQUIRED` com copy de upgrade (Pitfall 4 — as duas cópias editadas juntas nesta fase). Ler o tier: em `PlnMesInsights`, obter `isPremium` via `window.useFides()` no topo do componente (junto dos hooks de estado, respeitando Rules of Hooks — o arquivo já sofreu bug de hooks antes) OU receber `isPremium` como prop do pai `FidesOrcamento` (que já usa `window.useFides()` na linha ~1177) — escolher uma e ser consistente. Quando `!isPremium`, NÃO renderizar o botão "Análise da IA" ativo: no lugar, mostrar um CTA de upgrade (ex.: texto "Análise da IA é um recurso premium" + botão/link que aponta ao upgrade, ex.: navegar ao Perfil ou abrir aviso). O 403 PREMIUM_REQUIRED do servidor (Plan 13-03) continua sendo defesa em profundidade: se um free forçar a chamada, `handleAiClick`/`!res.ok` cai em `friendlyAiError('PREMIUM_REQUIRED')`. Sem diálogo nativo do browser — usar `window.FidesUI` se precisar de aviso.
  </action>
  <acceptance_criteria>
    - `grep -n "FREE_MONTHLY_LIMIT" assets/fides-orcamento.jsx` acerta e `grep -n "PREMIUM_REQUIRED" assets/fides-orcamento.jsx` acerta (ambos no friendlyAiError)
    - `grep -n "isPremium" assets/fides-orcamento.jsx` acerta (gate do botão lê o tier)
    - O botão `pln-mi-ai-btn` só é renderizado ativo quando premium; quando free há CTA de upgrade — confirmar por leitura do render
    - `grep -c "window.confirm(" assets/fides-orcamento.jsx` retorna 0 e `grep -c "alert(" assets/fides-orcamento.jsx` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FREE_MONTHLY_LIMIT" assets/fides-orcamento.jsx; grep -c "PREMIUM_REQUIRED" assets/fides-orcamento.jsx; grep -c "isPremium" assets/fides-orcamento.jsx; grep -c "window.confirm(" assets/fides-orcamento.jsx; grep -c "alert(" assets/fides-orcamento.jsx</automated>
    <human-check>Como free: o botão "Análise da IA" não aparece ativo — há CTA de upgrade; forçar mode:analysis via fetch retorna copy PREMIUM_REQUIRED. Como pro: Análise da IA funciona normalmente.</human-check>
  </verify>
  <done>friendlyAiError tem os 2 códigos; botão da Análise gateado por isPremium com CTA de upgrade para free; sem diálogo nativo novo.</done>
</task>

<task type="auto">
  <name>Task 3: PerfilView — badge de tier + CTA de upgrade + UpgradeModal</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx:706-780 — `PerfilView({ onNav })` (ES5 `React.createElement`, SEM JSX — obrigatório manter); estrutura prf-header / prf-card / prf-avatar / fds-field
    - assets/fides-studio.jsx:707-710 — destructuring de `window.useFides()` (userName, firstName, updateProfile) — ponto para adicionar userPlan/isPremium
    - assets/fides-metas.jsx:972-999 — `EmBreveModal` (shape do modal placeholder + `window.FidesUI.useModalClose`) — analog para o UpgradeModal (mas AQUI em React.createElement, não JSX)
    - .planning/phases/13-ia-3-gating-premium-in-app/13-PATTERNS.md §"assets/fides-studio.jsx PerfilView" + §"Paywall UI" + §Shared Patterns "ES5 React.createElement vs JSX"
  </read_first>
  <action>
    Adicionar `userPlan`/`isPremium` ao destructuring de `_s = window.useFides()` no topo de PerfilView. Dentro do `prf-card`, adicionar um bloco de badge de tier usando a classe `prf-badge`: `React.createElement('div', { className: 'prf-badge' }, isPremium ? 'Premium' : 'Free')`. Quando `!isPremium`, adicionar um botão CTA de upgrade (React.createElement, estilo dos botões existentes de PerfilView) que abre um novo `UpgradeModal` — estado local `React.useState(false)` para open/close, declarado no topo do componente (Rules of Hooks). Criar `UpgradeModal` no MESMO arquivo em React.createElement — a função PerfilView é escrita em React.createElement, então manter o markup novo consistente com ela (não misturar JSX dentro de PerfilView/UpgradeModal); os demais componentes do arquivo usam JSX, mas o markup desta fase segue o estilo local da PerfilView. Espelhar o shape do EmBreveModal via `window.FidesUI.useModalClose(open, onClose)`: backdrop + modal + head (título "Vire Premium") + body (copy de precificação P-2: premium R$ 89,90/ano libera IA/WhatsApp/análises; checkout "em breve" no M6) + foot com um botão de fechar. Sem captura de dado/checkout real (CONTEXT: não travar a fase na dependência do M6). Não usar diálogos nativos do browser.
  </action>
  <acceptance_criteria>
    - `grep -n "isPremium" assets/fides-studio.jsx` acerta dentro de PerfilView
    - `grep -n "prf-badge" assets/fides-studio.jsx` (ou classe de badge escolhida) acerta
    - `grep -n "function UpgradeModal" assets/fides-studio.jsx` acerta e usa `window.FidesUI.useModalClose`
    - Markup novo em createElement: `grep -c "React.createElement" assets/fides-studio.jsx` aumenta vs o baseline; o novo UpgradeModal/badge/CTA usa React.createElement, consistente com PerfilView (NÃO validar por grep global de `<div`, que já é > 0 porque outros componentes do arquivo usam JSX — validar por leitura do diff da região PerfilView/UpgradeModal)
    - `grep -c "window.confirm(" assets/fides-studio.jsx` retorna 0 e `grep -c "alert(" assets/fides-studio.jsx` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function UpgradeModal" assets/fides-studio.jsx; grep -c "useModalClose" assets/fides-studio.jsx; grep -c "prf-badge" assets/fides-studio.jsx; grep -c "React.createElement" assets/fides-studio.jsx; grep -c "window.confirm(" assets/fides-studio.jsx; grep -c "alert(" assets/fides-studio.jsx</automated>
    <human-check>App carrega sem erro de parse Babel-standalone. Como free: Perfil mostra badge "Free" + CTA que abre "Vire Premium" (copy M6 em breve). Como pro: badge "Premium", sem CTA.</human-check>
  </verify>
  <done>PerfilView mostra badge de tier; free tem CTA que abre UpgradeModal (React.createElement, EmBreve-style); sem JSX novo neste arquivo; sem diálogo nativo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client state (isPremium) → UI | O front esconde/mostra capacidades premium — CONVENIÊNCIA, não segurança (o servidor é autoritativo, D-01) |
| client → API (`/api/assistant`) | O 403/429 do servidor (Plan 13-03) é a defesa real; a UI só evita que o usuário legítimo esbarre no gate sem contexto |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02b | Elevation of Privilege | esconder botão de Análise/WRITE só no front | high | mitigate (via Plan 13-03) | A ocultação é a 2ª camada (D-01). Mesmo que o usuário reative o botão via DevTools, o servidor recusa (403 PREMIUM_REQUIRED / tools READ-only). Esta plan NÃO é a fonte de verdade — apenas evita esbarrão do usuário legítimo e comunica o upgrade. |
| T-13-07 | Information Disclosure | copy de paywall/precificação | low | accept | Copy estática (P-2: R$ 89,90/ano) sem dado sensível; placeholder de checkout (M6) sem captura de PII. |
| T-13-08 | Rules of Hooks (regressão de estabilidade) | leitura de isPremium em PlnMesInsights / estado do UpgradeModal | low | mitigate | Ler `window.useFides()`/`useState` no TOPO do componente, nunca dentro de handler (histórico: bug de hooks na Phase 07 e nota em fides-orcamento:1002). Confirmado por human-check "app carrega sem erro". |
| T-13-SC | Tampering (supply chain) | instalações de pacote | n/a | accept | Nenhum pacote instalado — só reuso de fides-ui/EmBreve. |
</threat_model>

<verification>
- Greps positivos: FREE_MONTHLY_LIMIT + PREMIUM_REQUIRED acertam em AMBOS fides-claude.jsx E fides-orcamento.jsx (Pitfall 4); isPremium em fides-orcamento.jsx e fides-studio.jsx; UpgradeModal + useModalClose em fides-studio.jsx.
- Greps negativos: `window.confirm(` e `alert(` == 0 nos 3 arquivos (sem diálogo nativo novo, ROADMAP B9). Em fides-studio.jsx, o markup novo segue React.createElement (consistente com PerfilView) — validado por diff da região + human-check de parse, NÃO por grep global de `<div` (o arquivo já usa JSX em outros componentes).
- Caminho `assets/` puro — não requer security/database-reviewer nesta plan (nenhum toque em api/ ou supabase/).
- Human-verify (batch em `/gsd-verify-work`): free vê paywall no chat (11ª msg) + CTA na Análise + badge Free/CTA no Perfil; pro sem paywall.
</verification>

<success_criteria>
- Os 2 mapas de erro (chat + análise) têm FREE_MONTHLY_LIMIT + PREMIUM_REQUIRED com copy de upgrade.
- Análise da IA gateada por isPremium com CTA para free; PerfilView com badge + CTA + UpgradeModal.
- Zero diálogo nativo novo; zero JSX novo em fides-studio.jsx; app carrega sem erro de parse.
</success_criteria>

<output>
Create `.planning/phases/13-ia-3-gating-premium-in-app/13-04-SUMMARY.md` when done
</output>
