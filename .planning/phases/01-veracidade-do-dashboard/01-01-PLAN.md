---
phase: 01-veracidade-do-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides-store.jsx
  - assets/fides-studio.jsx
autonomous: false
requirements: [R1]
must_haves:
  truths:
    - "Com limites definidos no Planejamento para o mês corrente, o card 'Para onde foi' exibe o limite somado por grupo (Essencial/Estilo de vida/Dívidas) diferente de 0."
    - "O % exibido por grupo = gasto / limite real (não 0% quando há limite)."
    - "O limite do macro-grupo é a soma dos limites efetivos das categorias-filhas no mês selecionado, pela regra byMonth[selectedMonth] → default → null."
    - "Categoria sem limite efetivo contribui 0 ao grupo; se a soma do grupo for null (nenhuma filha tem limite), o card exibe estado 'sem limite definido' em vez de '0%' enganoso."
  artifacts:
    - path: "assets/fides-store.jsx"
      provides: "budgetGroups useMemo lendo categoryLimits via regra byMonth→default→null"
      contains: "categoryLimits"
    - path: "assets/fides-studio.jsx"
      provides: "render do card de orçamento com estado 'sem limite' quando g.limit é null"
      contains: "g.limit"
  key_links:
    - from: "assets/fides-store.jsx (budgetGroups)"
      to: "categoryLimits (state carregado de category_limits)"
      via: "leitura de limite por categoria com prioridade byMonth[selectedMonth] → default → null"
      pattern: "categoryLimits\\[id\\]"
    - from: "assets/fides-studio.jsx (card Para onde foi)"
      to: "budgetGroups (do store)"
      via: "g.limit / g.spent / g.pct alimentam BudgetBar e stu-budget-list"
      pattern: "budgetGroups\\.map"
  prohibitions:
    - statement: "NÃO exibir '0%' quando o grupo não tem nenhum limite efetivo definido — deve mostrar estado neutro 'sem limite definido'."
      status: resolved
      verification: "source assertion — o render do grupo usa pct === null (de g.limit === null) para alternar para 'Sem limite'; behavioral — grupo sem categoria com limite mostra 'Sem limite', não '0%'."
---

<objective>
Corrigir Issue #1 (R1): o card de orçamento 50-30-20 ("Para onde foi") deve ler os limites reais definidos no Planejamento (`categoryLimits`) em vez de `plannedOverrides`, que nunca é preenchido (`setPlanned` jamais é chamado) e portanto sempre resulta em limite 0 / 0%.

Purpose: o DashboardStudio passa a refletir a verdade financeira — o usuário vê os limites que definiu, não R$0/0% enganoso.
Output: `budgetGroups` em `fides-store.jsx` derivando limite de `categoryLimits` pela regra byMonth→default→null (molde exato em `categoryUsage`); soma por grupo null-safe; estado "sem limite definido" no render quando o grupo não tem limite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-veracidade-do-dashboard/01-SPEC.md
@.planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md
@.planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md
@assets/fides-store.jsx
@assets/fides-studio.jsx
</context>

## Artifacts this phase produces (Plan 01 / R1)

- `assets/fides-store.jsx` — `budgetGroups` useMemo reescrito: nova leitura de limite por categoria (`categoryLimits[id]` com prioridade `byMonth[selectedMonth]` → `default` → `null`); per-category retornado com campo `limit` (null quando sem limite); filtro de visibilidade `c._custom || c.limit != null || c.spent > 0`; soma de grupo null-safe `groupLimit` (null quando todas as filhas são null); novo campo `pct` por grupo (null quando `groupLimit` é null); dependências do useMemo atualizadas para `[monthTransactions, categories, categoryLimits, selectedMonth, groupTargets]`.
- `assets/fides-studio.jsx` — render do card de orçamento (`stu-budget-list`, ~linha 854) consome `g.pct`/`g.limit` com estado "Sem limite" quando `g.limit` é null.

<tasks>

<task type="auto">
  <name>Task 1: Reescrever budgetGroups em fides-store.jsx para ler categoryLimits</name>
  <files>assets/fides-store.jsx</files>
  <read_first>
    - assets/fides-store.jsx §1023-1056 (`categoryUsage`) — MOLDE EXATO da regra de leitura de limite a copiar.
    - assets/fides-store.jsx §1058-1079 (`budgetGroups`) — o código atual a substituir.
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"R1" — diffs conceituais linha a linha.
    - .planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md §"Pitfall 1" — atualização obrigatória do filtro de visibilidade.
  </read_first>
  <action>
    No `budgetGroups` useMemo (§1058-1079), dentro do `.map(([id, c]) => { ... })`: substituir a linha `const limit = plannedOverrides[id] ?? 0;` (§1066) pela leitura de `categoryLimits[id]` usando a MESMA expressão de `categoryUsage` §1028-1033 — resolver `lim = categoryLimits[id]`, então `limit = lim ? (lim.byMonth && lim.byMonth[selectedMonth] != null ? lim.byMonth[selectedMonth] : (lim.default != null ? lim.default : null)) : null`. O `limit` por categoria agora pode ser `null`. Retornar o objeto da categoria com o campo `limit` resolvido (não mais `?? 0`). Atualizar o filtro de visibilidade (§1073) de `c._custom || plannedOverrides[c.cat] != null || c.spent > 0` para `c._custom || c.limit != null || c.spent > 0` (Pitfall 1: o filtro antigo dependia de `plannedOverrides` que era sempre `{}`). Substituir a soma de grupo (§1075) `const limit = cats.reduce((s, c) => s + c.limit, 0);` por uma soma null-safe que distingue "sem limite" de "limite 0": `const groupLimit = cats.every(c => c.limit == null) ? null : cats.reduce((s, c) => s + (c.limit || 0), 0);`. Adicionar ao objeto retornado por grupo o campo `limit: groupLimit` e um campo `pct: groupLimit != null ? Math.round((spent / groupLimit) * 100) : null`. Atualizar o array de dependências do useMemo (§1079) de `[monthTransactions, categories, plannedOverrides, groupTargets]` para `[monthTransactions, categories, categoryLimits, selectedMonth, groupTargets]`. Atualizar o comentário §1059-1060 que afirma "O limite por categoria vem exclusivamente de plannedOverrides" para refletir a nova fonte `categoryLimits`. NÃO remover `plannedOverrides`/`setPlanned` do store — limpeza de estado morto é opcional e fora do escopo (SPEC boundaries).
  </action>
  <verify>
    <automated>grep -n "categoryLimits\[id\]" assets/fides-store.jsx && grep -n "byMonth\[selectedMonth\]" assets/fides-store.jsx | grep -c . | { read n; [ "$n" -ge 2 ]; }</automated>
  </verify>
  <acceptance_criteria>
    - Source: `budgetGroups` não contém mais `plannedOverrides[id] ?? 0` na resolução de limite por categoria.
    - Source: a expressão de leitura de limite por categoria em `budgetGroups` é idêntica em estrutura à de `categoryUsage` (`lim.byMonth && lim.byMonth[selectedMonth] != null ? ... : (lim.default != null ? lim.default : null)`).
    - Source: a soma de grupo usa `cats.every(c => c.limit == null) ? null : ...` (groupLimit é null quando todas as filhas são null).
    - Source: o objeto de grupo retornado inclui `pct` derivado de `groupLimit` (null quando groupLimit é null).
    - Source: o array de dependências do useMemo inclui `categoryLimits` e `selectedMonth` e NÃO inclui `plannedOverrides`.
    - Source: o filtro de visibilidade usa `c.limit != null` (não `plannedOverrides[c.cat] != null`).
  </acceptance_criteria>
  <done>budgetGroups deriva limite de categoryLimits pela regra byMonth→default→null; soma de grupo é null-safe e distingue "sem limite" de 0; campo pct adicionado; dependências corrigidas.</done>
</task>

<task type="auto">
  <name>Task 2: Render do card de orçamento com estado "sem limite definido"</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx §849-877 (`stu-budget` / `stu-budget-list`) — render atual que consome `budgetGroups`.
    - assets/fides-studio.jsx §854-872 — bloco `budgetGroups.map(g => { const pct = g.limit ? g.spent / g.limit : 0; ... })` que precisa usar o `g.pct`/`g.limit === null` novo.
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"Edge-case rendering pattern" — uso de pct null.
  </read_first>
  <action>
    No bloco `budgetGroups.map(g => { ... })` (§854): trocar `const pct = g.limit ? g.spent / g.limit : 0;` para usar o campo já calculado no store quando disponível — `const hasLimit = g.limit != null;` e `const pct = hasLimit ? g.spent / g.limit : null;`. Onde hoje exibe `{Math.round(pct*100)}%` (§865): quando `pct === null` (grupo sem limite), exibir o texto "Sem limite" no lugar do percentual, mantendo a mesma `<span className="fds-budget-pct ...">` (sem a classe over/near quando sem limite). No `<ProgressBar value={pct} ... />` (§868): passar `value={pct != null ? pct : 0}` para a barra ficar vazia quando sem limite (não quebrar com null). No `stu-budget-meta` (§871) trocar `limite {fmtBRL(g.limit)}` para exibir "sem limite definido" quando `g.limit == null`, senão `limite {fmtBRL(g.limit)}`. Manter o cálculo de `over`/`tint` apenas quando `hasLimit` (when sem limite, `over` é false). NÃO redesenhar o card — só ajustar os textos/valores condicionais. Confirmar que o `BudgetBar` (§851) continua recebendo `limit: g.limit` (pode ser null) sem quebrar — se o `BudgetBar` usar `g.limit` em divisão, garantir guarda; ele usa `g.target` para o segmento, então `limit` null é tolerado.
  </action>
  <verify>
    <automated>grep -n "Sem limite" assets/fides-studio.jsx && grep -n "g.limit != null" assets/fides-studio.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: o render de grupo trata `g.limit == null` exibindo "Sem limite" no lugar de "0%".
    - Source: `ProgressBar` recebe `value` numérico (0 quando pct é null), nunca null.
    - Source: o texto de meta exibe "sem limite definido" quando `g.limit == null`, senão `limite {fmtBRL(g.limit)}`.
    - Behavioral (pós-deploy): grupo com limites definidos no Planejamento exibe limite somado > 0 e % real ≠ 0; grupo sem nenhuma categoria com limite exibe "Sem limite" (não "0%").
  </acceptance_criteria>
  <done>O card de orçamento mostra o limite real somado por grupo e % real quando há limite; mostra estado neutro "Sem limite" quando o grupo não tem limite efetivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar limites reais no card "Para onde foi"</name>
  <action>Pausar para verificação humana pós-deploy. Apresentar ao usuário o que foi construído e os passos de verificação; aguardar o sinal de retorno antes de prosseguir.</action>
  <what-built>budgetGroups passou a ler categoryLimits (limite real do Planejamento) com regra byMonth→default→null; o card "Para onde foi" exibe limite somado por grupo, % real, e estado "Sem limite" quando o grupo não tem limite definido.</what-built>
  <how-to-verify>
    1. Após deploy (push em main → Vercel ~2 min), abrir https://fides-money.vercel.app no Dashboard, num mês com limites definidos no Planejamento.
    2. No card "Para onde foi" (Capítulo II), conferir que cada grupo (Essencial / Estilo de vida / Dívidas) mostra um limite somado > 0 e um % = gasto/limite ≠ 0%.
    3. Conferir manualmente que o limite do grupo = soma dos limites das categorias-filhas definidos no Planejamento para esse mês.
    4. Num grupo sem nenhuma categoria com limite, conferir que aparece "Sem limite" e não "0%".
    5. Verificar em 400×512px (iOS Safari ou DevTools mobile) que o card não quebra layout.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que diverge (limite, %, ou estado sem-limite).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → store (categoryLimits) | dados já carregados em memória via query autenticada com RLS; nenhuma nova entrada do usuário cruza aqui nesta mudança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | budgetGroups (leitura de category_limits) | accept | Dados são do próprio usuário, protegidos por RLS no Supabase; a mudança só troca a fonte de leitura em memória — nenhum dado novo exposto |
| T-01-02 | Tampering | render do card (divisão por limite) | mitigate | Guarda null-safe: `pct = groupLimit != null ? ... : null` e `ProgressBar value` nunca null — evita NaN/crash que distorceria o número exibido |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (stack Babel standalone, sem npm no front — RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `budgetGroups` lê `categoryLimits` (não `plannedOverrides`) pela regra byMonth→default→null idêntica a `categoryUsage`.
- Soma de grupo null-safe distingue "sem limite" de "limite 0".
- Render do card exibe limite real, % real, e "Sem limite" no edge case.
- Sem regressão de layout em 400×512px.
</verification>

<success_criteria>
- Card "Para onde foi" exibe limite por grupo > 0 e % real quando há limites no Planejamento para o mês.
- O limite de grupo = soma dos limites efetivos das categorias-filhas (regra byMonth→default→null).
- Grupo sem limite mostra "Sem limite", não "0%".
</success_criteria>

<output>
Create `.planning/phases/01-veracidade-do-dashboard/01-01-SUMMARY.md` when done
</output>
