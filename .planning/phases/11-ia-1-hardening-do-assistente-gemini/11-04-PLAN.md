---
phase: 11-ia-1-hardening-do-assistente-gemini
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - assets/fides-orcamento.jsx
  - assets/fides-claude.jsx
autonomous: true
requirements: [WR-01, WR-02, WR-03]

must_haves:
  truths:
    - "Duplo-tap rápido no botão 'Análise da IA' dispara só 1 chamada (não queima cota nem faz 2ª chamada)"
    - "Os dois callers (chat e análise) enviam o JWT no header Authorization: Bearer, ausente do corpo do POST"
    - "O caller de análise envia mode='analysis' e não trata mais functionCall como erro (branch morto removido)"
  artifacts:
    - path: "assets/fides-orcamento.jsx"
      provides: "Cooldown no handleAiClick + fetch com Authorization header + flag mode=analysis + remoção do branch tool_calls"
      contains: "setCooldown"
    - path: "assets/fides-claude.jsx"
      provides: "callAssistant envia JWT no header Authorization"
      contains: "Authorization"
  key_links:
    - from: "assets/fides-orcamento.jsx"
      to: "api/assistant.js"
      via: "fetch com header Authorization Bearer + body com mode analysis"
      pattern: "Authorization"
    - from: "assets/fides-claude.jsx"
      to: "api/assistant.js"
      via: "fetch do callAssistant com header Authorization Bearer"
      pattern: "Authorization"
---

<objective>
Frente cliente do phase: WR-01 (cooldown no botão "Análise da IA"), WR-03 (os DOIS callers migram o JWT do body para o header `Authorization: Bearer`) e a limpeza cliente do WR-02 (o caller de análise passa a enviar `mode:'analysis'` e o branch que tratava `functionCall` como erro vira código morto e é removido).

Purpose: fechar as dívidas de UX/segurança da fase 05 no lado cliente. O cooldown (mesmo padrão do chat, COOLDOWN_NORMAL_SEC=4 / COOLDOWN_RATELIMIT_SEC=60) impede duplo-tap no mobile de fazer 2ª chamada e queimar a cota de 100/dia. A migração do token para o header casa com o servidor do plano 11-02 (deploy atômico — os dois callers e o servidor sobem no mesmo push).

Output: `fides-orcamento.jsx` (cooldown + header + mode + branch removido) e `fides-claude.jsx` (header) atualizados.
</objective>

## Artifacts this phase produces (deste plano)

- **`assets/fides-orcamento.jsx`** (`PlnMesInsights` / `handleAiClick`):
  - Estado `cooldown` + effect de countdown + guarda no topo de `handleAiClick` + re-arme em todos os caminhos terminais + botão desabilitado durante cooldown (WR-01).
  - `fetch` com header `Authorization: Bearer <jwt>`, sem `jwt` no body, com `mode:'analysis'` no body (WR-03 + WR-02).
  - Remoção do branch que tratava resposta `functionCall`/`tool_calls` como erro (WR-02 — código morto após toolConfig NONE no servidor).
  - Duas constantes de cooldown replicadas (mesmos nomes/valores do chat).
- **`assets/fides-claude.jsx`** (`callAssistant`):
  - `fetch` com header `Authorization: Bearer <jwt>`, sem `jwt` no body (WR-03).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ia-1-hardening-do-assistente-gemini/11-CONTEXT.md
@.planning/phases/11-ia-1-hardening-do-assistente-gemini/11-RESEARCH.md
@.planning/phases/11-ia-1-hardening-do-assistente-gemini/11-VALIDATION.md
@CLAUDE.md
@assets/fides-orcamento.jsx
@assets/fides-claude.jsx

# React via Babel-standalone: CUIDADO com Rules of Hooks (já causou bug na Phase 07).
#   Hooks (useState/useEffect) SEMPRE no topo do componente, nunca dentro de handler nem condicional.
# fides-orcamento.jsx usa padrão de state array-indexado (var stX = React.useState(); var x = stX[0]; var setX = stX[1];).
# Deploy atômico com o servidor (11-02): os 2 callers e o servidor sobem no mesmo push (WR-03).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-01 — cooldown no botão "Análise da IA" (fides-orcamento.jsx)</name>
  <files>assets/fides-orcamento.jsx</files>
  <read_first>
    - assets/fides-orcamento.jsx — linhas 986-993 (bloco de state array-indexado de PlnMesInsights: aiLoading/aiResult/aiError — ADICIONAR o cooldown aqui, no topo), 1036-1097 (handleAiClick e seus caminhos terminais: JWT ausente, !res.ok, tool_calls, empty reply, sucesso, catch NETWORK), 1128-1138 (botão: disabled/label).
    - assets/fides-claude.jsx — linhas 9-10 (COOLDOWN_NORMAL_SEC=4, COOLDOWN_RATELIMIT_SEC=60), 84-91 (effect de countdown), 470 (guarda cooldown > 0), 503-539 (re-arme: 60s em 429/USER_DAILY_LIMIT, 4s nos demais terminais). Padrão canônico a replicar.
    - .planning/phases/11-ia-1-hardening-do-assistente-gemini/11-RESEARCH.md — seção "WR-01" (porte do padrão, armadilhas Rules of Hooks e IN-01, mapeamento dos caminhos terminais) e "Open Questions #3" (replicar as mesmas constantes/nomes é aceitável).
  </read_first>
  <action>
    Porte o padrão de cooldown do chat para `PlnMesInsights` (WR-01), sem inventar mecanismo novo. Declare as duas constantes com os MESMOS nomes e valores do chat — COOLDOWN_NORMAL_SEC igual a 4 e COOLDOWN_RATELIMIT_SEC igual a 60 — em escopo de módulo em fides-orcamento.jsx (não redeclarar dentro do componente; replicação de 2 constantes é aceitável já que os dois .jsx não compartilham módulo).

    No TOPO do componente PlnMesInsights (junto do bloco de state existente aiLoading/aiResult/aiError, seguindo o MESMO padrão array-indexado do arquivo: var stCooldown = React.useState(0); var cooldown = stCooldown[0]; var setCooldown = stCooldown[1];), adicione o estado de cooldown. Adicione também, no topo do componente (NUNCA dentro de handleAiClick nem condicional — Rules of Hooks, bug histórico da fase 07), um React.useEffect que faz o countdown: se cooldown <= 0 retorna; senão agenda um setTimeout de 1000ms que decrementa o cooldown em 1 (usando updater funcional com Math.max(0, c - 1)), retornando a função de clear do timeout; dependência [cooldown].

    Em handleAiClick: troque a guarda de entrada de "só aiLoading" para bloquear também durante cooldown (retornar cedo se aiLoading OU cooldown > 0). Arme o cooldown em CADA caminho terminal (a pesquisa lista IN-01: setAiLoading(false) já está duplicado em vários caminhos — adicione setCooldown a cada um e verifique por inspeção que nenhum ficou de fora): sucesso (após setAiResult), erro !res.ok, resposta vazia (empty reply), e o catch de NETWORK. No caminho de erro, se for limite diário/rate limit (status 429 ou error USER_DAILY_LIMIT/RATE_LIMIT), arme COOLDOWN_RATELIMIT_SEC (60s); nos demais terminais, COOLDOWN_NORMAL_SEC (4s) — igual ao chat.

    No botão (disabled hoje só por aiLoading), passe a desabilitar também quando cooldown > 0. Opcional (discrição): mostrar "Aguarde {cooldown}s" no label enquanto cooldown > 0 — o mínimo travado é bloquear o reclique por N segundos.
  </action>
  <acceptance_criteria>
    - As constantes COOLDOWN_NORMAL_SEC (4) e COOLDOWN_RATELIMIT_SEC (60) existem em fides-orcamento.jsx (mesmos nomes do chat).
    - Estado cooldown declarado no topo de PlnMesInsights (padrão array-indexado); o effect de countdown está no topo do componente, não dentro de handleAiClick.
    - handleAiClick retorna cedo quando cooldown > 0; cada caminho terminal (sucesso, erro, empty, network) arma setCooldown; caminhos de limite diário/rate limit usam 60s.
    - O botão fica disabled quando cooldown > 0.
    - Nenhum hook novo dentro de handler ou condicional (Rules of Hooks — verificar por inspeção).
    - Human-verify: no app deployado (400×512px iOS Safari), tocar "Análise da IA" 2× rápido → só 1 chamada; botão desabilita ~4s; em erro 429 → ~60s.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "COOLDOWN_NORMAL_SEC" assets/fides-orcamento.jsx && grep -nE "COOLDOWN_RATELIMIT_SEC" assets/fides-orcamento.jsx && grep -nEc "setCooldown" assets/fides-orcamento.jsx | grep -vq '^0$' && grep -nE "cooldown > 0" assets/fides-orcamento.jsx</automated>
    <human-check>App deployado, viewport 400×512 iOS Safari: tocar "Análise da IA" 2× rápido → uma só resposta e cota +1 (não +2) em assistant_usage; botão desabilitado por ~4s; forçar limite → ~60s de espera.</human-check>
  </verify>
  <done>Cooldown portado do chat: estado+effect no topo, guarda de entrada, re-arme em todos os terminais (60s em rate limit), botão desabilitado durante cooldown; sem violar Rules of Hooks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-03 + WR-02 (cliente) — caller de análise: header Authorization + mode analysis + remover branch morto</name>
  <files>assets/fides-orcamento.jsx</files>
  <read_first>
    - assets/fides-orcamento.jsx — linhas 1044-1054 (obtenção do jwt via window.fidesAuth.getSession), 1056-1065 (fetch: headers só Content-Type; body com o token e toolResults), 1069-1088 (branches: !res.ok, o branch que trata resposta com functionCall como erro, empty reply), 1090-1091 (sucesso).
    - .planning/phases/11-ia-1-hardening-do-assistente-gemini/11-RESEARCH.md — seção "WR-03" (ponto c, caller análise: header + remover token do body + mode) e "WR-02" (efeito colateral: branch de functionCall vira código morto e pode ser removido do handleAiClick).
  </read_first>
  <action>
    No caller de análise de `fides-orcamento.jsx` (dentro de handleAiClick), migre o token para o header (WR-03) e adote o modo análise (WR-02):

    1. No fetch para /api/assistant, adicione ao objeto headers, além do Content-Type, o header Authorization com o esquema Bearer seguido do jwt já obtido via window.fidesAuth.getSession (o jwt continua sendo lido do getSession como hoje — só muda o destino, de body para header).
    2. Remova o token do corpo JSON do POST (o body deixa de carregar o token). Adicione ao body o flag de modo `mode` com o valor de análise (`'analysis'`) — é o flag que o servidor do 11-02 usa para proibir tools (toolConfig NONE). Mantenha messages/context/toolResults como hoje.
    3. Remova o branch de código morto (WR-02): como o servidor no modo análise nunca retorna uma resposta de function-calling, o trecho que hoje trata uma resposta com functionCall/tool_calls como erro (fechando com a mensagem de indisponibilidade) é inalcançável — remova-o do handleAiClick. Os demais caminhos (erro !res.ok, empty reply, sucesso) permanecem e continuam armando o cooldown da Task 1.

    NÃO manter compat retroativa mandando o token no body "por via das dúvidas" — a decisão travada (WR-03) é migração atômica: o servidor exige header, o body não leva mais o token.
  </action>
  <acceptance_criteria>
    - O fetch de análise inclui um header Authorization com esquema Bearer.
    - O corpo do POST de análise não carrega mais o token; carrega o flag de modo com valor de análise.
    - O branch que tratava resposta de function-calling como erro foi removido de handleAiClick (verificar por inspeção do diff).
    - Os caminhos remanescentes (erro/empty/sucesso) continuam armando o cooldown (não regredir a Task 1).
    - Human-verify: DevTools mostra o token no header, ausente do payload; a análise responde autenticada e sempre com texto.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Authorization" assets/fides-orcamento.jsx && grep -nE "Bearer" assets/fides-orcamento.jsx && grep -nE "'analysis'|\"analysis\"|mode:" assets/fides-orcamento.jsx</automated>
    <human-check>Após deploy: DevTools → Network na chamada de análise mostra o token no request header Authorization e ausente do corpo JSON; o body traz mode 'analysis'; a análise responde autenticada.</human-check>
  </verify>
  <done>Caller de análise envia token no header Authorization, mode 'analysis' no body, sem token no body; branch de function-calling removido; cooldown intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: WR-03 — caller do chat envia JWT no header (fides-claude.jsx)</name>
  <files>assets/fides-claude.jsx</files>
  <read_first>
    - assets/fides-claude.jsx — linhas 452-466 (callAssistant: fetch com headers só Content-Type e body carregando o token), 479-499 (jwt obtido via window.fidesAuth.getSession em send() e passado a callAssistant).
    - .planning/phases/11-ia-1-hardening-do-assistente-gemini/11-RESEARCH.md — seção "WR-03" (ponto b, caller chat: header + remover token do body; assinatura de callAssistant pode manter jwt como parâmetro).
  </read_first>
  <action>
    Em `fides-claude.jsx`, no `callAssistant(history, toolResults, jwt)`, migre o token do body para o header (WR-03). No fetch para /api/assistant: adicione ao objeto headers, além do Content-Type, o header Authorization com o esquema Bearer seguido do jwt (a função continua recebendo o jwt como parâmetro — só muda o destino). Remova o token do corpo JSON do POST (o body passa a carregar apenas messages/context/toolResults, sem o token). NÃO alterar a lógica de tools/iterações do chat (o chat MANTÉM suas tools READ — WR-02 não toca o chat). NÃO manter compat de token no body.
  </action>
  <acceptance_criteria>
    - callAssistant monta um header Authorization com esquema Bearer usando o jwt recebido.
    - O corpo do POST do chat não carrega mais o token (verificar por inspeção do diff).
    - A lógica de tools/iterações do chat permanece inalterada.
    - Human-verify: chat responde autenticado; DevTools mostra o token no header, ausente do payload.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Authorization" assets/fides-claude.jsx && grep -nE "Bearer" assets/fides-claude.jsx</automated>
    <human-check>Após deploy: DevTools → Network numa mensagem do chat mostra o token no request header Authorization e ausente do corpo JSON; o chat responde autenticado e as consultas de saldo/extrato (tools READ) seguem funcionando.</human-check>
  </verify>
  <done>Caller do chat envia o token no header Authorization; token fora do body; tools/iterações do chat inalteradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → api/assistant | Os dois callers passam a transportar o token de sessão no header Authorization, não no corpo POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | Transporte do token (WR-03 cliente) | mitigate | Os 2 callers movem o token do body para o header Authorization: Bearer — reduz captura por logging/proxies; nenhum caller pode ficar mandando no body após a mudança (deploy atômico com 11-02) |
| T-11-04-02 | Denial of Service / abuso de cota | handleAiClick (WR-01) | mitigate | Cooldown cliente impede duplo-tap de disparar 2ª chamada e queimar a cota diária; rate limit do servidor é a 2ª camada |
| T-11-04-03 | Elevation of Privilege | Superfície de análise (WR-02) | mitigate | mode 'analysis' + toolConfig NONE no servidor: o caminho de análise nunca invoca tools; IA-1 mantém só-leitura |
| T-11-04-SC | Tampering | npm installs | accept | Zero pacotes novos (RESEARCH: N/A) |
</threat_model>

<verification>
- Estática: cooldown no topo do componente + guarda + re-arme em todos os terminais; os 2 callers montam Authorization e não levam token no body; branch de function-calling removido da análise.
- Segurança: WR-03 é fix de segurança — `security-reviewer` no diff (cobre os callers + servidor 11-02) antes do commit.
- Human-verify: duplo-tap → 1 chamada; token no header (DevTools) em chat e análise; análise sempre com texto; chat com tools READ intactas.
</verification>

<success_criteria>
- Cooldown funcional no botão "Análise da IA" (mesmo padrão do chat, 60s em rate limit), sem violar Rules of Hooks.
- Os dois callers enviam o token no header Authorization: Bearer, ausente do body.
- Caller de análise envia mode 'analysis' e não trata mais function-calling como erro (branch removido).
</success_criteria>

<output>
Create `.planning/phases/11-ia-1-hardening-do-assistente-gemini/11-04-SUMMARY.md` when done
</output>
