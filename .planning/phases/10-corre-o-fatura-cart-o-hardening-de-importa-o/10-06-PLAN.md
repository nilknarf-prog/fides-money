---
phase: 10-corre-o-fatura-cart-o-hardening-de-importa-o
plan: 06
type: execute
wave: 2
depends_on: ["10-04"]
files_modified:
  - assets/fides-transacoes.jsx
autonomous: true
gap_closure: true
requirements: [UX-04]
must_haves:
  truths:
    - "No modo Período, o widget 'Gasto por categoria' (donut + total + barras) reflete por padrão o MÊS selecionado no masthead (ex.: Julho), não o total agregado do range (Mai–Jul) (G5)"
    - "A quebra por categoria termina no top-7 (ex.: Restaurante/Delivery), sem categorias sem barra e sem repetição de categorias (G4)"
    - "O hover/tap numa fatia do Donut ainda mostra categoria + valor + % (relativo ao total do mês), voltando a 'Total' ao sair"
  artifacts:
    - path: "assets/fides-transacoes.jsx"
      provides: "bloco de analytics do modo Período escopado ao mês selecionado (spendByCategory) e sem a legenda textual redundante"
      contains: "spendByCategory"
  key_links:
    - from: "Donut / CategoryChart (bloco rangeMode analytics)"
      to: "spendByCategory (mês selecionado)"
      via: "widget consome spendByCategory + total do mês, não spendByCategoryRange/rangeTotal"
      pattern: "spendByCategory"
    - from: "header do widget"
      to: "lbl.long"
      via: "rótulo mostra o mês selecionado, não rangeLabel"
      pattern: "lbl\\.long"
---

<objective>
Fechar G4 (legenda longa/repetida/sem barra) e G5 (donut/total = total do range, não do mês do Head) do 10-UAT — ambos do teste 8, ambos regressões da mudança "legenda completa" do 10-03.

Root cause:
- G5: o bloco de analytics do modo Período (assets/fides-transacoes.jsx:1034-1084) consome `rangeSpend`/`rangeTotal` = `spendByCategoryRange(fromYM, toYM)` (todo o intervalo). Com o masthead marcando "Julho", o donut mostra R$ 19.229,78 (Mai–Jul) → mismatch confuso.
- G4: além do `CategoryChart` (que já desenha o top-7 com rótulo + valor + %, fides-charts.jsx:256 `slice(0,7)`), o 10-03 adicionou uma legenda textual `.fds-cats-list` (linhas 1067-1078) que lista TODAS as categorias de `rangeSpend` — o que repete o top-7 (já mostrado nas barras) e ainda acrescenta categorias sem barra abaixo de Restaurante/Delivery. O usuário quer voltar ao top-7.

Correção: escopar o widget ao MÊS selecionado (`spendByCategory`, já exposto pelo store e derivado de `monthTransactions`/`selectedMonth`) e remover a legenda textual redundante — o `CategoryChart` já é a legenda visual (top-7 rotulado). Isso resolve G4 e G5 juntos: quebra por categoria = top-7 do mês selecionado.

Purpose: consistência entre o mês do Head e o gráfico; quebra por categoria enxuta (top-7).
Output: `assets/fides-transacoes.jsx` com o bloco de analytics do modo Período escopado ao mês e sem a legenda redundante.

<!-- planner-discipline-allow: fds-cats-list -->
<!-- planner-discipline-allow: fds-cat-row -->
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-UAT.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-03-SUMMARY.md

# Contratos relevantes (não modificar):
# assets/fides-store.jsx:1133 → spendByCategory (useMemo, escopado a monthTransactions/selectedMonth), já exposto via useFides() (linha 1389)
# assets/fides-charts.jsx:253  → CategoryChart já faz slice(0,7) e desenha rótulo + R$ + % por barra (é a legenda visual)
# assets/fides-charts.jsx:116  → Donut expõe onActiveSlice(arcs[i]) com { label, val, tint }
@assets/fides-transacoes.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escopar analytics do modo Período ao mês selecionado (G5) + remover legenda redundante, voltar ao top-7 (G4)</name>
  <files>assets/fides-transacoes.jsx</files>
  <action>
Editar `assets/fides-transacoes.jsx` (UX-04, fecha G4+G5).

1. **Expor spendByCategory no componente.** Adicionar `spendByCategory` ao destructure de `useFides()` (linha 423, hoje já traz `spendByCategoryRange`, `monthTransactions`, `selectedMonth`, etc.). `spendByCategory` já existe no store (fides-store.jsx:1133/1389) e é escopado ao `selectedMonth`.

2. **Total do mês.** Adicionar um `monthTotal` via `React.useMemo` derivado de `spendByCategory` (soma de `d.val`), análogo ao `rangeTotal` existente (linhas 540-542). Deps `[spendByCategory]`.

3. **Trocar a fonte de dados do widget (G5).** No bloco `{rangeMode && (<section className="fds-card fds-tx-v2-analytics" ...>` (linhas 1034-1084), substituir as referências ao range pelo mês selecionado:
   - Header (linha 1039): o `<span className="fds-muted">{rangeLabel}</span>` passa a `{lbl.long}` (o rótulo do mês selecionado; `lbl` já existe, linha 512).
   - Total do header (linha 1042): `{fmtBRL(rangeTotal)}` → `{fmtBRL(monthTotal)}`.
   - Condição de conteúdo (linha 1046) e Donut (linha 1049): `rangeSpend` → `spendByCategory`.
   - Centro do Donut: o `%` da fatia ativa (linha 1054) e o total (linha 1057) passam de `rangeTotal` para `monthTotal`.
   - `CategoryChart` (linha 1062): `data={rangeSpend}` → `data={spendByCategory}`.
   - Mensagem de vazio (linha 1082): trocar "Nenhuma despesa no período selecionado." por algo que reflita o mês (ex.: "Nenhuma despesa em " + o rótulo do mês).

4. **Remover a legenda textual redundante (G4).** Apagar por completo o bloco da legenda que hoje lista todas as categorias (o `<div>` de classe de lista de categorias e suas linhas, atualmente nas linhas 1063-1078, incluindo o comentário acima dele). O `CategoryChart` já desenha o top-7 com rótulo + R$ + % por barra — ele É a legenda visual e o Donut compartilha as mesmas cores (`tint`). Sem a lista extra, a quebra por categoria termina no top-7 (Restaurante/Delivery), sem duplicação nem categorias sem barra.

5. **Limpeza de código morto.** Após as trocas, `rangeSpend` (useMemo linha 534-536) e `rangeTotal` (useMemo linha 540-542) provavelmente ficam sem uso. Verificar (grep) e remover os `useMemo` que não tiverem mais nenhuma referência no arquivo. Se ainda houver referência (ex.: em outro ponto do arquivo), mantê-los. NÃO tocar em `rangeList`/`baseList` — a LISTA continua mostrando o range (o escopo Período da lista permanece; só o widget de analytics passa a refletir o mês).

Não criar CSS novo (todas as classes reusadas já existem). `activeSlice`/`setActiveSlice` (10-03) permanecem — o hover/tap continua funcionando, agora com % relativo ao mês.
  </action>
  <verify>
    <automated>cd "C:/Users/dark_/OneDrive/Documentos/FidesMoney/fides-money" && node -e "const s=require('fs').readFileSync('assets/fides-transacoes.jsx','utf8'); const destOk=/spendByCategory[ ,}]/.test(s.slice(s.indexOf('= useFides()')-400, s.indexOf('= useFides()'))); const monthTotal=/const monthTotal = React\.useMemo/.test(s); const donutMonth=/Donut data=\{spendByCategory\}|<Donut data=\{spendByCategory\}/.test(s); const chartMonth=/CategoryChart data=\{spendByCategory\}/.test(s); const legendGone=(s.match(/fds-cat-row/g)||[]).length===0; const open=(s.match(/\{/g)||[]).length, close=(s.match(/\}/g)||[]).length; console.log('destructure',destOk,'monthTotal',monthTotal,'donut',donutMonth,'chart',chartMonth,'legendGone',legendGone,'braces',open===close); if(!(destOk&&monthTotal&&donutMonth&&chartMonth&&legendGone&&open===close))process.exit(1);"</automated>
  </verify>
  <done>Donut, total do header e CategoryChart consomem `spendByCategory` (mês selecionado); header mostra `lbl.long`; a legenda textual redundante foi removida (a quebra volta ao top-7); chaves balanceadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | mudança é UI-only (troca de fonte de dados de leitura + remoção de markup) — sem escrita no Supabase, sem nova fronteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | XSS refletido no rótulo de categoria | mitigate | `{c.label}`/`lbl.long` renderizados como texto React puro — nenhum `dangerouslySetInnerHTML` introduzido. |
| T-10-06-SC | Tampering | dependências | accept | zero pacotes novos — UI-only. |
</threat_model>

<verification>
- Entrar no modo Período (3m/6m/12m/Ano) com o masthead em Julho → "Gasto por categoria" mostra o total e o donut de Julho (não os ~19k de Mai–Jul); header mostra o mês.
- A quebra por categoria termina no top-7 (Restaurante/Delivery); sem categorias sem barra abaixo; sem categorias repetidas.
- Hover/tap numa fatia do Donut → centro muda para categoria + valor + % (do mês); sair → volta a "Total".
- A LISTA de transações continua respeitando o escopo Período (range) — só o widget de analytics reflete o mês.
</verification>

<success_criteria>
- G5 fechado: widget de analytics reflete o mês selecionado no Head, não o range agregado.
- G4 fechado: quebra por categoria = top-7, sem legenda redundante/sem-barra/repetida.
- Sem CSS novo; chaves/parênteses balanceados (sem build/lint — Babel-standalone).
</success_criteria>

<output>
Create `.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-06-SUMMARY.md` when done
</output>
