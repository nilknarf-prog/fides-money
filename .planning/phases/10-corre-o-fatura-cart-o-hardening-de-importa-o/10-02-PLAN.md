---
phase: 10-corre-o-fatura-cart-o-hardening-de-importa-o
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - assets/fides-transacoes.jsx
autonomous: true
requirements: [IMP-01, IMP-02]
must_haves:
  truths:
    - "Importar CSV ou OFX abre um modal de preview ANTES de gravar qualquer coisa; clicar Cancelar não grava nada [D-06]"
    - "Linhas novas vêm marcadas por default; linhas detectadas como duplicatas vêm desmarcadas e com marca visual 'já importada' (não ocultas) [D-07/D-08]"
    - "Reimportar o mesmo arquivo já importado resulta em 0 novas gravações (dedupe por description+value+date normalizado, dia exato) [D-09/D-10 — must_have não-inferível]"
    - "Cada linha usa o mês/fatura correto pela DATA DA PRÓPRIA LINHA (não selectedMonth global) [D-11]"
    - "Quando a conta destino escolhida é um cartão, a gravação usa card_id (não account_id com card_id null) [D-12]"
    - "Import nunca marca is_transfer — CSV/OFX descreve despesa/receita normal, nunca pagamento de fatura (comportamento atual preservado)"
  artifacts:
    - path: "assets/fides-transacoes.jsx"
      provides: "dedupeKey + buildDedupeIndex (dedupe normalizado D-09/D-10)"
      contains: "function dedupeKey"
    - path: "assets/fides-transacoes.jsx"
      provides: "resolveRowForImport (mês por linha + card_id resolvido D-11/D-12)"
      contains: "resolveRowForImport"
    - path: "assets/fides-transacoes.jsx"
      provides: "ImportPreviewModal (preview/seleção/confirmação IMP-01) + handleImport reescrito (parse-then-preview)"
      contains: "ImportPreviewModal"
  key_links:
    - from: "handleImport"
      to: "ImportPreviewModal"
      via: "handleImport faz parse + flags de dedupe e abre o modal via setImportPreview (não grava)"
      pattern: "setImportPreview\\("
    - from: "ImportPreviewModal (confirm)"
      to: "addTransactions (fides-store.jsx)"
      via: "onConfirm mapeia cada linha selecionada por resolveRowForImport e grava em lote via addTransactions"
      pattern: "addTransactions\\("
---

<objective>
IMP-01 + IMP-02 (Onda 2, P2 — débito). Transformar o import de CSV/OFX de "grava linha a linha sem checagem" em "parse → preview → seleção → confirmação → dedupe", corrigindo os três bugs do incidente real (196 duplicatas revertidas por SQL no UAT da Fase 09):

1. **IMP-01 (D-06/D-07/D-08):** `handleImport` (fides-transacoes.jsx:554) passa a parsear o arquivo e abrir um `ImportPreviewModal` ANTES de gravar. Linhas novas marcadas por default; duplicatas desmarcadas + badge "já importada"; Cancelar não grava nada; Confirmar grava só as linhas marcadas.
2. **IMP-02 dedupe (D-09/D-10):** chave `description`+`value`+`date` normalizada (desc trim+lowercase+colapso de espaços; value em centavos inteiro; date `YYYY-MM-DD`, dia exato sem tolerância) contra as transações já em memória (`transactions` do store).
3. **IMP-02 mês/card_id (D-11/D-12):** cada linha calcula o mês/fatura pela sua PRÓPRIA data (parar de forçar o mês global — bug fides-transacoes.jsx:612); a conta/cartão destino vem de um seletor no preview; quando é cartão, grava `card_id`. Gravação final via `addTransactions` (bulk existente).

Todos os padrões são cópia-adaptação de analogs já em produção (EditTxModal.handleSave / FIX-EDIT-MES, PagarFaturaModal, addTransactions) — não há design novo. Domínio de cartão sensível: rodar database/security review sobre a lógica de resolução account_id/card_id antes de commitar.

Purpose: parar a corrupção silenciosa de dados financeiros por reimport.
Output: helpers de dedupe/resolução + ImportPreviewModal + handleImport reescrito, tudo em fides-transacoes.jsx.

Nota de dependência: `depends_on: [10-01]` é ordenação de PRIORIDADE (D-01: Onda 1 FAT-01 entregue antes da Onda 2), não dependência técnica de arquivo — este plano toca arquivo distinto (fides-transacoes.jsx).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-CONTEXT.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-RESEARCH.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md
</context>

## Artifacts This Phase Produces (Plano 10-02)

Símbolos NOVOS criados por este plano (excluídos de verificação de drift/source-grounding — criações intencionais desta fase):

- `dedupeKey(tx)` — função pura nova (chave de dedupe normalizada, D-09/D-10).
- `buildDedupeIndex(transactions)` — Set de chaves das transações existentes.
- `resolveRowForImport(row, destAcctId, cards)` — resolve mês/fatura por linha + card_id (D-11/D-12).
- `parseCsvRows(text)` / `parseOfxRows(text)` — extração da lógica de parse já existente em rows brutas (sem gravar).
- `ImportPreviewModal` — componente de modal novo (preview/seleção/confirmação, IMP-01).
- `importPreview` — novo estado (`useState`) no componente `Transacoes` que controla a abertura do modal.

Símbolos MODIFICADOS:

- `handleImport` (fides-transacoes.jsx:554) — reescrito para parse-then-preview.
- Destructure de `useFides()` no componente `Transacoes` (linha 264) — passa a incluir `addTransactions`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers de dedupe/resolução + reescrever handleImport para parse-then-preview (D-06/D-09/D-10/D-11/D-12)</name>
  <files>assets/fides-transacoes.jsx</files>
  <behavior>
    - dedupeKey({desc:'  Mercado  Extra ', val:-12.5, date:'2026-06-19'}) === dedupeKey({desc:'mercado extra', val:12.50, date:'2026-06-19'}) (trim+lowercase+colapso de espaço; value absoluto em centavos; date dia exato).
    - dedupeKey de mesma desc/valor mas datas de dias diferentes → chaves DIFERENTES (janela dia-exato, sem tolerância ±1 — D-10).
    - buildDedupeIndex(transactions) retorna um Set; toda linha cujo dedupeKey pertence ao Set é marcada _isDuplicate:true.
    - resolveRowForImport(row, destAcctId, cards): se destAcctId é id de cartão → mes = mesFaturaFor(row.d, card, row.ano) e card_id = destAcctId; senão → mes = mês calendário da própria data da linha e sem card_id.
    - handleImport(fmt): após parse, se 0 linhas → toast.error e retorna; senão calcula flags de dedupe e abre o modal (setImportPreview) — NENHUMA gravação acontece nesta etapa.
  </behavior>
  <read_first>
    - assets/fides-transacoes.jsx:264 — destructure de `useFides()` no componente `Transacoes` (hoje tem `addTransaction`, NÃO tem `addTransactions`; `addTransactions` existe no store — ver fides-store.jsx:411 e uso em fides-transacoes.jsx:1449).
    - assets/fides-transacoes.jsx:554-622 — `handleImport` atual (parse CSV/OFX + gravação linha a linha; os três bugs: resolução só por nome, força mês global, card_id null).
    - assets/fides-transacoes.jsx:1299-1321 — `EditTxModal.handleSave` (FIX-EDIT-MES): padrão real de recálculo de mês via `window.mesFaturaFor(dd + '/' + mm, card, ano)` — replicar por linha.
    - assets/fides-store.jsx:411-431 — `addTransactions` (bulk insert; trata mode live vs mock). Pitfall 6 do RESEARCH: em modo mock o mês NÃO é recalculado por txToRow — por isso resolveRowForImport calcula o mês explicitamente antes de gravar.
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-RESEARCH.md:476-492 — dedupeKey/buildDedupeIndex e a nota de normalizar a data das linhas parseadas para `YYYY-MM-DD` antes de comparar (as duas pontas comparam o mesmo formato).
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md:243-272 — resolveRowForImport + dedupeKey (corpo exato) + nota Pitfall 6.
  </read_first>
  <action>
    Adicionar `addTransactions` ao destructure de `useFides()` do componente `Transacoes` (linha 264, ao lado de `addTransaction`).

    Criar três funções puras (fora do componente, no topo do módulo, ao lado dos outros helpers do arquivo):
    - `dedupeKey(tx)`: `desc` = `String(tx.desc||'').trim().toLowerCase().replace(/\s+/g,' ')`; `cents` = `Math.round(Math.abs(Number(tx.val))*100)`; `day` = `String(tx.date||'').slice(0,10)` (YYYY-MM-DD, dia exato — D-10); retorna a string `desc|cents|day`.
    - `buildDedupeIndex(transactions)`: retorna `new Set((transactions||[]).map(dedupeKey))`.
    - `resolveRowForImport(row, destAcctId, cards)`: `cardIdSet = new Set((cards||[]).map(c=>c.id))`; `isCard = cardIdSet.has(destAcctId)`; `mes = row.mesFromCsv` (fallback = `${row.ano}-${mm}` calendário da própria data); se `isCard`, achar o card e `mes = window.mesFaturaFor(row.d, card, row.ano)` quando retornar valor; devolver `{ ...row, acct: destAcctId, mes, card_id: isCard ? destAcctId : undefined }`.

    Extrair a lógica de parse atual em duas funções puras `parseCsvRows(text)` e `parseOfxRows(text)` que retornam um array de objetos-linha SEM gravar. Cada linha deve carregar: `desc`, `val` (número), `d` ('dd/mm'), `ano` (número, do próprio arquivo — ano do CSV/OFX, não selectedMonth), `date` ('YYYY-MM-DD' montado a partir de ano/mm/dd para o dedupe casar com o store), `cat` (catKey resolvido como hoje), `acctNameRaw` (nome cru do CSV para sugestão de destino), `status`, `recur`, e um `_key` estável (índice). Preservar o mapeamento de categoria por label já existente e o parse de valor (`parseFloat(...replace(',','.'))`). Ignorar linhas inválidas (sem desc/val) como hoje, contando-as para o resumo.

    Reescrever `handleImport(fmt)` para: abrir o file input (mantendo `accept` por formato), ler o texto via FileReader, chamar `parseOfxRows`/`parseCsvRows`, e — se vazio — `window.FidesUI.toast.error(...)` e retornar. Caso contrário: `const existing = buildDedupeIndex(transactions);` marcar cada linha com `_isDuplicate: existing.has(dedupeKey(row))`, e abrir o modal chamando `setImportPreview({ rows, fmt })`. NÃO chamar `addTransaction`/`addTransactions` aqui — o parse-then-preview grava NADA até a confirmação (D-06). Parar de forçar o mês global no import (bug da linha 612): o mês passa a ser resolvido por linha em resolveRowForImport (D-11). Ajustar as deps do `useCallback` para incluir `transactions`.

    Declarar o estado `const [importPreview, setImportPreview] = React.useState(null);` no topo do componente `Transacoes`, incondicional (Rules of Hooks — nunca dentro de bloco/condicional).
  </action>
  <acceptance_criteria>
    - `grep -c "function dedupeKey" assets/fides-transacoes.jsx` retorna 1; `grep -c "buildDedupeIndex" assets/fides-transacoes.jsx` >= 2 (definição + uso); `grep -c "resolveRowForImport" assets/fides-transacoes.jsx` >= 1.
    - handleImport abre o modal em vez de gravar: `grep -c "setImportPreview(" assets/fides-transacoes.jsx` >= 1.
    - O import não força mais o mês global: `grep -c 'mes: selectedMonth' assets/fides-transacoes.jsx` retorna 0.
    - Lógica pura de dedupe validada (normalização + janela dia-exato) extraindo dedupeKey do arquivo shipado:
      `node -e "const fs=require('fs');const s=fs.readFileSync('assets/fides-transacoes.jsx','utf8');const m=s.match(/function dedupeKey[\s\S]*?\n}/);eval(m[0]);const a=dedupeKey({desc:'  Mercado  Extra ',val:-12.5,date:'2026-06-19'});const b=dedupeKey({desc:'mercado extra',val:12.50,date:'2026-06-19'});const c=dedupeKey({desc:'mercado extra',val:12.50,date:'2026-06-20'});if(a!==b)throw new Error('normalizacao FALHOU');if(a===c)throw new Error('janela dia-exato FALHOU');console.log('DEDUPE OK');"`
      imprime `DEDUPE OK`.
    - `addTransactions` está no destructure do componente Transacoes: `grep -n "addTransactions" assets/fides-transacoes.jsx` mostra ocorrência dentro/próxima da linha 264.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function dedupeKey" assets/fides-transacoes.jsx | grep -qx 1 && grep -c 'mes: selectedMonth' assets/fides-transacoes.jsx | grep -qx 0 && node -e "const fs=require('fs');const s=fs.readFileSync('assets/fides-transacoes.jsx','utf8');const m=s.match(/function dedupeKey[\s\S]*?\n}/);eval(m[0]);const a=dedupeKey({desc:'  Mercado  Extra ',val:-12.5,date:'2026-06-19'});const b=dedupeKey({desc:'mercado extra',val:12.50,date:'2026-06-19'});const c=dedupeKey({desc:'mercado extra',val:12.50,date:'2026-06-20'});if(a!==b)throw new Error('norm');if(a===c)throw new Error('dia');console.log('DEDUPE OK');"</automated>
  </verify>
  <done>dedupeKey/buildDedupeIndex/resolveRowForImport/parseCsvRows/parseOfxRows existem; handleImport parseia e abre o modal sem gravar; o mês global forçado sumiu; addTransactions no destructure; script DEDUPE OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImportPreviewModal (preview/seleção/confirmação) + montar + confirmar via addTransactions (D-07/D-08/D-11/D-12)</name>
  <files>assets/fides-transacoes.jsx</files>
  <behavior>
    - Ao abrir com N linhas: o Set inicial de selecionadas contém exatamente as linhas novas (`!_isDuplicate`); duplicatas começam desmarcadas (D-07/D-08).
    - Cada linha duplicada exibe um badge textual "já importada"; ainda é marcável manualmente (usuário pode forçar).
    - "Selecionar todas"/"limpar" alterna o Set inteiro; contador reflete quantas serão importadas.
    - Seletor de conta/cartão destino (dropdown) lista contas E cartões; default sugerido pelo nome cru da linha quando casar, senão a primeira conta.
    - Cancelar (ou fechar/backdrop/Escape) fecha o modal e NÃO grava nada (D-06).
    - Confirmar: mapeia cada linha marcada por resolveRowForImport(row, destAcctId, cards), grava em lote via addTransactions, fecha o modal e mostra toast de resumo (`N transação(ões) importada(s)`).
  </behavior>
  <read_first>
    - assets/fides-contas.jsx:179-330 — `PagarFaturaModal`: analog de checklist + `toggle`/`toggleAll` sobre um Set, `allSelected`/`noneSelected`, e o shell `.fds-modal-backdrop`/`.fds-modal`/`.fds-modal-head`/`.fds-modal-body` (copiar estrutura de estado e de shell).
    - assets/fides-transacoes.jsx:1281-1321 — toggle Conta/Cartão (`pay`/`_cardIds`) + FIX-EDIT-MES do EditTxModal (o seletor de destino do preview espelha esse toggle, agora por dropdown de conta/cartão).
    - assets/fides-transacoes.jsx:1200-1235 — como TxAdvFiltersModal é montado condicionalmente no render de Transacoes (padrão de montagem `{state && <Modal .../>}` a espelhar para importPreview).
    - assets/fides-ui.jsx — `window.FidesUI.toast` (resumo pós-confirmação) e `useModalClose`/shell de modal (animação abrir/fechar, Escape/backdrop) já resolvidos.
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md:276-355 — ImportPreviewModal: estado inicial do Set (novas marcadas / duplicatas desmarcadas), shell reaproveitado, e a NOTA de Rules of Hooks.
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-RESEARCH.md:374-378 — Pitfall 3 (Rules of Hooks): já causou bug real na Phase 07/MetasStudio.
  </read_first>
  <action>
    Criar o componente `ImportPreviewModal({ preview, accounts, cards, onCancel, onConfirm })` como irmão de `EditTxModal`/`TxAdvFiltersModal` no mesmo módulo. `preview` = `{ rows, fmt }`.

    Estado (TODOS os hooks incondicionais no topo do componente — Rules of Hooks, Pitfall 3): um único `const [selected, setSelected] = React.useState(() => new Set(preview.rows.filter(r => !r._isDuplicate).map(r => r._key)))` (novas marcadas, duplicatas desmarcadas — D-07/D-08, calculado UMA vez na abertura, nunca recalculado por render); e `const [destAcct, setDestAcct] = React.useState(() => <primeira conta ou sugestão por nome>)`. NUNCA declarar `useState` dentro do `.map()` das linhas; NUNCA colocar `return null` condicional antes de todos os hooks.

    `toggle(_key)` adiciona/remove do Set (padrão PagarFaturaModal); `toggleAll()` alterna entre "todas" e "vazio". Derivar `allSelected`/`noneSelected`/contagem de selecionadas.

    Render (shell `.fds-modal-backdrop`/`.fds-modal` copiado de PagarFaturaModal): cabeçalho com título "Revisar importação" e contagem "X de Y serão importadas"; um `<select className="fds-select">` de destino listando `accounts` e `cards` (agrupados/rotulados o suficiente para o usuário distinguir conta de cartão); e a lista de linhas — colunas suficientes para decidir (data, descrição, valor, categoria — a critério, Claude's Discretion do CONTEXT.md). Cada linha: checkbox ligado a `selected.has(row._key)`, e, quando `row._isDuplicate`, um badge textual "já importada" (marca visual, sem ocultar — D-08). IMPORTANTE (mitiga XSS armazenado): renderizar `desc`/memo como texto React puro (`{row.desc}`), NUNCA via `dangerouslySetInnerHTML`.

    Rodapé: botão "Cancelar" chama `onCancel` (fecha, grava nada — D-06); botão primário "Importar (N)" chama `onConfirm(selectedRows, destAcct)` onde `selectedRows` são as linhas cujo `_key` está em `selected`.

    Montar no render de `Transacoes`: `{importPreview && <ImportPreviewModal preview={importPreview} accounts={safeAccounts} cards={safeCards} onCancel={() => setImportPreview(null)} onConfirm={handleImportConfirm} />}`.

    Escrever `handleImportConfirm(selectedRows, destAcctId)` no componente `Transacoes` (`React.useCallback`): mapear `selectedRows.map(r => resolveRowForImport(r, destAcctId, safeCards))` para o payload que `addTransactions` espera (desc, val, cat, acct, d, status, recur, mes, e card_id quando presente); chamar `await addTransactions(payloads)`; `setImportPreview(null)`; `window.FidesUI.toast.success(\`${payloads.length} transação(ões) importada(s).\`)`. Deps do useCallback: `addTransactions`, `safeCards`.
  </action>
  <acceptance_criteria>
    - `grep -c "ImportPreviewModal" assets/fides-transacoes.jsx` >= 2 (definição + montagem no render).
    - O confirm grava em lote pelo caminho novo: `grep -c "addTransactions(" assets/fides-transacoes.jsx` >= 1 dentro de handleImportConfirm.
    - O confirm resolve por linha: `grep -c "resolveRowForImport" assets/fides-transacoes.jsx` >= 2 (definição na Task 1 + uso aqui).
    - Sem XSS por HTML bruto no modal novo: `grep -c "dangerouslySetInnerHTML" assets/fides-transacoes.jsx` retorna 0.
    - Sem regressão de Rules of Hooks: nenhum `React.useState`/`React.useMemo`/`React.useCallback` aparece dentro de um `.map(` no corpo de ImportPreviewModal (revisão manual guiada pelo Pitfall 3).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ImportPreviewModal" assets/fides-transacoes.jsx | awk '$1>=2{ok=1} END{exit ok?0:1}' && grep -c "dangerouslySetInnerHTML" assets/fides-transacoes.jsx | grep -qx 0 && grep -c "resolveRowForImport" assets/fides-transacoes.jsx | awk '$1>=2{ok=1} END{exit ok?0:1}' && echo IMPORT_UI_OK</automated>
    <human-check>Roteiro determinístico do incidente (reimport → 0 duplicatas): no app, exportar o CSV do próprio app (botão Exportar), depois reimportar imediatamente o mesmo arquivo. Todas as linhas devem aparecer no preview marcadas como "já importada" e DESMARCADAS; confirmar deve resultar em 0 novas gravações (contagem de transações inalterada após refresh). Depois: importar um CSV com 1 linha nova + destino = um CARTÃO; confirmar; via Supabase MCP `select account_id, card_id from transactions order by created_at desc limit 1` — `card_id` NÃO é null e `account_id` é null (D-12). Testar Cancelar num import com linhas novas: nada é gravado (D-06).</human-check>
  </verify>
  <done>ImportPreviewModal existe e está montado; abre com novas marcadas / duplicatas desmarcadas + badge; seletor de destino conta/cartão; Cancelar não grava; Confirmar grava só marcadas via addTransactions com card_id resolvido; grep IMPORT_UI_OK; roteiro de reimport resulta em 0 duplicatas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → cliente (memória) | Conteúdo de CSV/OFX é texto arbitrário fornecido pelo usuário; cruza para o estado React no parse. |
| Cliente → Supabase (bulk insert) | addTransactions grava `transactions` (RLS owner-scoped) com account_id XOR card_id conforme destino escolhido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering (integridade de dado) | dedupeKey / buildDedupeIndex | mitigate | Dedupe determinístico dia-exato (D-09/D-10); duplicatas mostradas desmarcadas mas NÃO ocultas (D-08) → minimiza falso-positivo que esconderia tx legítima; usuário pode forçar marcar. |
| T-10-02-02 | Tampering (saldo corrompido) | resolveRowForImport / payload account_id vs card_id | mitigate | Seletor explícito de destino (D-12) em vez de heurística por nome; cartão grava card_id (não account_id/null). Rodar database-reviewer sobre este diff antes de commitar. |
| T-10-02-03 | Tampering (movimentação vs despesa) | payload de gravação | mitigate | Import nunca seta is_transfer — CSV/OFX é despesa/receita normal, nunca pagamento de fatura; preservar comportamento atual (não inventar auto-detecção de pagamento). |
| T-10-02-04 | Tampering (Stored XSS) | ImportPreviewModal (render de desc/memo) | mitigate | Renderizar desc/memo como texto React puro (`{row.desc}`); proibido dangerouslySetInnerHTML (verificado por grep == 0). |
| T-10-02-05 | Information Disclosure / Access Control | seletor de conta destino | accept | O dropdown só oferece contas/cartões já retornados por RLS owner-scoped; impossível escolher id de outro usuário via esta tela. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase (parser continua vanilla/regex; zero deps novas). |
</threat_model>

<verification>
- Task 1: greps de símbolos novos + `mes: selectedMonth == 0` + script Node de dedupe (normalização e janela dia-exato).
- Task 2: greps de montagem/confirm + `dangerouslySetInnerHTML == 0`; roteiro humano de reimport (0 duplicatas) e inspeção Supabase MCP de card_id.
- UAT `/gsd-verify-work 10`: IMP-01 (preview/cancelar/confirmar) e IMP-02 (reimport → 0, card_id resolvido) dos success criteria #3/#4 do ROADMAP.
- Domínio de cartão sensível (CLAUDE.md): database/security review sobre a resolução account_id/card_id antes de commitar.
</verification>

<success_criteria>
- IMP-01: importar CSV/OFX abre preview com seleção e exige confirmação; cancelar não grava nada (ROADMAP #3).
- IMP-02: reimportar arquivo já importado não cria duplicatas; linhas usam mês/fatura correto por data e resolvem card_id quando destino é cartão (ROADMAP #4).
</success_criteria>

<output>
Create `.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-02-SUMMARY.md` when done
</output>
