---
phase: 10-corre-o-fatura-cart-o-hardening-de-importa-o
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - assets/fides-transacoes.jsx
autonomous: true
requirements: [UX-03, UX-04]
must_haves:
  truths:
    - "Um botão/chip 'Cartão' no masthead de Transações filtra as transações de crédito sem abrir o modal de Filtros avançados [UX-03]"
    - "No modo Período, toda categoria da legenda tem representação — nenhuma cor/fatia do Donut fica sem correspondência na legenda/gráfico [UX-04, fix do mismatch top-7 vs todas as fatias]"
    - "Hover/tap numa fatia do Donut do modo Período mostra a categoria + valor no centro do donut (equivalente ao modo Mês único) [UX-04]"
  artifacts:
    - path: "assets/fides-transacoes.jsx"
      provides: "Chip 'Cartão' no bloco fds-tx-v2-chips + Donut do modo Período com onActiveSlice/.fds-donut-center + legenda textual completa"
      contains: "onActiveSlice"
  key_links:
    - from: "chip Cartão"
      to: "advFilters.contasSelected"
      via: "onClick alterna todos os ids de cartão em advFilters via setAdvFilters"
      pattern: "setAdvFilters\\("
    - from: "Donut (modo Período)"
      to: ".fds-donut-center"
      via: "onActiveSlice={setActiveSlice} alimenta o centro dinâmico com categoria + valor"
      pattern: "onActiveSlice"
---

<objective>
UX-03 + UX-04 (Onda 3, P3 — polish). Puramente aditivo sobre componentes já preparados para isso, tudo em fides-transacoes.jsx:

1. **UX-03 (Claude's Discretion):** um chip "Cartão" no masthead (`fds-tx-v2-chips`) que alterna todos os ids de cartão dentro de `advFilters.contasSelected`, filtrando crédito sem abrir "Filtros avançados". A lógica de toggle já existe presa dentro de `TxAdvFiltersModal.toggleAllCards` (fides-transacoes.jsx:126-141) — só é exposta como atalho fora do modal.
2. **UX-04 (Claude's Discretion):** o Donut do modo Período (fides-transacoes.jsx:867-869) hoje roda sem `onActiveSlice` e sem centro dinâmico. Ligar `onActiveSlice`/`.fds-donut-center` replicando o padrão já em produção em `DashboardStudio` (fides-studio.jsx:1005-1017). Corrigir o mismatch "nem toda cor tem barra" (causa: `CategoryChart` trunca em top-7 enquanto o Donut desenha todas as fatias) adicionando uma legenda textual completa ao lado do Donut (padrão `fds-cats-list`), sem alterar `CategoryChart` (opção mais barata/menor risco do RESEARCH).

Purpose: fechar os itens de UX descobertos no UAT da Fase 09 (09-FOLLOWUPS.md P3a/P3b), reusando mecanismos prontos.
Output: chip "Cartão" + Donut do modo Período com hover/tap e legenda completa, tudo em fides-transacoes.jsx.

Nota de dependência: `depends_on: [10-02]` é dependência REAL de arquivo (ambos os planos editam fides-transacoes.jsx) — este é wave 3, sequencial após o import (Onda 2). Também respeita a ordenação de prioridade de D-01 (UX por último).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-CONTEXT.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-RESEARCH.md
@.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md
</context>

## Artifacts This Phase Produces (Plano 10-03)

Símbolos NOVOS criados por este plano (excluídos de verificação de drift/source-grounding):

- `activeSlice` — novo estado (`useState`) no componente `Transacoes` para o centro dinâmico do Donut do modo Período (setActiveSlice).
- Chip "Cartão" no bloco `fds-tx-v2-chips` (markup novo, sem símbolo nomeado).
- Legenda textual completa (padrão `fds-cats-list`) ao lado do Donut do modo Período (markup novo).

Símbolos MODIFICADOS:

- Bloco `{rangeMode && (...)}` da seção de analytics (fides-transacoes.jsx:853-878) — Donut passa a receber `onActiveSlice` + `.fds-donut-center`.

<tasks>

<task type="auto">
  <name>Task 1: Chip "Cartão" no masthead (UX-03)</name>
  <files>assets/fides-transacoes.jsx</files>
  <read_first>
    - assets/fides-transacoes.jsx:918-944 — bloco `fds-tx-v2-chips` (chips Todas/Receitas/Despesas/Pendentes + "Filtros avançados"); local recomendado para inserir o chip "Cartão".
    - assets/fides-transacoes.jsx:126-141 — `TxAdvFiltersModal.toggleAllCards` (lógica de toggle de todos os cartões em `contasSelected` — analog exato).
    - assets/fides-transacoes.jsx:342-343 — `safeAccounts`/`safeCards` em escopo no componente `Transacoes`; `advFilters`/`setAdvFilters` (linhas 282, 403-405).
    - assets/fides-data.jsx:313 — `Icon.Card` existe (ícone de cartão para o chip).
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md:359-398 — chip "Cartão" adaptado (opera direto sobre `advFilters`, não sobre o `draft` local do modal).
  </read_first>
  <action>
    Inserir um botão/chip "Cartão" dentro do bloco `fds-tx-v2-chips` (~linha 936, antes do separador `fds-chip-sep` ou logo após os chips de tipo — a critério visual). Calcular `const cardIds = safeCards.map(c => c.id);` e `const allCardsActive = cardIds.length > 0 && cardIds.every(id => advFilters.contasSelected.includes(id));`. O chip usa `className={'fds-chip' + (allCardsActive ? ' on' : '')}` e um `<Icon.Card size={13}/>` + texto "Cartão".

    onClick: `setAdvFilters(prev => ({ ...prev, contasSelected: allCardsActive ? prev.contasSelected.filter(id => !cardIds.includes(id)) : Array.from(new Set([...prev.contasSelected, ...cardIds])) }))` — adiciona todos os ids de cartão quando desligado, remove todos quando ligado. Reusa o MESMO estado de filtro por conta já existente (nenhum estado novo), então o filtro efetivo (`filtered`, linhas 403-405, que já filtra por `advFilters.contasSelected`) passa a refletir o chip imediatamente, sem abrir o modal de Filtros avançados.

    Não introduzir novo mecanismo de filtro; não duplicar `contasSelected`. Se não houver cartões (`safeCards` vazio), o chip pode ficar oculto ou desabilitado (a critério).
  </action>
  <acceptance_criteria>
    - Chip presente e ligado ao estado de filtro por cartão: `grep -c "allCardsActive" assets/fides-transacoes.jsx` >= 1 e `grep -c "Icon.Card" assets/fides-transacoes.jsx` >= 1.
    - Reusa advFilters (não cria estado novo): o onClick chama `setAdvFilters(` — `grep -c "setAdvFilters(" assets/fides-transacoes.jsx` >= 2 (já existe o clear + este uso).
    - Nenhum novo campo de filtro introduzido: `grep -c "contasSelected" assets/fides-transacoes.jsx` continua referindo o mesmo campo (sem `cardOnly`/flags novas).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "allCardsActive" assets/fides-transacoes.jsx | awk '$1>=1{ok=1} END{exit ok?0:1}' && grep -c "Icon.Card" assets/fides-transacoes.jsx | awk '$1>=1{ok=1} END{exit ok?0:1}' && echo UX03_OK</automated>
    <human-check>No app, na tela de Transações, clicar o chip "Cartão": a lista passa a mostrar só transações de crédito (cartões), o chip fica com estado `on`, e o modal de Filtros avançados NÃO abre. Clicar de novo desliga o filtro.</human-check>
  </verify>
  <done>Chip "Cartão" no masthead alterna todos os cartões em advFilters.contasSelected, filtra crédito sem abrir Filtros avançados, sem estado novo; grep UX03_OK; verificação visual confere.</done>
</task>

<task type="auto">
  <name>Task 2: Donut do modo Período — hover/tap (centro dinâmico) + legenda completa (UX-04)</name>
  <files>assets/fides-transacoes.jsx</files>
  <read_first>
    - assets/fides-transacoes.jsx:852-878 — bloco `{rangeMode && (...)}` da seção de analytics: Donut hoje sem `onActiveSlice` e sem `.fds-donut-center` (linhas 867-869); `CategoryChart` ao lado.
    - assets/fides-studio.jsx:1005-1018 — `DashboardStudio`: Donut com `onActiveSlice={setActiveSlice}` + `.fds-donut-center` (ativo → label/valor/%; senão → Total) + `fds-cats-list` (legenda textual completa). Analog exato a replicar.
    - assets/fides-charts.jsx:116-143 — assinatura de `Donut` ({ data, size, thickness, onActiveSlice }) e o shape de `arcs[i]` passado ao callback (label/val).
    - assets/fides-charts.jsx:253-256 — `CategoryChart` trunca em `slice(0,7)` (causa raiz do mismatch "nem toda cor tem barra"); NÃO alterar CategoryChart — resolver via legenda completa.
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-RESEARCH.md:287-314 e :380-384 — Pattern 4 (centro dinâmico) e Pitfall 4 (mismatch top-7, opção recomendada = legenda textual).
    - .planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-PATTERNS.md:402-443 — Donut modo Período + estado `activeSlice` incondicional + Pitfall 4.
  </read_first>
  <action>
    Declarar `const [activeSlice, setActiveSlice] = React.useState(null);` no TOPO do componente `Transacoes`, incondicional (Rules of Hooks — nunca dentro do bloco `{rangeMode && ...}`). Confirmar que `activeSlice`/`setActiveSlice` ainda não existem no arquivo (não existem hoje).

    No bloco do Donut do modo Período (dentro de `.fds-donut-wrap`, linha ~867): passar `onActiveSlice={setActiveSlice}` ao `Donut` e adicionar, como irmão do `<Donut>`, uma `<div className="fds-donut-center">` que — quando `activeSlice` — mostra `activeSlice.label`, `fmtBRL(activeSlice.val, { compact: true })` e o percentual `Math.round((activeSlice.val / rangeTotal) * 100)}%`; quando não há slice ativo, mostra "Total" + `fmtBRL(rangeTotal, { compact: true })`. Calcular `rangeTotal = rangeSpend.reduce((s,d)=>s+d.val,0)` (já existe esse reduce inline no header — reutilizar/derivar). O CSS `.fds-donut-center`/`.fds-donut-label`/`.fds-donut-value` já existe em fides.css — não criar CSS novo.

    Corrigir o mismatch "nem toda cor tem barra" (Pitfall 4): adicionar ao lado do Donut uma legenda textual completa listando TODAS as categorias de `rangeSpend` (não truncada), no padrão `fds-cats-list` já usado em DashboardStudio — cada item com o tint da categoria, o label e o valor. Isso torna o truncamento em top-7 do `CategoryChart` irrelevante para a legenda, sem tocar em `CategoryChart` (menor risco). Renderizar labels como texto React puro (sem HTML bruto).
  </action>
  <acceptance_criteria>
    - Donut do modo Período agora reporta a fatia ativa: `grep -c "onActiveSlice" assets/fides-transacoes.jsx` >= 1 e `grep -c "setActiveSlice" assets/fides-transacoes.jsx` >= 1.
    - Centro dinâmico presente: `grep -c "fds-donut-center" assets/fides-transacoes.jsx` >= 1.
    - Legenda completa presente (fix do mismatch): `grep -c "fds-cats-list" assets/fides-transacoes.jsx` >= 1.
    - CategoryChart NÃO foi alterado para resolver o mismatch: `grep -c "slice(0, 7)\|slice(0,7)" assets/fides-charts.jsx` permanece 1 (inalterado).
    - `activeSlice` declarado uma vez, incondicional: `grep -c "const \[activeSlice" assets/fides-transacoes.jsx` retorna 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "onActiveSlice" assets/fides-transacoes.jsx | awk '$1>=1{ok=1} END{exit ok?0:1}' && grep -c "fds-donut-center" assets/fides-transacoes.jsx | awk '$1>=1{ok=1} END{exit ok?0:1}' && grep -c "fds-cats-list" assets/fides-transacoes.jsx | awk '$1>=1{ok=1} END{exit ok?0:1}' && echo UX04_OK</automated>
    <human-check>No app, entrar no modo Período (range 3m/6m/…) com várias categorias: passar mouse/tocar uma fatia do Donut → o centro muda para categoria + valor + %; sair → volta a "Total". Conferir que TODA categoria da legenda tem representação (legenda textual completa lista todas, mesmo além das 7 barras do CategoryChart).</human-check>
  </verify>
  <done>Donut do modo Período tem onActiveSlice + centro dinâmico (categoria/valor/%); legenda textual completa cobre todas as categorias; CategoryChart intacto; activeSlice incondicional no topo; grep UX04_OK; verificação visual confere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres (RLS) → cliente | `rangeSpend`/`cards` derivam de dados já filtrados por dono; plano é UI-only (filtro + chart), sem escrita nem nova fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering (XSS refletido) | Legenda textual de categoria (modo Período) | mitigate | Labels de categoria renderizados como texto React puro (padrão fds-cats-list existente); sem dangerouslySetInnerHTML. |
| T-10-03-02 | Tampering | Chip "Cartão" (reuso de advFilters) | accept | Não introduz novo estado nem escrita; só reusa o filtro por conta já validado; sem superfície nova. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase (só edição de .jsx existente + CSS já presente). |
</threat_model>

<verification>
- Task 1: greps do chip + reuso de advFilters; verificação visual do filtro por cartão sem abrir Filtros avançados.
- Task 2: greps de onActiveSlice/fds-donut-center/fds-cats-list + CategoryChart inalterado; verificação visual do hover/tap e da cobertura de todas as categorias.
- UAT `/gsd-verify-work 10`: UX-03/UX-04 do success criteria #5 do ROADMAP.
</verification>

<success_criteria>
- UX-03: botão "Cartão" no masthead filtra crédito sem abrir Filtros avançados (ROADMAP #5).
- UX-04: no modo Período toda categoria da legenda tem representação e o valor por categoria aparece no hover/tap (ROADMAP #5).
</success_criteria>

<output>
Create `.planning/phases/10-corre-o-fatura-cart-o-hardening-de-importa-o/10-03-SUMMARY.md` when done
</output>
