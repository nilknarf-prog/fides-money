---
phase: 12-ia-2-destravar-write-no-assistente-in-app-b8
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - assets/fides-studio.jsx
autonomous: true
requirements: [HONEST-01]

must_haves:
  truths:
    - "Com um card de confirmação WRITE pendente, ⌘K/Ctrl+K NÃO abre o Command Palette por cima (P6)"
    - "Sem confirmação pendente, ⌘K continua abrindo o palette normalmente (sem regressão da fase 09)"
  artifacts:
    - path: "assets/fides-studio.jsx"
      provides: "Guard no handler global de Cmd/Ctrl+K que respeita a confirmação WRITE pendente"
      contains: "__fidesWriteConfirmPending"
  key_links:
    - from: "handler de keydown ⌘K (fides-studio.jsx)"
      to: "window.__fidesWriteConfirmPending (Plan 04)"
      via: "early-return no keydown quando a flag está true"
      pattern: "__fidesWriteConfirmPending"
---

<objective>
Corrigir P6 (⌘K sobrepõe o card de confirmação de escrita): o listener global de `Cmd/Ctrl+K` (`fides-studio.jsx:50-59`) hoje abre o `CommandPalette` de qualquer lugar sem checar se há uma confirmação de escrita pendente no assistente. Adicionar um guard que faz early-return quando `window.__fidesWriteConfirmPending` (publicado pelo Plan 04) está `true`, para o palette NÃO abrir por cima do card WRITE. Sem confirmação pendente, o atalho segue idêntico (nenhuma regressão da busca ⌘K da fase 09).

Purpose: Fecha o 6º bug do UAT e reforça HONEST-01 — a confirmação sempre-obrigatória não pode ser "encoberta" por outro overlay.
Output: `assets/fides-studio.jsx` com o guard de ⌘K.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-04-SUMMARY.md
@assets/fides-studio.jsx
</context>

<artifacts>
## Artifacts this plan produces
- **`assets/fides-studio.jsx`**: early-return no handler `onKeyDown` do `React.useEffect` de ⌘K (:50-59) quando `window.__fidesWriteConfirmPending === true`.
- Consome o artifact do Plan 04: `window.__fidesWriteConfirmPending`.
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Guard de ⌘K — não abrir o palette com confirmação WRITE pendente (P6)</name>
  <files>assets/fides-studio.jsx</files>
  <behavior>
    - Card de confirmação WRITE aberto + ⌘K → palette NÃO abre; o card continua sendo a interação em foco.
    - Sem confirmação pendente + ⌘K → palette abre (comportamento da fase 09 intacto).
  </behavior>
  <read_first>
    - assets/fides-studio.jsx (:49-59 useEffect do atalho global ⌘K com `onKeyDown`; :63 auto-close do palette em navegação; :44 paletteOpen useState)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-04-SUMMARY.md (contrato da flag `window.__fidesWriteConfirmPending`: true enquanto card WRITE pendente)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md (P6 — root cause + regression guard)
  </read_first>
  <action>
    No handler `onKeyDown` do `React.useEffect` de ⌘K, ANTES de `e.preventDefault()`/`setPaletteOpen(true)`, adicionar um early-return quando há confirmação WRITE pendente: `if (window.__fidesWriteConfirmPending) return;` (a flag é lida no momento do evento — sem stale-closure, mesmo com o useEffect de deps `[]`). Manter o resto do handler idêntico (mesmo teste `(e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k'`). Não mudar as deps do useEffect (continua `[]` — a leitura é da `window` no event-time, não de estado React capturado). O comportamento com card pendente é "ignora o atalho" (o card segue em foco) — não fechar o card automaticamente (menos surpreendente para o usuário).
    Guardar contra `window` indefinido não é necessário (browser). Não introduzir dependência do store nem novo estado (D-02 — store intocado; a ponte é a flag global do Plan 04).
  </action>
  <verify>
    <automated>grep -q "__fidesWriteConfirmPending" assets/fides-studio.jsx; grep confirma o early-return está DENTRO do bloco do teste de ⌘K e ANTES de `setPaletteOpen(true)`; grep negativo: as deps do useEffect de ⌘K continuam `[]` (nenhum estado React novo capturado).</automated>
    <human-check>UAT (fase gate): abrir card de confirmação de lancar_transacao → apertar ⌘K → palette NÃO abre; cancelar/confirmar o card → ⌘K volta a abrir o palette.</human-check>
  </verify>
  <acceptance_criteria>
    - ⌘K com `window.__fidesWriteConfirmPending===true` → early-return, palette não abre.
    - ⌘K sem confirmação pendente → palette abre (sem regressão fase 09).
    - Sem novo estado React / sem dependência do store; deps do useEffect inalteradas.
  </acceptance_criteria>
  <done>P6 fechado: ⌘K respeita a confirmação WRITE pendente sem regredir a busca.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input de teclado global → overlays de UI | atalho global pode sobrepor um controle de confirmação de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-ui-01 | Tampering/Repudiation (confirmação encoberta) | handler de ⌘K | mitigate | Early-return quando há confirmação WRITE pendente — o card de confirmação (controle de HONEST-01) não pode ser sobreposto pelo palette (P6). |
| T-12-ui-02 | Denial of Service (atalho travado permanentemente) | flag global | mitigate | Cleanup do `useEffect` do Plan 04 zera a flag ao desmontar/resolver — a flag não fica presa em true, então ⌘K não fica bloqueado após o card fechar. |
</threat_model>

<verification>
- **Estático (grep):** early-return com a flag; deps `[]` preservadas; sem estado novo.
- **UAT (fase gate):** os 2 casos (com/sem card pendente) — comportamento correto e sem regressão da busca ⌘K.
</verification>

<success_criteria>
- P6 fechado; HONEST-01 reforçado (confirmação sempre visível, nunca encoberta).
- Busca ⌘K da fase 09 sem regressão.
</success_criteria>

<output>
Create `.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-05-SUMMARY.md` when done. Registrar: local exato do early-return e confirmação de que a busca ⌘K normal segue funcionando.
</output>
