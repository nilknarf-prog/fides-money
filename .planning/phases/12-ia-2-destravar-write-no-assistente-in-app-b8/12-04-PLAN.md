---
phase: 12-ia-2-destravar-write-no-assistente-in-app-b8
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - assets/fides-claude.jsx
autonomous: true
requirements: [WRITE-04, HONEST-01]

must_haves:
  truths:
    - "criar_categoria standalone agora passa por card de confirmação (SD-1) — nenhuma tool WRITE executa direto"
    - "O toast '✓ Categoria criada' só aparece DEPOIS do commit real no banco (P5 morto — await addCategory)"
    - "O cliente devolve ao servidor o nonce recebido, para não re-cobrar cota no mesmo turno (D-06 cliente)"
    - "Uma confirmação WRITE pendente é observável fora do FidesAssistant (flag p/ o guard de ⌘K, Plan 05)"
  artifacts:
    - path: "assets/fides-claude.jsx"
      provides: "criar_categoria em TOOLS_REQUIRING_CONFIRMATION + card + await; nonce threading; window.__fidesWriteConfirmPending"
      contains: "TOOLS_REQUIRING_CONFIRMATION"
  key_links:
    - from: "callAssistant / send loop"
      to: "api/assistant.js (nonce)"
      via: "guarda o nonce recebido em data.nonce e reenvia como body.nonce na próxima chamada"
      pattern: "nonce"
    - from: "FidesAssistant (pendingConfirmation)"
      to: "guard de ⌘K (fides-studio.jsx, Plan 05)"
      via: "React.useEffect espelha !!pendingConfirmation em window.__fidesWriteConfirmPending"
      pattern: "__fidesWriteConfirmPending"
---

<objective>
Fechar os 2 itens restantes do cliente:
1. **SD-1 + P5:** mover `criar_categoria` do caminho de execução-direta para o fluxo de confirmação (entra em `TOOLS_REQUIRING_CONFIRMATION`). Isso resolve P5 de raiz — a execução (`await addCategory`) só acontece depois do `onDecide('confirm')`, dentro de um `await` natural, então o toast de sucesso nunca mais aparece antes do commit real. Nenhuma das 4 tools WRITE executa direto (`<specifics>` travado).
2. **D-06 (cliente):** threading do nonce — `callAssistant` guarda o `nonce` que vem junto de `data.tool_calls` e o reenvia como `body.nonce` na próxima chamada, para o servidor (Plan 02) não re-cobrar cota do mesmo turno. Fail-safe: se não houver nonce, o fluxo funciona igual (só conta cota).
3. **Ponte p/ o guard de ⌘K (Plan 05):** expor `!!pendingConfirmation` em `window.__fidesWriteConfirmPending` via `React.useEffect`, para o handler global de ⌘K (em `fides-studio.jsx`) poder checar sem lifting de estado.

Purpose: WRITE-04 (criar categoria honesto) + HONEST-01 (confirmação sempre) + fecha o lado cliente do D-06 e prepara o P6.
Output: `assets/fides-claude.jsx` com criar_categoria confirmado, nonce round-trip e a flag de pendência.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-PATTERNS.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-02-SUMMARY.md
@assets/fides-claude.jsx
</context>

<artifacts>
## Artifacts this plan produces
- **`assets/fides-claude.jsx`**: `'criar_categoria'` adicionado a `TOOLS_REQUIRING_CONFIRMATION`; branch `criar_categoria` em `resolveWriteToolArgs` (valida label, gera catKey), em `renderConfirmationCard` (mostra "Criar categoria X"), e em `executeWriteTool` (`await addCategory` + toast só após sucesso); remoção do executor direto `toolExecutors.criar_categoria` e do comentário "WRITE leve executa direto" (:5); `callAssistant` com parâmetro `nonce` + estado `lastNonce`; `React.useEffect` que espelha `pendingConfirmation` em `window.__fidesWriteConfirmPending`.
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mover criar_categoria para o fluxo de confirmação (SD-1) e matar o toast falso (P5)</name>
  <files>assets/fides-claude.jsx</files>
  <behavior>
    - "cria a categoria Viagens" → card "Criar categoria: Viagens" com Confirmar/Cancelar.
    - Confirmar com sucesso no banco → toast "✓ Categoria criada" aparece; resposta ao modelo diz success só então.
    - Confirmar mas insert falha (ex. colisão de catKey / rede) → SEM toast de sucesso; retorno `{ error: 'EXECUTION_ERROR' }`.
    - Cancelar → nada gravado.
  </behavior>
  <read_first>
    - assets/fides-claude.jsx (:5 comentário "WRITE leve executa direto" a remover; :17 TOOLS_REQUIRING_CONFIRMATION; :269-283 toolExecutors.criar_categoria — o executor direto com `addCategory` SEM await + toast antecipado = P5; :287-340 resolveWriteToolArgs; :343-387 executeWriteTool; :574-608 renderConfirmationCard; :390-433 executeTools loop)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md (P5 root cause; Q1/SD-1 — mover p/ confirmação resolve de raiz)
  </read_first>
  <action>
    (a) Adicionar `'criar_categoria'` ao array `TOOLS_REQUIRING_CONFIRMATION` (:17) (SD-1).
    (b) Remover o executor de `criar_categoria` do objeto `toolExecutors` (:269-283) — ele não deve mais rodar sem confirmação. Remover também o comentário obsoleto de cabeçalho (:5) que hoje classifica criar_categoria como escrita leve de execução imediata (descreva por conceito; não reintroduza a frase antiga em nenhum comentário).
    (c) Em `resolveWriteToolArgs`, adicionar branch `if (name === 'criar_categoria')`: validar `args.label` (não vazio → senão `{ error: 'Label da categoria é obrigatório.' }`), derivar `catKey` (lowercase, `[^a-z0-9]→_`, colapsar/aparar `_`; se vazio → erro), normalizar `emoji` (default `'🏷️'`) e `group` (whitelist `essenciais|estilo|futuro`, default `estilo`). Retornar `{ resolved: { label, catKey, emoji, group } }`.
    (d) Em `renderConfirmationCard`, adicionar branch `criar_categoria`: título tipo "🏷️ Criar categoria" e detalhes `Nome`, `Emoji`, `Grupo`.
    (e) Em `executeWriteTool`, adicionar branch `if (name === 'criar_categoria')`: `await addCategory(resolved.catKey, { label: resolved.label, emoji: resolved.emoji, group: resolved.group, custom: true })`; SÓ após o await resolver com sucesso, `setToast({ text: '✓ Categoria "<label>" criada', ts: Date.now() })` e `return { success: true, categoria_id: resolved.catKey, ... }`. O wrapper try/catch existente (:383-386) cobre a falha → `{ error: 'EXECUTION_ERROR' }` sem toast (P5 morto).
    Manter o mesmo shape de retorno das outras tools WRITE.
  </action>
  <verify>
    <automated>grep -q "'criar_categoria'" assets/fides-claude.jsx && grep -q "TOOLS_REQUIRING_CONFIRMATION = \[" assets/fides-claude.jsx (criar_categoria no array); grep negativo P5: no branch/execução de criar_categoria não há `addCategory(` sem `await` — verificar que toda ocorrência de `addCategory(` no arquivo é precedida por `await` (`grep -n "addCategory(" assets/fides-claude.jsx` e inspecionar: todas com await); grep negativo: `toolExecutors` não contém mais uma chave `criar_categoria:`; grep negativo do comentário antigo de cabeçalho: `grep -c 'executa direto' assets/fides-claude.jsx` == 0.</automated>
    <human-check>UAT (fase gate): criar categoria via chat → card de confirmação aparece; toast só após confirmar+gravar; simular colisão de nome não mostra toast de sucesso.</human-check>
  </verify>
  <acceptance_criteria>
    - `criar_categoria` está em `TOOLS_REQUIRING_CONFIRMATION`; não há executor direto.
    - `await addCategory` antes de qualquer toast/retorno de sucesso (P5 eliminado).
    - Card de confirmação de `criar_categoria` renderiza (SD-1).
    - Comentário obsoleto (:5) removido.
  </acceptance_criteria>
  <done>criar_categoria confirmado + await; toast falso morto; nenhuma tool WRITE executa direto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Threading do nonce (D-06 cliente) + flag window.__fidesWriteConfirmPending p/ o guard de ⌘K</name>
  <files>assets/fides-claude.jsx</files>
  <behavior>
    - Turno com tool_call: servidor devolve `nonce`; o cliente o guarda e reenvia na próxima chamada como body.nonce → servidor não re-cobra cota.
    - Sem nonce (secret ausente/primeira chamada): fluxo idêntico ao atual, só conta cota (fail-safe).
    - Card de confirmação aberto → window.__fidesWriteConfirmPending === true; fechado/resolvido → false.
  </behavior>
  <read_first>
    - assets/fides-claude.jsx (:452-468 callAssistant — hoje sem nonce no body; :497-530 send loop — `toolResults`/`iteration`; :44+ declarações React.useState do componente — onde adicionar `lastNonce`; onde `pendingConfirmation` é setado/limpo :402-409)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-02-SUMMARY.md (contrato: servidor emite `data.nonce` junto de `tool_calls`; espera `body.nonce` de volta)
    - CLAUDE.md (§Convenções — Rules of Hooks: novo useState/useEffect no TOPO do componente, nunca condicional)
  </read_first>
  <action>
    (a) Nonce threading: estender `callAssistant(history, toolResults, jwt, nonce)` para incluir `nonce` no `JSON.stringify` do body (`nonce: nonce || null`). No `send` loop, manter uma variável local `let lastNonce = null;` (ou `React.useRef`) — após cada resposta, se `data.nonce` vier, `lastNonce = data.nonce`; passar `lastNonce` na próxima iteração de `callAssistant(history, toolResults, jwt, lastNonce)`. O nonce é efêmero por turno; não precisa persistir em sessionStorage. Fail-safe: se `data.nonce` for null/ausente, `lastNonce` fica null e a próxima chamada manda `nonce: null` (servidor cobra cota — comportamento aceitável).
    (b) Flag de pendência: adicionar (no topo do componente, respeitando Rules of Hooks) um `React.useEffect(() => { window.__fidesWriteConfirmPending = !!pendingConfirmation; return () => { window.__fidesWriteConfirmPending = false; }; }, [pendingConfirmation])`. Isso publica para fora do componente se há um card WRITE aguardando — consumido pelo guard de ⌘K no Plan 05, sem lifting de estado para o store (D-02 — store intocado). O flag é lido no event-time do keydown (Plan 05), evitando stale-closure.
    Não alterar a lógica de rate-limit/cooldown do cliente (fase 11) além do parâmetro nonce.
  </action>
  <verify>
    <automated>grep -q "callAssistant(history, toolResults, jwt" assets/fides-claude.jsx e confirmar 4º arg de nonce; grep -q "data.nonce\|data?.nonce" assets/fides-claude.jsx; grep -q "__fidesWriteConfirmPending" assets/fides-claude.jsx; grep confirma que o useEffect da flag está no nível de componente (não dentro de outra função). Teste de fumaça de parse: abrir o app localmente/estático — nenhum erro de "Rendered more hooks than during the previous render".</automated>
    <human-check>UAT (fase gate): DevTools — forjar toolResults sem nonce ⇒ conta cota; fluxo normal ⇒ não cobra 2x no mesmo turno. Abrir card de confirmação ⇒ `window.__fidesWriteConfirmPending` é true no console.</human-check>
  </verify>
  <acceptance_criteria>
    - `callAssistant` envia `body.nonce`; o valor reenviado é o `data.nonce` da resposta anterior do mesmo turno.
    - Ausência de nonce não quebra o fluxo (fail-safe).
    - `window.__fidesWriteConfirmPending` reflete `!!pendingConfirmation` via useEffect no nível de componente (Rules of Hooks respeitado).
  </acceptance_criteria>
  <done>Nonce round-trip fechado no cliente; flag de pendência publicada p/ o Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → servidor (nonce) | o cliente reenvia o nonce; se forjado/ausente, o servidor (Plan 02) trata fail-safe |
| FidesAssistant → escopo global (window flag) | expõe só um booleano de "há confirmação pendente", sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-cli-05 | Repudiation (toast/sucesso falso) | criar_categoria | mitigate | `await addCategory` antes de toast/retorno; falha → `EXECUTION_ERROR` sem toast (P5). |
| T-12-cli-06 | Elevation (execução sem confirmação) | criar_categoria | mitigate | Tool movida p/ `TOOLS_REQUIRING_CONFIRMATION` (SD-1) — nenhuma WRITE executa direto. |
| T-12-cli-07 | Info Disclosure (window flag) | window.__fidesWriteConfirmPending | accept | Booleano sem PII; só sinaliza pendência de UI. |
| T-12-cli-08 | Tampering (nonce forjado pelo cliente) | body.nonce | mitigate (servidor) | Validação é no servidor (Plan 02 `verify`); cliente forjar nonce inválido só cai no fail-safe (cobra cota). |
</threat_model>

<verification>
- **Estático (grep):** criar_categoria em TOOLS_REQUIRING_CONFIRMATION; nenhum `addCategory(` sem await; nonce no body; flag publicada.
- **Rules of Hooks:** useEffect no nível de componente; sem warning de hooks (CLAUDE.md — bug histórico da fase 07).
- **UAT (fase gate):** card de criar_categoria; toast pós-commit; nonce não cobra cota 2x; flag observável.
</verification>

<success_criteria>
- WRITE-04: criar_categoria honesto (confirmação + await); toast falso morto.
- HONEST-01: nenhuma tool WRITE executa direto (SD-1).
- D-06 (cliente): nonce round-trip com fail-safe.
- Flag de pendência pronta p/ o guard de ⌘K (Plan 05).
</success_criteria>

<output>
Create `.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-04-SUMMARY.md` when done. Registrar: nome exato da flag global (`window.__fidesWriteConfirmPending`) e semântica (true enquanto card WRITE pendente) — consumido pelo Plan 05.
</output>
