---
phase: 12-ia-2-destravar-write-no-assistente-in-app-b8
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - assets/fides-claude.jsx
autonomous: true
requirements: [WRITE-01, WRITE-04, DERIVED-SAFE-01]

must_haves:
  truths:
    - "Lançar uma transação via chat chama window.fidesDb.rpc('wa_log_transaction', ...), não mais addTransaction"
    - "O month gravado vem da DATA REAL da transação (mesFaturaFor p/ cartão), nunca do selectedMonth da UI (P2)"
    - "Lançar em cartão sempre grava status pendente (Q3, regra do modal commit 2343086)"
    - "Categoria inexistente vira 1 confirmação só: 'criar categoria X e lançar' (D-04), sem 2 passos manuais"
  artifacts:
    - path: "assets/fides-claude.jsx"
      provides: "Branch lancar_transacao reescrito p/ RPC + resolução D-04 de categoria nova + card bundlado"
      contains: "wa_log_transaction"
  key_links:
    - from: "executeWriteTool (lancar_transacao)"
      to: "wa_log_transaction (RPC LIVE, Plan 01)"
      via: "window.fidesDb.rpc('wa_log_transaction', { p_description, p_value, p_category, p_date, p_month, p_status, p_account_id, p_card_id })"
      pattern: "fidesDb\\.rpc\\(['\"]wa_log_transaction"
    - from: "executeWriteTool (destino cartão)"
      to: "window.mesFaturaFor (fides-data.jsx)"
      via: "p_month = window.mesFaturaFor(dateStr, card, ano) para cartão; YYYY-MM da data real p/ conta"
      pattern: "mesFaturaFor"
---

<objective>
Reescrever o caminho de `lancar_transacao` no cliente para o insert atômico via RPC (D-01) e corrigir o bug P2 (mês hard-coded) que reencarnaria o FIX-1 se religado sem correção:
1. O branch `lancar_transacao` de `executeWriteTool` passa a chamar `window.fidesDb.rpc('wa_log_transaction', {...})` em vez de `addTransaction` (D-01). `addTransaction`/o modal "Nova Transação" NÃO são tocados (D-02).
2. Corrigir P2: `p_month` deriva da DATA REAL da transação — `window.mesFaturaFor(dateStr, card, ano)` para cartão, `YYYY-MM` da data real para conta — nunca do `selectedMonth` da UI. Espelhar exatamente a lógica de `txToRow` (o modal já faz certo via commit `7335fed`).
3. Regra de status de cartão (Q3): destino cartão sempre grava `pending` (a compra em cartão nunca é "paga" na hora — regra do modal, commit `2343086`). Mapear status UI→DB (`pago→cleared`, `pendente→pending`, espelhando `STATUS_TO_DB` de fides-store.jsx).
4. D-04: categoria inexistente NÃO retorna mais erro pedindo criar antes — vira 1 confirmação só ("vou criar categoria X e lançar"); no confirm, `await addCategory(...)` e então a RPC, com a nova categoria.

Os branches `recategorizar_transacao`/`editar_transacao` continuam usando `updateTransaction` (D-03) e ficam INTACTOS. `resolved.patch` nunca inclui `acct` (D-05).

Purpose: Fecha WRITE-01 no cliente + DERIVED-SAFE-01 (mês/saldo corretos) + D-04. É onde 2 dos 6 bugs do UAT vivem ativos.
Output: `assets/fides-claude.jsx` com o caminho de insert atômico e correto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-PATTERNS.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-01-SUMMARY.md
@assets/fides-claude.jsx
@assets/fides-store.jsx
@assets/fides-data.jsx
</context>

<artifacts>
## Artifacts this plan produces
- **`assets/fides-claude.jsx`** — branch `lancar_transacao` de `executeWriteTool` reescrito para `window.fidesDb.rpc('wa_log_transaction', {...})`; helper de derivação de `p_month` (conta vs cartão via `window.mesFaturaFor`); mapeamento status UI→DB + force pendente em cartão; `resolveWriteToolArgs` para `lancar_transacao` passa a sinalizar categoria-a-criar (D-04) em vez de retornar erro; `renderConfirmationCard` do `lancar_transacao` mostra a linha "criar categoria X" quando aplicável.
- Consumo do artifact do Plan 01: `wa_log_transaction` (RPC LIVE) e sua assinatura de parâmetros.
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescrever o branch lancar_transacao p/ wa_log_transaction + fix P2 (mês) + status de cartão</name>
  <files>assets/fides-claude.jsx</files>
  <behavior>
    - Conta corrente, data de HOJE: RPC recebe p_month = mês da data de hoje; transação aparece no mês certo.
    - Conta corrente, data RETROATIVA (mês anterior ao selectedMonth): p_month segue a DATA da transação, não o mês exibido (P2).
    - Cartão: p_month = window.mesFaturaFor(dateStr, card, ano); p_status = pending sempre (Q3).
    - Despesa: p_value negativo; cartão tem used incrementado pela RPC (Plan 01), não em JS.
  </behavior>
  <read_first>
    - assets/fides-claude.jsx (:343-387 executeWriteTool — branch lancar_transacao :346-364 com o bug `mes: selectedMonth`; branches recategorizar/editar :365-381 = padrão de wrapper try/catch a preservar)
    - assets/fides-store.jsx (:30-31 STATUS_TO_UI/STATUS_TO_DB; :187-228 ensureMes + txToRow — a lógica de mês por conta/cartão que este branch deve ESPELHAR; :193-228 uso de window.mesFaturaFor p/ cartão)
    - assets/fides-data.jsx (:52-64 mesFaturaFor — assinatura `(dStr, card, year)`, formato de data `dd/mm`)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-01-SUMMARY.md (assinatura final de parâmetros da RPC)
  </read_first>
  <action>
    Reescrever SOMENTE o branch `if (name === 'lancar_transacao')` de `executeWriteTool`, dentro do mesmo wrapper try/catch existente (preservar o `catch` que retorna `{ error: 'EXECUTION_ERROR', ... }`). Passos:
    (a) Remover a montagem do objeto `tx` com o campo de mês vindo do estado da UI (a atribuição do mês-selecionado da UI ao campo de mês da transação é o bug P2 — eliminá-la por completo, não mantê-la como caso especial).
    (b) Derivar `dateStr` de `resolved.dateStr` (já resolvido em `resolveWriteToolArgs` a partir de `args.data` ou hoje).
    (c) Calcular `p_month` ESPELHANDO `txToRow`: se `resolved.target.type === 'card'` → `window.mesFaturaFor(resolved.dateStr, card, ano)` (obter o objeto `card` pelo id do target; `ano` pelo mesmo critério que `txToRow` usa); se `type === 'account'` → o `YYYY-MM` derivado da data real (mesma derivação que `ensureMes`/`txToRow` fazem para conta). NUNCA usar o mês selecionado da UI.
    (d) Mapear status UI→DB: `pago→cleared`, `pendente→pending` (espelhar `STATUS_TO_DB`); se destino é cartão, FORÇAR `pending` (Q3, regra do modal 2343086) ignorando o status proposto pelo usuário.
    (e) Chamar `const { data, error } = await window.fidesDb.rpc('wa_log_transaction', { p_description: resolved.desc, p_value: resolved.val, p_category: resolved.cat, p_date: <dateStr em formato date que a RPC aceita>, p_month, p_status, p_account_id: type==='account'?id:null, p_card_id: type==='card'?id:null })`. Se `error`, `throw error` (cai no catch → `EXECUTION_ERROR`). `resolved.val` já vem com sinal de `resolveWriteToolArgs`.
    (f) Após sucesso, garantir que a UI reflita (o store recarrega via `refreshData` no fluxo normal — se a RPC não dispara refresh, chamar o mecanismo de refresh existente do store para a lista/saldos atualizarem, SEM reimplementar cálculo de saldo em JS — DERIVED-SAFE-01).
    (g) Retornar `{ success: true, message: 'Lancei ...' }` como hoje.
    NÃO tocar `addTransaction` nem os branches recategorizar/editar (D-02/D-03). NÃO expor `acct` em nenhum patch (D-05).
    Atenção Rules of Hooks (CLAUDE.md): nenhum hook novo neste branch — é código de evento/handler, não render.
  </action>
  <verify>
    <automated>grep -q "fidesDb.rpc('wa_log_transaction'" assets/fides-claude.jsx || grep -q 'fidesDb.rpc("wa_log_transaction"' assets/fides-claude.jsx; grep negativo P2 (o campo de mês da transação NÃO recebe mais o mês selecionado da UI): `grep -n "mes:\s*selectedMonth" assets/fides-claude.jsx` retorna 0 linhas; grep positivo `mesFaturaFor` presente no branch de cartão; grep confirma o branch lancar não chama mais `addTransaction`.</automated>
    <human-check>UAT (fase gate): lançar despesa em conta (hoje) e em data retroativa; lançar em cartão perto do fechamento; conferir via Supabase MCP que `transactions.month` bate com a data real / fatura, e saldo/`cards.used` batem.</human-check>
  </verify>
  <acceptance_criteria>
    - O branch chama `window.fidesDb.rpc('wa_log_transaction', ...)`; não chama `addTransaction`.
    - Nenhuma atribuição do mês-selecionado-da-UI ao campo de mês da transação permanece (P2 eliminado) — grep 0.
    - Cartão: `p_month` via `window.mesFaturaFor`; `p_status` forçado a `pending`.
    - Conta: `p_month` = YYYY-MM da data real (retroativo cai no mês da data, não no exibido).
    - Nenhuma mutação incremental de saldo em JS (DERIVED-SAFE-01) — `used`/saldo vêm da RPC/refresh.
  </acceptance_criteria>
  <done>Insert via RPC com mês e status corretos; P2 morto; saldo derivado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fluxo D-04 — categoria nova bundlada em 1 confirmação ("criar categoria X e lançar")</name>
  <files>assets/fides-claude.jsx</files>
  <behavior>
    - Usuário: "lança R$30 em pilates" com "pilates" não existindo → card mostra "Criar categoria: Pilates" + os detalhes do lançamento; UMA confirmação.
    - Confirmar → cria a categoria (await) e lança a transação usando a nova categoria; sem 2 passos manuais; sem toast falso.
    - Cancelar → nada é criado nem lançado.
  </behavior>
  <read_first>
    - assets/fides-claude.jsx (:287-314 resolveWriteToolArgs branch lancar_transacao — hoje retorna `{ error: 'Categoria não encontrada...' }` quando `findCategoryByName` falha :296-299; :574-608 renderConfirmationCard branch lancar; :390-433 executeTools loop — como resolved vira card e depois execução)
    - assets/fides-store.jsx (addCategory — assinatura `addCategory(catKey, { label, emoji, group, custom })` e comportamento async/otimista :774-778)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md (D-04 — 1 confirmação só; não manter fluxo de 2 passos)
  </read_first>
  <action>
    Alterar `resolveWriteToolArgs` para `lancar_transacao`: quando `findCategoryByName(args.categoria)` NÃO casar, em vez de retornar `{ error: ... }`, retornar `resolved` com um flag de categoria-a-criar — por ex. `resolved.createCategory = { label: args.categoria, catKey: <derivar como criar_categoria faz: lowercase, [^a-z0-9]→_, colapsar/aparar _>, emoji: '🏷️', group: 'estilo' }` e usar esse `catKey` como `resolved.cat` (a categoria que a transação vai referenciar). Manter o erro de conta/cartão não encontrado (esse continua sendo erro — só a categoria ganha o caminho de criação; honestidade: conta/cartão o usuário PRECISA ter, categoria pode ser criada).
    Em `renderConfirmationCard` (branch `lancar_transacao`): quando `resolved.createCategory` existir, adicionar ao topo dos detalhes uma linha explícita tipo `{ label: 'Nova categoria', value: 'Vou criar "<label>"' }` para o usuário ver que a confirmação cria+lança (D-04).
    Em `executeWriteTool` (branch `lancar_transacao`, Task 1): ANTES da chamada da RPC, se `resolved.createCategory` existir, `await addCategory(resolved.createCategory.catKey, { label, emoji, group, custom: true })` e só então chamar a RPC com `p_category = resolved.createCategory.catKey`. Se `addCategory` falhar (throw), o catch existente retorna `EXECUTION_ERROR` e a transação NÃO é lançada (atômico do ponto de vista do usuário: falha em criar categoria aborta o lançamento). O `await` é obrigatório (mesma disciplina do fix P5 — nunca reportar sucesso antes da escrita concluir).
    NÃO chamar a tool `criar_categoria` separadamente (D-04 — o system prompt do Plan 02 já instrui o modelo a não fazer isso).
  </action>
  <verify>
    <automated>grep negativo: o branch lancar de resolveWriteToolArgs NÃO retorna mais o texto de erro pedindo criar categoria antes (`grep -c "criar essa categoria primeiro" assets/fides-claude.jsx` == 0); grep positivo: `createCategory` referenciado em resolveWriteToolArgs, renderConfirmationCard e executeWriteTool; grep confirma `await addCategory` no caminho do lancar (não sem await).</automated>
    <human-check>UAT (fase gate): pedir lançamento com categoria inexistente → card mostra "criar categoria X e lançar" (1 card); confirmar cria+lança; conferir a categoria em user_categories e a transação via MCP.</human-check>
  </verify>
  <acceptance_criteria>
    - Categoria inexistente NÃO retorna mais erro de "criar primeiro" — vira `resolved.createCategory`.
    - Card único mostra a criação da categoria + o lançamento (D-04).
    - No confirm: `await addCategory(...)` ANTES da RPC; falha na categoria aborta o lançamento.
    - Conta/cartão inexistente CONTINUA sendo erro (honestidade — não inventa destino).
  </acceptance_criteria>
  <done>D-04 em 1 confirmação; categoria criada com await antes do insert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| args do Gemini → resolveWriteToolArgs | valor/conta/categoria vêm do parse do LLM — não confiáveis; resolvidos p/ UUID por guarda determinística |
| cliente → RPC | o cliente resolve nomes→UUID e passa à RPC; a RPC revalida dono (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-cli-01 | Tampering (destino inventado) | resolveWriteToolArgs | mitigate | `findAccountByName`/`findCardByName` falham fechado com erro explícito se o nome não casa (guarda hard); categoria pode ser criada (D-04), conta/cartão não. |
| T-12-cli-02 | Repudiation (mês errado → tx invisível/fatura errada) | derivação de p_month | mitigate | `mesFaturaFor` (cartão) / data real (conta); NUNCA `selectedMonth` (P2/P4). Defesa dupla com fallback da RPC (Plan 01). |
| T-12-cli-03 | Tampering (status errado em cartão) | mapeamento de status | mitigate | Cartão força `pending` (Q3); mapeamento UI→DB espelha STATUS_TO_DB (sem drift). |
| T-12-cli-04 | Repudiation (sucesso antes da escrita) | fluxo D-04 addCategory | mitigate | `await addCategory` antes da RPC e antes de qualquer sucesso reportado (mesma disciplina do P5). |
</threat_model>

<verification>
- **Estático (grep):** chama `wa_log_transaction`; P2 morto (grep 0 do mês-da-UI); `mesFaturaFor` no branch de cartão; `await addCategory`; sem erro "criar primeiro".
- **UAT (fase gate):** casos de mês retroativo, cartão perto do fechamento, categoria nova bundlada — verificados via chat + Supabase MCP (month/used/saldo).
- **Regressão:** deletar via UI uma transação criada pelo chat estorna saldo (P3 — comportamento igual ao modal; nenhuma mudança em delete_transaction).
</verification>

<success_criteria>
- WRITE-01: lançar via chat usa a RPC atômica; mês e status corretos.
- WRITE-04: categoria nova bundlada (D-04) em 1 confirmação.
- DERIVED-SAFE-01: nenhuma mutação incremental de saldo em JS; mês via fonte única.
- D-02/D-03/D-05 preservados (addTransaction/modal, updateTransaction, sem acct).
</success_criteria>

<output>
Create `.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-03-SUMMARY.md` when done. Registrar: assinatura final da chamada da RPC, como p_month é derivado (conta vs cartão), estrutura do `resolved.createCategory` (consumido pelo Plan 04 no card).
</output>
