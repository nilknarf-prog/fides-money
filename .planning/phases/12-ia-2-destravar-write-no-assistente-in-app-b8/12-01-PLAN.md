---
phase: 12-ia-2-destravar-write-no-assistente-in-app-b8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/wa-log-transaction.sql
  - supabase/schema.sql
autonomous: false
requirements: [WRITE-01, DERIVED-SAFE-01]
user_setup: []

must_haves:
  truths:
    - "A função wa_log_transaction existe no Supabase LIVE (não só no arquivo .sql)"
    - "Uma transação lançada via a RPC aparece com month não-nulo e saldo/limite recalculados"
    - "A RPC recusa account_id/card_id que não pertençam ao auth.uid() da sessão"
    - "Nenhuma mutação incremental de saldo em JS — a RPC chama recalc_account_balance internamente (DERIVED-SAFE-01)"
  artifacts:
    - path: "supabase/wa-log-transaction.sql"
      provides: "Definição de record da RPC wa_log_transaction (doc-of-record, espelha o LIVE)"
      contains: "create or replace function public.wa_log_transaction"
    - path: "supabase/schema.sql"
      provides: "schema.sql sincronizado com a nova função (documentação)"
      contains: "wa_log_transaction"
  key_links:
    - from: "wa_log_transaction (RPC)"
      to: "recalc_account_balance (RPC existente)"
      via: "perform public.recalc_account_balance(p_account_id) dentro da função"
      pattern: "recalc_account_balance"
    - from: "wa_log_transaction (RPC)"
      to: "public.accounts / public.cards"
      via: "owner-guard: select id where id=... and user_id=v_uid antes de qualquer insert"
      pattern: "user_id\\s*=\\s*v_uid"
---

<objective>
Criar a RPC atômica `wa_log_transaction` (D-01) — o ÚNICO caminho de insert usado por `lancar_transacao` do assistente. Espelha o padrão SECURITY DEFINER + guarda de dono de `pay_card_invoice`, fazendo em UMA transação SQL o que hoje `addTransaction` faz em 3 passos não-atômicos: INSERT + `recalc_account_balance` (conta) + `cards.used` (cartão).

NÃO substitui `addTransaction` do modal "Nova Transação" (D-02 — blast radius controlado). A RPC usa `auth.uid()` INTERNAMENTE, sem `p_user_id` vindo do cliente (recomendação A1 do RESEARCH — `p_user_id` fica reservado ao caso service-role da Fase 14/WhatsApp).

Purpose: Fecha DERIVED-SAFE-01 no caminho de insert — saldo sempre derivado, nunca incremental em JS; atomicidade garantida dentro do Postgres.
Output: Função `wa_log_transaction` APLICADA no Supabase LIVE via MCP + arquivos `.sql` de documentação sincronizados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md
@.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-PATTERNS.md
@supabase/derived-balance.sql
</context>

<artifacts>
## Artifacts this plan produces
- **`wa_log_transaction`** (RPC PL/pgSQL, SECURITY DEFINER) — no Supabase LIVE + `supabase/wa-log-transaction.sql` (doc-of-record) + entrada em `supabase/schema.sql`.
  - Assinatura (tipos exatos são Claude's Discretion, espelhar `pay_card_invoice`): `(p_description text, p_value numeric, p_category text, p_date date, p_month text, p_status text, p_account_id uuid, p_card_id uuid) returns jsonb`.
  - `grant execute ... to authenticated` com a assinatura de tipos explícita.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Verificar o schema LIVE de transactions/cards/accounts via Supabase MCP</name>
  <files>(nenhum arquivo escrito — task de verificação/descoberta)</files>
  <read_first>
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md (Assumption A3: `schema.sql` está STALE — faltam `settled`/`paid_at`/`is_transfer`/`transfer_group`/`opening_balance`)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-PATTERNS.md (Caveat da RPC: verificar colunas contra o LIVE, não contra os `.sql`)
    - supabase/derived-balance.sql (pay_card_invoice:106-138 — o insert usa a lista de colunas `user_id, description, value, category, account, account_id, card_id, date, month, status, recurrent, subscription, settled, is_transfer`)
  </read_first>
  <action>
    Usar o Supabase MCP (`list_tables` para `transactions`, `cards`, `accounts`) e confirmar a lista REAL de colunas + tipos de `public.transactions` (especialmente `month`, `status`, `settled`, `is_transfer`, `account`, `account_id`, `card_id`, `category`, `recurrent`, `subscription`). Confirmar que `cards` tem coluna `used` e `accounts`/`cards` têm `user_id`. Registrar no SUMMARY qualquer divergência entre o LIVE e a lista de colunas do analog `pay_card_invoice`. Esta task NÃO escreve nada — só valida o contrato de colunas antes da Task 2 escrever a função. Não confiar em `supabase/schema.sql` (comprovadamente STALE por CLAUDE.md §Stack + RESEARCH A3).
  </action>
  <verify>
    <automated>MCP list_tables retorna as colunas de transactions/cards/accounts; a lista de INSERT da Task 2 é um subconjunto das colunas reais confirmadas.</automated>
  </verify>
  <acceptance_criteria>
    - Colunas reais de `public.transactions` capturadas e registradas (nome + tipo).
    - Confirmado que `transactions.month` (text) e `transactions.status` (text) existem — são os campos das classes de bug P1/P2/P4.
    - Confirmado `cards.used` e `cards.user_id`, `accounts.user_id` existem (guarda de dono + efeito colateral de cartão).
    - Qualquer coluna NOT NULL sem default anotada (precisa ser preenchida pelo INSERT da Task 2).
  </acceptance_criteria>
  <done>Contrato de colunas do LIVE confirmado; divergências vs analog registradas no SUMMARY.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2 [BLOCKING]: Escrever wa_log_transaction, APLICAR no Supabase LIVE via MCP e verificar em pg_proc</name>
  <files>supabase/wa-log-transaction.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/derived-balance.sql (analog EXATO: pay_card_invoice:106-138 — copiar a ESTRUTURA: header `security definer set search_path to 'public'`, `v_uid := auth.uid()`, guarda `if v_uid is null then raise exception 'AUTH'`, guarda de dono por tabela, `perform public.recalc_account_balance(...)`, `grant execute ... to authenticated`)
    - .planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-RESEARCH.md (Pattern 1 — rascunho SQL; Open Question Q3 — regra de status pendente para CARTÃO, commit 2343086)
    - Task 1 output (colunas reais confirmadas)
  </read_first>
  <action>
    Escrever `public.wa_log_transaction` espelhando `pay_card_invoice` (D-01). Requisitos NÃO-negociáveis:
    (a) Header `language plpgsql security definer set search_path to 'public'`.
    (b) `v_uid uuid := auth.uid()`; `if v_uid is null then raise exception 'AUTH'`. NUNCA aceitar `p_user_id` do cliente (A1 — este caminho é JWT autenticado, igual `pay_card_invoice`).
    (c) Exigir exatamente UM destino: erro se ambos `p_account_id` e `p_card_id` nulos, e erro se ambos preenchidos.
    (d) Guarda de dono ANTES de qualquer mutação: para o destino informado, `select id into v_tmp from public.<tabela> where id=p_x and user_id=v_uid; if v_tmp is null then raise exception '<CONTA|CARTAO>'`.
    (e) INSERT em `public.transactions` com a lista de colunas confirmada na Task 1; `p_value` já vem COM sinal (despesa negativa); `settled=false`, `is_transfer=false`, `recurrent=false`, `subscription=false`; `account = coalesce(p_account_id, p_card_id)::text`.
    (f) Fallback de mês na própria RPC: gravar `coalesce(nullif(p_month,''), to_char(p_date,'YYYY-MM'))` para NUNCA gravar month vazio (defesa-em-profundidade da classe de bug P1 — o cálculo primário do mês é do cliente, ver Plan 03).
    (g) Efeitos colaterais atômicos, DERIVED-SAFE-01: se `p_account_id` não nulo → `perform public.recalc_account_balance(p_account_id)` (nunca somar saldo em JS); se `p_card_id` não nulo E `p_value < 0` → `update public.cards set used = used + abs(p_value) where id=p_card_id and user_id=v_uid`.
    (h) `return jsonb_build_object('id', v_tx_id, 'inserted', true)`.
    (i) Linha final `grant execute on function public.wa_log_transaction(<tipos explícitos>) to authenticated`.
    Regra de status de cartão (Q3): a normalização de status (forçar pendente em cartão) é resolvida no cliente antes de chamar a RPC (Plan 03) — a RPC apenas grava `p_status` verbatim com fallback default. Documentar essa fronteira no comentário-cabeçalho da função.
    [BLOCKING] APLICAR a função no Supabase LIVE via MCP (`apply_migration` ou `execute_sql`) — escrever só no `.sql` NÃO cria a função no banco (schema_application_requirement). Depois sincronizar `supabase/wa-log-transaction.sql` (standalone doc-of-record) e adicionar a função a `supabase/schema.sql` como documentação. Fonte da verdade = banco LIVE.
  </action>
  <verify>
    <automated>MCP execute_sql: `select proname, prosecdef from pg_proc where proname='wa_log_transaction'` retorna 1 linha com `prosecdef=true`. Teste funcional em transação revertível: `begin; select public.wa_log_transaction('teste', -10, 'mercado', current_date, '', 'cleared', '<uma_account_id_do_dev>', null); ` confirma retorno `{inserted:true}` e a linha inserida tem `month` não-nulo; `rollback;` no fim. Guarda de dono: chamar com um `p_account_id` de outro user deve `raise exception 'CONTA'`.</automated>
  </verify>
  <acceptance_criteria>
    - `pg_proc` confirma a função existe LIVE com `prosecdef=true` e `proconfig` contendo `search_path=public`.
    - INSERT via RPC grava `month` não-nulo mesmo com `p_month=''` (fallback to_char).
    - `recalc_account_balance` é chamado dentro da função quando há `p_account_id` (grep no corpo LIVE via `pg_get_functiondef`).
    - `cards.used` só é incrementado para `p_value < 0` em cartão.
    - account_id/card_id de outro usuário → exceção (owner guard), nenhuma linha inserida.
    - `supabase/wa-log-transaction.sql` e `supabase/schema.sql` contêm o corpo idêntico ao aplicado no LIVE.
  </acceptance_criteria>
  <done>Função LIVE verificada por pg_proc + teste de insert/owner-guard revertível; arquivos `.sql` sincronizados.</done>
</task>

<task type="auto">
  <name>Task 3: database-reviewer na RPC nova antes do commit (caminho sensível)</name>
  <files>(revisão — nenhum arquivo de produto escrito além de ajustes apontados)</files>
  <read_first>
    - CLAUDE.md (§Segurança — `supabase/` é caminho sensível: database-reviewer obrigatório antes de commit)
    - supabase/wa-log-transaction.sql (output da Task 2)
  </read_first>
  <action>
    Invocar o agente `database-reviewer` (ECC, uso pontual DENTRO da fase GSD — CLAUDE.md §Orquestração) sobre `wa_log_transaction`. Foco: SECURITY DEFINER + `set search_path` presentes; owner-guard cobre AMBOS os branches (account e card); nenhuma escrita cross-tenant possível; nenhuma mutação incremental de saldo fora de `recalc_account_balance`; grant restrito a `authenticated`. Aplicar correções que o reviewer classificar como `high`/`medium` (block on `high`, security_asvs_level=1). Registrar disposição de findings no SUMMARY.
  </action>
  <verify>
    <automated>Relatório do database-reviewer anexado ao SUMMARY; zero findings `high` em aberto. Re-rodar o pg_proc check da Task 2 se a RPC foi alterada.</automated>
  </verify>
  <acceptance_criteria>
    - Nenhum finding `high` em aberto no database-reviewer.
    - Se o reviewer alterou a RPC, ela foi RE-aplicada no LIVE via MCP e re-verificada em pg_proc.
  </acceptance_criteria>
  <done>database-reviewer PASS (zero high), disposição registrada, RPC LIVE == arquivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client autenticado → RPC | Cliente passa `account_id`/`card_id`/`value`/`category` resolvidos; valores NÃO confiáveis (podem ser forjados via DevTools mesmo com JWT válido) |
| RPC (SECURITY DEFINER) → tabelas | A função roda com privilégio elevado — RLS não protege dentro de SECURITY DEFINER; a guarda é o owner-check explícito |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-rpc-01 | Tampering (cross-tenant write) | wa_log_transaction | mitigate | `v_uid := auth.uid()` interno + owner-guard `where id=p_x and user_id=v_uid` em AMBOS os branches; raise exception se `v_tmp is null`. NUNCA `p_user_id` do cliente (A1). |
| T-12-rpc-02 | Elevation of Privilege (search_path hijack) | SECURITY DEFINER fn | mitigate | `set search_path to 'public'` no header (mandatório, presente em todo RPC do arquivo). |
| T-12-rpc-03 | Tampering (saldo corrompido) | efeito colateral de saldo | mitigate | Saldo só via `recalc_account_balance`; `cards.used` só incrementa para `p_value<0`; tudo na mesma transação implícita (atômico). |
| T-12-rpc-04 | Tampering (LLM injeta valor/conta) | args da tool | mitigate | RPC valida destino pertence ao uid; valor gravado com sinal já normalizado pelo cliente; RPC não confia em texto livre para saldo. |
| T-12-rpc-05 | Repudiation (month vazio → tx invisível) | coluna month | mitigate | Fallback `coalesce(nullif(p_month,''), to_char(p_date,'YYYY-MM'))` — nunca grava month vazio (P1). |
</threat_model>

<verification>
- **Estático/SQL (MCP):** `pg_proc.prosecdef=true`; `pg_get_functiondef` contém `recalc_account_balance`, `auth.uid()`, owner-guard, `set search_path`.
- **Funcional (MCP, revertível):** insert via RPC → month não-nulo, saldo recalculado; owner-guard rejeita id de outro user.
- **Gate sensível:** database-reviewer PASS (zero high) antes do commit (CLAUDE.md).
</verification>

<success_criteria>
- `wa_log_transaction` existe LIVE (pg_proc), SECURITY DEFINER + search_path, owner-guard duplo, recalc interno.
- Arquivos `.sql` sincronizados com o LIVE.
- database-reviewer PASS.
- WRITE-01 (caminho de insert) e DERIVED-SAFE-01 (saldo derivado) satisfeitos no nível de banco.
</success_criteria>

<output>
Create `.planning/phases/12-ia-2-destravar-write-no-assistente-in-app-b8/12-01-SUMMARY.md` when done. Registrar: colunas reais do LIVE (Task 1), corpo final da RPC, disposição do database-reviewer.
</output>
