---
phase: 16-painel-admin-backoffice
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - api/admin.js
  - api/_lib/admin/guard.js
  - api/_lib/admin/accounts.js
  - api/_lib/admin/set-plan.js
  - api/_lib/admin/audit-list.js
autonomous: false
requirements: [ADMIN-02]
user_setup:
  - service: vercel
    why: "Env vars do backend admin (service_role + allowlist) — o painel não funciona sem elas, e o service_role JAMAIS pode ir ao client"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret; colar na aba Project do Vercel (não Preview/Production separadas)"
      - name: ADMIN_USER_IDS
        source: "CSV de UUIDs de contas admin dedicadas (a conta pessoal do dono NÃO entra); UUID obtido no Supabase Auth -> Users. Setar na aba Project do Vercel"
    dashboard_config:
      - task: "Criar conta admin dedicada NOVA (Supabase Auth) — não usar caduucomc@gmail.com"
        location: "Supabase Dashboard -> Authentication -> Users -> Add user"
      - task: "Rodar o teste fail-closed (ADMIN_USER_IDS vazia → 403 para TODOS) ANTES de preencher ADMIN_USER_IDS em produção"
        location: "Deploy de preview Vercel + curl"

must_haves:
  truths:
    - "GET /api/admin?action=whoami sem Authorization → 401 JWT_MISSING"
    - "Bearer inválido → 401 JWT_INVALID"
    - "ADMIN_USER_IDS ausente/vazia → 403 FORBIDDEN para TODOS (fail-closed), inclusive para quem seria admin"
    - "Usuário autenticado não-allowlisted → 403 FORBIDDEN + linha denied_access em admin_audit_log"
    - "Flood de negados do mesmo user/IP → linhas denied_access têm teto por janela (não 1:1 com as requisições)"
    - "Admin allowlisted → whoami 200 com user.id/email; set_plan muda plan + audit atômico"
    - "Toda action grava audit (leituras = linha leve sem before/after/reason; set_plan = via RPC com before/after/reason)"
    - "Respostas sempre com Cache-Control: no-store"
  artifacts:
    - "api/admin.js (roteador único por action)"
    - "api/_lib/admin/guard.js (requireAdmin + auditDenied com throttle + logAction leve)"
    - "api/_lib/admin/accounts.js, api/_lib/admin/set-plan.js, api/_lib/admin/audit-list.js"
  key_links:
    - "Ordem load-bearing do guard: 401 sem token → 401 inválido → 403 não-allowlisted (+audit denied+throttle) → SÓ ENTÃO instancia adminClient (service_role, persistSession:false)"
    - "getUser(token) usa o anon client (NEXT_PUBLIC_SUPABASE_*), não o service_role — identidade validada pelo Auth server, não decodificada localmente"
    - "Allowlist normalizada trim().toLowerCase() dos DOIS lados (M9) — UUID com caixa/espaço divergente não pode dar 403 silencioso"
  prohibitions:
    - "service_role JAMAIS serializado em HTML/JS/JSON de resposta — só lido via process.env.SUPABASE_SERVICE_ROLE_KEY no runtime"
    - "NÃO instanciar o adminClient antes do passo de allowlist passar"
    - "NÃO criar endpoint admin fora de api/admin.js (guard único, roteador único)"
    - "NÃO tratar ADMIN_USER_IDS ausente como 500 nem como acesso liberado — é 403 para todos (fail-closed)"
    - "inject-config.js NÃO muda"
---

<objective>
Backend serverless do painel (ADMIN-02): um único `api/admin.js` (CommonJS, roteador por `action`) protegido pelo guard `requireAdmin` que roda SEMPRE antes de qualquer dispatch, replicando o padrão Bearer + `auth.getUser` já em produção em `api/assistant.js:205-231`, com allowlist server-side `ADMIN_USER_IDS` (fail-closed) e throttle anti-flood de `denied_access` já aqui (não esperar o 16-04).

Implementa D-1b (auth admin: conta dedicada + allowlist de UUID server-side) e a decisão de que toda mutação passa por `service_role` server-side. O maior risco do produto é introduzir `service_role` (ignora TODA a RLS) numa function serverless — por isso a ordem do guard é load-bearing e o adminClient só nasce depois da allowlist.

Purpose: expor as RPCs do 16-01 ao front do 16-03 por um endpoint blindado, auditado e à prova de flood, sem nunca vazar o service_role ao browser.
Output: `api/admin.js` + `api/_lib/admin/{guard,accounts,set-plan,audit-list}.js` + env vars novas no Vercel (checkpoint humano).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/painel-admin-backoffice-PLAN-DRAFT.md
@.planning/phases/16-painel-admin-backoffice/16-RESEARCH.md
@.planning/phases/16-painel-admin-backoffice/16-VALIDATION.md

# Padrão real de guard Bearer + getUser + rate-limit por count (ler para replicar; nomes de env exatos)
@api/assistant.js
@api/inject-config.js

# SUMMARY do 16-01 — assinaturas reais das RPCs a chamar
@.planning/phases/16-painel-admin-backoffice/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard requireAdmin (ordem load-bearing) + auditDenied com throttle + helper de audit leve</name>
  <files>api/_lib/admin/guard.js</files>
  <read_first>
    - api/assistant.js:205-231 (padrão Bearer + getUser + nomes de env NEXT_PUBLIC_SUPABASE_URL/ANON_KEY)
    - api/assistant.js:264-311 (padrão de rate-limit por count sobre tabela — adaptar para o throttle)
    - .planning/research/painel-admin-backoffice-PLAN-DRAFT.md §1 (sketch do guard, ordem exata)
    - .planning/phases/16-painel-admin-backoffice/16-RESEARCH.md §Guard order & auth
  </read_first>
  <action>
    Criar `api/_lib/admin/guard.js` (CommonJS, `module.exports`, NÃO-roteável) exportando `requireAdmin(req)` com a ordem EXATA (load-bearing para o review): (a) extrai `Bearer` de `req.headers.authorization` → sem token retorna `{ fail: { status: 401, error: 'JWT_MISSING' } }`; (b) instancia o client anon com `process.env.NEXT_PUBLIC_SUPABASE_URL` / `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY` (mesmos nomes de assistant.js:218-219 — M7; se ausentes, 500 SUPABASE_CONFIG_MISSING como o projeto já faz) e chama `authClient.auth.getUser(token)` → erro/sem user retorna `{ fail: { status: 401, error: 'JWT_INVALID' } }`; (c) parse da allowlist: `(process.env.ADMIN_USER_IDS || '').split(',').map(s => s.trim().toLowerCase()).filter(Boolean)` e compara com `data.user.id.toLowerCase()` (normalização dos DOIS lados — M9) → se não incluído, chama `auditDenied(user, req)` e retorna `{ fail: { status: 403, error: 'FORBIDDEN' } }`; fail-closed: allowlist vazia ⇒ ninguém passa ⇒ 403 para TODOS. (d) SÓ ENTÃO cria `adminClient = createClient(url, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } })` e retorna `{ user, adminClient }`.

    No mesmo arquivo: `auditDenied(user, req)` — best-effort via service_role, com THROTTLE anti-flood já aqui (M1): antes do INSERT de `denied_access`, contar linhas `admin_audit_log` com `status='denied'` do MESMO user_id OU ip do request numa janela curta (ex.: 60s, padrão count de assistant.js:264-291 adaptado — janela curta, chave = user/IP negado, tabela = admin_audit_log); se já houver >= N no período, NÃO inserir nova linha (evita que qualquer JWT válido martele e infle a tabela). Extrair `ip`/`user_agent` do request para os campos do audit. E `logAction(adminClient, { action, admin_id, admin_email, target_user?, ip, user_agent, status })` — helper para a linha LEVE de leitura (sem before/after/reason). Erros de audit/throttle são fail-open (nunca bloqueiam a resposta ao admin legítimo).
  </action>
  <verify>
    <automated>MISSING — sem harness local; verificação por smoke curl no deploy de preview (Task 3) + leitura do código. Checagem estática: grep confirma que createClient com SERVICE_ROLE_KEY aparece SÓ depois do return de 403 no fluxo (adminClient nunca instanciado antes da allowlist).</automated>
  </verify>
  <acceptance_criteria>
    `requireAdmin` segue a ordem 401→401→403(+auditDenied+throttle)→adminClient; o adminClient (service_role) só é criado no ramo autorizado. Allowlist normaliza os dois lados. `auditDenied` tem throttle por user/IP em janela curta e é fail-open. `logAction` grava linha leve. `service_role` só é lido de `process.env`.
  </acceptance_criteria>
  <done>guard.js com ordem load-bearing 401→401→403(+audit+throttle)→adminClient; allowlist normalizada; auditDenied com throttle fail-open; service_role só de process.env.</done>
</task>

<task type="auto">
  <name>Task 2: Roteador api/admin.js (whoami/accounts/audit/set_plan) + handlers + no-store</name>
  <files>api/admin.js, api/_lib/admin/accounts.js, api/_lib/admin/set-plan.js, api/_lib/admin/audit-list.js</files>
  <read_first>
    - api/assistant.js (forma de export CommonJS, parse de req, res.status().json())
    - .planning/phases/16-painel-admin-backoffice/16-01-SUMMARY.md (assinaturas de admin_list_accounts / admin_set_plan)
    - .planning/research/painel-admin-backoffice-PLAN-DRAFT.md §Architecture Diagram (dispatch por action)
  </read_first>
  <action>
    Criar `api/admin.js` (CommonJS, `module.exports = async (req, res) => {...}`) — roteador ÚNICO: primeiro seta `Cache-Control: no-store` na resposta; chama `const g = await requireAdmin(req)`; se `g.fail`, responde `res.status(g.fail.status).json({ error: g.fail.error })` e retorna (nenhum dispatch acontece sem guard). Só com `g.user`/`g.adminClient` válidos, faz dispatch por `action` (query string p/ GET, body p/ POST):
    - GET `whoami` → retorna `{ id: g.user.id, email: g.user.email }` sem tocar o banco; SEMPRE grava audit leve via `logAction` com action própria `whoami` (linha leve, sem before/after/reason) — obrigatório (NÃO pular) para cumprir "toda ação auditada" do ROADMAP e a truth "Toda action grava audit" deste plano.
    - GET `accounts` → `g.adminClient.rpc('admin_list_accounts', { p_search, p_plan, p_limit, p_offset })`; repassa o JSON (page + total_count); grava audit leve (action `list_accounts`).
    - GET `audit` → `g.adminClient.from('admin_audit_log').select(...)` paginado/ordenado por created_at desc; grava audit leve (action `view_audit`).
    - POST `set_plan` → valida body (`target_user`, `new_plan`, `reason` obrigatório e não-vazio), chama `g.adminClient.rpc('admin_set_plan', { p_admin_id: g.user.id, p_admin_email: g.user.email, p_target_user, p_new_plan, p_reason })` (a RPC já grava o audit before/after/reason atômico — não duplicar INSERT aqui); repassa `{target, before, after}`.
    - action desconhecida → 400. Method errado para a action → 405.
    Criar os handlers em `api/_lib/admin/accounts.js`, `api/_lib/admin/set-plan.js`, `api/_lib/admin/audit-list.js` (cada um recebe `adminClient` + params, devolve o resultado; o roteador cuida de guard/audit-leve/erros). Formato de resposta/erros é discrição do Claude — manter consistente e nunca vazar detalhes internos/service_role. NENHUM segredo em resposta.
  </action>
  <verify>
    <automated>MISSING — smoke curl no deploy (Task 3). Estático: grep confirma que toda resposta passa por Cache-Control: no-store e que requireAdmin é chamado antes de qualquer dispatch/rpc.</automated>
  </verify>
  <acceptance_criteria>
    `api/admin.js` roteia whoami/accounts/audit/set_plan só após guard PASS; set_plan usa a RPC (audit atômico, sem INSERT duplicado no serverless); reads gravam linha leve; toda resposta é no-store; reason obrigatório em set_plan; action desconhecida/method errado → 400/405; nenhum segredo na resposta.
  </acceptance_criteria>
  <done>roteador único com guard antes de todo dispatch; 4 actions funcionando; set_plan via RPC; reads com audit leve; no-store em tudo; sem segredo vazado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint humano — setup Vercel (env vars + conta admin) + teste fail-closed</name>
  <action>Checkpoint de verificação humana (não autônomo): o humano cria a conta admin dedicada, seta SUPABASE_SERVICE_ROLE_KEY e ADMIN_USER_IDS na aba Project do Vercel, roda o teste fail-closed (ADMIN_USER_IDS vazia → 403 para todos) ANTES de liberar a allowlist, e confirma os smoke curls + throttle + security-reviewer PASS. Detalhes em what-built/how-to-verify/resume-signal.</action>
  <what-built>
    `api/admin.js` + `api/_lib/admin/{guard,accounts,set-plan,audit-list}.js` deployados (push-to-main → Vercel). Guard fail-closed, allowlist, throttle de denied_access e audit em toda action.
  </what-built>
  <how-to-verify>
    O código está no ar, mas o backend admin exige setup humano no Vercel e um teste fail-closed que DEVE rodar antes de liberar a allowlist em produção:

    1. Criar uma conta admin dedicada NOVA no Supabase Auth (Authentication → Users → Add user) — NÃO usar caduucomc@gmail.com. Anotar o UUID dela.
    2. Setar `SUPABASE_SERVICE_ROLE_KEY` na aba **Project** do Vercel (valor = service_role secret do Supabase → Project Settings → API). Ainda NÃO preencher `ADMIN_USER_IDS` (deixar ausente/vazia).
    3. Com `ADMIN_USER_IDS` vazia, testar o fail-closed (o teste mais importante da fase):
       - `curl -i https://<deploy>/api/admin?action=whoami` (sem header) → esperar `401 JWT_MISSING`.
       - `curl -i -H "Authorization: Bearer token-invalido" https://<deploy>/api/admin?action=whoami` → esperar `401 JWT_INVALID`.
       - `curl` com um JWT VÁLIDO de qualquer usuário (mesmo o que será admin) → esperar `403 FORBIDDEN` para 100% dos casos (allowlist vazia bloqueia todos).
    4. Só agora preencher `ADMIN_USER_IDS` com o UUID da conta admin dedicada (aba Project) e re-deployar.
    5. `curl` com o JWT do admin → `action=whoami` deve retornar `200` com id/email.
    6. `curl` com JWT de usuário comum → `403 FORBIDDEN`; conferir via MCP `select * from admin_audit_log where action='denied_access' order by created_at desc limit 1`.
    7. Disparar ~10 requisições negadas rápidas do mesmo user/IP → contar linhas `denied_access` no minuto: deve haver teto (não 10 linhas).
    8. Rodar `security-reviewer` no caminho `api/` (obrigatório) — CSRF é não-issue (auth por Bearer header, não cookies): registrar para o reviewer não gastar ciclo.
  </how-to-verify>
  <resume-signal>Digite "aprovado" quando os passos 3 (fail-closed → 403 para todos), 5 (admin 200), 6 (comum 403 + audit), 7 (throttle) passarem e o security-reviewer der PASS; ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api/admin.js | JWT via Authorization: Bearer; qualquer usuário autenticado pode bater no endpoint |
| api/admin.js → service_role | adminClient só nasce após guard PASS; ignora RLS por design |
| env (Vercel Project) → runtime | SUPABASE_SERVICE_ROLE_KEY / ADMIN_USER_IDS só existem no runtime serverless |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Elevation of Privilege | requireAdmin (ordem do guard) | critical | mitigate | Ordem load-bearing 401→401→403(+audit)→adminClient; roteador/guard único; adminClient jamais instanciado antes da allowlist. Verificado por curl + leitura estática |
| T-16-06 | Elevation of Privilege | allowlist ADMIN_USER_IDS | high | mitigate | Fail-closed: env vazia/ausente → 403 para todos (testado ANTES de setar em prod). Normalização trim().toLowerCase() dos dois lados (M9) evita 403 silencioso do admin real |
| T-16-07 | Denial of Service | flood de denied_access na tabela de audit | high | mitigate | Throttle por user/IP negado em janela curta já no 16-02 (não esperar 16-04); fail-open para não bloquear admin legítimo |
| T-16-08 | Information Disclosure | vazamento de service_role para o client | critical | mitigate | SUPABASE_SERVICE_ROLE_KEY só lido de process.env; nunca serializado em resposta; inject-config.js não muda (só URL+anon key) |
| T-16-09 | Spoofing | JWT forjado / identidade | high | mitigate | auth.getUser(token) contra o Supabase Auth server (não decodifica localmente) — mesmo padrão de assistant.js:227 |
| T-16-CSRF | Tampering | CSRF | low | accept | Não-issue — auth por Authorization: Bearer header, não cookies; registrado para o security-reviewer |
</threat_model>

<verification>
- Smoke curl (deploy preview): sem token→401 JWT_MISSING; Bearer inválido→401 JWT_INVALID; ADMIN_USER_IDS vazia + JWT válido→403 para TODOS.
- Após setar allowlist: admin→whoami 200; usuário comum→403 + linha denied_access (MCP).
- Throttle: N requisições negadas rápidas → linhas denied_access com teto por janela.
- set_plan (via admin) muda plan + audit before/after/reason atômico (MCP).
- Toda resposta Cache-Control: no-store; nenhum segredo vazado.
- security-reviewer PASS (caminho sensível api/).
</verification>

<success_criteria>
O endpoint `api/admin.js` só serve requests de admins allowlisted (fail-closed comprovado com env vazia → 403 para todos), audita toda action (leve para leituras, atômico para set_plan), resiste a flood de denied_access por throttle, e nunca expõe o service_role. Guard e roteador são únicos — não há superfície admin fora deles.
</success_criteria>

<artifacts_this_plan_produces>
- `api/admin.js` — roteador admin único; actions: `whoami`, `accounts`, `audit`, `set_plan`
- `api/_lib/admin/guard.js` — `requireAdmin(req)`, `auditDenied(user, req)` (com throttle), `logAction(...)` (linha leve)
- `api/_lib/admin/accounts.js` — chama `admin_list_accounts`
- `api/_lib/admin/set-plan.js` — chama `admin_set_plan`
- `api/_lib/admin/audit-list.js` — lê `admin_audit_log`
- env vars novas (Vercel Project): `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_USER_IDS`
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/16-painel-admin-backoffice/16-02-SUMMARY.md` when done
</output>
