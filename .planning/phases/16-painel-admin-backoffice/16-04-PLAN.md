---
phase: 16-painel-admin-backoffice
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - api/_lib/admin/guard.js
  - supabase/admin-backoffice.sql
  - docs/admin-lgpd.md
autonomous: false
requirements: [ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "A superfície admin tem rate-limit geral por user/IP requisitante (além do throttle de denied_access do 16-02)"
    - "A retenção do audit log (proposta 2 anos, expurgo manual) está documentada e ratificada com o dono (base legal + minimização, LGPD)"
    - "A memória testar-tier-free-pro aponta para o painel como caminho primário de troca de tier (SQL Editor vira fallback)"
    - "Os 5 UATs pendentes da Phase 13 + os UATs da Phase 12 foram re-executados VIA PAINEL (dogfooding), destravando o gate da fase"
  artifacts:
    - "rate-limit geral no guard/roteador admin"
    - "docs/admin-lgpd.md (base legal, minimização, retenção do audit log)"
    - "memória testar-tier-free-pro atualizada"
  key_links:
    - "Rate-limit geral usa o padrão count de assistant.js:264-311 (chave = user/IP requisitante, tabela admin_audit_log ou assistant_usage-like)"
    - "Dogfooding dos UATs 12/13 é o gate da fase (D-seq) — verificação, não task de código"
  prohibitions:
    - "NADA destrutivo no MVP (banir/excluir/reset senha ficam no backlog, gateados em B12/staging)"
    - "NÃO reabrir a trava 13-02 nem alterar o guard de forma que o adminClient nasça antes da allowlist"
    - "NÃO logar dado sensível/segredo no audit (V7.1) — só metadados + before/after de plan"
---

<objective>
Hardening + LGPD + integração final (ADMIN-04): fechar a superfície admin com um rate-limit geral por requisitante (complementa o throttle de `denied_access` do 16-02), documentar a nota LGPD (base legal, minimização, retenção do audit log — proposta 2 anos com expurgo manual, a ratificar), atualizar a memória `testar-tier-free-pro` para apontar ao painel, e destravar o gate da fase: re-executar via painel (dogfooding) os 5 UATs pendentes da Phase 13 + os UATs da Phase 12 (D-seq).

Purpose: transformar o MVP funcional num MVP operável e conforme, e converter a construção do painel na validação prática das travas de tier (13-01/13-02/13-03) — que é a razão de a Phase 16 rodar antes da 14.
Output: rate-limit geral + `docs/admin-lgpd.md` + memória atualizada + UATs 12/13 fechados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/painel-admin-backoffice-PLAN-DRAFT.md
@.planning/phases/16-painel-admin-backoffice/16-RESEARCH.md
@.planning/phases/16-painel-admin-backoffice/16-VALIDATION.md

# Padrão de rate-limit por count (chave/janela a adaptar)
@api/assistant.js

# Artefatos das waves anteriores
@.planning/phases/16-painel-admin-backoffice/16-02-SUMMARY.md
@.planning/phases/16-painel-admin-backoffice/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rate-limit geral da superfície admin + nota LGPD (docs/admin-lgpd.md)</name>
  <files>api/_lib/admin/guard.js, docs/admin-lgpd.md</files>
  <read_first>
    - api/assistant.js:264-311 (padrão count; USER_DAILY_LIMIT como referência de forma)
    - api/_lib/admin/guard.js (onde encaixar o rate-limit geral, sem quebrar a ordem load-bearing)
    - .planning/phases/16-painel-admin-backoffice/16-RESEARCH.md §LGPD e retenção (base legal, minimização, OWASP ASVS V7)
  </read_first>
  <action>
    (a) Adicionar um rate-limit GERAL à superfície admin (caminho sensível `api/`): distinto do throttle de `denied_access` do 16-02 (que protege a tabela de audit contra não-admins), este protege o endpoint contra um admin/token comprometido martelando — chave = user_id OU ip do REQUISITANTE, janela + limite no padrão count de assistant.js:264-311 (fail-open em erro de leitura do count; 429 ao estourar). Encaixar DEPOIS do guard passar (não antes da allowlist — não mexer na ordem load-bearing) e valer para as actions autenticadas. Manter a mudança mínima e revisada.

    (b) Criar `docs/admin-lgpd.md` com: base legal para o admin ver dados agregados de terceiros; princípio de minimização (a RPC retorna só metadados/contagens, nunca lançamentos individuais — reforça V7.1: nenhum segredo/PII desnecessária no audit); política de retenção do audit log (proposta 2 anos, expurgo manual — dentro da faixa aceitável, acima do mínimo prático de ~6 meses, sem prazo máximo legal fixo, ver RESEARCH §LGPD); e a nota de que a retenção é uma decisão de produto a ratificar com o dono (Task 3). Referenciar OWASP ASVS V7.1/V7.2/V7.3 como padrão seguido.
  </action>
  <verify>
    <automated>MISSING — smoke curl: disparar requisições autenticadas de admin acima do limite na janela → esperar 429 quando estourar (fail-open se o count falhar). docs/admin-lgpd.md existe e cobre base legal + minimização + retenção. security-reviewer no caminho api/.</automated>
  </verify>
  <acceptance_criteria>
    Rate-limit geral por user/IP requisitante ativo após o guard, fail-open, 429 ao estourar, sem alterar a ordem load-bearing do guard. `docs/admin-lgpd.md` documenta base legal, minimização e retenção (2 anos a ratificar) com referência a ASVS V7. security-reviewer PASS.
  </acceptance_criteria>
  <done>rate-limit geral por user/IP após o guard (fail-open, 429 no estouro) sem mexer na ordem load-bearing; docs/admin-lgpd.md cobre base legal+minimização+retenção; security-reviewer PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar memória testar-tier-free-pro para apontar ao painel</name>
  <files>memória testar-tier-free-pro (arquivo do índice de memória do usuário)</files>
  <read_first>
    - .planning/research/painel-admin-backoffice.md §6 (nota tática atual: alternar via SQL Editor)
    - a memória testar-tier-free-pro (arquivo referenciado no índice de memória do usuário)
  </read_first>
  <action>
    Atualizar a memória `testar-tier-free-pro` para que o caminho PRIMÁRIO de alternar `profiles.plan` (free/pro/family) e testar features premium/degustação passe a ser o painel `/painel` (logar como admin dedicado → aba Contas → Alterar plano → F5 no app), rebaixando o Supabase SQL Editor a fallback documentado (owner/service_role ainda ignora a trava 13-02). Manter a nota de que o owner ignora a trava e o passo do F5.
  </action>
  <verify>
    <automated>MISSING — verificação por leitura: a memória cita /painel como caminho primário e SQL Editor como fallback.</automated>
  </verify>
  <acceptance_criteria>
    Memória `testar-tier-free-pro` atualizada: painel `/painel` como caminho primário de troca de tier; SQL Editor como fallback; passo do F5 preservado.
  </acceptance_criteria>
  <done>memória testar-tier-free-pro aponta ao painel (primário) com SQL Editor como fallback e F5 preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint humano — gate da fase (dogfooding UATs 12/13 via painel + ratificar retenção)</name>
  <action>Checkpoint de verificação humana (não autônomo) e gate da Phase 16 (D-seq): o humano roda o checklist de segurança final (reviewers PASS), ratifica a retenção do audit (2 anos), executa via painel os 5 UATs pendentes da Phase 13 e os UATs da Phase 12 (dogfooding), confirma que nada destrutivo entrou no MVP e roda /gsd-verify-work 16. Detalhes em what-built/how-to-verify/resume-signal.</action>
  <what-built>
    MVP do painel completo (16-01..16-03) + rate-limit geral + nota LGPD + memória atualizada. Falta o gate da fase: dogfooding dos UATs 12/13 via painel e ratificação da retenção.
  </what-built>
  <how-to-verify>
    Este é o gate da Phase 16 (D-seq — a fase existe para destravar isto):
    1. Checklist de segurança final: `security-reviewer` + `database-reviewer` PASS em todo o caminho sensível da fase (supabase/ do 16-01, api/ do 16-02/16-04, vercel.json do 16-03) sem findings high/critical abertos.
    2. Ratificar a retenção do audit log: 2 anos com expurgo manual está ok? (confirmar ou ajustar em docs/admin-lgpd.md).
    3. Dogfooding — Phase 13 (5 UATs de gating free/pro): usando o painel para alternar o tier de uma conta de teste (free↔pro), executar os 5 UATs pendentes da Phase 13 no app (registrá-los em 13-UAT.md conforme a decisão de sequência). O código já está live e verificado 16/16 — isto valida na prática a mesma trava que o painel exercita.
    4. Dogfooding — Phase 12 (UATs WRITE in-app): re-executar os UATs humanos da Phase 12 pelo app, com o tier controlado via painel quando necessário.
    5. Confirmar que NADA destrutivo entrou no MVP (sem banir/excluir/reset senha).
    Rodar `/gsd-verify-work 16` ao final para a UAT conversacional da própria Phase 16.
  </how-to-verify>
  <resume-signal>Digite "aprovado" quando: reviewers PASS, retenção ratificada, os 5 UATs da Phase 13 e os UATs da Phase 12 executados via painel, e nada destrutivo no MVP; ou descreva o que falta.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin autenticado → api/admin.js | Token válido de admin pode ser abusado/comprometido para flood |
| admin_audit_log → retenção | Dados pessoais de terceiros retidos ao longo do tempo (LGPD) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-15 | Denial of Service | flood da superfície admin por admin/token comprometido | medium | mitigate | Rate-limit geral por user/IP requisitante (padrão count assistant.js), após o guard, fail-open, 429 ao estourar |
| T-16-16 | Information Disclosure | retenção excessiva / PII desnecessária no audit | medium | mitigate | Minimização (só agregados/metadados + before/after de plan, nunca lançamentos/segredos — V7.1); retenção 2 anos documentada + expurgo manual (LGPD art.15/16), ratificada com o dono |
| T-16-17 | Elevation of Privilege | ação destrutiva entrar no MVP sem staging | high | mitigate | Escopo MVP = só ação reversível (trocar plan); destrutivas (banir/excluir/reset) ficam no backlog gateadas em B12/staging (D-1c) — verificado no checkpoint |
</threat_model>

<verification>
- Rate-limit geral: requisições autenticadas acima do limite → 429; erro de count → fail-open.
- `docs/admin-lgpd.md` cobre base legal + minimização + retenção (ASVS V7).
- Memória `testar-tier-free-pro` aponta ao painel (primário) + SQL Editor (fallback).
- Gate D-seq: 5 UATs da Phase 13 + UATs da Phase 12 executados via painel (dogfooding).
- Nada destrutivo no MVP.
- security-reviewer + database-reviewer PASS em todo o caminho sensível da fase.
- `/gsd-verify-work 16` conversacional.
</verification>

<success_criteria>
A superfície admin fica operável e conforme: rate-limit geral protege o endpoint, a retenção/minimização do audit está documentada e ratificada, a memória de teste aponta ao painel, e — o objetivo estrutural da fase — os UATs pendentes das Phases 12 e 13 são finalmente fechados por dogfooding no próprio painel, sem nenhuma ação destrutiva no MVP.
</success_criteria>

<artifacts_this_plan_produces>
- Rate-limit geral da superfície admin (em `api/_lib/admin/guard.js` / roteador) — chave user/IP requisitante
- `docs/admin-lgpd.md` — base legal, minimização, retenção do audit log (2 anos, a ratificar)
- Memória `testar-tier-free-pro` atualizada (painel primário, SQL Editor fallback)
- Gate da fase: 5 UATs da Phase 13 + UATs da Phase 12 fechados via painel (dogfooding, D-seq)
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/16-painel-admin-backoffice/16-04-SUMMARY.md` when done
</output>
