---
phase: 16-painel-admin-backoffice
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - painel.html
  - assets/fides-admin.jsx
  - assets/fides-admin.css
  - vercel.json
autonomous: false
requirements: [ADMIN-03]
user_setup: []

must_haves:
  truths:
    - "curl -sI https://<deploy>/painel serve painel.html (não index.html do app) — rewrite antes do catch-all"
    - "Headers X-Frame-Options: DENY e Referrer-Policy: no-referrer presentes em /painel"
    - "Admin loga no painel, vê a lista de contas reais (e-mail, nome, badge de plano, criado, último login, msgs IA/mês) com busca+filtro"
    - "Fluxo Alterar plano: select de tier + motivo obrigatório + ConfirmDialog → toast + linha nova na aba Auditoria"
    - "Trocar free↔pro de uma conta teste reflete no app após F5 (useFides lê profiles.plan fail-closed)"
    - "Usuário comum que abre /painel e loga vê acesso restrito (403 do backend)"
  artifacts:
    - "painel.html (entrada isolada, tokens.css primeiro, /fides-config.js, supabase-js v2 UMD, storageKey fides-admin-auth)"
    - "assets/fides-admin.jsx (React via Babel-standalone; telas login/negado/contas/auditoria; single-page com abas por hash)"
    - "assets/fides-admin.css"
    - "rewrite /painel → /painel.html e headers de segurança em vercel.json"
  key_links:
    - "Rewrite /painel inserido ANTES do catch-all /((?!api/).*) no array rewrites (ordem importa — Pitfall 4)"
    - "Client Supabase do painel usa storageKey 'fides-admin-auth' (isola a sessão admin da sessão do app na mesma origem)"
    - "Fetch /api/admin?action=... com Authorization: Bearer <JWT> — todo dado admin vem do backend do 16-02, nunca do client SDK direto"
  prohibitions:
    - "service_role JAMAIS referenciado no client — painel só carrega /fides-config.js (URL + anon key)"
    - "NÃO adicionar rewrite /painel/(.*) — painel é single-page com abas por hash/estado (M10)"
    - "NENHUMA mutação de tier pelo client SDK — só via POST /api/admin?action=set_plan (não reabrir 13-02)"
    - "NÃO travar uma variante do sketch como definitiva — usar como direção visual, registrar a escolha como decisão revisável"
    - "NÃO expor lançamentos individuais de terceiros — só os agregados que a RPC retorna (minimização LGPD)"
---

<objective>
Front do painel em `/painel` (ADMIN-03): `painel.html` isolado (entrada própria, NÃO acopla à migração Vite/Next B11 — D-1a) + `assets/fides-admin.jsx`/`.css` (React via Babel-standalone), servido antes do catch-all do `vercel.json` e com headers anti-clickjacking na rota. Single-page com abas por hash/estado (sem sub-paths — evita F5 caindo no catch-all, M10). Consome exclusivamente o backend do 16-02 (`fetch /api/admin?action=...` com Bearer JWT) — nenhum acesso a dado admin pelo client SDK.

UI é discrição do Claude, guiada pelo sketch `.planning/sketches/001-painel-admin/index.html` (3 variantes A/B/C, `winner: TBD`) — respeitando `tokens.css` (sage paper #F4F5F1, forest green #2D5A3D, Manrope, JetBrains Mono para números) e reusando `fides-ui` (ConfirmDialog/Toast). A troca de plano exige ConfirmDialog + motivo obrigatório (auditoria é cidadã de primeira classe na UI).

Purpose: dar ao dono a superfície visual para gerir tier sem o Supabase SQL Editor, destravando o dogfooding dos UATs 12/13 (D-seq).
Output: painel funcional em `/painel` + rewrite/headers no `vercel.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/painel-admin-backoffice-PLAN-DRAFT.md
@.planning/phases/16-painel-admin-backoffice/16-RESEARCH.md
@.planning/phases/16-painel-admin-backoffice/16-VALIDATION.md

# Padrão de carregamento (tokens.css primeiro, /fides-config.js, supabase-js v2 UMD) e ordem de rewrites/headers
@index.html
@vercel.json
@api/inject-config.js

# Componentes reutilizáveis (ConfirmDialog/Toast/useConfirm) + identidade visual
@assets/fides-ui.jsx
@assets/tokens.css

# Direção visual (3 variantes, winner TBD) — usar como DIREÇÃO, não travar
@.planning/sketches/001-painel-admin/README.md
@.planning/sketches/001-painel-admin/index.html

# Contrato do backend a consumir (actions, formato de erro)
@.planning/phases/16-painel-admin-backoffice/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: painel.html isolado + assets/fides-admin.jsx/.css (login, contas, alterar plano, auditoria)</name>
  <files>painel.html, assets/fides-admin.jsx, assets/fides-admin.css</files>
  <read_first>
    - index.html:1-30 (ordem de carregamento: tokens.css primeiro, /fides-config.js, supabase-js@2 UMD via jsdelivr, fontes Manrope/JetBrains Mono)
    - assets/fides-ui.jsx (window.FidesUI: ConfirmDialog, useConfirm, useToast, toast, ToastViewport)
    - .planning/sketches/001-painel-admin/index.html + README.md (3 variantes; fluxo Alterar plano é o coração do MVP)
    - .planning/phases/16-painel-admin-backoffice/16-02-SUMMARY.md (actions e shape de resposta do /api/admin)
  </read_first>
  <action>
    Criar `painel.html` como documento HTML SEPARADO (contexto de página isolado — não compartilha window com index.html/Fides-app.html): `tokens.css` como PRIMEIRO CSS (regra CLAUDE.md), depois `assets/fides-admin.css` + reuso de `assets/fides-ui.css`; carregar `/fides-config.js` (público — só URL + anon key; service_role JAMAIS chega aqui) e o supabase-js v2 UMD via jsdelivr (mesma tag `@2` de index.html:15 — suporta `auth.storageKey`); React/ReactDOM + Babel-standalone como no app. Instanciar o client Supabase do painel com `createClient(FIDES_CONFIG.supabaseUrl, FIDES_CONFIG.supabaseAnonKey, { auth: { storageKey: 'fides-admin-auth', persistSession: true } })` — o `storageKey` próprio isola a sessão admin da sessão do app na mesma origem.

    Criar `assets/fides-admin.jsx` (React via Babel-standalone; cuidado com Rules of Hooks — já causou bug na Phase 07): app single-page com abas por hash/estado (`#contas`, `#auditoria`) — SEM sub-paths/rotas. Telas:
    - Login: form e-mail/senha via `adminSupabase.auth.signInWithPassword`; ao logar, obter o JWT da sessão para os fetches.
    - Acesso negado: quando o backend responde 403 (usuário logado mas não-allowlisted), mostrar estado "acesso restrito" claro (não expor detalhes).
    - Contas: `fetch('/api/admin?action=accounts&p_search=...&p_plan=...&p_limit=50&p_offset=...', { headers: { Authorization: 'Bearer ' + jwt } })`; tabela com e-mail, nome, badge de plano (free/pro/family), criado, último login, msgs IA/mês (mostrar o cap free 10/mês); busca + filtro de plano; paginação usando `total_count` da RPC.
    - Alterar plano: botão por linha → modal com `select` de tier + campo motivo OBRIGATÓRIO (confirmar desabilitado enquanto vazio) + `ConfirmDialog` (fides-ui); ao confirmar, `POST /api/admin` com `action=set_plan`, body `{ target_user, new_plan, reason }`, Bearer JWT; sucesso → `FidesUI.toast.success` + atualizar a linha + refletir na aba Auditoria. NENHUMA escrita de plan pelo client SDK.
    - Auditoria: `fetch('/api/admin?action=audit', ...)` → tabela de linhas (quem, quando, action, target, before/after/reason, status) ordenada por created_at desc.

    Densidade desktop-first. UI/estrutura é discrição do Claude guiada pelo sketch: escolher uma direção coerente (a variante A/sidebar escala melhor para a fase 2 conforme o README, mas a decisão é do Claude), registrar a escolha em Decisions do SUMMARY e atualizar `winner:` no README do sketch — tratando como escolha REVISÁVEL (o checkpoint humano da Task 3 confirma o tom visual), NÃO como variante travada. Criar `assets/fides-admin.css` reusando tokens.
  </action>
  <verify>
    <automated>MISSING — sem harness; verificação por abertura no browser + smoke E2E no checkpoint (Task 3). Estático: grep confirma que 'service_role' / 'SERVICE_ROLE' NÃO aparece em painel.html nem em assets/fides-admin.jsx, e que toda mutação de plan passa por fetch('/api/admin' ...action=set_plan), sem .from('profiles').update.</automated>
  </verify>
  <acceptance_criteria>
    `/painel` renderiza login → contas → alterar plano (com motivo obrigatório + ConfirmDialog) → auditoria, tudo consumindo `/api/admin` com Bearer. `storageKey: 'fides-admin-auth'` presente. Nenhuma referência a service_role no client; nenhuma escrita de plan pelo SDK. tokens.css é o primeiro CSS. Escolha de layout registrada como decisão revisável (winner atualizado no README).
  </acceptance_criteria>
  <done>painel single-page (login/negado/contas/alterar-plano/auditoria) consumindo só /api/admin com Bearer; storageKey próprio; sem service_role nem update de plan no client; tokens.css primeiro.</done>
</task>

<task type="auto">
  <name>Task 2: vercel.json — rewrite /painel antes do catch-all + headers anti-clickjacking</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (array rewrites: par /teste antes do catch-all /((?!api/).*); array headers: entrada /fides-config.js)
    - .planning/phases/16-painel-admin-backoffice/16-RESEARCH.md §vercel.json rewrite ordering
  </read_first>
  <action>
    Editar `vercel.json` (caminho sensível — não quebrar rewrites existentes): (1) inserir `{ "source": "/painel", "destination": "/painel.html" }` no array `rewrites` em QUALQUER posição ANTES do catch-all `/((?!api/).*)` (seguir o modelo do par `/teste` que já prova a ordenação em produção — Pitfall 4). NÃO adicionar `/painel/(.*)` (painel é single-page — M10). (2) Adicionar nova entrada no array `headers`: `{ "source": "/painel", "headers": [ { "key": "X-Frame-Options", "value": "DENY" }, { "key": "Referrer-Policy", "value": "no-referrer" } ] }` (M11 — anti-clickjacking; os arrays rewrites/headers não interagem, ordem de headers não importa). Manter a entrada de `Cache-Control` de `/fides-config.js` intacta.
  </action>
  <verify>
    <automated>MISSING — validar pós-deploy: `curl -sI https://<deploy>/painel` deve retornar corpo/headers de painel.html (não index.html) e conter `x-frame-options: DENY` + `referrer-policy: no-referrer`. Estático: JSON válido; entrada /painel aparece antes do catch-all no array rewrites.</automated>
  </verify>
  <acceptance_criteria>
    `/painel` serve `painel.html` (rewrite antes do catch-all); headers `X-Frame-Options: DENY` + `Referrer-Policy: no-referrer` presentes na rota; rewrites/headers pré-existentes intactos; sem `/painel/(.*)`.
  </acceptance_criteria>
  <done>vercel.json com rewrite /painel→/painel.html antes do catch-all + headers anti-clickjacking na rota; JSON válido; entradas pré-existentes intactas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint humano — E2E do painel + reflexo no app + tom visual</name>
  <action>Checkpoint de verificação humana (não autônomo): o humano roda o E2E pelo deploy (curl da rota + headers, logar admin, listar contas reais, Alterar plano com motivo/confirmação, reflexo no app após F5, usuário comum vê acesso restrito), confirma o tom visual da direção de sketch escolhida e roda security-reviewer. Detalhes em what-built/how-to-verify/resume-signal.</action>
  <what-built>
    Painel `/painel` no ar (login, tabela de contas com busca/filtro, Alterar plano com motivo+confirmação, aba Auditoria), servido antes do catch-all com headers anti-clickjacking. Consome só o backend do 16-02.
  </what-built>
  <how-to-verify>
    E2E pelo deploy (push-to-main auto-deploya):
    1. `curl -sI https://<deploy>/painel` → confirmar que é painel.html (não o app) e que traz `x-frame-options: DENY` e `referrer-policy: no-referrer`.
    2. Abrir `https://<deploy>/painel`, logar com a conta admin dedicada → deve carregar a lista de contas REAIS (e-mail, nome, badge de plano, criado, último login, msgs IA/mês); testar busca + filtro de plano.
    3. Numa conta de TESTE, usar "Alterar plano" free→pro: confirmar que o botão confirmar fica desabilitado sem motivo, que o ConfirmDialog aparece, e que ao confirmar surge o toast e uma linha nova na aba Auditoria (com before/after/reason).
    4. Logar no app (index.html) com essa conta de teste → F5 → o tier deve refletir a mudança (`useFides()` lê `profiles.plan` fail-closed). Reverter pro→free e reconfirmar.
    5. Abrir `/painel` e logar com um usuário comum (não-allowlisted) → deve ver "acesso restrito" (403), sem lista.
    6. Confirmar o tom visual do painel (direção do sketch escolhida) — ajustar se necessário. Rodar `security-reviewer` (mudança em vercel.json/rota é caminho sensível).
  </how-to-verify>
  <resume-signal>Digite "aprovado" quando 1–5 passarem (painel servido + headers, contas reais, alterar plano com auditoria, reflexo no app após F5, comum vê acesso restrito) e o visual estiver ok; ou descreva ajustes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN (Vercel) → /painel | Rota pública mostra login; obscuridade não é defesa, o 403 do backend é |
| Browser (painel.html) → api/admin.js | Todo dado admin passa pelo backend com Bearer; client nunca toca service_role |
| painel.html ↔ app (mesma origem) | localStorage compartilhado; storageKey próprio isola as sessões |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10 | Tampering | clickjacking na rota /painel | high | mitigate | Headers X-Frame-Options: DENY + Referrer-Policy: no-referrer na rota /painel do vercel.json (M11) |
| T-16-11 | Information Disclosure | service_role vazando pelo client do painel | critical | mitigate | painel.html só carrega /fides-config.js (URL + anon key); nenhuma referência a service_role no HTML/JSX; dado admin só via /api/admin |
| T-16-12 | Elevation of Privilege | mutação de plan pelo client SDK (reabrir 13-02) | critical | mitigate | Nenhuma escrita de plan no client — só POST /api/admin?action=set_plan; grep garante ausência de .from('profiles').update no painel |
| T-16-13 | Spoofing | sessão do app reaproveitada indevidamente no painel | medium | mitigate | Client Supabase do painel com storageKey 'fides-admin-auth' isola a sessão admin da sessão do app na mesma origem |
| T-16-14 | Information Disclosure | XSS futuro no app compromete o painel (mesma origem) | medium | accept | Risco residual aceito conscientemente para MVP friends-and-family (draft §4.4); mitigado em camadas (HTML isolado + storageKey + conta dedicada + MFA pós-MVP) |
</threat_model>

<verification>
- `curl -sI /painel` → painel.html (não index.html) + `x-frame-options: DENY` + `referrer-policy: no-referrer`.
- Admin loga → lista de contas reais com busca/filtro; msgs IA/mês visível.
- Alterar plano: motivo obrigatório + ConfirmDialog → toast + linha na Auditoria (before/after/reason).
- Troca free↔pro reflete no app após F5.
- Usuário comum → acesso restrito (403).
- Estático: sem service_role no client; sem escrita de plan pelo SDK.
- security-reviewer PASS (vercel.json/rota).
</verification>

<success_criteria>
`/painel` entrega o MVP visual — logar, listar contas reais, trocar tier com motivo+confirmação+auditoria, e ver a mudança refletida no app após F5 — servido antes do catch-all e protegido contra clickjacking, sem jamais tocar `service_role` no client nem escrever `profiles.plan` pelo SDK.
</success_criteria>

<artifacts_this_plan_produces>
- `painel.html` — entrada isolada (storageKey `fides-admin-auth`), single-page com abas `#contas`/`#auditoria`
- `assets/fides-admin.jsx` — telas login / acesso negado / contas (busca+filtro) / alterar plano (motivo+ConfirmDialog) / auditoria
- `assets/fides-admin.css`
- `vercel.json`: rewrite `/painel → /painel.html` (antes do catch-all) + headers `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer` para `/painel`
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/16-painel-admin-backoffice/16-03-SUMMARY.md` when done
</output>
