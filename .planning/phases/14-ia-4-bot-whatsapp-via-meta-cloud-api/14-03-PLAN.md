---
phase: 14-ia-4-bot-whatsapp-via-meta-cloud-api
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/ratelimit-bypass-toolresults.md
  - .planning/todos/done/ratelimit-bypass-toolresults.md
autonomous: false

requirements: [WA-RATELIMIT-01]

must_haves:
  truths:
    - "Está confirmado por evidência (leitura do HEAD + git) que o bypass estrutural do rate-limit via toolResults forjado está fechado desde o commit 9abd83e."
    - "Há uma decisão registrada sobre o gap residual (replay de nonce dentro do TTL de 120s): aceitar como baixo risco OU endurecer."
    - "O todo ratelimit-bypass-toolresults está formalmente fechado (movido para done/ com nota do commit resolvedor) ou reaberto com escopo revisado."
  artifacts:
    - ".planning/todos/done/ratelimit-bypass-toolresults.md"
  key_links:
    - "A decisão de hardening (ou não) é registrada com rationale de severidade — WA-RATELIMIT-01 é o item high herdado da security-review da Phase 11."
---

<objective>
Fechar formalmente WA-RATELIMIT-01 (todo `ratelimit-bypass-toolresults`, high, herdado da Phase 11). O RESEARCH.md (HIGH confidence, via `git log -p` + leitura do HEAD) já provou que o bypass ESTRUTURAL foi corrigido no commit `9abd83e` (Phase 12-02): o gate de cota não é mais pulável enviando `toolResults` forjado sem um nonce válido. Este plano é AUDITORIA + DECISÃO + CLOSEOUT — NÃO é "implementar o fix do zero" (ele já existe).

Purpose: converter uma pendência de segurança "aberta" (que STATE.md e o CONTEXT.md ainda descrevem como não-resolvida) em um item fechado com evidência, e decidir explicitamente sobre o único gap residual conhecido (replay do MESMO nonce dentro da janela de 120s). Encaixa no tema da fase: controle de custo/abuso do canal de IA.
Output: evidência de auditoria no SUMMARY, decisão registrada, e o arquivo de todo movido para `done/` (ou reaberto com escopo revisado).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-RESEARCH.md
@.planning/todos/pending/ratelimit-bypass-toolresults.md
@api/assistant.js
@api/_lib/nonce.js
</context>

## Artifacts this phase produces

Este plano não cria símbolos de código novos (é auditoria/closeout). A lista completa de artefatos da fase está em `14-01-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Auditar que o bypass estrutural está fechado (evidência)</name>
  <files>.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-03-SUMMARY.md</files>
  <read_first>
    - api/assistant.js (linhas 264-327: gate de rate-limit; confirmar que `isFirstCallOfTurn = !hasToolResults || !nonceValid` — toolResults sem nonce válido cai em isFirstCallOfTurn=true e É contado)
    - api/_lib/nonce.js (HMAC stateless, TTL 120s, sem jti/uso-único — origem do gap residual)
    - .planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-RESEARCH.md (§"Achado crítico", linhas 397-416)
    - .planning/todos/pending/ratelimit-bypass-toolresults.md (descrição original do vetor)
  </read_first>
  <action>
    Auditar o código HEAD e registrar a evidência no SUMMARY. Confirmar objetivamente que:
    - A forma antiga vulnerável (`isFirstCallOfTurn = !Array.isArray(toolResults) || toolResults.length === 0`) NÃO existe mais em `api/assistant.js` (grep confirmando ausência).
    - A forma atual em `api/assistant.js` computa `hasToolResults`, valida `nonce.verify(clientNonce, userId, NONCE_SECRET)` e só considera `isFirstCallOfTurn=false` quando há toolResults E nonce válido — ou seja, `toolResults` forjado por um atacante sem o `ASSISTANT_NONCE_SECRET` cai em `isFirstCallOfTurn=true` e conta na cota.
    - Confirmar via `git log --oneline` que o commit `9abd83e` (12-02) é o que introduziu o fix.

    Registrar no SUMMARY a conclusão: "bypass estrutural FECHADO — evidência: HEAD de api/assistant.js + commit 9abd83e".
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; grep -q 'isFirstCallOfTurn = !hasToolResults || !nonceValid' api/assistant.js && grep -q 'nonce.verify' api/assistant.js && ! grep -q 'toolResults.length === 0' api/assistant.js && git log --oneline | grep -q '9abd83e' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A linha vulnerável antiga (`toolResults.length === 0` como condição do gate) NÃO está presente em api/assistant.js (grep negativo passa).
    - A linha atual `isFirstCallOfTurn = !hasToolResults || !nonceValid` está presente.
    - O commit `9abd83e` aparece no `git log`.
    - O SUMMARY registra a conclusão da auditoria com essa evidência.
  </acceptance_criteria>
  <done>Auditoria concluída com evidência de que o bypass estrutural está fechado desde 9abd83e.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decisão sobre o gap residual (replay de nonce dentro do TTL)</name>
  <action>
    Checkpoint de decisão: apresentar ao dono as duas opções abaixo (accept como baixo risco vs harden com nonce de uso único) com o rationale de severidade LOW (Assumption A5 do RESEARCH.md), e registrar a escolha para orientar a Task 3 (closeout). Não escrever código de hardening sem seleção explícita de "harden".
  </action>
  <decision>O gap residual — um nonce válido capturado pode ser reenviado múltiplas vezes dentro da janela de 120s, e cada replay dentro da janela também não conta na cota — merece hardening agora, ou é aceito como baixo risco?</decision>
  <context>
    `api/_lib/nonce.js` é HMAC stateless (sem armazenamento de nonces usados). Impacto do gap: janela de abuso de 120s por nonce capturado, e o atacante já precisa ter feito ≥1 chamada contada para obter um nonce válido. RESEARCH.md Assumption A5 classifica como BAIXO risco (impacto de custo limitado, exige captura prévia via XSS/log vazado). A config de segurança da fase bloqueia em severidade HIGH — este item é LOW, então não bloqueia. A decisão do dono é relevante porque WA-RATELIMIT-01 nasceu como item HIGH na security-review da Phase 11.
  </context>
  <options>
    <option id="accept">
      <name>Aceitar como baixo risco (recomendado)</name>
      <pros>Zero código novo; alinhado com Assumption A5 (LOW); o vetor estrutural já está fechado; o replay exige captura prévia de nonce e só rende 120s de janela.</pros>
      <cons>Persiste uma janela teórica de inflar cota se um nonce vazar.</cons>
    </option>
    <option id="harden">
      <name>Endurecer com nonce de uso único</name>
      <pros>Fecha a janela de replay por completo.</pros>
      <cons>Introduz estado (tabela leve de jti consumido, padrão wa_messages) só para um vetor LOW já mitigado no estrutural; custo/benefício desfavorável no MVP.</cons>
    </option>
  </options>
  <resume-signal>Selecione: accept ou harden. Em "harden", o executor abrirá um plano/task adicional de nonce single-use; em "accept", segue para o closeout (Task 3).</resume-signal>
  <verify>
    <automated>MISSING — decisão humana (checkpoint): a escolha accept/harden é registrada no SUMMARY e propaga para a nota de resolução da Task 3.</automated>
  </verify>
  <done>Decisão accept/harden registrada com rationale de severidade LOW.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar formalmente o todo (mover pending -> done com nota)</name>
  <files>.planning/todos/pending/ratelimit-bypass-toolresults.md, .planning/todos/done/ratelimit-bypass-toolresults.md</files>
  <read_first>
    - .planning/todos/pending/ratelimit-bypass-toolresults.md (conteúdo atual do todo)
  </read_first>
  <action>
    Mover `.planning/todos/pending/ratelimit-bypass-toolresults.md` para `.planning/todos/done/ratelimit-bypass-toolresults.md` (git mv se rastreado). Anexar ao final do arquivo uma nota de resolução: (1) bypass estrutural fechado no commit `9abd83e` (Phase 12-02) via nonce assinado; (2) a decisão da Task 2 sobre o gap residual (accept/harden) com o rationale de severidade LOW; (3) a referência a este plano (14-03) como o closeout formal. Se a Task 2 resultou em "harden", registrar a pendência de hardening como um novo item (não fechar como resolvido sem o hardening); se "accept", fechar como resolvido/não-issue.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; test -f .planning/todos/done/ratelimit-bypass-toolresults.md && ! test -f .planning/todos/pending/ratelimit-bypass-toolresults.md && grep -q '9abd83e' .planning/todos/done/ratelimit-bypass-toolresults.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe em `.planning/todos/done/` e NÃO existe mais em `.planning/todos/pending/`.
    - A nota de resolução cita o commit `9abd83e`, a decisão do gap residual, e o plano 14-03.
    - Se a decisão foi "harden", há um item de acompanhamento aberto para o nonce single-use (o todo não é fechado como resolvido sem ele).
  </acceptance_criteria>
  <done>Todo movido para done/ com nota de resolução; WA-RATELIMIT-01 formalmente fechado (ou com hardening rastreado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente autenticado → gate de cota do assistant.js | Um JWT válido tenta inflar a cota de IA forjando o corpo da requisição. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08 | Denial of Service (custo) | Gate de rate-limit via toolResults forjado | high | mitigate | JÁ mitigado em 9abd83e (nonce assinado): toolResults sem nonce válido conta na cota. Auditado na Task 1. |
| T-14-09 | Repudiation / DoS de custo | Replay de nonce dentro do TTL de 120s | low | accept | Decisão explícita na Task 2 (accept como baixo risco por padrão, ou harden). Não bloqueia (severidade LOW < threshold HIGH). |
| T-14-SC | Tampering | npm installs | high | mitigate | Nenhum pacote novo neste plano. |
</threat_model>

<verification>
- Task 1: greps de presença/ausência sobre api/assistant.js + git log.
- Task 3: presença do arquivo em done/, ausência em pending/, nota com commit.
- Decisão de severidade registrada (Task 2 checkpoint).
</verification>

<success_criteria>
- Bypass estrutural confirmado fechado (evidência no SUMMARY).
- Gap residual decidido (accept/harden) com rationale.
- Todo formalmente fechado em done/ (ou hardening rastreado se harden).
</success_criteria>

<output>
Create `.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-03-SUMMARY.md` when done
</output>
