---
phase: 14-ia-4-bot-whatsapp-via-meta-cloud-api
plan: 06
type: execute
wave: 3
depends_on: [14-05]
files_modified:
  - api/whatsapp.js
autonomous: false

requirements: [WA-OPTIN-01, WA-PARSE-01, WA-CONFIRM-01]

must_haves:
  truths:
    - "Uma mensagem 'Conectar Fides <codigo>' de um número não-vinculado, com código válido e não-expirado em wa_link_codes, vincula o número: seta profiles.phone + wa_linked_at e marca o código consumed_at."
    - "Uma mensagem de lançamento (ex.: 'gastei 45 no mercado hoje no nubank') é parseada por Gemini em JSON estruturado (responseSchema, toolMode NONE), e o valor extraído passa por uma guarda determinística contra a mensagem original."
    - "Toda proposta de lançamento gera um card de confirmação numerado (1 Confirmar / 2 Cancelar / 3 Trocar categoria) e grava wa_pending — NUNCA insere direto (D-1)."
    - "Campo essencial ausente (valor/destino) faz o bot PERGUNTAR, sem gravar wa_pending nem transação."
  artifacts:
    - "api/whatsapp.js (caminho opt-in + parser + proposta)"
  key_links:
    - "buildParsePrompt recebe só data + listas de nomes (minimização WA-LGPD-01); nunca saldos/extrato."
    - "wa_pending guarda o payload proposto; o insert real só acontece no plano 07 após '1'."
---

<objective>
A metade "identificar → propor" da lógica de negócio do webhook, empilhada sobre a espinha de segurança do plano 05 (mesmo arquivo `api/whatsapp.js`): consumo do código de opt-in (WA-OPTIN-01), parser NL→JSON com guarda determinística de valor (WA-PARSE-01) e a confirmação SEMPRE antes do insert (WA-CONFIRM-01, D-1). Este plano NÃO insere transação — ele propõe e persiste a pendência; o insert vive no plano 07.

Purpose: transformar a mensagem de texto do usuário premium num card de confirmação estruturado, resolvendo antes o vínculo do número (opt-in) e sem nunca pular a confirmação (D-1 travado em CONTEXT.md — auto-insert é proibido, nem atrás de flag).
Output: caminhos de opt-in + parser + proposta em `api/whatsapp.js`. Reviewers obrigatórios (caminho sensível).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-CONTEXT.md
@.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-RESEARCH.md
@api/whatsapp.js
@api/_lib/whatsapp/parser-schema.js
@api/_lib/gemini.js
@api/_lib/whatsapp/graph-api.js
@assets/fides-data.jsx
</context>

## Artifacts this phase produces

Este plano adiciona a `api/whatsapp.js` os caminhos de opt-in, parser e proposta (não cria arquivos novos). Lista completa em `14-01-PLAN.md`.

<tasks>

<task type="auto">
  <name>Task 1: Consumo do código de opt-in (vincular phone->user)</name>
  <files>api/whatsapp.js</files>
  <read_first>
    - api/whatsapp.js (o ponto de extensão pós-dedupe/pré-gate deixado no plano 05; a resolução phone→profiles já existe)
    - supabase/wa-schema.sql (wa_link_codes: code/user_id/expires_at/consumed_at; profiles.phone/wa_linked_at)
    - .planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-CONTEXT.md (WA-OPTIN-01: opt-in por código de posse; consentimento registrado em wa_linked_at = base legal LGPD)
  </read_first>
  <action>
    Antes do gate premium (um número não-vinculado precisa poder vincular), detectar mensagens de vínculo: se o texto casa com o padrão do opt-in (`Conectar Fides <codigo>` — o mesmo texto pré-preenchido pelo /api/wa-link do plano 04; extrair o código por regex tolerante a espaços/caixa). Fluxo:
    - Buscar o código em `wa_link_codes` where `code = <codigo>` and `consumed_at is null` and `expires_at > now()`.
    - Código válido: setar `profiles.phone = <from>` e `profiles.wa_linked_at = now()` para o `user_id` do código (client service_role); marcar `wa_link_codes.consumed_at = now()`. Se `from` já estiver vinculado a OUTRO user, tratar (re-vínculo/limpeza) de forma fail-closed — não sobrescrever silenciosamente outro usuário; na dúvida, recusar com mensagem neutra. Responder confirmação curta em PT-BR ("✅ WhatsApp conectado à sua conta Fides."). O `wa_linked_at` registra o consentimento explícito do opt-in (base legal LGPD, WA-LGPD-01).
    - Código inválido/expirado/consumido: responder mensagem neutra ("Código inválido ou expirado. Gere um novo em Perfil → Conectar WhatsApp.") — sem revelar se o número existe em outra conta.
    - Este caminho de vínculo NÃO chama LLM e termina com 200 (não segue para o parser).
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; node --check api/whatsapp.js && grep -q 'wa_link_codes' api/whatsapp.js && grep -q 'wa_linked_at' api/whatsapp.js && grep -q 'consumed_at' api/whatsapp.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Mensagem `Conectar Fides <codigo>` com código válido seta `profiles.phone` + `wa_linked_at` e marca `consumed_at` (verificável via SQL pós-teste: a linha de profiles ganha phone/wa_linked_at, o código ganha consumed_at).
    - Código inválido/expirado responde mensagem neutra, sem vincular e sem revelar existência de conta alheia.
    - O caminho de vínculo não chama Gemini e responde 200.
    - Vínculo de um número já pertencente a outro user é tratado fail-closed (não sobrescreve silenciosamente).
  </acceptance_criteria>
  <done>Opt-in por posse funciona: código válido vincula o número (consentimento em wa_linked_at) e marca o código consumido.</done>
</task>

<task type="auto">
  <name>Task 2: Parser NL->JSON (Gemini structured output) + guarda determinística de valor</name>
  <files>api/whatsapp.js</files>
  <read_first>
    - api/_lib/whatsapp/parser-schema.js (PARSE_SCHEMA + buildParsePrompt — plano 02)
    - api/_lib/gemini.js (buildPayload com toolMode 'NONE' + generationConfig responseSchema; callGemini; parseResponse → textReply é o JSON)
    - assets/fides-data.jsx (linhas 52-64 mesFaturaFor: formato dd/mm + year, default hardcoded 2026 — Pitfall 3: ao derivar mês de cartão no servidor, converter YYYY-MM-DD → {dd/mm, year real}, nunca reusar às cegas)
    - .planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-CONTEXT.md (CU-04 Gemini paid tier; guarda determinística de valor; ES-02 texto numerado)
  </read_first>
  <action>
    Para o usuário premium que passou o gate/caps (plano 05), com mensagem que NÃO é opt-in nem confirmação pendente:
    - Montar o contexto MÍNIMO (minimização WA-LGPD-01): data de hoje (YYYY-MM-DD), lista de chaves/nomes de categorias do usuário e nomes de contas/cartões (buscar via service_role: categorias custom + contas + cartões do userId). NUNCA incluir saldos/valores/extrato no prompt.
    - `payload = gemini.buildPayload({ systemPrompt: buildParsePrompt({hoje, categorias, contas, cartoes}), contents:[{role:'user',parts:[{text: userMessage}]}], toolMode:'NONE', generationConfig:{ temperature:0.2, maxOutputTokens:200, responseMimeType:'application/json', responseSchema: PARSE_SCHEMA } })`. `gemini.callGemini(payload, GEMINI_API_KEY)`. `JSON.parse(parseResponse(data).textReply)` → objeto do schema (try/catch; parse falho → resposta amigável + 200, sem gravar nada).
    - GUARDA DETERMINÍSTICA DE VALOR (WA-PARSE-01): rodar um regex sobre a MENSAGEM ORIGINAL para extrair valores monetários candidatos (padrão BR: R$?, milhar com ponto, decimal com vírgula). Se o `valor` do LLM não corresponder a nenhum valor presente na mensagem (dentro de tolerância), rebaixar para `confianca:'baixa'` e adicionar 'valor' a `faltando` — nunca confiar cegamente no número do LLM.
    - Registrar a telemetria da chamada em `wa_usage` (UPDATE da linha criada no plano 05, fail-open: prompt_tokens/completion_tokens/latency_ms — nunca gravar texto de mensagem, LGPD).
    - Derivação de mês para CARTÃO (Pitfall 3): quando o destino resolvido for cartão, computar o `p_month` da fatura a partir da data ISO — portar/adaptar a lógica de `mesFaturaFor` para o servidor com um adapter explícito YYYY-MM-DD → {dd, mm, year REAL da data} (nunca o default 2026). Para conta, `p_month` = YYYY-MM da data. Guardar esse mês no payload proposto (não inserir ainda).
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; node --check api/whatsapp.js && grep -q 'PARSE_SCHEMA' api/whatsapp.js && grep -q "toolMode" api/whatsapp.js && grep -q 'responseSchema' api/whatsapp.js && grep -Eq 'buildParsePrompt' api/whatsapp.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - O parser usa `buildPayload` com `toolMode:'NONE'` + `responseSchema: PARSE_SCHEMA` (não function calling — WA-PARSE-01 / elimina WR-02 por construção).
    - O prompt recebe SÓ data + listas de nomes (nenhum saldo/valor/extrato — verificável por leitura do objeto de contexto montado).
    - A guarda determinística rebaixa para `confianca:'baixa'` quando o valor do LLM não bate com a mensagem original.
    - Para cartão, o mês é derivado com o ANO REAL da data (adapter explícito), não o default 2026 (Pitfall 3).
    - Telemetria em wa_usage é fail-open e nunca grava texto de mensagem.
  </acceptance_criteria>
  <done>Parser estruturado + guarda de valor produzem um payload proposto confiável (ou pergunta o que falta), com mês de cartão correto.</done>
</task>

<task type="auto">
  <name>Task 3: Card de confirmação numerado + wa_pending (D-1, nunca auto-insert)</name>
  <files>api/whatsapp.js</files>
  <read_first>
    - api/whatsapp.js (o objeto proposto da Task 2)
    - api/_lib/whatsapp/graph-api.js (sendTextMessage)
    - supabase/wa-schema.sql (wa_pending: user_id PK, payload jsonb, expires_at)
    - .planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-CONTEXT.md (CU-01 D-1 confirmar SEMPRE; ES-02 texto numerado "1 Confirmar 2 Cancelar 3 Trocar categoria"; card exemplo no bloco specifics)
  </read_first>
  <action>
    A partir do payload proposto:
    - Se algum campo essencial faltar (`confianca === 'baixa'` OU `faltando` inclui valor/destino): responder uma PERGUNTA curta em PT-BR pedindo só o que falta (ex.: "Qual o valor? E foi em qual conta/cartão?") e NÃO gravar wa_pending nem transação. 200.
    - Se o payload está completo e confiante: montar o CARD de confirmação numerado (ES-02, exemplo do CONTEXT.md): "Confirmar lançamento? 💸 R$ 45,00 · Mercado · Nubank · 05/07 — 1 · Confirmar 2 · Cancelar 3 · Trocar categoria" (formatar valor em R$ padrão BR). Enviar via sendTextMessage.
    - Persistir a proposta em `wa_pending` `{ user_id, payload: <objeto resolvido com IDs de conta/cartão + p_month + status + categoria>, expires_at: now + 10 min }` (upsert por user_id — 1 pendência por usuário; substitui pendência anterior). O `payload` guarda TUDO que o insert do plano 07 precisa (não re-parsear no confirm).
    - NUNCA inserir a transação aqui (D-1 travado — auto-insert proibido, nem atrás de flag). O caminho de insert é exclusivamente o "1" tratado no plano 07.
    - Resolver categoria inexistente: se a categoria proposta não existir, marcar no payload para o card propor criar+lançar (mesma semântica do chat in-app), sem criar nada antes da confirmação.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; node --check api/whatsapp.js && grep -q 'wa_pending' api/whatsapp.js && grep -Eq 'Confirmar|1 · Confirmar|Trocar categoria' api/whatsapp.js && grep -q 'sendTextMessage' api/whatsapp.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Payload incompleto → o bot pergunta o que falta e NÃO grava wa_pending nem transação (verificável: nenhuma linha em wa_pending para esse fluxo).
    - Payload completo → envia card numerado (1/2/3) e grava exatamente 1 linha em wa_pending por usuário (upsert), com todo o payload necessário para o insert.
    - Em NENHUM caminho deste plano ocorre INSERT em transactions / chamada ao RPC wa_log_transaction_service (grep: `wa_log_transaction_service` NÃO aparece neste plano — o insert é do plano 07).
    - `wa_pending.expires_at` definido (janela ~10 min).
  </acceptance_criteria>
  <done>Toda proposta vira card numerado + wa_pending; nada é inserido sem confirmação (D-1 respeitado por construção).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Texto do usuário → Gemini | Mensagem vai ao LLM; minimização garante que nenhum dado financeiro sensível acompanhe. |
| Saída do LLM → payload de insert | JSON do modelo não é confiável cegamente; guarda determinística de valor + confirmação humana. |
| Código de opt-in → vínculo de identidade | Código de posse liga um número a um user_id; validade/consumo controlam a janela. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-20 | Tampering | LLM alucina valor/destino | high | mitigate | Guarda determinística de valor (regex sobre a mensagem) + confirmação SEMPRE (D-1) antes de qualquer insert. |
| T-14-21 | Spoofing | Sequestro de vínculo via código roubado | medium | mitigate | Código de `crypto.randomBytes` + `expires_at` curto + `consumed_at` (uso único); vínculo de número já-de-outro-user é fail-closed. |
| T-14-22 | Information Disclosure | Dado financeiro no prompt do LLM | medium | mitigate | buildParsePrompt só recebe data + nomes; nunca saldos/extrato (WA-LGPD-01). |
| T-14-23 | Repudiation | Fatura de cartão no ano errado | medium | mitigate | Adapter explícito YYYY-MM-DD → ano real ao derivar mês (Pitfall 3); nunca o default 2026. |
| T-14-SC | Tampering | npm installs | high | mitigate | Zero pacotes novos. |
</threat_model>

<verification>
- `node --check api/whatsapp.js` + greps de cada caminho.
- UAT (checkpoint no plano 07/verify-work): "gastei no mercado" sem valor → pergunta, nada gravado; lançamento completo → card + wa_pending, nenhuma transação ainda.
- Protótipo Gemini (plano 02) já provou responseSchema+toolMode NONE.
</verification>

<success_criteria>
- Opt-in vincula número; parser produz payload confiável com mês de cartão correto; toda proposta vira card + wa_pending; zero insert sem confirmação.
- security-reviewer + database-reviewer PASS (rodar antes do commit; caminho sensível).
</success_criteria>

<output>
Create `.planning/phases/14-ia-4-bot-whatsapp-via-meta-cloud-api/14-06-SUMMARY.md` when done
</output>
