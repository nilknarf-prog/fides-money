---
phase: 01-veracidade-do-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - assets/fides-charts.jsx
  - assets/fides-studio.jsx
autonomous: false
requirements: [R2]
must_haves:
  truths:
    - "Passar o mouse sobre uma fatia do donut no desktop mostra '{categoria} · {R$ valor} · {%}' no centro do donut."
    - "Tocar uma fatia do donut no mobile mostra a mesma identificação (categoria + valor + %)."
    - "Tocar/clicar fora do donut fecha a identificação e o centro volta a 'Total'."
    - "Donut com 1 fatia (100%) ainda mostra a identificação ao interagir."
    - "O percentual exibido = val da fatia / total computado dentro do Donut (D-05)."
  artifacts:
    - path: "assets/fides-charts.jsx"
      provides: "Donut com activeIdx state + handlers hover/tap + prop opcional onActiveSlice"
      contains: "onActiveSlice"
    - path: "assets/fides-studio.jsx"
      provides: "centro do donut dinâmico (activeSlice) + dismiss via document listener"
      contains: "activeSlice"
  key_links:
    - from: "assets/fides-charts.jsx (Donut paths)"
      to: "consumidor (fds-donut-center)"
      via: "callback onActiveSlice(arcs[i] | null) comunica a fatia ativa ao consumidor"
      pattern: "onActiveSlice\\?\\."
    - from: "assets/fides-studio.jsx (fds-donut-center)"
      to: "activeSlice state"
      via: "render condicional do centro: activeSlice ? label·val·pct : Total"
      pattern: "activeSlice \\?"
  prohibitions:
    - statement: "NÃO renderizar o centro (label/valor) DENTRO do SVG do Donut — o centro é HTML no consumidor; renderizá-lo no Donut conflitaria com o fds-donut-center existente em fides-studio.jsx e fides-dashboard.jsx."
      status: resolved
      verification: "source assertion — o Donut só expõe a fatia ativa via callback onActiveSlice; não adiciona <text>/<foreignObject> de centro no SVG."
    - statement: "onActiveSlice DEVE ser prop opcional (chamada via ?.) — o consumidor /teste (fides-dashboard.jsx:249) não passa o callback e não pode quebrar."
      status: resolved
      verification: "source assertion — toda chamada do callback usa `onActiveSlice?.(...)`."
---

<objective>
Corrigir Issue #2 (R2): cada fatia do donut "Para onde foi" passa a ser identificável — hover no desktop e tap no mobile revelam categoria + valor (BRL) + % do total; tocar/clicar fora fecha. A exibição reusa o CENTRO do donut existente (D-03), zero risco de overflow na borda 400px.

Purpose: o usuário consegue saber que categoria é a fatia (hoje a fatia vermelha dominante é anônima), no desktop e no mobile.
Output: `Donut` em `fides-charts.jsx` (arquivo protegido — edição aprovada D-01) com `activeIdx` state + handlers `onMouseEnter`/`onMouseLeave`/`onPointerDown` nas paths + prop opcional `onActiveSlice`; consumidor em `fides-studio.jsx` com `activeSlice` state, centro condicional e dismiss "tap fora" via listener de documento.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-veracidade-do-dashboard/01-SPEC.md
@.planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md
@.planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md
@.planning/phases/01-veracidade-do-dashboard/01-CONTEXT.md
@assets/fides-charts.jsx
@assets/fides-studio.jsx
</context>

## Artifacts this phase produces (Plan 02 / R2)

- `assets/fides-charts.jsx` — `Donut`: nova prop opcional `onActiveSlice`; novo state `const [activeIdx, setActiveIdx] = React.useState(null)`; handlers `handleEnter`/`handleLeave`/`handleTap`; cada `<path>` ganha `onMouseEnter`/`onMouseLeave`/`onPointerDown` + `style` com `cursor:'pointer'` e dimming da fatia inativa.
- `assets/fides-studio.jsx` — `DashboardStudio`: novo state `const [activeSlice, setActiveSlice] = React.useState(null)`; ref `donutWrapRef`; `useEffect` de dismiss via `document.addEventListener('pointerdown', ...)`; `<Donut ... onActiveSlice={setActiveSlice}/>`; `.fds-donut-center` com render condicional (`activeSlice ? label·val·pct : Total`).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar interação de fatia ao Donut (fides-charts.jsx — protegido, edição aprovada)</name>
  <files>assets/fides-charts.jsx</files>
  <read_first>
    - assets/fides-charts.jsx §115-151 (`Donut`) — assinatura, cálculo de `total` (§117), array `arcs` (§123-135), render dos paths (§147).
    - .planning/phases/01-veracidade-do-dashboard/01-CONTEXT.md §"decisions" — D-01 (edição aprovada, toque restrito ao necessário), D-02 (estado + handlers DENTRO do Donut), D-05 (% = val/total).
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"R2" — patterns de activeIdx + handlers + augment dos paths.
    - .planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md §"Pitfall 5" — onActiveSlice DEVE ser opcional (consumidor /teste não passa).
  </read_first>
  <action>
    Estender a assinatura do `Donut` (§116) adicionando a prop opcional `onActiveSlice` ao destructure: `function Donut({ data, size = 200, thickness = 22, gap = 0.012, accent = '#B45309', glow = false, onActiveSlice })`. Adicionar dentro do componente, logo após a assinatura: `const [activeIdx, setActiveIdx] = React.useState(null);` (mesmo estilo `React.useState` usado no projeto). Manter `total` (§117) e `arcs` (§123-135) sem nenhuma alteração — `arcs[i]` já carrega `{ d, tint, label, val }` (D-02: geometria já existe). Adicionar três handlers (D-04: hover revela no desktop, tap revela no mobile, dismiss ao sair/tocar fora): `handleEnter(i)` faz `setActiveIdx(i)` e `onActiveSlice?.(arcs[i])`; `handleLeave()` faz `setActiveIdx(null)` e `onActiveSlice?.(null)`; `handleTap(i, e)` faz `e.stopPropagation()` e toggla — se `i === activeIdx` então `setActiveIdx(null)` + `onActiveSlice?.(null)`, senão `setActiveIdx(i)` + `onActiveSlice?.(arcs[i])`. TODA chamada do callback usa `?.` (Pitfall 5 — consumidor /teste não passa o prop). Augmentar o render dos paths (§147) de `{arcs.map((a, i) => <path key={i} d={a.d} fill={a.tint}/>)}` para incluir em cada `<path>`: `onMouseEnter={() => handleEnter(i)}`, `onMouseLeave={handleLeave}`, `onPointerDown={(e) => handleTap(i, e)}`, e `style={{ cursor: 'pointer', opacity: activeIdx != null && activeIdx !== i ? 0.55 : 1, transition: 'opacity 0.15s' }}` (dimming das fatias inativas é melhoria de legibilidade dentro do escopo do tooltip — D-01 "toque restrito ao necessário"). NÃO adicionar nenhum elemento de centro (`<text>`/`<foreignObject>`) ao SVG — o centro é responsabilidade do consumidor (Prohibition: evitar conflito com fds-donut-center). NÃO tocar em mais nada do `fides-charts.jsx` (arquivo protegido).
  </action>
  <verify>
    <automated>grep -n "onActiveSlice" assets/fides-charts.jsx && grep -c "onActiveSlice?\." assets/fides-charts.jsx | { read n; [ "$n" -ge 3 ]; } && grep -n "onPointerDown" assets/fides-charts.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: a assinatura do `Donut` inclui `onActiveSlice` no destructure de props.
    - Source: existe `const [activeIdx, setActiveIdx] = React.useState(null)` dentro do `Donut`.
    - Source: cada `<path>` de fatia tem `onMouseEnter`, `onMouseLeave` e `onPointerDown`.
    - Source: toda invocação do callback usa `onActiveSlice?.(` (opcional — consumidor /teste não quebra).
    - Source: NÃO há `<text>` nem `<foreignObject>` de centro adicionado ao SVG do Donut.
    - Source: `total` e `arcs` permanecem inalterados (mesmas linhas §117/§123-135).
  </acceptance_criteria>
  <done>Donut expõe a fatia ativa via callback opcional onActiveSlice; paths têm handlers hover (desktop) e pointerdown (mobile, toggle); centro não é renderizado dentro do SVG.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire do centro do donut + dismiss "tap fora" em fides-studio.jsx</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx §826-832 (`fds-donut-wrap` + `fds-donut-center`) — superfície de exibição a tornar condicional.
    - assets/fides-studio.jsx §688 (`const [flowMode, setFlowMode] = React.useState('fluxo')`) — estilo de declaração de state a seguir para `activeSlice`.
    - assets/fides-studio.jsx §665 (`totalSpend`) — usado para o % da fatia.
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"donut center" e §"Dismiss pattern" — render condicional e useEffect de dismiss.
    - .planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md §"Pitfall 2" — `.fds-donut-center` tem `pointer-events:none`; dismiss precisa de listener no documento.
  </read_first>
  <action>
    Adicionar, junto às outras declarações de state do `DashboardStudio` (próximo a §688, onde está `flowMode`): `const [activeSlice, setActiveSlice] = React.useState(null);` e `const donutWrapRef = React.useRef(null);`. Adicionar um `useEffect` de dismiss que só age quando há fatia ativa: quando `activeSlice` é truthy, registrar `document.addEventListener('pointerdown', handler)` onde `handler(e)` chama `setActiveSlice(null)` se `donutWrapRef.current` existe e NÃO contém `e.target` (`!donutWrapRef.current.contains(e.target)`); retornar cleanup que remove o listener; dependência `[activeSlice]` (Pitfall 2: `.fds-donut-center` é `pointer-events:none`, então dismiss precisa de listener global, não de onClick no wrapper). No JSX do donut (§826-832): adicionar `ref={donutWrapRef}` ao `<div className="fds-donut-wrap">`; passar `onActiveSlice={setActiveSlice}` ao `<Donut .../>`; tornar o conteúdo do `.fds-donut-center` condicional — quando `activeSlice` truthy renderizar três linhas: `<div className="fds-donut-label">{activeSlice.label}</div>`, `<div className="fds-donut-value">{fmtBRL(activeSlice.val, { compact: true })}</div>`, e uma terceira `<div className="fds-donut-label">{Math.round((activeSlice.val / totalSpend) * 100)}%</div>` (D-05: % = val/total; usar `totalSpend` já computado §665); quando `activeSlice` é null, manter o estado atual ("Total" + `fmtBRL(totalSpend, { compact: true })`). Usar `fmtBRL` para todo valor (nunca formatar BRL manualmente). NÃO alterar o CSS `.fds-donut-center` (mantém `pointer-events:none`). NÃO redesenhar — só tornar o conteúdo dinâmico.
  </action>
  <verify>
    <automated>grep -n "const \[activeSlice, setActiveSlice\]" assets/fides-studio.jsx && grep -n "onActiveSlice={setActiveSlice}" assets/fides-studio.jsx && grep -n "donutWrapRef" assets/fides-studio.jsx && grep -n "addEventListener('pointerdown'" assets/fides-studio.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: existe `const [activeSlice, setActiveSlice] = React.useState(null)` e `const donutWrapRef = React.useRef(null)` no DashboardStudio.
    - Source: o `<Donut>` em §826-832 recebe `onActiveSlice={setActiveSlice}` e o wrap tem `ref={donutWrapRef}`.
    - Source: existe um `useEffect` com `document.addEventListener('pointerdown', ...)`, cleanup com `removeEventListener`, e dependência `[activeSlice]`.
    - Source: o centro renderiza condicionalmente `activeSlice.label`, `fmtBRL(activeSlice.val, {compact:true})` e `Math.round((activeSlice.val / totalSpend) * 100)%` quando há fatia ativa; senão "Total".
    - Behavioral (pós-deploy, desktop): hover numa fatia mostra "{categoria} · {R$ valor} · {%}" no centro; hover-out volta a "Total".
    - Behavioral (pós-deploy, mobile 400px): tap numa fatia mostra a mesma info; tap fora fecha.
  </acceptance_criteria>
  <done>O centro do donut exibe a fatia ativa (label + valor + %) ao interagir e volta a "Total" ao sair/tocar fora; dismiss funciona no mobile via listener de documento.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar tooltip de fatia (hover/tap/dismiss) + /teste</name>
  <action>Pausar para verificação humana pós-deploy. Apresentar ao usuário o que foi construído e os passos de verificação; aguardar o sinal de retorno antes de prosseguir.</action>
  <what-built>Donut com interação por fatia: hover (desktop) e tap (mobile) revelam categoria + valor + % no centro do donut; tap/clique fora fecha. Centro reusado (D-03), zero tooltip flutuante. Mudança no Donut também afeta o /teste (fallback) — onActiveSlice é opcional, então /teste não quebra.</what-built>
  <how-to-verify>
    1. Após deploy, abrir https://fides-money.vercel.app no Dashboard (Capítulo II "Para onde foi"), em desktop.
    2. Passar o mouse sobre a fatia vermelha dominante: o centro deve trocar de "Total" para "{categoria} · {R$ valor} · {%}"; ao tirar o mouse, volta a "Total".
    3. Abrir em 400×512px (iOS Safari real ou DevTools mobile emulation): TOCAR uma fatia → centro mostra a info da fatia; TOCAR fora do donut → centro volta a "Total". Confirmar que nada corta na borda esquerda/direita.
    4. Abrir https://fides-money.vercel.app/teste e confirmar que o donut do fallback continua mostrando "Total" normalmente (sem erro de console, sem centro duplicado).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que diverge (hover, tap, dismiss, ou regressão no /teste).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → DOM (pointer events) | eventos de hover/tap sobre o SVG; nenhum input textual nem dado persistido cruza aqui — interação puramente local de exibição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | useEffect com document listener | mitigate | Listener só é registrado quando `activeSlice` é truthy e é removido no cleanup (dependência `[activeSlice]`) — sem vazamento de listeners acumulados |
| T-02-02 | Tampering | regressão no consumidor /teste (fides-dashboard.jsx) | mitigate | `onActiveSlice` é prop opcional (`?.`); /teste não passa o callback e mantém comportamento atual — verificado no checkpoint humano |
| T-02-03 | Information Disclosure | label/valor da fatia exibidos | accept | Dados são do próprio usuário (gastos por categoria já visíveis na lista ao lado); exibir no centro não expõe nada novo |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (tooltip = estado React + SVG inline; RESEARCH: zero libs externas) |
</threat_model>

<verification>
- Donut expõe fatia ativa via callback opcional; paths têm handlers hover + pointerdown.
- Consumidor renderiza centro condicional (label + valor + %) e dismiss via listener de documento.
- Sem regressão no /teste (onActiveSlice opcional).
- Funciona em 400×512px sem corte na borda (centro reusado, não tooltip flutuante).
</verification>

<success_criteria>
- Hover sobre fatia (desktop) mostra categoria + valor + %.
- Tap sobre fatia (mobile) mostra a mesma info; tap fora fecha.
- /teste continua funcionando sem centro duplicado.
</success_criteria>

<output>
Create `.planning/phases/01-veracidade-do-dashboard/01-02-SUMMARY.md` when done
</output>
