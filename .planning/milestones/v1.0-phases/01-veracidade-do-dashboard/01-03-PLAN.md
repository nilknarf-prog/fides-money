---
phase: 01-veracidade-do-dashboard
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - assets/fides-studio.jsx
autonomous: false
requirements: [R3]
must_haves:
  truths:
    - "Para um mês com saldo de contas positivo suficiente, o hero mostra valor positivo ('terminará … livre') em vez de negativo, mesmo com fluxo do mês negativo."
    - "O número de fechamento = Σ(balance das contas, excluindo cartões) + receitas previstas não recebidas − despesas previstas em aberto, para o mês selecionado."
    - "Transações já liquidadas (settled / status 'pago') NÃO são recontadas no saldo projetado."
    - "A headline alterna 'terminará … livre' (saldoProjetado ≥ 0) vs 'fechará … no vermelho' (< 0) com base no novo número."
    - "Sem contas / saldo negativo / mês 100% liquidado (sem pendentes) → projeção = saldo atual das contas; nunca quebra (NaN) nem reconta."
  artifacts:
    - path: "assets/fides-studio.jsx"
      provides: "saldoProjetado inline + hero headline baseada no novo número + fluxo mensal negativo ainda visível"
      contains: "saldoProjetado"
  key_links:
    - from: "assets/fides-studio.jsx (saldoProjetado)"
      to: "accounts (de useFides) + pendentes do mês"
      via: "saldoContas = Σ(accounts.balance excl. cartões); + receitasPendentes − despesasPendentes"
      pattern: "accounts\\.reduce"
    - from: "assets/fides-studio.jsx (EditorialHero headline)"
      to: "saldoProjetado"
      via: "headline alterna livre/no vermelho pelo sinal de saldoProjetado; splitBRL(Math.abs(saldoProjetado))"
      pattern: "saldoProjetado >= 0"
  prohibitions:
    - statement: "NÃO apresentar o saldo projetado positivo de forma que esconda um fluxo mensal genuinamente negativo (receitas − despesas < 0) — o fluxo negativo deve permanecer visível (Prohibition P1)."
      status: resolved
      verification: "judgment — revisão humana confirma que, com saldoProjetado ≥ 0 mas fluxoMensal < 0, o hero ainda comunica o déficit do fluxo (métricas Receitas/Despesas mantidas + texto no lede); source — métricas Receitas/Despesas pagas/Em aberto NÃO removidas; variável fluxoMensal preservada."
    - statement: "NÃO contar transações já liquidadas (settled/'pago') no saldo projetado — elas já entraram em balance via recalc_account_balance; recontá-las é dupla contagem."
      status: resolved
      verification: "source — receitasPendentes/despesasPendentes filtram apenas status 'pendente' (não 'pago'); behavioral — mês 100% liquidado: saldoProjetado === saldoContas."
---

<objective>
Corrigir Issue #3 (R3, Opção A): o número de fechamento do hero deixa de ser fluxo isolado e passa a ser saldo projetado real, incorporando o saldo atual das contas. Hoje `saldoFinal = receitaTotal − despesas − pendentes` ignora `accounts.balance` (~R$7.346 em contas correntes + Reserva), por isso o hero anuncia "−R$655 no vermelho" sem motivo.

Purpose: o hero reflete a verdade financeira — saldo positivo das contas + pendentes do mês — sem esconder o fluxo mensal negativo quando existir (P1, diferencial declarado do produto: sem projeções enganosas).
Output: `saldoProjetado` inline em `DashboardStudio`; headline do `EditorialHero` baseada no novo número; `fluxoMensal` preservado e visível; sem dupla contagem de transações liquidadas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-veracidade-do-dashboard/01-SPEC.md
@.planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md
@.planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md
@assets/fides-studio.jsx
</context>

## Artifacts this phase produces (Plan 03 / R3)

- `assets/fides-studio.jsx` — `DashboardStudio`: novas derivações `saldoContas` (Σ accounts.balance, null-safe), `receitasPendentes` / `despesasPendentes` (apenas transações pendentes do mês, exclui transferências), `saldoProjetado = saldoContas + receitasPendentes − despesasPendentes`; `fluxoMensal` (renomeado de `saldoFinal`, preservado para P1); `splitBRL(Math.abs(saldoProjetado))`; headline do hero alternando "terminará livre" / "fechará no vermelho" pelo sinal de `saldoProjetado`; texto no lede comunicando déficit de fluxo quando `fluxoMensal < 0 && saldoProjetado >= 0`.

<tasks>

<!-- planner-discipline-allow: saldoFinal -->
<!-- planner-region-allow: saldoFinal -->
<!-- Justificativa: `saldoFinal` é o identificador EXISTENTE sendo renomeado para `fluxoMensal`.
     Ele precisa ser nomeado nos <action>/<read_first> para instruir a renomeação, e o gate
     `grep -c saldoFinal == 0` é a verificação legítima de que a renomeação ficou completa.
     Tanto Task 1 (rename) quanto Task 2 (headline) tocam o mesmo arquivo durante a transição. -->

<task type="auto" tdd="false">
  <name>Task 1: Derivar saldoProjetado inline (exclui cartões, sem dupla contagem)</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx §625 (destructure de useFides — `accounts` já disponível).
    - assets/fides-studio.jsx §644-663 (derivações `flow`, `receitaTotal`, `despesas`, `pendentes`, `saldoFinal`, `splitBRL`) — base e ponto de troca.
    - .planning/PROJECT.md §4 — "balance = opening_balance + SUM(cleared); Pendente nunca afeta saldo" + "tx.acct = id de conta OU cartão" + "is_transfer exclui de agregações".
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"saldoProjetado" — fórmula e guarda null-safe.
    - .planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md §"Pitfall 3" (NaN), §"Pitfall 4" (excluir transferências), §"Anti-Patterns" (não filtrar accounts por tipo; não recontar settled).
  </read_first>
  <action>
    Em `DashboardStudio`, após o bloco de derivações de fluxo (após §652): adicionar `const saldoContas = accounts.reduce((s, a) => s + (a.balance || 0), 0);` — guarda `(a.balance || 0)` previne NaN (Pitfall 3); NÃO filtrar `accounts` por tipo (Anti-Pattern: `accounts` e `cards` são arrays SEPARADOS no store, cartões nunca estão em `accounts` — exclusão é estrutural). Derivar os pendentes a partir do `flow` já existente (que já tem `!t.isTransfer` — Pitfall 4): `const receitasPendentes = flow.filter(t => t.val > 0 && t.status === 'pendente').reduce((s, t) => s + t.val, 0);` e `const despesasPendentes = flow.filter(t => t.val < 0 && t.status === 'pendente').reduce((s, t) => s + Math.abs(t.val), 0);`. Usar `t.status === 'pendente'` (a UI status mapeada já existente, mesma usada na linha §651 para `pendentes`) — é o sinal canônico de "não liquidado" em ambos os modos, evitando a ambiguidade live/mock do campo `settled` (Anti-Pattern: não recontar settled — só pendentes entram sobre o saldo). Calcular `const saldoProjetado = saldoContas + receitasPendentes - despesasPendentes;`. RENOMEAR a variável `saldoFinal` (§652) para `fluxoMensal` (mantém o mesmo cálculo `receitaTotal - despesas - pendentes`) — preserva o fluxo mensal para P1. Atualizar todas as referências de `saldoFinal` que NÃO sejam a headline/valor do hero — especificamente §659 `deltaSaldo = saldoFinal - prevSaldo` passa a usar `fluxoMensal` (a comparação com o mês anterior é de fluxo, não de saldo projetado; manter coerência: `prevSaldo` é fluxo do mês anterior). Trocar `splitBRL(Math.abs(saldoFinal))` (§663) para `splitBRL(Math.abs(saldoProjetado))`. NÃO mover o cálculo para o store (fora do escopo — derivação inline segue o padrão atual).
  </action>
  <verify>
    <automated>grep -n "const saldoProjetado =" assets/fides-studio.jsx && grep -n "const saldoContas =" assets/fides-studio.jsx && grep -n "const fluxoMensal =" assets/fides-studio.jsx</automated>
    <automated>grep -c "saldoFinal" assets/fides-studio.jsx | { read n; [ "$n" -eq 0 ]; } && grep -n "splitBRL(Math.abs(saldoProjetado))" assets/fides-studio.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: existe `const saldoContas = accounts.reduce((s, a) => s + (a.balance || 0), 0)` (guarda null-safe, sem filtro de tipo).
    - Source: `receitasPendentes` e `despesasPendentes` filtram `t.status === 'pendente'` e derivam de `flow` (que já exclui transferências).
    - Source: `const saldoProjetado = saldoContas + receitasPendentes - despesasPendentes`.
    - Source: a variável `saldoFinal` foi renomeada para `fluxoMensal` e não há mais nenhuma ocorrência de `saldoFinal` no arquivo.
    - Source: `splitBRL` usa `Math.abs(saldoProjetado)`.
    - Source: NÃO há filtro de `accounts` por tipo/cartão (exclusão é estrutural).
    - Behavioral (pós-deploy): mês 100% liquidado (sem pendentes) → saldoProjetado === saldoContas (sem dupla contagem).
  </acceptance_criteria>
  <done>saldoProjetado derivado inline = saldoContas + pendentes a receber − pendentes a pagar; cartões excluídos estruturalmente; settled não recontado; fluxoMensal preservado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hero headline pelo novo número + manter fluxo negativo visível (P1)</name>
  <files>assets/fides-studio.jsx</files>
  <read_first>
    - assets/fides-studio.jsx §722-782 (`EditorialHero`) — headline (§724-732), lede (§733-739), métricas Receitas/Despesas/Em aberto (§748-781).
    - .planning/phases/01-veracidade-do-dashboard/01-SPEC.md §"Prohibitions" P1 — fluxo negativo deve permanecer visível.
    - .planning/phases/01-veracidade-do-dashboard/01-RESEARCH.md §"Open Questions" Q1 — recomendação de texto no lede quando fluxo negativo + saldo positivo.
    - .planning/phases/01-veracidade-do-dashboard/01-PATTERNS.md §"headline" — substituição saldoFinal → saldoProjetado mantendo o estilo exato.
  </read_first>
  <action>
    Na headline do `EditorialHero` (§724-732): trocar TODAS as referências de `saldoFinal` por `saldoProjetado` — mantendo o estilo EXATO: `{saldoProjetado >= 0 ? 'Você terminará' : 'Você fechará'} ...`, cor `saldoProjetado < 0 ? 'var(--bad)' : 'var(--accent)'`, prefixo `{saldoProjetado < 0 ? '−R$' : 'R$'}`, e sufixo `{saldoProjetado >= 0 ? 'livre' : 'no vermelho'}`. NÃO remover nem alterar as métricas Receitas / Despesas pagas / Em aberto (§748-781) — elas mostram o fluxo do mês e DEVEM permanecer (Prohibition P1). Para tornar o déficit de fluxo explícito quando o saldo projetado é positivo mas o fluxo é negativo: adicionar ao `lede` (§733-739) um trecho condicional que aparece somente quando `fluxoMensal < 0 && saldoProjetado >= 0` — texto curto comunicando que, apesar do saldo positivo, o fluxo do mês é negativo, exibindo o valor do déficit com `fmtBRL(Math.abs(fluxoMensal))` (ex.: "Apesar do saldo positivo, o fluxo do mês é negativo (−{valor})."). Usar `fmtBRL` para o valor. NÃO redesenhar o hero — só inserir o trecho condicional no lede e trocar a variável da headline. Confirmar que `deltaSaldo` (já passado a `fluxoMensal` na Task 1) continua coerente no lede/métricas.
  </action>
  <verify>
    <automated>grep -c "saldoProjetado >= 0" assets/fides-studio.jsx | { read n; [ "$n" -ge 2 ]; } && grep -n "fluxoMensal < 0 && saldoProjetado >= 0" assets/fides-studio.jsx && grep -n "label: 'Receitas'" assets/fides-studio.jsx && grep -n "label: 'Em aberto'" assets/fides-studio.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: a headline usa `saldoProjetado` em todas as 4 ramificações (saudação, cor, prefixo de moeda, sufixo livre/vermelho); não há `saldoFinal`.
    - Source: as métricas `label: 'Receitas'`, `label: 'Despesas pagas'` e `label: 'Em aberto'` continuam presentes no array de `metrics` (P1 — fluxo visível).
    - Source: existe um trecho condicional no lede ativado por `fluxoMensal < 0 && saldoProjetado >= 0` que exibe `fmtBRL(Math.abs(fluxoMensal))`.
    - Behavioral (pós-deploy): com contas ~R$7.346 e fluxo do mês negativo, o hero mostra "terminará … livre" (positivo) E o lede/métricas comunicam o déficit do fluxo.
    - Behavioral (pós-deploy): quando saldoProjetado < 0, headline mostra "fechará … no vermelho".
  </acceptance_criteria>
  <done>Headline alterna livre/no vermelho pelo saldoProjetado; métricas de fluxo preservadas; déficit de fluxo comunicado no lede quando saldo positivo mas fluxo negativo (P1 satisfeito).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar saldo projetado no hero + fluxo negativo visível (P1)</name>
  <action>Pausar para verificação humana pós-deploy. Apresentar ao usuário o que foi construído e os passos de verificação; aguardar o sinal de retorno antes de prosseguir.</action>
  <what-built>O número de fechamento do hero agora é o saldo projetado real (saldo das contas + pendentes a receber − pendentes a pagar), não mais o fluxo isolado. A headline alterna "terminará livre" / "fechará no vermelho" pelo novo número. As métricas Receitas/Despesas/Em aberto continuam visíveis e, quando o saldo é positivo mas o fluxo do mês é negativo, o lede comunica explicitamente o déficit do fluxo (P1).</what-built>
  <how-to-verify>
    1. Após deploy, abrir https://fides-money.vercel.app no Dashboard, no mês real do usuário (Junho), com saldo de contas ~R$7.346.
    2. Confirmar que o hero mostra valor POSITIVO ("Você terminará Junho com R$… livre") em vez do antigo "−R$655 no vermelho".
    3. Confirmar que as métricas Receitas / Despesas pagas / Em aberto continuam aparecendo no hero.
    4. Se o fluxo do mês (receitas − despesas − pendentes) for negativo, confirmar que o lede comunica o déficit ("Apesar do saldo positivo, o fluxo do mês é negativo (−R$…)") — o número negativo NÃO foi escondido (P1).
    5. Conferir um mês 100% liquidado (sem pendentes): o saldo projetado deve igualar exatamente o saldo das contas (sem dupla contagem).
    6. Conferir que, num cenário de saldo projetado negativo, a headline mostra "fechará … no vermelho".
    7. Verificar em 400×512px que o hero não quebra.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que diverge (valor do saldo, headline, visibilidade do fluxo, ou dupla contagem).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store (accounts.balance + transações) → cálculo de exibição | dados já carregados e derivados em memória; nenhuma entrada nova do usuário; risco é de cálculo incorreto (engano), não de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | saldoContas (reduce sobre balance) | mitigate | Guarda `(a.balance || 0)` evita NaN quando balance é null/undefined (Pitfall 3) — número exibido nunca quebra |
| T-03-02 | Repudiation | dupla contagem de transações liquidadas | mitigate | Só `status === 'pendente'` entra sobre o saldo; settled/'pago' já está em balance — evita inflar o número projetado (engano que violaria o princípio de honestidade do produto) |
| T-03-03 | Information Disclosure | exibição de saldo agregado | accept | Saldo é do próprio usuário, já exibido em Contas; nenhuma exposição nova |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (cálculo inline em JS existente; RESEARCH: zero libs externas) |
</threat_model>

<verification>
- saldoProjetado = saldoContas + pendentes a receber − pendentes a pagar; cartões excluídos; settled não recontado.
- Headline alterna livre/no vermelho pelo novo número.
- Fluxo mensal negativo permanece visível (métricas + lede condicional) — P1.
- Sem NaN quando accounts vazio ou balance null.
</verification>

<success_criteria>
- Hero usa saldo projetado = saldo das contas (excl. cartões) + pendentes a receber − pendentes a pagar.
- Transações liquidadas não contadas duas vezes.
- Headline alterna corretamente livre / no vermelho.
- Fluxo mensal negativo continua visível (P1).
</success_criteria>

<output>
Create `.planning/phases/01-veracidade-do-dashboard/01-03-SUMMARY.md` when done
</output>
