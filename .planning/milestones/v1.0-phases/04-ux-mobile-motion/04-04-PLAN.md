---
phase: 04-ux-mobile-motion
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - assets/fides-studio.css
  - assets/fides-store.css
autonomous: true
requirements: [MOTION-02]
must_haves:
  truths:
    - "No desktop, hover em card de categoria (.cat-card) e de conta (.stu-acct) mostra elevação + sombra"
    - "No mobile (touch), tocar um card mostra scale-down em :active e NÃO deixa hover 'grudado' após soltar (:hover isolado em @media (hover:hover))"
    - "Nenhuma transição de transform/opacity de card excede 300ms sem GPU hint"
    - "Com prefers-reduced-motion: reduce, os cards não animam"
  artifacts:
    - path: "assets/fides-studio.css"
      provides: ".stu-acct (conta): :hover lift isolado em @media (hover:hover) + :active scale + GPU hint + gate reduced-motion"
      contains: "hover: hover"
    - path: "assets/fides-store.css"
      provides: ".cat-card (categoria): :hover lift isolado em @media (hover:hover) + :active scale + GPU hint"
      contains: "hover: hover"
  key_links:
    - from: "assets/fides-studio.css (.stu-acct)"
      to: "viewport com hover (desktop)"
      via: "@media (hover:hover) isola o :hover para evitar lift grudado em touch"
      pattern: "hover: hover"
---

<objective>
Adicionar micro-feedback CSS puro em cards de categoria e conta: `:hover` lift no desktop (isolado em `@media (hover:hover)`) e `:active` scale-down no toque mobile, com GPU hint e gate reduced-motion. Implementa MOTION-02 + dobra MOTION-PERF na camada de cards.

Purpose: Hoje `.stu-acct` já eleva no `:hover` mas SEM isolamento `@media (hover:hover)` (risco de hover "grudado" em iOS touch) e sem `:active`; `.cat-card` (categoria) só muda border no hover, sem lift nem feedback de toque. Esta plan padroniza o micro-feedback nos dois cards do escopo, isola o `:hover` para touch e garante GPU hint + reduced-motion.
Output: regras de `:hover`/`:active` consistentes e seguras em touch para `.stu-acct` e `.cat-card`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-mobile-motion/04-SPEC.md
@.planning/phases/04-ux-mobile-motion/04-CONTEXT.md
@assets/fides-studio.css
@assets/fides-store.css
</context>

<artifacts_this_phase_produces>
Regras CSS NOVAS / alteradas (escopo restrito a 2 classes de card):
- `.stu-acct` (conta, fides-studio.css:525): o `:hover` existente (:534) passa a viver dentro de `@media (hover:hover)` (isolamento touch); nova regra `.stu-acct:active` com `transform: scale(~0.98)` (feedback de toque mobile); GPU hint (`will-change: transform`/`translateZ(0)`) na transição existente (`transition: all 0.15s` já em :532); gate `@media (prefers-reduced-motion: reduce)` que zera os transforms do card.
- `.fds-studio .cat-card` (categoria, fides-store.css:206): novo lift no `:hover` (translateY −1px + sombra) dentro de `@media (hover:hover)`; nova regra `.cat-card:active` com `transform: scale(~0.98)`; GPU hint; gate reduced-motion. A regra existente que revela `.cat-card-del` no hover (fides-store.css:236) deve continuar funcionando.
- Bloco(s) `@media (hover:hover)` e `@media (prefers-reduced-motion: reduce)` novos nesses dois arquivos CSS.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Card de conta .stu-acct — isolar :hover, adicionar :active, GPU hint e reduced-motion</name>
  <files>assets/fides-studio.css</files>
  <read_first>
    - assets/fides-studio.css:525-534: `.stu-acct` tem `transition: all 0.15s` (:532) e `.stu-acct:hover { transform: translateY(-1px); border-color: var(--border-strong); box-shadow: ... }` (:534). Este é o padrão de lift estabelecido a replicar (e a isolar).
    - SPEC AC 6 + Edge Coverage: `:hover` isolado em `@media (hover:hover)` para não grudar em touch; `:active` scale-down no mobile.
    - NOTA: a Task 3 da plan 03 também edita fides-studio.css (engrenagem). Esta plan está na Wave 2 (depois da 03), então o arquivo já terá as regras da engrenagem — NÃO sobrescrevê-las; só adicionar/ajustar as regras de `.stu-acct`.
  </read_first>
  <action>
    Em `assets/fides-studio.css`, region restrita a `.stu-acct`:
    - Mover a regra de lift do hover para isolamento touch: o `.stu-acct:hover` (atualmente :534) deve passar a estar dentro de um bloco `@media (hover: hover) { .stu-acct:hover { ... } }` (mantendo o mesmo translateY −1px + border-strong + box-shadow). Isso evita hover "grudado" após tap em iOS (Edge Coverage). <!-- planner-discipline-allow: hover: hover -->
    - Adicionar `.stu-acct:active { transform: scale(0.98); }` (feedback de toque mobile; valor ~0.98 por SPEC/CONTEXT). Como o card tem `transition: all 0.15s`, o scale anima suave.
    - GPU hint (MOTION-PERF): garantir que a transição de transform tenha hint — adicionar `will-change: transform;` (ou `transform: translateZ(0)` na base) na regra base `.stu-acct`. Nenhuma transição >300ms (atual é 0.15s — ok).
    - Gate reduced-motion (D-05/MOTION-PERF): adicionar `@media (prefers-reduced-motion: reduce) { .stu-acct, .stu-acct:hover, .stu-acct:active { transform: none !important; transition: none !important; } }`.
    NÃO tocar em `.stu-tx` (fora de escopo) nem em outras classes de card.
  </action>
  <acceptance_criteria>
    - `grep -n "hover: hover" assets/fides-studio.css` mostra o bloco que envolve `.stu-acct:hover`.
    - `grep -n ".stu-acct:active" assets/fides-studio.css` mostra a regra com `scale(0.98)`.
    - `grep -nE "will-change|translateZ" assets/fides-studio.css` mostra o GPU hint no contexto de `.stu-acct`.
    - `grep -n "prefers-reduced-motion" assets/fides-studio.css` mostra o gate que zera transforms do card.
    - `grep -c "stu-tx:hover" assets/fides-studio.css` mostra que `.stu-tx` NÃO foi alterado (boundary — a regra original permanece sem novo isolamento).
    - Observável (desktop): hover em card de conta eleva+sombra; (mobile iOS): tap mostra scale-down e NÃO deixa hover grudado.
  </acceptance_criteria>
  <verify>
    grep -nE "hover: hover|\.stu-acct:active|will-change|translateZ|prefers-reduced-motion" assets/fides-studio.css
  </verify>
  <done>
    `.stu-acct` tem `:hover` isolado em `@media (hover:hover)`, `:active` scale-down, GPU hint e gate reduced-motion; `.stu-tx` intocado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Card de categoria .cat-card — lift no :hover (isolado), :active, GPU hint e reduced-motion</name>
  <files>assets/fides-store.css</files>
  <read_first>
    - assets/fides-store.css:206-240: `.fds-studio .cat-card` tem `transition: all 0.15s` (:213) e `.fds-studio .cat-card:hover { border-color: var(--border-strong); }` (:215 — só border, SEM lift). A regra `.fds-studio .cat-card:hover .cat-card-del { opacity: 1; }` (:236) revela o botão excluir no hover — DEVE continuar funcionando.
    - assets/fides-studio.css:534 (padrão de lift de `.stu-acct` a replicar: translateY −1px + box-shadow).
    - SPEC: cards de categoria ganham `:hover` lift (desktop, isolado em `@media (hover:hover)`) + `:active` scale (mobile).
  </read_first>
  <action>
    Em `assets/fides-store.css`, region restrita a `.fds-studio .cat-card`:
    - Adicionar o lift dentro de isolamento touch: bloco `@media (hover: hover) { .fds-studio .cat-card:hover { transform: translateY(-1px); box-shadow: <espelhar o de .stu-acct>; } }` (replicando o padrão de `.stu-acct`). <!-- planner-discipline-allow: hover: hover -->
    - Manter a regra existente `.fds-studio .cat-card:hover { border-color: var(--border-strong); }` (:215) E a regra de revelar o del no hover (:236) — NÃO removê-las; o novo lift é aditivo. (Se preferir, deixar o border-change como está fora do @media para não regredir; o lift novo fica dentro do @media (hover:hover).)
    - Adicionar `.fds-studio .cat-card:active { transform: scale(0.98); }` (feedback de toque mobile).
    - GPU hint (MOTION-PERF): `will-change: transform;` (ou `translateZ(0)`) na base `.fds-studio .cat-card`. Transição já é 0.15s (≤300ms).
    - Gate reduced-motion: `@media (prefers-reduced-motion: reduce) { .fds-studio .cat-card, .fds-studio .cat-card:hover, .fds-studio .cat-card:active { transform: none !important; transition: none !important; } }`.
    NÃO tocar em outras classes.
  </action>
  <acceptance_criteria>
    - `grep -n "hover: hover" assets/fides-store.css` mostra o bloco que envolve o `.cat-card:hover` com lift.
    - `grep -n "cat-card:active" assets/fides-store.css` mostra a regra com `scale(0.98)`.
    - `grep -nE "will-change|translateZ" assets/fides-store.css` mostra o GPU hint no contexto de `.cat-card`.
    - `grep -n "prefers-reduced-motion" assets/fides-store.css` mostra o gate de reduced-motion.
    - `grep -n "cat-card:hover .cat-card-del" assets/fides-store.css` confirma que a regra de revelar o del no hover (:236) permanece (não regrediu).
    - Observável (desktop): hover em card de categoria eleva+sombra e o botão excluir aparece; (mobile iOS): tap mostra scale-down sem hover grudado.
  </acceptance_criteria>
  <verify>
    grep -nE "hover: hover|cat-card:active|will-change|translateZ|prefers-reduced-motion|cat-card:hover .cat-card-del" assets/fides-store.css
  </verify>
  <done>
    `.cat-card` tem lift no `:hover` (isolado em `@media (hover:hover)`), `:active` scale-down, GPU hint e gate reduced-motion; o reveal do botão excluir continua funcionando.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Fase CSS puro de cards. Sem JS, sem dados/API/auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | (n/a) | CSS de cards | accept | Mudança puramente declarativa de CSS; sem superfície de ameaça. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Sem bundler/npm; nenhum pacote instalado. |
</threat_model>

<verification>
- `.stu-acct` e `.cat-card` têm `:hover` lift isolado em `@media (hover:hover)`, `:active` scale-down, GPU hint e gate reduced-motion.
- `.stu-tx` e o reveal do `.cat-card-del` no hover permanecem inalterados (boundary).
- Nenhuma transição de card >300ms sem GPU hint.
</verification>

<success_criteria>
- AC SPEC 6/7/8 (MOTION-02 + MOTION-PERF para cards) satisfeitos; Edge Coverage "sticky :hover em touch" coberto.
</success_criteria>

<output>
Create `.planning/phases/04-ux-mobile-motion/04-04-SUMMARY.md` when done
</output>
