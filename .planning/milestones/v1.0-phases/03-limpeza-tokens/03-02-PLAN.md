---
phase: 03-limpeza-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - assets/fides.css
  - assets/fides-studio.css
  - assets/fides-orcamento.css
autonomous: false
requirements:
  - DESIGN-04
must_haves:
  truths:
    - "fides.css, fides-studio.css e fides-orcamento.css não contêm regras CSS duplicadas (mesmo seletor com mesmas propriedades) nem propriedades órfãs cuja classe não existe em nenhum JSX"
    - "Tokens fantasma (variáveis CSS referenciadas via var() mas nunca definidas) são identificados; cada um recebe definição ou é removido o consumo"
    - "Nenhuma regra ativa do app vivo (escopo .fds-studio) é removida — a auditoria preserva comportamento visual"
  artifacts:
    - path: "assets/fides.css"
      provides: "CSS auditado — duplicatas e propriedades mortas removidas"
    - path: "assets/fides-studio.css"
      provides: "CSS auditado — duplicatas e propriedades mortas removidas"
    - path: "assets/fides-orcamento.css"
      provides: "CSS auditado — tokens fantasma resolvidos"
  key_links:
    - from: "assets/*.jsx"
      to: "assets/*.css"
      via: "toda classe CSS preservada deve ter className correspondente em algum JSX (referência viva)"
      pattern: "className"
---

<objective>
Auditar `fides.css`, `fides-studio.css` e `fides-orcamento.css` para remover (a) regras duplicadas (mesmo seletor declarado duas vezes com mesmas propriedades), (b) propriedades/seletores mortos (classes sem nenhum `className` correspondente em JSX), e (c) tokens fantasma (`var(--x)` consumido sem `--x` definido em lugar nenhum). Esta é a auditoria conservadora do DESIGN-04: o escopo legado `.fds-app[data-variant=...]` em `fides.css` existe mas o app vivo renderiza no escopo `.fds-studio` — qualquer remoção deve preservar 100% do comportamento visual atual.

Purpose: Reduzir peso morto e divergência de CSS rastreada desde v15, sem regressão visual.
Output: Os 3 arquivos CSS com duplicatas e propriedades órfãs removidas, tokens fantasma resolvidos, diff documentado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/tokens.css
@.planning/phases/03-limpeza-tokens/03-01-PLAN.md
</context>

<artifacts_this_phase_produces>
- `assets/fides.css`, `assets/fides-studio.css`, `assets/fides-orcamento.css` auditados.
- Relatório de auditoria no SUMMARY: lista de seletores removidos (com prova de que não têm className em JSX), duplicatas consolidadas, e tokens fantasma resolvidos (definidos ou consumo removido).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mapear seletores vivos vs órfãos e tokens fantasma (auditoria — sem edição)</name>
  <read_first>
    - assets/fides.css (arquivo inteiro — 1196 linhas)
    - assets/fides-studio.css (arquivo inteiro — 807 linhas)
    - assets/fides-orcamento.css (arquivo inteiro — 824 linhas)
    - assets/tokens.css (fonte da verdade dos tokens)
    - todos os assets/*.jsx (para cruzar className contra seletores CSS)
  </read_first>
  <files>(nenhum arquivo modificado — fase de mapeamento)</files>
  <action>
    Construir três listas, salvas como evidência no SUMMARY (não criar arquivo .md separado de relatório — registrar inline no SUMMARY ao final do plano):
    1. SELETORES ÓRFÃOS: para cada classe `.xxx` definida nos 3 CSS, verificar se existe `className="...xxx..."` ou `className={...xxx...}` em qualquer assets/*.jsx. Classes sem nenhuma referência em JSX são candidatas a remoção. ATENÇÃO: incluir classes compostas (ex.: `.pln-mi-card--warn` usado via template string ou concatenação) — buscar o fragmento base (`pln-mi-card`) antes de marcar como órfão. Marcar como órfão SOMENTE se nem a classe completa nem variações por modificador aparecerem.
    2. DUPLICATAS: seletores idênticos declarados duas ou mais vezes no mesmo arquivo com o mesmo conjunto de propriedades (regra redundante). NÃO marcar como duplicata seletores iguais em escopos diferentes (ex.: `.fds-avatar` definido só uma vez; `--warn` em `.fds-app` vs `.fds-studio` são escopos legítimos diferentes).
    3. TOKENS FANTASMA: cada `var(--nome)` consumido nos 3 CSS cujo `--nome` não está definido em tokens.css, fides.css, fides-studio.css nem inline. Listar com a linha de consumo. (Ex. conhecido: `--warn-bg` é consumido em fides-orcamento.css:725 com fallback `#fbf1e5` mas pode não ter definição global.)
    NÃO editar nenhum arquivo nesta tarefa. Esta tarefa só produz as três listas que guiam a Task 2.
  </action>
  <verify>
    <automated>cd assets &amp;&amp; echo "tokens consumidos:" &amp;&amp; grep -rhoE "var\(--[a-z0-9-]+" *.css | sort -u | head -60</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contém lista de SELETORES ÓRFÃOS com a prova (`grep className` em JSX retornou 0) para cada um
    - SUMMARY contém lista de DUPLICATAS (seletor + linhas onde se repete)
    - SUMMARY contém lista de TOKENS FANTASMA (`var(--x)` sem definição) com linha de consumo
    - Nenhuma das três listas inclui seletores do escopo vivo `.fds-studio` que tenham className correspondente em JSX
  </acceptance_criteria>
  <done>Três listas de auditoria produzidas com evidência; base factual para remoção segura na Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Remover duplicatas/órfãos e resolver tokens fantasma (DESIGN-04)</name>
  <read_first>
    - assets/fides.css
    - assets/fides-studio.css
    - assets/fides-orcamento.css
    - assets/tokens.css (para confirmar valores corretos ao resolver tokens fantasma)
    - SUMMARY/listas produzidas na Task 1
  </read_first>
  <files>assets/fides.css, assets/fides-studio.css, assets/fides-orcamento.css</files>
  <action>
    Aplicar as remoções derivadas EXCLUSIVAMENTE das três listas validadas na Task 1. Regras de segurança (conservador por padrão):
    1. ÓRFÃOS: remover apenas blocos de regra cujo seletor NÃO tem nenhum className em JSX (confirmado na Task 1). Se houver QUALQUER dúvida (uso dinâmico, template string, classe construída por concatenação), NÃO remover — registrar como "mantido por precaução" no SUMMARY.
    2. DUPLICATAS: quando o mesmo seletor com as mesmas propriedades aparece duas vezes no mesmo arquivo, manter a primeira ocorrência e remover a segunda. Se as propriedades diferem (override intencional), NÃO remover.
    3. TOKENS FANTASMA: para cada `var(--x)` sem definição — se há fallback inline coerente com a marca (ex.: `var(--warn-bg, #fbf1e5)`), preferir DEFINIR o token em tokens.css NÃO é permitido (tokens.css é read-only). Em vez disso, substituir o consumo `var(--x, fallback)` pelo token de marca equivalente já existente OU manter o fallback inline se ele já produz a cor correta. Para `--warn-bg` especificamente: trocar `var(--warn-bg, #fbf1e5)` por `var(--warn-soft)` (token de marca existente = #FEF0D6/#FEF3C7), alinhando o alerta ao sistema de tokens. Documentar cada decisão no SUMMARY.
    NÃO tocar em `tokens.css` (read-only, fonte da verdade). NÃO remover nenhum seletor do escopo `.fds-studio` que tenha className vivo. NÃO alterar valores de propriedades de regras mantidas — esta tarefa só REMOVE duplicatas/órfãos e RESOLVE consumos de token fantasma.
  </action>
  <verify>
    <automated>cd assets &amp;&amp; ! grep -nE "var\(--warn-bg" *.css &amp;&amp; echo OK-NO-PHANTOM-WARNBG; node -e "for(const f of ['fides.css','fides-studio.css','fides-orcamento.css']){const c=require('fs').readFileSync(f,'utf8');const opens=(c.match(/\{/g)||[]).length;const closes=(c.match(/\}/g)||[]).length;if(opens!==closes){console.error('BRACE MISMATCH '+f+': '+opens+' vs '+closes);process.exit(1);}}console.log('OK-BRACES-BALANCED')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "var\(--warn-bg" assets/*.css` retorna 0 resultados (token fantasma resolvido)
    - Chaves `{` e `}` balanceadas em cada um dos 3 arquivos (nenhuma regra cortada pela metade)
    - SUMMARY lista cada seletor/duplicata removido com a justificativa e cada "mantido por precaução"
    - Nenhuma classe com className vivo em JSX foi removida (re-verificável: grep da classe em JSX ainda retorna match)
  </acceptance_criteria>
  <done>Duplicatas e órfãos removidos com segurança; tokens fantasma resolvidos para tokens de marca; CSS balanceado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificar ausência de regressão visual após auditoria</name>
  <action>Verificação humana visual de todas as páginas e viewports após a auditoria de CSS (ver how-to-verify).</action>
  <what-built>
    Auditoria conservadora dos 3 CSS: regras duplicadas consolidadas, seletores órfãos (sem className em JSX) removidos, token fantasma `--warn-bg` resolvido para `var(--warn-soft)`.
  </what-built>
  <how-to-verify>
    1. Abrir o app (push em main → Vercel, ou servidor estático local sobre index.html).
    2. Navegar por todas as páginas: Dashboard, Transações, Orçamento/Planejamento, Contas, Metas, Perfil.
    3. Confirmar que NADA quebrou visualmente — cards, modais, badges de status (.pln-status.warn), gráficos e avatares aparecem idênticos ao estado anterior.
    4. Em 400×512px (iOS Safari / DevTools responsivo) confirmar que o layout permanece intacto.
    5. Verificar especificamente o alerta de orçamento que usava `--warn-bg` (`.pln-resumo-alert--warn`): o fundo de aviso deve continuar com a cor âmbar suave de marca.
  </how-to-verify>
  <resume-signal>Digite "approved" se nada regrediu visualmente em nenhuma página/viewport, ou descreva o que quebrou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório → deploy Vercel | CSS estático servido ao browser; remoção de regras não introduz execução |
| Browser → CSS | Tokens e regras lidos pelo browser; sem dados sensíveis nem entrada de usuário |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Remoção de regras CSS | mitigate | Remoção dirigida por evidência (grep de className em JSX) na Task 1; verificação de chaves balanceadas; checkpoint humano percorre todas as páginas para detectar regressão visual. |
| T-03-04 | Denial of Service | Regra cortada pela metade quebra parsing CSS | mitigate | Verificação automatizada conta `{`/`}` balanceados em cada arquivo; CSS malformado seria detectado antes do checkpoint. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | Nenhuma instalação de pacote — front sem bundler/npm. Sem dependências novas. |
</threat_model>

<verification>
1. Task 1 produz três listas de auditoria com evidência (órfãos, duplicatas, tokens fantasma)
2. `grep -nE "var\(--warn-bg" assets/*.css` → 0 resultados (token fantasma resolvido)
3. Chaves balanceadas em fides.css, fides-studio.css, fides-orcamento.css
4. Toda classe removida tem prova de 0 className em JSX; toda classe viva preservada
5. Checkpoint humano confirma zero regressão visual em todas as páginas e em 400×512px
</verification>

<success_criteria>
- DESIGN-04: 3 arquivos CSS auditados — duplicatas e propriedades mortas removidas, tokens fantasma resolvidos
- Nenhuma regressão visual (checkpoint aprovado)
- CSS sintaticamente válido (chaves balanceadas)
- Auditoria documentada no SUMMARY com evidência por seletor
</success_criteria>

<output>
Create `.planning/phases/03-limpeza-tokens/03-02-SUMMARY.md` when done
</output>
