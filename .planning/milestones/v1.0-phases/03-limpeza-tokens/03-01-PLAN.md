---
phase: 03-limpeza-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fides.css
  - assets/fides-studio.css
autonomous: false
requirements:
  - CLEAN-01
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
must_haves:
  truths:
    - "Nenhum arquivo fides-diario.* existe no repositório e nenhuma <link>/import o referencia em index.html ou Fides-app.html"
    - "O avatar da sidebar (.fds-avatar, .fds-sb-slim-avatar) exibe gradiente de cor de marca (verde floresta) — sem roxo #6366F1/#8B5CF6"
    - "A view de perfil (.prf-view, .prf-avatar) usa tokens de marca (var(--accent)) — sem roxo hardcoded"
    - "O token --warn tem o mesmo valor (#B45309) em fides.css (todas as definições), fides-studio.css e tokens.css"
  artifacts:
    - path: "assets/fides.css"
      provides: ".fds-avatar com gradiente de marca; --warn consistente em todas as definições"
      contains: "var(--accent)"
    - path: "assets/fides-studio.css"
      provides: ".fds-sb-slim-avatar com gradiente de marca; --warn #B45309"
      contains: "var(--accent)"
  key_links:
    - from: "assets/fides-shell.jsx"
      to: "assets/fides.css"
      via: ".fds-avatar className renderiza o avatar e consome o gradiente de marca"
      pattern: "fds-avatar"
    - from: "assets/fides-studio.jsx"
      to: "assets/fides-studio.css"
      via: ".fds-sb-slim-avatar className renderiza o avatar slim da sidebar"
      pattern: "fds-sb-slim-avatar"
---

<objective>
Confirmar que o dead code `fides-diario.*` está realmente fora do repositório (CLEAN-01) e migrar todos os avatares e o token `--warn` para a identidade de marca (DESIGN-01, DESIGN-02, DESIGN-03). O roxo hardcoded `#6366F1`→`#8B5CF6` aparece em DOIS avatares vivos (`.fds-avatar` em `fides.css`, `.fds-sb-slim-avatar` em `fides-studio.css`); ambos devem virar gradiente verde floresta de marca. A view de perfil (`.prf-view`/`.prf-avatar`) já usa `var(--accent)` — esta tarefa apenas audita e confirma. O token `--warn` diverge em uma única definição de variante (`#D97706` no bloco `.fds-app[data-variant="v3"]` de `fides.css`); deve ser alinhado a `#B45309`.

Purpose: A UI deve usar exclusivamente tokens de marca; roxo off-brand é débito de identidade rastreado desde v15.
Output: `fides.css` e `fides-studio.css` editados — avatares em verde de marca, `--warn` consistente; CLEAN-01 verificado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/tokens.css
</context>

<artifacts_this_phase_produces>
- `assets/fides.css` modificado: `.fds-avatar` usa `linear-gradient(135deg, var(--accent), var(--accent-2))`; definição `--warn: #D97706` no bloco `.fds-app[data-variant="v3"]` alterada para `#B45309`.
- `assets/fides-studio.css` modificado: `.fds-sb-slim-avatar` usa `linear-gradient(135deg, var(--accent), var(--accent-2))`.
- Confirmação verificável de que `fides-diario.jsx`/`.css` não existem e não são referenciados (nenhuma edição necessária — estado já correto; a tarefa documenta a verificação no SUMMARY).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verificar ausência de fides-diario.* (CLEAN-01)</name>
  <read_first>
    - index.html (confirmar que não há &lt;link&gt; nem &lt;script&gt; para fides-diario)
    - Fides-app.html (protegido — apenas leitura; confirmar ausência de referência)
  </read_first>
  <files>(nenhum arquivo modificado — verificação de estado)</files>
  <action>
    Confirmar via comandos que o dead code já foi removido (commit d4db34f) e nada o referencia. Não criar, não deletar, não editar arquivos nesta tarefa — apenas provar o estado e registrar o resultado no SUMMARY. Se algum dos greps abaixo retornar resultado inesperado (arquivo presente ou referência viva), PARAR e reportar como gap antes de prosseguir.
    Rodar e registrar a saída de cada comando:
    1. Listar arquivos diario: deve não encontrar nada.
    2. Grep por referência em index.html e Fides-app.html: deve retornar 0.
    3. Grep por exports do componente (FidesDiario, SectionMark, DiaCatChip) em assets/: deve retornar 0.
  </action>
  <verify>
    <automated>cd assets && ls fides-diario.* 2>&1 | grep -q "No such file\|cannot access\|not find" &amp;&amp; echo OK-NOFILE; cd .. &amp;&amp; ! grep -rEi "fides-diario|FidesDiario|SectionMark|DiaCatChip" index.html Fides-app.html assets/*.jsx assets/*.css &amp;&amp; echo OK-NOREF</automated>
  </verify>
  <acceptance_criteria>
    - `ls assets/fides-diario.*` retorna erro "No such file" (arquivo inexistente)
    - `grep -rEi "fides-diario" index.html Fides-app.html` retorna 0 resultados
    - `grep -rEi "FidesDiario|SectionMark|DiaCatChip" assets/` retorna 0 resultados
    - SUMMARY registra que CLEAN-01 já estava satisfeito pelo commit d4db34f (nenhuma edição necessária)
  </acceptance_criteria>
  <done>CLEAN-01 verificado: arquivos diario ausentes, zero referências em HTML/JSX/CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar avatares de roxo hardcoded para gradiente de marca (DESIGN-01, DESIGN-02)</name>
  <read_first>
    - assets/fides.css (linhas 224-232 — bloco .fds-avatar)
    - assets/fides-studio.css (linhas 129-137 — bloco .fds-sb-slim-avatar)
    - assets/fides-studio.css (linhas 730-808 — bloco .prf-view / .prf-avatar, para auditar DESIGN-02)
    - assets/tokens.css (linhas 31-35 — tokens --accent #2D5A3D e --accent-2 #1F4029)
    - assets/fides-shell.jsx (linha 99 — render de .fds-avatar, confirmar que está vivo)
    - assets/fides-studio.jsx (linha 294 — render de .fds-sb-slim-avatar, confirmar que está vivo)
  </read_first>
  <files>assets/fides.css, assets/fides-studio.css</files>
  <action>
    Em `assets/fides.css`, no bloco `.fds-avatar` (linha 227), substituir `background: linear-gradient(135deg, #6366F1, #8B5CF6);` por `background: linear-gradient(135deg, var(--accent), var(--accent-2));`. NÃO alterar nenhuma outra propriedade do bloco (width, height, border-radius, color, display permanecem).
    Em `assets/fides-studio.css`, no bloco `.fds-sb-slim-avatar` (linha 132), substituir `background: linear-gradient(135deg, #6366F1, #8B5CF6);` por `background: linear-gradient(135deg, var(--accent), var(--accent-2));`. NÃO alterar nenhuma outra propriedade.
    Justificativa dos valores: `--accent` = `#2D5A3D` (verde floresta) e `--accent-2` = `#1F4029` (verde escuro) são os tokens de marca definidos em `tokens.css` e re-declarados no escopo `.fds-studio` de `fides-studio.css`. O gradiente 135deg preserva o efeito visual original, trocando apenas a paleta roxa pela verde.
    DESIGN-02: O bloco `.prf-view`/`.prf-avatar` em `fides-studio.css` JÁ usa `background: var(--accent)` (linha 775) e `color: #fff` nas iniciais — sem roxo. Auditar o bloco inteiro `.prf-*` (linhas 730-808) e confirmar que nenhum valor de cor roxo hardcoded (`#6366F1`, `#8B5CF6`, `#7C3AED`, `#A855F7`) aparece. Se confirmado limpo, NÃO editar — registrar no SUMMARY que DESIGN-02 já estava conforme. Se houver roxo, trocar por `var(--accent)`.
  </action>
  <verify>
    <automated>cd assets &amp;&amp; ! grep -nEi "#6366F1|#8B5CF6" fides.css fides-studio.css &amp;&amp; echo OK-NO-PURPLE; grep -c "linear-gradient(135deg, var(--accent), var(--accent-2))" fides.css fides-studio.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ei "#6366F1|#8B5CF6" assets/fides.css assets/fides-studio.css` retorna 0 resultados
    - `.fds-avatar` em fides.css contém exatamente `background: linear-gradient(135deg, var(--accent), var(--accent-2));`
    - `.fds-sb-slim-avatar` em fides-studio.css contém exatamente `background: linear-gradient(135deg, var(--accent), var(--accent-2));`
    - Bloco `.prf-avatar` permanece com `background: var(--accent)` (DESIGN-02 conforme)
    - Nenhuma outra propriedade dos blocos avatar foi alterada (diff mostra apenas a linha `background`)
  </acceptance_criteria>
  <done>Ambos os avatares vivos exibem gradiente verde de marca; perfil confirmado on-brand; zero roxo hardcoded em fides.css/fides-studio.css.</done>
</task>

<task type="auto">
  <name>Task 3: Alinhar token --warn divergente (DESIGN-03)</name>
  <read_first>
    - assets/tokens.css (linha 42 — fonte da verdade: --warn: #B45309)
    - assets/fides.css (linha 39 — --warn: #B45309 no bloco V1; linha 101 — --warn: #D97706 no bloco .fds-app[data-variant="v3"])
    - assets/fides-studio.css (linha 37 — --warn: #B45309)
    - assets/fides-orcamento.css (linhas 724-726 — consome var(--warn) com fallback inline #B45309; NÃO define --warn)
  </read_first>
  <files>assets/fides.css</files>
  <action>
    Fonte da verdade: `tokens.css` define `--warn: #B45309`. O único ponto divergente é `assets/fides.css` linha 101, dentro do bloco `.fds-app[data-variant="v3"]`, que declara `--warn: #D97706;` (amber-600). Substituir `--warn: #D97706;` por `--warn: #B45309;` nessa linha 101. NÃO alterar `--warn-soft` (permanece `#FEF3C7`). NÃO alterar a definição da linha 39 (já é `#B45309`).
    Contexto importante: o app vivo renderiza no escopo `.fds-studio` (que já usa `#B45309`), não no escopo legado `.fds-app[data-variant=...]`. Mesmo assim, alinhar a variante v3 elimina a única divergência de valor de `--warn` no repositório e satisfaz a verificação por grep do DESIGN-03.
    `fides-orcamento.css` NÃO define `--warn` (apenas consome via `var(--warn)`); o fallback inline na linha 726 já é `#B45309` (consistente) — não editar. NÃO adicionar uma definição `--warn` nova em `fides-orcamento.css`: ela introduziria um novo ponto de manutenção; o arquivo deve continuar herdando o token.
  </action>
  <verify>
    <automated>cd assets &amp;&amp; ! grep -nE "\-\-warn:\s*#D97706" fides.css fides-studio.css fides-orcamento.css &amp;&amp; echo OK-NO-DIVERGENCE; grep -hoE "\-\-warn:\s*#[0-9A-Fa-f]{6}" tokens.css fides.css fides-studio.css | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\-\-warn:\s*#D97706" assets/*.css` retorna 0 resultados
    - Toda definição `--warn:` em tokens.css, fides.css e fides-studio.css tem o valor `#B45309`
    - `--warn-soft` permanece inalterado (`#FEF3C7` em fides.css)
    - `fides-orcamento.css` continua sem definir `--warn` (apenas consome)
  </acceptance_criteria>
  <done>Token --warn idêntico (#B45309) em todos os arquivos que o definem; nenhuma divergência de valor restante.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — verificar avatares verdes de marca na UI</name>
  <action>Verificação humana visual dos avatares migrados e do token --warn (ver how-to-verify).</action>
  <what-built>
    Avatares (sidebar slim e shell) migrados de gradiente roxo `#6366F1→#8B5CF6` para gradiente verde de marca `var(--accent)→var(--accent-2)`. Token `--warn` alinhado a `#B45309` em todas as definições.
  </what-built>
  <how-to-verify>
    1. Abrir o app (push em main → Vercel, ou abrir index.html localmente via servidor estático).
    2. Olhar o avatar no rodapé da sidebar slim (iniciais do usuário): deve exibir gradiente VERDE floresta, não roxo.
    3. Abrir o DevTools no elemento `.fds-avatar` / `.fds-sb-slim-avatar` e confirmar `background: linear-gradient(135deg, var(--accent), var(--accent-2))` — sem `#6366F1`/`#8B5CF6` computado roxo.
    4. Abrir a PerfilView (avatar grande de 72px): deve estar verde sólido (`var(--accent)`), consistente com o resto da UI.
    5. Confirmar em 400×512px (iOS Safari ou DevTools responsivo) que os avatares continuam renderizando corretamente — sem quebra de layout.
  </how-to-verify>
  <resume-signal>Digite "approved" se os avatares estão verdes de marca em todos os pontos, ou descreva o que ainda aparece roxo/off-brand.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório → deploy Vercel | Mudanças em CSS estático são servidas diretamente ao browser; nenhuma entrada de usuário processada |
| Browser → CSS | Tokens CSS são lidos pelo browser; não há execução de código nem dados sensíveis |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Edição de fides.css/fides-studio.css | accept | Mudança é puramente cosmética (cor de gradiente e valor de token). Verificação por grep + checkpoint humano garantem que apenas a propriedade `background`/`--warn` mudou. Sem superfície de execução. |
| T-03-02 | Denial of Service | Variável CSS inexistente | mitigate | `var(--accent)`/`var(--accent-2)` são definidos em tokens.css E re-declarados no escopo .fds-studio; ausência cairia no gradiente sem fallback, mas o checkpoint visual detecta qualquer avatar sem cor. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | Nenhuma instalação de pacote nesta fase — front sem bundler/npm (Babel standalone). Sem dependências novas. |
</threat_model>

<verification>
1. `grep -rEi "fides-diario|FidesDiario" index.html Fides-app.html assets/` → 0 resultados (CLEAN-01)
2. `grep -Ei "#6366F1|#8B5CF6" assets/fides.css assets/fides-studio.css` → 0 resultados (DESIGN-01)
3. Bloco `.prf-avatar` em fides-studio.css contém `var(--accent)` e nenhum roxo (DESIGN-02)
4. `grep -E "\-\-warn:\s*#D97706" assets/*.css` → 0 resultados; todas as definições `--warn` = `#B45309` (DESIGN-03)
5. Checkpoint humano confirma avatares verdes em todos os pontos de render
</verification>

<success_criteria>
- CLEAN-01 verificado: nenhum arquivo/referência fides-diario.*
- DESIGN-01: `.fds-avatar` e `.fds-sb-slim-avatar` usam gradiente de marca, zero roxo hardcoded
- DESIGN-02: `.prf-view`/`.prf-avatar` confirmados on-brand (var(--accent))
- DESIGN-03: `--warn` idêntico (#B45309) em todos os arquivos que o definem
- Checkpoint visual aprovado
</success_criteria>

<output>
Create `.planning/phases/03-limpeza-tokens/03-01-SUMMARY.md` when done
</output>
